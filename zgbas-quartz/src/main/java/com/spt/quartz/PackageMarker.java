package com.spt.quartz;

/**
 * Package marker for zgbas-quartz (Phase 1 skeleton).
 * Reserved home for scheduled job handlers migrated from xxl-job -> RuoYi quartz (Phase 6).
 * Placeholder class to avoid empty-source-directory edge cases in Maven/IDE.
 */
public final class PackageMarker {

    private PackageMarker() {
    }
}
