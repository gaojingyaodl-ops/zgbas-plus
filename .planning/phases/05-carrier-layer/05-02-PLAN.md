---
plan_id: 05-02
phase: 5
wave: 1
title: 承托底座 II — util(含 stub 替换 ResponseUtil/UserContext + HttpUtils 死码核实)
objective: 把 basWx util 中的 16 个工具类 verbatim 落入包飞地,把 P4 预置 stub(ResponseUtil/UserContext)用源实测实现替换,并核实 HttpUtils 是否死码(单体已有 spt-tools HTTPUtility);OcrUtils/OcrHelper 留 plan 05-04(依赖 OcrConfig)
depends_on:
  - 05-01
files_modified:
  - zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/util/
autonomous: true
requirements_addressed:
  - WX-BFF-03

must_haves:
  truths:
    - "util/ 16 类就位(CommonUtil/ConvertUtils/DateUtils/DeptUtils/FreemarkerUtil/HttpUtils/NumUtils/RsaUtil/SignUtil/SM3/SMSUtils/StrUtils/UploadHelper/ResponseUtil/UserContext/UserHelper)"
    - "ResponseUtil + UserContext 以源实测实现替换 P4 stub(D-P5-14)"
    - "JwtUtil 不重复迁(P4 已就位 enclave)"
    - "OcrUtils/OcrHelper 不在本 plan(留 05-04 随 OcrConfig)"
    - "HttpUtils 死码核实结论记入 SUMMARY(有 caller→迁 / 无 caller→不迁)"
  artifacts:
    - path: "zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/util/UserContext.java"
      provides: "WX 用户上下文 ThreadLocal(源实测,替换 stub)"
      contains: "class UserContext"
    - path: "zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/util/ResponseUtil.java"
      provides: "WX 响应工具(源实测,替换 stub)"
      contains: "class ResponseUtil"
  key_links:
    - from: "util/UserContext.java"
      to: "RequestListener(plan 05-05)+ JwtAuthenticationFilter(P4)"
      via: "UserContext.setUser/removeUser ThreadLocal"
      pattern: "UserContext"
---

<objective>
承托层第二波:迁入工具类 util。其中 ResponseUtil/UserContext 是 P4 为打通编译预置的 stub,本 plan 用源实测实现替换(D-P5-14)。HttpUtils 需核实是否死码 —— 单体已有 spt-tools `com.spt.tools.http.util.HTTPUtility`(EweChatApi 等实际用它),WX 自带 HttpUtils 可能无 caller。

Purpose: util 是 cache/横切/启动接线的依赖(UserContext 被 RequestListener 清理、UserHelper 被认证用)。
Output: 16 个 util 类就位,2 个 stub 替换,HttpUtils 死码结论落定。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-carrier-layer/05-CONTEXT.md
@.planning/phases/05-carrier-layer/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: HttpUtils 死码核实(先决)</name>
  <files>zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/util/HttpUtils.java</files>
  <read_first>
    - 源 /Users/alan/WorkSpace/IDEA/zgbas/basWx/purchase-server/src/main/java/com/spt/bas/purchase/wx/server/util/HttpUtils.java
    - 在整个源 purchase-server 树 grep `import com.spt.bas.purchase.wx.server.util.HttpUtils` 与 `HttpUtils.` 调用点 —— 确认 caller
    - 05-RESEARCH.md §Q6 util 备注(HttpUtils 与单体 spt-tools HTTPUtility 重名风险)
  </read_first>
  <action>
    核实 WX `util/HttpUtils` 的 caller:
    - 若源中有 ≥1 个类 import 并调用它 → 本 plan Task 2 verbatim 迁入。
    - 若零 caller(所有外部调用实际走 spt-tools HTTPUtility,见 EweChatApi:9)→ **不迁**,记入 SUMMARY "HttpUtils 死码,未迁"。
    把核实结论(grep 命中行数 + 调用类名)写入 SUMMARY,作为 Task 2 是否包含 HttpUtils 的依据。
  </action>
  <verify>
    <automated>grep -rn "util.HttpUtils\|HttpUtils\." /Users/alan/WorkSpace/IDEA/zgbas/basWx/purchase-server/src/main/java | grep -v "util/HttpUtils.java" | head</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY 记录 HttpUtils caller 数 + 调用类清单
    - 明确结论:迁入(有 caller)或不迁(死码)
  </acceptance_criteria>
  <done>HttpUtils 死码结论落定,指导 Task 2 范围</done>
</task>

<task type="auto">
  <name>Task 2: verbatim 迁入 util(16 或 15 类)+ 替换 ResponseUtil/UserContext stub</name>
  <files>zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/util/</files>
  <read_first>
    - 源 /Users/alan/WorkSpace/IDEA/zgbas/basWx/purchase-server/src/main/java/com/spt/bas/purchase/wx/server/util/ — 逐类读取
    - enclave 现有 stub:zgbas-system/.../wx/server/util/ResponseUtil.java、UserContext.java、JwtUtil.java(P4,P4 实测非 stub,保留)
    - 05-01 产出的 common/(UserHelper 可能依赖 common 常量)
    - Task 1 的 HttpUtils 结论
  </read_first>
  <action>
    把源 util/ 以下类按原包名 com.spt.bas.purchase.wx.server.util verbatim 落入 enclave:
    CommonUtil, ConvertUtils, DateUtils, DeptUtils, FreemarkerUtil, NumUtils, RsaUtil, SignUtil, SM3, SMSUtils, StrUtils, UploadHelper, UserHelper, **ResponseUtil(覆盖 P4 stub,源实测实现,D-P5-14)**, **UserContext(覆盖 P4 stub,源实测实现,D-P5-14)**

    HttpUtils:依 Task 1 结论 —— 有 caller 则加入,无则跳过。

    不迁:JwtUtil(P4 已就位 enclave 且为实测实现)、OcrUtils/OcrHelper(留 05-04,依赖 OcrConfig)。

    规则:① ResponseUtil/UserContext 必须以源实测覆盖 stub(D-P5-14);② 包名保留;③ UserContext 的 ThreadLocal 字段类型若引用 UserInfoVo,依赖 05-01 已迁的 UserInfoVo(源实测);④ 不引入对 P6 service 的 import。
  </action>
  <verify>
    <automated>ls zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/util/*.java | wc -l ; grep -c "class UserContext\|class ResponseUtil" zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/util/UserContext.java zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/util/ResponseUtil.java</automated>
  </verify>
  <acceptance_criteria>
    - util/ 含 CommonUtil/ConvertUtils/DateUtils/DeptUtils/FreemarkerUtil/NumUtils/RsaUtil/SignUtil/SM3/SMSUtils/StrUtils/UploadHelper/UserHelper/ResponseUtil/UserContext 共 15(+HttpUtils 依 Task1)
    - ResponseUtil.java 与源 `diff` 一致(非 stub)
    - UserContext.java 与源 `diff` 一致(非 stub)
    - JwtUtil.java 不被覆盖(P4 实测保留)
    - 每个文件首行 `package com.spt.bas.purchase.wx.server.util;`
  </acceptance_criteria>
  <done>util 15-16 类就位,ResponseUtil/UserContext stub 替换完成</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 源码 → enclave | verbatim 复制,UserContext ThreadLocal 涉及用户身份上下文 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Information Disclosure | UserContext ThreadLocal 未清理 | mitigate | RequestListener(05-05)requestDestroyed 调 UserContext.removeUser,防内存泄漏 + 身份串线程 |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | 纯 Java 复制,无包管理器操作 |
</threat_model>

<verification>
util 类可能引用 common(05-01)/payload,跨 plan 引用在 phase 完成前可能残留编译错误 —— 以 05-06 编译门为权威。自检:
```bash
ls zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/util/*.java | wc -l   # 期望 17(JwtUtil 已有 + 新迁 15-16)
```
</verification>

<success_criteria>
1. util 15-16 类就位(HttpUtils 依死码结论)
2. ResponseUtil + UserContext stub 替换为源实测实现(D-P5-14)
3. JwtUtil 保留 P4 实测、OcrUtils/OcrHelper 留 05-04
4. HttpUtils 死码结论记入 SUMMARY
</success_criteria>

<output>
Create `.planning/phases/05-carrier-layer/05-02-SUMMARY.md` when done
</output>
