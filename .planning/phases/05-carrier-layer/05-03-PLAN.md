---
plan_id: 05-03
phase: 5
wave: 2
title: 横切 bean 收口(灰区 A)— exception/ + WebAppConfig CORS + GlobalExceptionHandler basePackages + ServiceAop 注册 + FrameworkConfig→WxCarrierConfig
objective: 收口 4 个全局横切 bean 防启动崩溃/行为不等价:① WebAppConfig 仅贡献 CORS 不重复 multipart/转换器(D-P5-01);② GlobalExceptionHandler 限 basePackages 不改写主域错误信封(D-P5-02);③ ServiceAop 显式 @Bean 注册且 pointcut 自限 WX service 不双增强(D-P5-03);④ FrameworkConfig 剥减为 WX-only WxCarrierConfig 只含 eweChatConfig()(D-P5-15);并迁入 exception/ 3 类(SecurityException 替换 stub)
depends_on:
  - 05-01
  - 05-02
files_modified:
  - zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/exception/
  - zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/aop/ServiceAop.java
  - zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/config/JinXinConfig.java
  - zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/config/EweChatConfig.java
  - zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/config/OcrConfig.java
  - zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/config/WxCarrierConfig.java
  - zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/config/WxAopConfig.java
  - zgbas-system/src/main/java/com/spt/bas/server/config/WebAppConfig.java
autonomous: true
requirements_addressed:
  - WX-BFF-03

must_haves:
  truths:
    - "exception/GlobalExceptionHandler 带 @ControllerAdvice(basePackages = \"com.spt.bas.purchase.wx.server\"),不改写主域 bas 控制器错误响应(D-P5-02)"
    - "exception/SecurityException 以源实测替换 P4 stub;UserNameOrPasswordException verbatim 迁入"
    - "单体 bas.server.config.WebAppConfig 含 WX 的 CORS 配置(allowedOrigins/methods/credentials),且不重复 multipartConfigElement/PageInterceptor/转换器 @Bean(D-P5-01)"
    - "aop/ServiceAop 迁入(仅 @Aspect),由新建 config 类显式 @Bean 注册(参照 spt-tools ToolsAopConfig);pointcut execution(* com.spt.bas.purchase.wx.server.service..*(..)) 自限 WX(D-P5-03)"
    - "新建 config/WxCarrierConfig(@Configuration)只含 @Bean @ConfigurationProperties(prefix=ewechat.config) EweChatConfig eweChatConfig();不 verbatim 迁 FrameworkConfig(D-P5-15)"
    - "单体不存在两个 @Bean taskExecutor / 两个 eweChatConfig / 重复外部 SDK @Bean"
  artifacts:
    - path: "zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/exception/GlobalExceptionHandler.java"
      provides: "WX 限域全局异常处理"
      contains: "@ControllerAdvice(basePackages"
    - path: "zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/config/WxCarrierConfig.java"
      provides: "WX-only 配置(EweChatConfig @Bean)"
      contains: "eweChatConfig"
  key_links:
    - from: "GlobalExceptionHandler"
      to: "common/ApiResult(05-01)"
      via: "catch Exception → ApiResult"
      pattern: "ApiResult"
    - from: "WxCarrierConfig.eweChatConfig()"
      to: "EweChatConfig @Bean(prefix ewechat.config)"
      via: "@ConfigurationProperties"
      pattern: "ewechat.config"
---

<objective>
灰区 A 收口:verbatim 全迁 4 个全局横切 bean 会启动崩溃或行为不等价,本 plan 逐 bean 最小消歧(用户选定,行为等价优先于 verbatim 字面)。同时把 exception/ 3 类落位(SecurityException 替换 stub)。

Purpose: WebAppConfig 重复 multipartConfigElement @Bean → BeanDefinitionStoreException;GlobalExceptionHandler 不限域 → 主域 bas 控制器错误响应被改写为 WX ApiResult;ServiceAop 无 @Component 不注册 + pointcut 需自限;FrameworkConfig verbatim → 外部 SDK bean 重名。
Output: 4 个横切 bean 安全就位,exception/ 3 类落位。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-carrier-layer/05-CONTEXT.md
@.planning/phases/05-carrier-layer/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: WebAppConfig CORS 合并(D-P5-01,非 verbatim)</name>
  <files>zgbas-system/src/main/java/com/spt/bas/server/config/WebAppConfig.java</files>
  <read_first>
    - 源 /Users/alan/WorkSpace/IDEA/zgbas/basWx/purchase-server/src/main/java/com/spt/bas/purchase/wx/server/config/WebAppConfig.java — 提取 CORS 配置(addCorsMappings / CorsRegistration / allowedOrigins)
    - 单体 zgbas-system/src/main/java/com/spt/bas/server/config/WebAppConfig.java — 合并目标,确认已含 multipartConfigElement/PageInterceptor/转换器,确认当前无 CORS
    - 05-RESEARCH.md §Q5 D-P5-01 + §D-P5-15(CORS 是 WX WebAppConfig 唯一独有)
  </read_first>
  <action>
    不整体迁入 WX WebAppConfig。把 WX WebAppConfig 的 **CORS 配置**并入单体已有 WebAppConfig:
    - 在单体 WebAppConfig 的 WebMvcConfigurer#addCorsMappings(若已有则补,若无则新增 override)中加入 WX 的 CORS 注册(逐字复制 WX 的 mapping path / allowedOrigins / allowedMethods / allowCredentials / maxAge)。
    - **不重复** @Bean multipartConfigElement / PageInterceptor / 任何 HttpMessageConverter / characterEncodingFilter(单体已有)。
    - 若单体 WebAppConfig 结构不便加 CORS override,则新建一个 **WX-only** `com.spt.bas.purchase.wx.server.config.WxCorsConfig`(@Configuration + WebMvcConfigurer,只含 addCorsMappings),不碰单体 WebAppConfig。二选一,记入 SUMMARY。
  </action>
  <verify>
    <automated>grep -rn "addCorsMappings\|allowedOrigins\|CorsRegistration" zgbas-system/src/main/java/com/spt/bas/server/config/WebAppConfig.java zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/config/ 2>/dev/null | head ; echo "--- duplicate multipart check ---" ; grep -rn "multipartConfigElement" zgbas-system/src/main/java/com/spt/bas/server/config/WebAppConfig.java</automated>
  </verify>
  <acceptance_criteria>
    - 单体 WebAppConfig 或新建 WxCorsConfig 含 WX CORS 配置(allowedOrigins 等可 grep)
    - 单体 WebAppConfig 的 multipartConfigElement @Bean 仍只有 1 个(无重复)
    - SUMMARY 记录合并方式(并入单体 / 新建 WxCorsConfig)
  </acceptance_criteria>
  <done>WX CORS 就位,无重复 multipart/转换器 @Bean</done>
</task>

<task type="auto">
  <name>Task 2: exception/ 3 类迁入 — GlobalExceptionHandler 限 basePackages + SecurityException 替换 stub</name>
  <files>zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/exception/</files>
  <read_first>
    - 源 .../exception/GlobalExceptionHandler.java、SecurityException.java、UserNameOrPasswordException.java
    - enclave stub:zgbas-system/.../wx/server/exception/SecurityException.java(P4 stub)
    - 05-01 产出的 common/ApiResult.java(GlobalExceptionHandler 返回类型)+ common/BaseException
    - 05-RESEARCH.md §Q5 D-P5-02(单体当前零 @ControllerAdvice,限 basePackages 安全)
  </read_first>
  <action>
    迁入 exception/ 3 类到 enclave .../wx/server/exception/:
    1. **GlobalExceptionHandler**:verbatim 复制源类,**但** @ControllerAdvice 注解加 basePackages 限定:`@ControllerAdvice(basePackages = "com.spt.bas.purchase.wx.server")`(D-P5-02)。保留源内所有 @ExceptionHandler 方法体。
    2. **SecurityException**:覆盖 P4 stub,用源实测实现(D-P5-14)。
    3. **UserNameOrPasswordException**:verbatim 迁入。
    包名 com.spt.bas.purchase.wx.server.exception 保留。
  </action>
  <verify>
    <automated>grep -n "basePackages" zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/exception/GlobalExceptionHandler.java ; ls zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/exception/*.java | wc -l ; grep -c "@ControllerAdvice" zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/exception/GlobalExceptionHandler.java</automated>
  </verify>
  <acceptance_criteria>
    - GlobalExceptionHandler.java 含 `@ControllerAdvice(basePackages = "com.spt.bas.purchase.wx.server")`(逐字)
    - exception/ 含 3 类(GlobalExceptionHandler/SecurityException/UserNameOrPasswordException)
    - SecurityException.java 与源 `diff` 一致(非 stub)
    - 全单体 @ControllerAdvice 仅此一处限域(basePackages 指 WX),不改写主域
  </acceptance_criteria>
  <done>exception 3 类就位,GlobalExceptionHandler 限域,SecurityException 替换 stub</done>
</task>

<task type="auto">
  <name>Task 3: ServiceAop 迁入 + 显式 @Bean 注册(D-P5-03)</name>
  <files>zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/aop/ServiceAop.java, zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/config/WxAopConfig.java</files>
  <read_first>
    - 源 .../aop/ServiceAop.java(@Aspect,无 @Component;pointcut `execution(* com.*.*.*.*.*.service..*.*(..))`)
    - spt-tools 注册模式参照:zgbas-common/src/main/java/com/spt/tools/aop/config/ToolsAopConfig.java + EnableToolsAopConfig.java
    - zgbas-common/.../aop/interceptor/ServiceAop.java(pointcut 不重叠佐证)
    - 05-RESEARCH.md §D-P5-03(pointcut 5 段通配 = 7 段路径,自限 wx.server.service)
  </read_first>
  <action>
    1. verbatim 迁入 aop/ServiceAop.java 到 enclave .../wx/server/aop/(保留 @Aspect,**不加** @Component)。
    2. **核实 pointcut**:确认源 ServiceAop 的 pointcut 仅命中 `com.spt.bas.purchase.wx.server.service..`(7 段路径),不命中主域 `com.spt.bas.server.service`(6 段)也不与 spt-tools ServiceAop 重叠。若源 pointcut 通配过宽会命中主域,则**收窄**为 `execution(* com.spt.bas.purchase.wx.server.service..*(..))`(D-P5-03 行为等价前提)。把 pointcut 原文 + 收窄决策记入 SUMMARY。
    3. 新建 `com.spt.bas.purchase.wx.server.config.WxAopConfig`(@Configuration),含 `@Bean public ServiceAop wxServiceAop(){ return new ServiceAop(); }`(参照 ToolsAopConfig 显式注册 @Aspect bean 的模式)。
  </action>
  <verify>
    <automated>grep -n "@Aspect\|@Component" zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/aop/ServiceAop.java ; grep -n "execution" zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/aop/ServiceAop.java ; grep -rn "new ServiceAop\|ServiceAop wxServiceAop\|@Bean" zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/config/WxAopConfig.java</automated>
  </verify>
  <acceptance_criteria>
    - ServiceAop.java 含 @Aspect 且**不含** @Component
    - WxAopConfig.java 含 `@Bean ... new ServiceAop()`(显式注册)
    - SUMMARY 记录 pointcut 原文 + 是否收窄 + 理由
    - 不与单体 spt-tools ServiceAop 双增强(pointcut 域分离)
  </acceptance_criteria>
  <done>ServiceAop 迁入并由 WxAopConfig 显式注册,pointcut 自限 WX</done>
</task>

<task type="auto">
  <name>Task 4: 迁入 config POJO(JinXinConfig/EweChatConfig/OcrConfig)+ FrameworkConfig 剥减 → WxCarrierConfig(D-P5-15)</name>
  <files>zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/config/{JinXinConfig,EweChatConfig,OcrConfig,WxCarrierConfig}.java</files>
  <read_first>
    - 源 .../config/JinXinConfig.java(@ConfigurationProperties prefix=jinxin)、EweChatConfig.java(纯 @Data POJO,经 @Bean 绑定)、OcrConfig.java(@ConfigurationProperties prefix=aliyun.ocr)、FrameworkConfig.java(6 个 @Bean)
    - 单体已注入证据(05-RESEARCH.md §D-P5-15):IAuthOpenFacade/FileRemote/ThreadPoolTaskExecutor 被 20+ controller 注入 → 单体已自有
    - enclave .../wx/server/config/(P4 已有 RedisConfig/JwtConfig/WxConfiguration 等,不覆盖)
  </read_first>
  <action>
    1. verbatim 迁入 3 个 config POJO 到 enclave .../wx/server/config/,包名保留:
       - **JinXinConfig**(`@ConfigurationProperties(prefix="jinxin")`,Q3 决定 Phase 5 迁 POJO;消费者 JinXinApi 留 Phase 6)
       - **EweChatConfig**(纯 `@Data` POJO,无注解 —— 由 WxCarrierConfig @Bean 绑定)
       - **OcrConfig**(`@ConfigurationProperties(prefix="aliyun.ocr")`)
    2. **不** verbatim 迁 FrameworkConfig。新建 `com.spt.bas.purchase.wx.server.config.WxCarrierConfig`:
       ```java
       @Configuration
       public class WxCarrierConfig {
           @Bean
           @ConfigurationProperties(prefix = "ewechat.config")
           public EweChatConfig eweChatConfig() { return new EweChatConfig(); }
       }
       ```
    3. **DROP**(单体已自有/冲突):FrameworkConfig 的 authOpenFacade / fileRemote / pushClientHttp / threadPoolTaskExecutor / dataSourceConfig / customConfig 六个 @Bean 不迁。把 FrameworkConfig 原文 + 剥减决策记入 SUMMARY。
  </action>
  <verify>
    <automated>ls zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/config/{JinXinConfig,EweChatConfig,OcrConfig,WxCarrierConfig}.java 2>&1 ; grep -n "ewechat.config" zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/config/WxCarrierConfig.java ; echo "--- no verbatim FrameworkConfig ---" ; ls zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/config/FrameworkConfig.java 2>/dev/null && echo "ERROR: FrameworkConfig migrated" || echo "FrameworkConfig NOT migrated (correct)"</automated>
  </verify>
  <acceptance_criteria>
    - JinXinConfig.java(prefix jinxin)、EweChatConfig.java(@Data POJO)、OcrConfig.java(prefix aliyun.ocr)三 POJO 就位
    - WxCarrierConfig.java 存在,含唯一 @Bean eweChatConfig()(prefix ewechat.config)
    - enclave **不**存在 FrameworkConfig.java(未 verbatim 迁)
    - 单体无重复外部 SDK @Bean(authOpenFacade/fileRemote/pushClientHttp 各只 1 个)
    - SUMMARY 记录 6 个 DROP @Bean 清单 + 理由
  </acceptance_criteria>
  <done>3 config POJO + WxCarrierConfig 就位,FrameworkConfig 未 verbatim 迁</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WX 横切 bean → 全局 Spring context | @ControllerAdvice/@Aspect/WebMvcConfigurer 是全局生效 bean,误限域会改写主域行为 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | GlobalExceptionHandler 改写主域错误响应 | mitigate | basePackages 限 com.spt.bas.purchase.wx.server(D-P5-02),单体零既有 @ControllerAdvice |
| T-05-05 | Denial of Service | ServiceAop pointcut 过宽双增强主域 | mitigate | pointcut 收窄到 wx.server.service,与 spt-tools ServiceAop 域分离(D-P5-03) |
| T-05-06 | Tampering | 重复 multipartConfigElement @Bean 崩启动 | mitigate | WebAppConfig 仅贡献 CORS,不重复 @Bean(D-P5-01) |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | 纯 Java,无包管理器 |
</threat_model>

<verification>
横切 bean 涉及全局 context,本 plan 编译由 05-06 门把关。静态自检:
```bash
grep -rn "@ControllerAdvice" zgbas-system/src/main/java | wc -l           # 期望 1(限域)
grep -rn "@Bean.*taskExecutor\|public Executor taskExecutor" zgbas-system/src/main/java | wc -l  # 期望 1(单体 ScheduleConfig,无重复)
```
</verification>

<success_criteria>
1. GlobalExceptionHandler 限 basePackages(D-P5-02);SecurityException 替换 stub;exception/ 3 类就位
2. WebAppConfig CORS 就位,无重复 multipart/转换器(D-P5-01)
3. ServiceAop 由 WxAopConfig 显式注册,pointcut 自限 WX(D-P5-03)
4. WxCarrierConfig 仅 eweChatConfig() @Bean,FrameworkConfig 未 verbatim 迁(D-P5-15)
</success_criteria>

<output>
Create `.planning/phases/05-carrier-layer/05-03-SUMMARY.md` when done
</output>
