---
plan_id: 05-06
phase: 5
wave: 5
title: 编译门 — mvn compile -pl zgbas-system 零 [ERROR](SC#3 集成验证)
objective: 承托层全量迁入后,以 `JAVA_HOME=Corretto-1.8 mvn compile -pl zgbas-system` 零 `[ERROR]` 为权威门,验证 SC#1(逐类可独立解析无孤立 import)+ SC#3(承托层独立编译通过);并跑 stub 替换 diff + 孤立 import 扫描兜底
depends_on:
  - 05-01
  - 05-02
  - 05-03
  - 05-04
  - 05-05
files_modified: []
autonomous: true
requirements_addressed:
  - WX-BFF-03

must_haves:
  truths:
    - "`JAVA_HOME=/Library/Java/JavaVirtualMachines/amazon-corretto-8.jdk/Contents/Home mvn compile -pl zgbas-system --settings /Users/alan/App/apache-maven-3.8.6/zg_settings.xml` 输出零 `[ERROR]` 行(SC#3)"
    - "承托类间引用闭环,无对 Phase 6 service / Phase 7 controller 的孤立 import(除显式排除的 EweChatApi/JinXinApi/PurchaseCommand→P6)"
    - "D-P5-14 六个 stub(UserInfoVo/ApiResult/BaseException/SecurityException/ResponseUtil/UserContext)与源 `diff` 一致"
  artifacts:
    - path: ".planning/phases/05-carrier-layer/05-06-SUMMARY.md"
      provides: "编译门通过证据 + ERROR 清零过程"
      contains: "BUILD SUCCESS"
  key_links:
    - from: "05-01..05-05 全部承托类"
      to: "zgbas-system 编译单元"
      via: "mvn compile -pl zgbas-system"
      pattern: "BUILD SUCCESS"
---

<objective>
承托层编译门:Phase 5 所有承托类已落位(05-01..05-05),本 plan 做集成编译验证 + 兜底静态扫描。这是 SC#3 的权威门,也是 Phase 6 service 迁入的编译前提。承托层迁移期残留的跨 plan 编译错误必须在此清零。

Purpose: 承托层零错编译是 Phase 6/7 的底座 —— 若本阶段残留 ERROR,Phase 6 service 迁入会被承托层错误淹没。
Output: `mvn compile -pl zgbas-system` 零 `[ERROR]`,SUMMARY 记录清零过程。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-carrier-layer/05-CONTEXT.md
@.planning/phases/05-carrier-layer/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: stub 替换 diff 兜底 + 孤立 import 扫描</name>
  <files></files>
  <read_first>
    - D-P5-14 六个 stub:enclave .../wx/server/{common/ApiResult,common/BaseException,exception/SecurityException,util/ResponseUtil,util/UserContext,vo/UserInfoVo}.java
    - 对应源文件 .../basWx/purchase-server/.../wx/server/ 同名类
    - 05-RESEARCH.md §Q6(显式排除 P6 的三类:EweChatApi/JinXinApi/PurchaseCommand)
  </read_first>
  <action>
    1. 对 D-P5-14 六个 stub 逐个 `diff` enclave vs 源,确认已替换为实测实现(非 stub 占位)。残留 stub 则回 05-01/02/03 修。
    2. 孤立 import 扫描:在 zgbas-system 内 grep `import com.spt.bas.purchase.wx.server.*`,确认承托类间引用闭环。允许的"悬空"仅限显式排除类(EweChatApi→IBuyMessageService[P6]、JinXinApi→cfca[P6]、PurchaseCommand→P6 service[未迁])。若发现承托类 import 了未迁且非排除清单的类,记录并回对应 plan 补迁。
    3. 确认显式排除类**未**出现在 enclave(EweChatApi/JinXinApi/PurchaseCommand 应不在 Phase 5 enclave)。
  </action>
  <verify>
    <automated>echo "--- exclusions NOT migrated ---" ; ls zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/{ewechat/EweChatApi,service/impl/JinXinApi,command/PurchaseCommand}.java 2>/dev/null && echo "ERROR: excluded class migrated" || echo "excluded classes correctly absent (P6)" ; echo "--- stub diff (expect empty) ---" ; for c in common/ApiResult common/BaseException exception/SecurityException util/ResponseUtil util/UserContext vo/UserInfoVo; do diff &lt;(cat zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/$c.java) &lt;(cat /Users/alan/WorkSpace/IDEA/zgbas/basWx/purchase-server/src/main/java/com/spt/bas/purchase/wx/server/$c.java) >/dev/null 2>&1 && echo "$c OK" || echo "$c DIFFERS"; done</automated>
  </verify>
  <acceptance_criteria>
    - 六个 stub 与源 diff 全部 OK(无残留 stub)
    - EweChatApi/JinXinApi/PurchaseCommand 不在 Phase 5 enclave(正确排除 P6)
    - 无非排除清单的孤立 import(承托类引用闭环)
  </acceptance_criteria>
  <done>stub 替换确认 + 孤立 import 清零</done>
</task>

<task type="auto">
  <name>Task 2: 编译门 — mvn compile -pl zgbas-system 零 [ERROR](SC#3)</name>
  <files></files>
  <read_first>
    - 05-01..05-05 SUMMARY(已迁类清单 + 已知跨 plan 残留)
    - zgbas-system/pom.xml(确认 P4 依赖 Redis/miniapp SDK 就位)
  </read_first>
  <action>
    运行权威编译门(JDK 1.8 + 私服 settings):
    ```
    JAVA_HOME=/Library/Java/JavaVirtualMachines/amazon-corretto-8.jdk/Contents/Home \
      mvn compile -pl zgbas-system \
      --settings /Users/alan/App/apache-maven-3.8.6/zg_settings.xml
    ```
    locale 无关 grep `^\[ERROR\]`(中文 locale 可能"找不到符号")。逐个修掉残留 ERROR(承托类引用 P6 service 的 → 确认在排除清单;真实遗漏的 → 回对应 plan 补迁;签名不匹配的 → 对齐源)。记录 ERROR 清零过程到 SUMMARY。
    期望终态:零 `[ERROR]` 行,`BUILD SUCCESS`。
  </action>
  <verify>
    <automated>JAVA_HOME=/Library/Java/JavaVirtualMachines/amazon-corretto-8.jdk/Contents/Home mvn compile -pl zgbas-system --settings /Users/alan/App/apache-maven-3.8.6/zg_settings.xml 2>&1 | tee /tmp/p5-compile.log | grep -E "^\[ERROR\]|BUILD SUCCESS|BUILD FAILURE" | head -40 ; echo "ERROR_COUNT=$(grep -cE '^\[ERROR\]' /tmp/p5-compile.log)"</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/p5-compile.log` 含 `BUILD SUCCESS`
    - `grep -cE '^\[ERROR\]' /tmp/p5-compile.log` = 0
    - SUMMARY 记录初始 ERROR 数 → 清零过程 → 终态零错
  </acceptance_criteria>
  <done>zgbas-system 编译零 [ERROR],承托层独立编译通过(SC#3)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 承托层 → zgbas-system 编译单元 | 编译门验证类间引用闭环,无悬空依赖 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Tampering | 承托类引用未迁 P6/P7 类致编译假绿 | mitigate | 孤立 import 扫描 + 显式排除清单(EweChatApi/JinXinApi/PurchaseCommand→P6),编译门零 ERROR 为权威 |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | 仅 mvn compile,无包管理器 |
</threat_model>

<verification>
权威门 = Task 2 的 `mvn compile -pl zgbas-system` 零 `[ERROR]` + BUILD SUCCESS。locale 无关 grep。
</verification>

<success_criteria>
1. `mvn compile -pl zgbas-system` 零 `[ERROR]` + BUILD SUCCESS(SC#3)
2. D-P5-14 六个 stub 与源 diff 一致(SC#1 子项)
3. 无非排除清单的孤立 import;EweChatApi/JinXinApi/PurchaseCommand 正确排除出 Phase 5
4. SUMMARY 记录 ERROR 清零过程
</success_criteria>

<output>
Create `.planning/phases/05-carrier-layer/05-06-SUMMARY.md` when done
</output>
