---
plan_id: 05-01
phase: 5
wave: 1
title: 承托底座 I — payload + vo + enums + common(含 stub 替换)
objective: 把 basWx payload(22)/vo(18)/enums(1)/common(8) verbatim 落入 zgbas-system 的 com.spt.bas.purchase.wx.server.* 包飞地,并把 P4 预置 stub(ApiResult/BaseException/UserInfoVo)用源实测实现替换,为后续 util/横切/cache 提供自洽的 POJO/常量底座
depends_on: []
files_modified:
  - zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/payload/
  - zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/vo/
  - zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/enums/
  - zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/common/
autonomous: true
requirements_addressed:
  - WX-BFF-03

must_haves:
  truths:
    - "payload/ 22 个 Request 类全部就位于 zgbas-system .../wx/server/payload/,包名 com.spt.bas.purchase.wx.server.payload 不变"
    - "vo/ 18 个类全部就位,UserInfoVo 以源实测实现替换 P4 stub(D-P5-14)"
    - "enums/MessageEnums 就位"
    - "common/ 8 个类就位,ApiResult + BaseException 以源实测实现替换 P4 stub(D-P5-14);Status 核实 stub-vs-real 后定夺"
    - "本批类间引用闭环,不引入对 Phase 6 service / Phase 7 controller 的 import"
  artifacts:
    - path: "zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/common/ApiResult.java"
      provides: "WX 统一返回信封(源实测实现)"
      contains: "class ApiResult"
    - path: "zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/payload/LoginRequest.java"
      provides: "登录请求体"
      contains: "class LoginRequest"
  key_links:
    - from: "common/ApiResult.java(源实测)"
      to: "WX controller 错误信封(Phase 7)"
      via: "GlobalExceptionHandler(D-P5-02, plan 05-03)返回 ApiResult"
      pattern: "ApiResult"
---

<objective>
承托层第一波:迁入所有"叶节点"承托类 —— payload(请求体 POJO)、vo(值对象)、enums(枚举)、common(常量/信封/基异常)。这些类几乎无跨层依赖,是 util/横切 bean/cache 的编译前提。同时把 Phase 4 为打通编译预置的 3 个 stub(ApiResult / BaseException / UserInfoVo)用源实测实现替换(D-P5-14),消除 stub 形态。

Purpose: 没有这些 POJO,util 的 UserHelper/UserContext、cache 的返回类型、Phase 6 service 的方法签名都无法解析。
Output: 49 个承托类就位于包飞地,3 个 stub 替换为实测实现。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-carrier-layer/05-CONTEXT.md
@.planning/phases/05-carrier-layer/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: verbatim 迁入 payload(22)+ enums(1)+ vo(18,UserInfoVo 替换 stub)</name>
  <files>zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/{payload,vo,enums}/</files>
  <read_first>
    - 源目录 /Users/alan/WorkSpace/IDEA/zgbas/basWx/purchase-server/src/main/java/com/spt/bas/purchase/wx/server/{payload,vo,enums}/ — 逐类读取,确认包声明为 com.spt.bas.purchase.wx.server.*
    - zgbas-system 现有 enclave:zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/vo/UserInfoVo.java(P4 stub,将被覆盖)
    - 05-RESEARCH.md §Q6 payload/vo/enums inventory(确认 22/18/1 清单 + 无 P6/P7 依赖)
  </read_first>
  <action>
    把源 payload/(22 类)、enums/MessageEnums、vo/(18 类)按原文件名、原包名(com.spt.bas.purchase.wx.server.*)verbatim 复制到 zgbas-system 对应包目录:

    payload(22):ApplyIouRequest, BaseRequest, BrandRequest, CfcaRequest, CommonRequest, CompanyBaseInfoRequest, CompanyInfoRequest, ContractNoRequest, CustomRequest, DeliveryOutNoRequest, DepositRequest, EntrustRequest, FeedbackRequest, LoginRequest, PayBankInfoRequest, QuotaTestRequest, ServiceOpeningInfoRequest, SupplyInfoRequest, TempSaveRequest, UploadBase64Request, WarehouseRequest, WxLoginRequest

    vo(18):ApplyIouVo, BaseVo, CfcaVo, CompanyVo, CustomVo, EweChatAccessTokenCallBackDto, IdentityCardVo, JinXinAuthFaceVo, LicenseVo, OcrLicenseVo, QuotaInfoVo, ServiceOpeningInfoVo, SupplyInfoVo, TemplateCardMessage, UploadFileVo, UserChangeVo, **UserInfoVo(覆盖 P4 stub,源实测实现)**, ZyVehicleTrackReqVo

    enums(1):MessageEnums

    规则:① 包名逐字保留(方案1 verbatim / D-06);② UserInfoVo.java 覆盖 enclave 中的 P4 stub(D-P5-14);③ 其余 vo 若 enclave 已有同名则覆盖;④ 不修改任何 import 指向 P6 service / P7 controller —— 若某 vo(如 JinXinAuthFaceVo/OcrLicenseVo)import 了 service/controller,记入 SUMMARY 待 P6/P7 处理(但 RESEARCH 已确认 vo 为纯值对象,不应有此类 import)。
  </action>
  <verify>
    <automated>ls zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/payload/*.java | wc -l ; ls zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/vo/*.java | wc -l ; ls zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/enums/*.java | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - payload/ 目录 .java 文件数 = 22
    - vo/ 目录 .java 文件数 ≥ 18(UserInfoVo 已覆盖,非 stub)
    - enums/ 含 MessageEnums.java
    - 每个文件首行 `package com.spt.bas.purchase.wx.server.{payload|vo|enums};`
    - UserInfoVo.java 与源文件 `diff` 一致(非 stub 占位)
  </acceptance_criteria>
  <done>41 类(22+18+1)就位,UserInfoVo 替换 stub</done>
</task>

<task type="auto">
  <name>Task 2: 迁入 common(8)+ 替换 ApiResult/BaseException stub + Status stub 核实</name>
  <files>zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/common/</files>
  <read_first>
    - 源 /Users/alan/WorkSpace/IDEA/zgbas/basWx/purchase-server/src/main/java/com/spt/bas/purchase/wx/server/common/ — 全 8 类
    - enclave 现有 stub:zgbas-system/.../wx/server/common/ApiResult.java、BaseException.java、Status.java(P4/P3 预置)
    - 05-RESEARCH.md §Q6 common inventory + §D-P5-14(stub 替换清单)
    - 05-CONTEXT.md §D-P5-14(P4 预置 stub 用源实测实现替换)
  </read_first>
  <action>
    把源 common/ 全 8 类 verbatim 落入 enclave .../wx/server/common/:
    ApiResult(**覆盖 P4 stub,源实测实现,D-P5-14**)、BasConstants、BaseException(**覆盖 P4 stub,源实测实现,D-P5-14**)、CardType、Constant、CustomSetting、InfoStep、Status

    Status 处置:enclave 已有 Status.java —— `diff` 源 vs enclave。若 enclave 为 stub(字段缺失/方法空),用源实测覆盖;若 enclave 已是源实测一致(Phase 3 可能已迁),保留不动。把结论写入 SUMMARY。

    规则:① ApiResult/BaseException 必须以源实测实现覆盖 stub(D-P5-14);② 包名 com.spt.bas.purchase.wx.server.common 保留;③ ApiResult 的 swagger import 若源中已注释则保持注释(springfox 不在单体 classpath,RESEARCH Q2)。
  </action>
  <verify>
    <automated>grep -c "class ApiResult" zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/common/ApiResult.java ; grep -c "class BaseException" zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/common/BaseException.java ; ls zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/common/*.java | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - common/ 目录 .java 文件数 = 8
    - ApiResult.java `diff` 与源一致(非 stub,含源实测字段/方法)
    - BaseException.java `diff` 与源一致(非 stub)
    - Status.java 处置结论记入 SUMMARY(覆盖 or 保留 + 理由)
    - 每个文件首行 `package com.spt.bas.purchase.wx.server.common;`
  </acceptance_criteria>
  <done>common 8 类就位,ApiResult/BaseException stub 替换完成</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 源码 → zgbas-system enclave | verbatim 复制,包名保留,无外部网络调用 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | verbatim 复制保真 | mitigate | 逐类 `diff` 源 vs enclave,UserInfoVo/ApiResult/BaseException 必须与源一致 |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | 本计划纯 Java 源码复制,无包管理器操作 |
</threat_model>

<verification>
本 plan 类为叶节点 POJO,跨 plan 引用(util/common/横切)在 phase 完成前可能残留编译错误 —— 以 05-06 编译门为权威。本 plan 自检:
```bash
ls zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/{payload,vo,enums,common}/*.java | wc -l   # 期望 ≥ 49
```
</verification>

<success_criteria>
1. payload(22)+ vo(18,UserInfoVo 替换 stub)+ enums(1)+ common(8,ApiResult/BaseException 替换 stub)= 49 类就位于包飞地
2. 3 个 P4 stub(UserInfoVo/ApiResult/BaseException)以源实测实现替换(D-P5-14)
3. 所有文件包名 com.spt.bas.purchase.wx.server.* 保留
</success_criteria>

<output>
Create `.planning/phases/05-carrier-layer/05-01-SUMMARY.md` when done
</output>
