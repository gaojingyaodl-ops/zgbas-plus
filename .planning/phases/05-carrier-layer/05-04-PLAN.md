---
plan_id: 05-04
phase: 5
wave: 3
title: 外部 wrapper + cache + 明文密钥落 yml(灰区 B)— cache(BsDictUtil/RedisCache)+ OcrUtils/OcrHelper + ewechat/aliyun.ocr/jinxin 明文键 + rotate-credentials todo 登记
objective: 迁入 cache/(BsDictUtil WX 版 + RedisCache)与 OCR 外部 wrapper(OcrUtils/OcrHelper,维持 HTTP 边界 D-16/17),把 WX 专用密钥(ewechat.config/aliyun.ocr/jinxin)以明文实测值落入 application-dev.yml(D-P5-05),并在 rotate-leaked-prod-credentials todo 登记 EweChat/Aliyun OCR/JinXin 三组新泄漏密钥
depends_on:
  - 05-03
files_modified:
  - zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/cache/BsDictUtil.java
  - zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/cache/RedisCache.java
  - zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/util/OcrUtils.java
  - zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/util/OcrHelper.java
  - zgbas-admin/src/main/resources/application-dev.yml
  - .planning/todos/pending/rotate-leaked-prod-credentials.md
autonomous: true
requirements_addressed:
  - WX-BFF-03

must_haves:
  truths:
    - "cache/BsDictUtil(WX 版 com.spt.bas.purchase.wx.server.cache.BsDictUtil)+ RedisCache 就位,与单体 bas.server.cache.BsDictUtil 同名不同包共存"
    - "util/OcrUtils + OcrHelper 就位,远端 OCR 服务维持 HTTP 边界(D-16/17,用单体已有 HTTP 工具/HttpUtils)"
    - "application-dev.yml 含 ewechat.config.corpid/corpsecret/agentid/messageUrl + aliyun.ocr.appcode/host/*Url + jinxin.host/livingUrl/merchNo/merchKey 等明文实测值(D-P5-05)"
    - "rotate-leaked-prod-credentials todo 新增登记 EweChat corpid/corpsecret、Aliyun OCR appcode、JinXin merchKey 三组"
    - "不迁 EweChatApi(→P6,D-P5-18)、JinXinApi(→P6,D-P5-08)"
  artifacts:
    - path: "zgbas-admin/src/main/resources/application-dev.yml"
      provides: "WX 外部集成明文配置"
      contains: "ewechat.config"
    - path: "zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/cache/RedisCache.java"
      provides: "WX Redis 缓存工具"
      contains: "class RedisCache"
  key_links:
    - from: "application-dev.yml ewechat.config.*"
      to: "WxCarrierConfig.eweChatConfig() @Bean(05-03)"
      via: "@ConfigurationProperties(prefix=ewechat.config)"
      pattern: "ewechat\\.config"
    - from: "cache/BsDictUtil.init()"
      to: "ApplicationStartup(05-05)"
      via: "启动期 WX 字典缓存初始化"
      pattern: "BsDictUtil"
---

<objective>
灰区 B 落地:迁入缓存工具 + OCR 外部 wrapper,把 WX 外部集成密钥按用户锁定的明文策略(D-P4/D-P5-05,覆盖安全铁律)直进 dev yml,并显式登记新增泄漏密钥到 rotate-credentials todo。

Purpose: EweChatApi(Phase 6)依赖 RedisCache + EweChatConfig;OcrUtils 依赖 OcrConfig(05-03);启动期 BsDictUtil.init(05-05)依赖 cache 就位。密钥不落 yml 则 EweChatConfig @Bean 字段全 null。
Output: cache 2 类 + Ocr wrapper 2 类就位,dev yml 含三组 WX 明文配置,rotate todo 更新。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-carrier-layer/05-CONTEXT.md
@.planning/phases/05-carrier-layer/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 迁入 cache/(BsDictUtil WX 版 + RedisCache)</name>
  <files>zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/cache/</files>
  <read_first>
    - 源 .../cache/BsDictUtil.java、RedisCache.java
    - 单体同名对照:zgbas-system/src/main/java/com/spt/bas/server/cache/BsDictUtil.java(确认同名不同包,WX 版是独立类)
    - P4 已迁 RedisConfig(enclave .../wx/server/config/RedisConfig.java)— RedisCache 依赖它
    - 05-RESEARCH.md §Q6 cache 备注
  </read_first>
  <action>
    verbatim 迁入 cache/ 2 类到 enclave .../wx/server/cache/,包名 com.spt.bas.purchase.wx.server.cache 保留:
    - **BsDictUtil**(WX 版字典缓存工具;注意与单体 `com.spt.bas.server.cache.BsDictUtil` 同名不同包 —— 两类共存,编译期不冲突)
    - **RedisCache**(依赖 P4 RedisConfig 注入的 RedisTemplate)
    规则:包名保留;若 BsDictUtil 引用 WX entity/dao(Phase 3 已迁)则正常;不引入 P6 service import。
  </action>
  <verify>
    <automated>ls zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/cache/*.java | wc -l ; head -1 zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/cache/BsDictUtil.java</automated>
  </verify>
  <acceptance_criteria>
    - cache/ 含 BsDictUtil.java + RedisCache.java(2 类)
    - 两类首行 `package com.spt.bas.purchase.wx.server.cache;`
    - 单体 bas.server.cache.BsDictUtil 仍在(未被覆盖,两类共存)
  </acceptance_criteria>
  <done>cache 2 类就位,与单体同名类共存</done>
</task>

<task type="auto">
  <name>Task 2: 迁入 OCR 外部 wrapper(util/OcrUtils + OcrHelper,维持 HTTP 边界 D-16/17)</name>
  <files>zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/util/{OcrUtils,OcrHelper}.java</files>
  <read_first>
    - 源 .../util/OcrUtils.java、OcrHelper.java —— 确认远端 OCR 调用方式(HttpUtils.doPost + OcrConfig URL/key)
    - 05-03 迁入的 OcrConfig(prefix aliyun.ocr)
    - 05-02 迁入的 HttpUtils(若死码未迁,则 OcrUtils 须改用单体 spt-tools HTTPUtility)
    - 05-RESEARCH.md §D-P5-08(外部 wrapper 维持 HTTP 边界)
  </read_first>
  <action>
    verbatim 迁入 OcrUtils + OcrHelper 到 enclave .../wx/server/util/,包名保留。远端 OCR 服务维持 HTTP 边界(D-16/D-17)—— 不内联 OCR 服务,保留原 HTTP 调用方式(HttpUtils.doPost 或单体 HTTPUtility,依 05-02 HttpUtils 死码结论)。
    若 05-02 核实 HttpUtils 死码未迁,则把 OcrUtils/OcrHelper 中 `HttpUtils.doPost` 调用改为单体已有的 spt-tools HTTP 工具(保持 HTTP 边界 + 行为等价),记入 SUMMARY。
  </action>
  <verify>
    <automated>ls zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/util/Ocr*.java ; grep -n "import\| doPost\|HttpUtility\|HttpUtils" zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/util/OcrUtils.java | head</automated>
  </verify>
  <acceptance_criteria>
    - OcrUtils.java + OcrHelper.java 就位
    - 远端 OCR 服务调用维持 HTTP 边界(无内联 OCR 逻辑)
    - 若改用 HTTPUtility,SUMMARY 记录改动 + 行为等价说明
  </acceptance_criteria>
  <done>OcrUtils/OcrHelper 就位,OCR 远端维持 HTTP 边界</done>
</task>

<task type="auto">
  <name>Task 3: application-dev.yml — 追加 ewechat/aliyun.ocr/jinxin 明文实测键(D-P5-05)</name>
  <files>zgbas-admin/src/main/resources/application-dev.yml</files>
  <read_first>
    - zgbas-admin/src/main/resources/application-dev.yml — 确认现有内容(目前仅 datasource 块为主),确认追加位置(文件末尾)
    - 源实测值见 05-RESEARCH.md §Q4(ewechat.config/aliyun.ocr/jinxin 全量键值表)
    - 05-03 迁入的 config POJO 前缀(EweChatConfig=ewechat.config,OcrConfig=aliyun.ocr,JinXinConfig=jinxin)
  </read_first>
  <action>
    在 application-dev.yml 末尾追加三个配置块(YAML 缩进风格与文件一致,头部注释标注 Phase 5 D-P5-05),键值用源实测值(05-RESEARCH.md §Q4):

    块1 — ewechat(对应 WxCarrierConfig @Bean + EweChatConfig):
    - ewechat.config.corpid: ww67315c53a72c991f
    - ewechat.config.corpsecret: gn_IIS0Sv-dvJ9MnGwZSH0m_zVZ_FCHk0mEIVMS6iZs
    - ewechat.config.agentid: 1000007
    - ewechat.config.messageUrl: http://h5biztest.tosupply.cn/dev-api/initLogin

    块2 — aliyun.ocr(对应 OcrConfig):
    - aliyun.ocr.appcode: 52e1a1531c5a4e30969d93c16eb1251f
    - aliyun.ocr.host: https://cardpack.market.alicloudapi.com
    - aliyun.ocr.businessLicenseUrl: /rest/160601/ocr/ocr_business_license.json
    - aliyun.ocr.idCardUrl: /rest/160601/ocr/ocr_idcard.json
    - aliyun.ocr.vehicleUrl: /rest/160601/ocr/ocr_vehicle.json
    - aliyun.ocr.driverLicenseUrl: /rest/160601/ocr/ocr_driver_license.json
    - aliyun.ocr.householdRegisterUrl: /api/predict/ocr_household_register
    - aliyun.ocr.bankCardUrl: /rest/160601/ocr/ocr_bank_card.json
    - aliyun.ocr.passportUrl: /rest/160601/ocr/ocr_passport.json

    块3 — jinxin(对应 JinXinConfig,消费者 JinXinApi 留 Phase 6,但配置先就位):
    - jinxin.host: https://210.74.42.15:443
    - jinxin.livingUrl: /UAServer/Authenticate
    - jinxin.merchNo: 0000000000036500
    - jinxin.merchKey: LaYeIemv2WEY8Z7h
    - (merchRsaPrivateKey/keyStorePath/keyStorePassword/trustStorePath/trustStorePassword/connectTimeout/readTimeout/tmpFilePath:从源 application.properties:71-85 区段补全实测值)

    规则:① 不修改已有任何 key(datasource/spring.redis/wx.miniapp/jwt.config 等保持);② 仅 ADD WX 专用键;③ D-P5-05 明文策略(用户锁定,覆盖安全铁律)。
  </action>
  <verify>
    <automated>grep -cE "ewechat\.config|aliyun\.ocr|jinxin\." zgbas-admin/src/main/resources/application-dev.yml ; grep -n "ww67315c53a72c991f\|52e1a1531c5a4e30969d93c16eb1251f\|LaYeIemv2WEY8Z7h" zgbas-admin/src/main/resources/application-dev.yml</automated>
  </verify>
  <acceptance_criteria>
    - application-dev.yml 含 ewechat.config.corpid=ww67315c53a72c991f、aliyun.ocr.appcode=52e1a1531c5a4e30969d93c16eb1251f、jinxin.merchKey=LaYeIemv2WEY8Z7h
    - 不含重复的既有 key(datasource/spring.redis/wx.miniapp/jwt.config 不变)
    - YAML 可被 snakeyaml 解析(后续 mvn compile 间接验证)
  </acceptance_criteria>
  <done>三组 WX 明文配置落 dev yml,实测值就位</done>
</task>

<task type="auto">
  <name>Task 4: rotate-leaked-prod-credentials todo 登记 EweChat/Aliyun OCR/JinXin 新密钥</name>
  <files>.planning/todos/pending/rotate-leaked-prod-credentials.md</files>
  <read_first>
    - .planning/todos/pending/rotate-leaked-prod-credentials.md — 读取现有内容(v1.0 遗留 high-priority todo)
    - 05-CONTEXT.md §<deferred> Reviewed Todos(rotate-credentials 非 Phase 5 scope 但新增密钥加重债务)
    - 05-RESEARCH.md §Q4 安全债备注
  </read_first>
  <action>
    在 rotate-leaked-prod-credentials.md 追加(不删除已有条目)Phase 5 新增的明文泄漏密钥登记:
    - **EweChat**:corpid `ww67315c53a72c991f` + corpsecret `gn_IIS0Sv-...`(application-dev.yml)
    - **Aliyun OCR**:appcode `52e1a1531c5a4e30969d93c16eb1251f`(application-dev.yml)
    - **JinXin**:merchKey `LaYeIemv2WEY8Z7h` + 证书密码(application-dev.yml)
    每条标注:来源 Phase 5 / D-P5-05 / 落点 application-dev.yml / 轮换优先级。说明:用户锁定明文策略(D-P4/D-P5-05),轮换本身非承托层 scope,但新增真实密钥入 git 须显式登记跟进。
  </action>
  <verify>
    <automated>grep -c "EweChat\|Aliyun OCR\|JinXin\|Phase 5" .planning/todos/pending/rotate-leaked-prod-credentials.md</automated>
  </verify>
  <acceptance_criteria>
    - rotate-leaked-prod-credentials.md 含 EweChat/Aliyun OCR/JinXin 三组 Phase 5 新登记
    - 既有条目未删除(只追加)
    - 每条标注来源 Phase 5 + D-P5-05 + 落点
  </acceptance_criteria>
  <done>三组新泄漏密钥登记入 rotate todo</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WX → 远端 OCR / 企业微信 / 金信 | 维持 HTTP 边界,只迁本地 wrapper(D-16/17) |
| 明文密钥 → git(application-dev.yml) | 真实生产密钥入版本库 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Information Disclosure | ewechat/aliyun.ocr/jinxin 明文密钥入 git | accept | 用户锁定 D-P5-05 明文策略(覆盖安全铁律);rotate-credentials todo 登记 + 轮换跟进(本 plan Task 4) |
| T-05-08 | Spoofing | 远端 OCR/企业微信调用凭据 | mitigate | 凭据走 @ConfigurationProperties 注入(D-P5-05),不硬编码在 wrapper 源码;wrapper 仅迁本地 HTTP 调用 |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | 纯 Java + yml,无包管理器 |
</threat_model>

<verification>
```bash
grep -cE "ewechat\.config|aliyun\.ocr|jinxin\." zgbas-admin/src/main/resources/application-dev.yml   # 期望 ≥ 15
ls zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/{cache,util}/ 2>/dev/null | grep -E "BsDictUtil|RedisCache|Ocr"
```
</verification>

<success_criteria>
1. cache(BsDictUtil WX 版 + RedisCache)+ OcrUtils/OcrHelper 就位,OCR 远端维持 HTTP 边界
2. application-dev.yml 含 ewechat.config/aliyun.ocr/jinxin 三组明文实测键(D-P5-05)
3. rotate-leaked-prod-credentials todo 登记 EweChat/Aliyun OCR/JinXin 三组新密钥
4. EweChatApi/JinXinApi 未迁(留 Phase 6)
</success_criteria>

<output>
Create `.planning/phases/05-carrier-layer/05-04-SUMMARY.md` when done
</output>
