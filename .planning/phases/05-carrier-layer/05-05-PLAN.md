---
plan_id: 05-05
phase: 5
wave: 4
title: 启动接线 + D-15a/b inventory checklist(灰区 C)— ApplicationStartup 追加 WX BsDictUtil.init + RequestListener 迁入 + 五类承托 inventory 盘点
objective: 完成启动接线:① 单体 ApplicationStartup 追加 WX 版 BsDictUtil.init()(全限定消歧,不重复 DictUtil/executor,D-P5-09,直击 Phase 3 登录缺口);② RequestListener verbatim 迁入(UserContext ThreadLocal 清理,D-P5-10);③ 产出 D-15a/b 五类承托 inventory checklist(SC#4 交付物)
depends_on:
  - 05-03
  - 05-04
files_modified:
  - zgbas-system/src/main/java/com/spt/bas/server/listener/ApplicationStartup.java
  - zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/listener/RequestListener.java
  - .planning/phases/05-carrier-layer/05-INVENTORY-CHECKLIST.md
autonomous: true
requirements_addressed:
  - WX-BFF-03

must_haves:
  truths:
    - "单体 bas.server.listener.ApplicationStartup 含 WX 版 com.spt.bas.purchase.wx.server.cache.BsDictUtil.init() 调用(全限定 import 消歧),且不重复 DictUtil.init / new Thread(executor)(D-P5-09)"
    - "listener/RequestListener(@Component ServletRequestListener)verbatim 迁入 enclave,requestDestroyed → UserContext.removeUser(D-P5-10)"
    - "单体无第二个 ApplicationReadyEvent 监听器跑 DictUtil/executor(WX ApplicationStartup 不 verbatim 迁,并入单体)"
    - "PurchaseCommand 不在本 plan(D-P5-13 修正→Phase 6,依赖 P6 service + xxl-job scrub)"
    - "05-INVENTORY-CHECKLIST.md 产出,五类(return envelope/exception advice/user-context/auth helper/serialization-upload-wrapper)逐项 source→consumer→must-port? 盘点完成(SC#4)"
  artifacts:
    - path: "zgbas-system/src/main/java/com/spt/bas/server/listener/ApplicationStartup.java"
      provides: "含 WX 字典缓存初始化的启动监听器"
      contains: "purchase.wx.server.cache.BsDictUtil"
    - path: ".planning/phases/05-carrier-layer/05-INVENTORY-CHECKLIST.md"
      provides: "D-15a/b 五类承托 inventory"
      contains: "must-port"
  key_links:
    - from: "ApplicationStartup.onApplicationEvent"
      to: "WX BsDictUtil.init()(05-04 cache)"
      via: "ApplicationReadyEvent 启动期初始化 WX 字典缓存"
      pattern: "BsDictUtil"
    - from: "RequestListener.requestDestroyed"
      to: "util/UserContext.removeUser(05-02)"
      via: "ServletRequestListener 清理 ThreadLocal"
      pattern: "removeUser"
---

<objective>
灰区 C 收尾:启动接线 + inventory 交付。ApplicationStartup 合并是 Phase 3 登录缺口(memory authsdk-static-cache-init-gap)的同源修复 —— WX `/wx/*` 字典缓存未初始化的根因之一。RequestListener 防 UserContext ThreadLocal 内存泄漏/身份串线程。D-15a/b inventory 是 SC#4 的硬性交付物。

Purpose: 不并入 WX BsDictUtil.init → `/wx/*` 字典缓存空 → WX 业务报错;不迁 RequestListener → UserContext ThreadLocal 不清理 → 内存泄漏 + 身份串线程。
Output: 启动接线完成,五类 inventory 盘点交付。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-carrier-layer/05-CONTEXT.md
@.planning/phases/05-carrier-layer/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ApplicationStartup 追加 WX BsDictUtil.init(D-P5-09,非 verbatim 并入)</name>
  <files>zgbas-system/src/main/java/com/spt/bas/server/listener/ApplicationStartup.java</files>
  <read_first>
    - 单体 zgbas-system/src/main/java/com/spt/bas/server/listener/ApplicationStartup.java — 已含 DictUtil.init/ConfigUtil.init/BsDictUtil.init(单体版)/多 cache init/new Thread(executor)
    - 源 .../listener/ApplicationStartup.java(WX 版,DictUtil.init + BsDictUtil.init(WX 版)+ new Thread(executor))
    - 05-04 迁入的 cache/BsDictUtil(WX 版 com.spt.bas.purchase.wx.server.cache.BsDictUtil)
    - 05-RESEARCH.md §Q7 ApplicationStartup 精确化(单体已调自己的 BsDictUtil,需追加 WX 版,全限定消歧)
  </read_first>
  <action>
    在单体 ApplicationStartup.onApplicationEvent 的 init 序列中**追加一行** WX 版字典缓存初始化:
    - 新增 import:`import com.spt.bas.purchase.wx.server.cache.BsDictUtil;` **冲突消歧**:单体已有 `import com.spt.bas.server.cache.BsDictUtil;`(同名)。用全限定调用避免 import 冲突 —— 直接写 `com.spt.bas.purchase.wx.server.cache.BsDictUtil.init();`(不加 import,或加 import 并用全限定调用此句)。
    - 在现有 init 序列(如 WarehouseCache.init() 之后、new Thread(executor) 之前)追加:`com.spt.bas.purchase.wx.server.cache.BsDictUtil.init();`
    - **不**重复:`DictUtil.init(appCode)`(单体已有)、`new Thread(executor).start()`(单体已有)。
    - **不** verbatim 迁入 WX ApplicationStartup(避免第二个 ApplicationReadyEvent 监听器 → DictUtil 双调 + executor 双线程竞态)。
  </action>
  <verify>
    <automated>grep -n "purchase.wx.server.cache.BsDictUtil" zgbas-system/src/main/java/com/spt/bas/server/listener/ApplicationStartup.java ; echo "--- no duplicate DictUtil/executor ---" ; grep -c "DictUtil.init\|new Thread(executor)" zgbas-system/src/main/java/com/spt/bas/server/listener/ApplicationStartup.java</automated>
  </verify>
  <acceptance_criteria>
    - ApplicationStartup.java 含 `com.spt.bas.purchase.wx.server.cache.BsDictUtil.init()`(全限定,WX 版)
    - `DictUtil.init` 仍只 1 次(无重复)、`new Thread(executor)` 仍只 1 次
    - 单体无第二个 ApplicationReadyEvent @Component 监听器(WX ApplicationStartup 未 verbatim 迁)
  </acceptance_criteria>
  <done>单体 ApplicationStartup 调 WX BsDictUtil.init,无双跑</done>
</task>

<task type="auto">
  <name>Task 2: RequestListener verbatim 迁入(D-P5-10)</name>
  <files>zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/listener/RequestListener.java</files>
  <read_first>
    - 源 .../listener/RequestListener.java(@Component ServletRequestListener,requestDestroyed → UserContext.removeUser)
    - 05-02 迁入的 util/UserContext(removeUser 方法)
    - 05-RESEARCH.md §Q7(单体零 ServletRequestListener,无重名,D-14b runtime hard-dep)
  </read_first>
  <action>
    verbatim 迁入 listener/RequestListener.java 到 enclave .../wx/server/listener/,包名 com.spt.bas.purchase.wx.server.listener 保留。@Component 自注册为 ServletRequestListener,单体 ComponentScan 自动扫入。
    规则:包名保留;依赖 util/UserContext(05-02 已迁);不改 requestDestroyed 逻辑(UserContext.removeUser 防 ThreadLocal 泄漏)。
  </action>
  <verify>
    <automated>ls zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/listener/RequestListener.java ; grep -n "ServletRequestListener\|removeUser" zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/listener/RequestListener.java</automated>
  </verify>
  <acceptance_criteria>
    - RequestListener.java 就位,@Component + ServletRequestListener
    - requestDestroyed 调 UserContext.removeUser
    - 单体无同名 ServletRequestListener 重名(零既有,RESEARCH §Q7 证实)
  </acceptance_criteria>
  <done>RequestListener 迁入,UserContext ThreadLocal 清理就位</done>
</task>

<task type="auto">
  <name>Task 3: 产出 D-15a/b 五类承托 inventory checklist(SC#4 交付物)</name>
  <files>.planning/phases/05-carrier-layer/05-INVENTORY-CHECKLIST.md</files>
  <read_first>
    - 05-CONTEXT.md §code_context Integration Points(D-15a·b 五类预映射:return envelope/exception advice/user-context/auth helper/serialization-upload-wrapper)
    - 05-RESEARCH.md §Q6 inventory + §Q5 config 对账
    - 05-01..05-04 SUMMARY(已迁类清单)确认 must-port 结论
  </read_first>
  <action>
    创建 05-INVENTORY-CHECKLIST.md,五类承托逐项 `source → consumer → must-port?(Y/N + 落点 plan)` 盘点:

    1. **return envelope**:ApiResult(source common/ApiResult → consumer WX controller[P7]+GlobalExceptionHandler[05-03] → must-port Y,05-01 替换 stub)
    2. **exception advice**:GlobalExceptionHandler(source exception/ → consumer WX controller[P7] → must-port Y,05-03 限 basePackages);单体当前零 @ControllerAdvice
    3. **user-context**:UserContext + UserInfoVo(source util/+vo/ → consumer JwtAuthFilter[P4]+RequestListener[05-05] → must-port Y,05-01/02 替换 stub)+ RequestListener 清理
    4. **auth helper**:UserHelper + JwtUtil(source util/ → consumer WX 认证 → must-port Y/JwtUtil 已 P4);无其他 pending auth helper
    5. **serialization-upload-wrapper**:FastJson2JsonRedisSerializer(P4 已迁)+ UploadHelper + 单体 MultipartConfigElement + configureMessageConverters(source util/UploadHelper + config → must-port Y,05-02 迁 UploadHelper)

    每类填:source 文件 / consumer / 是否 must-port / 落点 plan / 状态。这是 SC#4 硬交付物。
  </action>
  <verify>
    <automated>grep -cE "return envelope|exception advice|user-context|auth helper|serialization-upload-wrapper" .planning/phases/05-carrier-layer/05-INVENTORY-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - 05-INVENTORY-CHECKLIST.md 存在,含全部 5 类标题
    - 每类填齐 source → consumer → must-port? → 落点 plan → 状态
    - 五类结论与 05-01..05-04 实际迁入一致(无遗漏/无虚报)
  </acceptance_criteria>
  <done>D-15a/b 五类 inventory checklist 交付(SC#4)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 启动期 cache 初始化 | ApplicationReadyEvent 触发,误双跑致竞态/重复执行 |
| 请求期 ThreadLocal | UserContext 跨请求未清理 → 身份串线程 + 内存泄漏 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Denial of Service | DictUtil/executor 双跑竞态 | mitigate | WX ApplicationStartup 不 verbatim 迁,仅追加 WX BsDictUtil.init 一行(D-P5-09) |
| T-05-10 | Information Disclosure | UserContext ThreadLocal 跨请求残留 | mitigate | RequestListener.requestDestroyed → UserContext.removeUser(D-P5-10) |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | 纯 Java + md,无包管理器 |
</threat_model>

<verification>
```bash
grep -n "purchase.wx.server.cache.BsDictUtil" zgbas-system/src/main/java/com/spt/bas/server/listener/ApplicationStartup.java   # WX 字典 init 就位
ls .planning/phases/05-carrier-layer/05-INVENTORY-CHECKLIST.md
```
启动 GREEN 留 Phase 8(本 plan 只做接线 + 静态断言)。
</verification>

<success_criteria>
1. ApplicationStartup 含 WX BsDictUtil.init(全限定),无双跑(D-P5-09)
2. RequestListener 迁入,UserContext 清理就位(D-P5-10)
3. 05-INVENTORY-CHECKLIST.md 五类盘点交付(SC#4)
4. PurchaseCommand 未迁(Phase 6,D-P5-13 修正)
</success_criteria>

<output>
Create `.planning/phases/05-carrier-layer/05-05-SUMMARY.md` when done
</output>
