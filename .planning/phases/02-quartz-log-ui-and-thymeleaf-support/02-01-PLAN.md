---
phase: 02-quartz-log-ui-and-thymeleaf-support
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - zgbas-admin/pom.xml
  - zgbas-admin/src/main/java/com/spt/bas/web/config/TemplateHelperConfig.java
autonomous: true
requirements:
  - QTZ-06
  - QTZ-08

must_haves:
  truths:
    - "Thymeleaf 模板中的 ${@dict.getType('sys_job_group')} 返回非空 List<SysDictDataSdk>"
    - "Thymeleaf 模板中的 ${@dict.getLabel('sys_common_status','0')} 返回字典标签字符串"
    - "Thymeleaf 模板中的 ${@permission.hasPermi('monitor:job:list')} 返回 boolean 且不抛 NoClassDefFoundError"
    - "Thymeleaf 模板中的 shiro:hasPermission=\"monitor:job:remove\" 属性被 ShiroDialect 处理（非静默忽略）"
  artifacts:
    - path: "zgbas-admin/src/main/java/com/spt/bas/web/config/TemplateHelperConfig.java"
      provides: "@Component(\"dict\") + @Component(\"permission\") + @Bean ShiroDialect"
      contains: "class TemplateHelperConfig"
    - path: "zgbas-admin/pom.xml"
      provides: "thymeleaf-extras-shiro:2.1.0 依赖"
      contains: "thymeleaf-extras-shiro"
  key_links:
    - from: "TemplateHelperConfig @Component(\"dict\")"
      to: "com.spt.auth.sdk.cache.DictUtil.getListByCategory()"
      via: "getType() / getLabel() 委派 auth-sdk DictUtil"
      pattern: "DictUtil\\.getListByCategory"
    - from: "TemplateHelperConfig @Component(\"permission\")"
      to: "com.spt.bas.web.shiro.ShiroUtil.isPermitted()"
      via: "hasPermi() 委派 ShiroUtil（非 spt-auth PermissionService）"
      pattern: "ShiroUtil\\.isPermitted"
    - from: "TemplateHelperConfig @Bean ShiroDialect"
      to: "at.pollux.thymeleaf.shiro.dialect.ShiroDialect"
      via: "Thymeleaf SpringTemplateEngine 自动发现 IDialect bean"
      pattern: "new ShiroDialect"
---

<objective>
为 Thymeleaf 模板渲染补齐三个辅助能力（QTZ-06 + QTZ-08）：(1) `@dict` Bean 提供字典查询；(2) `@permission` Bean 提供权限判断；(3) `thymeleaf-extras-shiro` 依赖 + `ShiroDialect` Bean 让 `shiro:hasPermission` 属性生效。

Purpose: 当前 monitor 模板引用了 `${@dict.getType(...)}`、`${@permission.hasPermi(...)}` 和 `shiro:hasPermission="..."`，但没有对应的 Spring Bean 和 Thymeleaf 方言，模板渲染抛 NPE 或权限属性被静默忽略（所有按钮对所有用户可见）。

Output: zgbas-admin/pom.xml 新增 thymeleaf-extras-shiro:2.1.0 依赖；新建 TemplateHelperConfig.java 含三个 bean 定义。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-quartz-log-ui-and-thymeleaf-support/02-CONTEXT.md
@.planning/phases/02-quartz-log-ui-and-thymeleaf-support/02-RESEARCH.md
@zgbas-system/src/main/java/com/spt/bas/web/shiro/ShiroUtil.java
@zgbas-system/src/main/java/com/spt/bas/server/listener/ApplicationStartup.java

<interfaces>
### auth-sdk DictUtil（@dict Bean 的委派目标）

来源：`/Users/alan/WorkSpace/IDEA/spt-auth/auth-sdk/src/main/java/com/spt/auth/sdk/cache/DictUtil.java`

```java
package com.spt.auth.sdk.cache;
public class DictUtil {
    // 初始化缓存（在 ApplicationStartup line 44 已调用：DictUtil.init(appCode)）
    public static LoadingCache<...> init(String appCode);
    // 按类别获取字典数据列表 — 返回 SysDictDataSdk，含 getDictLabel()/getDictValue()
    public static List<SysDictDataSdk> getListByCategory(String category);
}
```

### SysDictDataSdk 字段（auth-sdk）

来源：`/Users/alan/WorkSpace/IDEA/spt-auth/auth-sdk/src/main/java/com/spt/auth/sdk/entity/SysDictDataSdk.java`

```
private String dictLabel;   // getter: getDictLabel() — 模板用 ${dict.dictLabel}
private String dictValue;   // getter: getDictValue() — 模板用 ${dict.dictValue}
```

### ShiroUtil.isPermitted（@permission Bean 的委派目标）

来源：`zgbas-common/src/main/java/com/spt/tools/shiro/util/ShiroUtil.java` line 98-102

```java
package com.spt.tools.shiro.util;
public class ShiroUtil {
    public static boolean isPermitted(String permCode) {
        Subject currentUser = SecurityUtils.getSubject();
        return currentUser.isPermitted(permCode);
    }
}
```

zgbas-system 的 `com.spt.bas.web.shiro.ShiroUtil` 继承此基类（`extends com.spt.tools.shiro.util.ShiroUtil`），`isPermitted` 可直接调用。使用 admin 可见的子类 `com.spt.bas.web.shiro.ShiroUtil`。

### ShiroDialect 类（thymeleaf-extras-shiro 2.1.0）

```
at.pollux.thymeleaf.shiro.dialect.ShiroDialect  // 无参构造
```
thymeleaf-extras-shiro 2.1.0 无 spring.factories 自动配置，必须手动声明 @Bean。Thymeleaf SpringTemplateEngine 自动发现 ApplicationContext 中所有 IDialect bean。

### admin pom.xml 现有依赖结构（插入位置参考）

- spring-boot-starter-thymeleaf（line 39-42）
- spt-sign-client（line 43-48）
- easyexcel 3.1.0（line 49-54，直接声明 version）
- screw-core 1.0.5（line 55-60，直接声明 version）
- html2image 0.9（line 61-66，直接声明 version）
新依赖 thymeleaf-extras-shiro 2.1.0 同样直接声明 version（admin-only 依赖，不进 parent dependencyManagement）。

### admin config 包现有文件

`zgbas-admin/src/main/java/com/spt/bas/web/config/` 目前仅有 `BasicErrorController.java`。新建 TemplateHelperConfig.java 放入此包。

### 关键约束（RESEARCH Pitfalls）

- @dict 必须用 `com.spt.auth.sdk.cache.DictUtil`，不可用 `com.spt.bas.server.cache.BsDictUtil` 或 `com.spt.bas.client.cache.BsDictUtil`（它们返回 BsDictData，字段是 dictCd/dictName，不匹配模板的 dictLabel/dictValue）
- @permission 必须用 `ShiroUtil.isPermitted()`，不可用 spt-auth `PermissionService`（它依赖 Spring Security `com.spt.common.utils.SecurityUtils.getLoginUser()`，此项目用 Shiro，该类不存在 → NoClassDefFoundError）
- DictUtil.init(appCode) 已在 ApplicationStartup line 44 调用 — 字典缓存在启动时初始化，@dict Bean 不需要自己触发 init
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 TemplateHelperConfig.java（@dict + @permission + ShiroDialect）</name>
  <files>zgbas-admin/src/main/java/com/spt/bas/web/config/TemplateHelperConfig.java</files>
  <read_first>
    zgbas-system/src/main/java/com/spt/bas/web/shiro/ShiroUtil.java
    zgbas-system/src/main/java/com/spt/bas/server/listener/ApplicationStartup.java
    zgbas-admin/src/main/java/com/spt/bas/web/config/BasicErrorController.java
    zgbas-admin/src/main/resources/templates/monitor/operlog/operlog.html
  </read_first>
  <behavior>
    - @Component("dict") 的 getType("sys_job_group") 返回非 null List（DictUtil 缓存已由 ApplicationStartup 初始化）
    - @Component("dict") 的 getLabel("sys_common_status", "0") 返回 "成功"（匹配 spt-auth 字典数据）
    - @Component("dict") 的 getLabel("sys_common_status", "999") 返回 ""（未匹配值返回空串）
    - @Component("dict") 的 getLabel(null, "0") 返回 ""（null 输入安全返回空串）
    - @Component("permission") 的 hasPermi(null) 返回 false
    - @Component("permission") 的 hasPermi("") 返回 false
    - @Component("permission") 的 hasPermi("monitor:job:list") 委派 ShiroUtil.isPermitted，返回 boolean 不抛异常
    - @Component("permission") 的 lacksPermi("xxx") 返回 !hasPermi("xxx")
    - @Bean shiroDialect() 返回 at.pollux.thymeleaf.shiro.dialect.ShiroDialect 实例
  </behavior>
  <action>
创建新文件 `zgbas-admin/src/main/java/com/spt/bas/web/config/TemplateHelperConfig.java`，包含一个 @Configuration 类和三个 bean（per D-03/D-04/D-05）。

**类结构（per D-03 薄封装、D-04 独立 bean、D-05 双轨支持）：**

`TemplateHelperConfig` 类标注 `@Configuration`。在类内定义三个 public static nested 类作为 bean，或直接在配置类中用 @Bean/@Component 标注方法/类。推荐结构：一个 @Configuration 类 `TemplateHelperConfig`，内含三个 public static nested 类：`DictTemplateHelper`（@Component("dict")）、`PermissionTemplateHelper`（@Component("permission")）、以及一个 `@Bean` 方法 `shiroDialect()` 返回 ShiroDialect。

**@Component("dict") — DictTemplateHelper（per D-03，委派 auth-sdk DictUtil）：**

包路径 `com.spt.bas.web.config`，类名 `DictTemplateHelper`，标注 `@Component("dict")`。

导入 `com.spt.auth.sdk.cache.DictUtil` 和 `com.spt.auth.sdk.entity.SysDictDataSdk`（关键：不可导入 com.spt.bas.server.cache.BsDictUtil 或 com.spt.bas.client.cache.BsDictUtil — RESEARCH Pitfall 1）。

方法：
- `public List<SysDictDataSdk> getType(String dictType)`：委派 `DictUtil.getListByCategory(dictType)`。返回值类型 `List<SysDictDataSdk>`，模板用 `${dict.dictLabel}` / `${dict.dictValue}` 读取属性。
- `public String getLabel(String dictType, String dictValue)`：调用 `DictUtil.getListByCategory(dictType)` 获取列表，遍历查找 `dictValue.equals(item.getDictValue())`，匹配则返回 `item.getDictLabel()`。dictType 为 null 或列表为 null 时返回 ""。dictValue 为 null 时返回 ""。未匹配返回 ""。

**@Component("permission") — PermissionTemplateHelper（per D-04，委派 ShiroUtil）：**

包路径 `com.spt.bas.web.config`，类名 `PermissionTemplateHelper`，标注 `@Component("permission")`。

导入 `com.spt.bas.web.shiro.ShiroUtil`（admin 模块可见的子类；关键：不可导入 spt-auth 的 `com.spt.framework.web.service.PermissionService` — RESEARCH Pitfall 2，它依赖 Spring Security，此项目用 Shiro）。

方法：
- `public boolean hasPermi(String permission)`：permission 为 null 或 isEmpty 时返回 false。否则调用 `ShiroUtil.isPermitted(permission)` 返回 boolean。
- `public boolean lacksPermi(String permission)`：返回 `!hasPermi(permission)`。

**@Bean shiroDialect()（per D-05，让 shiro:hasPermission 生效）：**

在 `TemplateHelperConfig` @Configuration 类中定义方法：
- `@Bean public at.pollux.thymeleaf.shiro.dialect.ShiroDialect shiroDialect()`：方法体 `return new at.pollux.thymeleaf.shiro.dialect.ShiroDialect();`

全限定类名 `at.pollux.thymeleaf.shiro.dialect.ShiroDialect`（thymeleaf-extras-shiro 2.1.0 无自动配置 — RESEARCH Pitfall 4，必须手动声明 @Bean；Thymeleaf SpringTemplateEngine 自动发现 ApplicationContext 中所有 IDialect bean）。

不要修改 BasicErrorController.java 或 config 包内其他文件。不要修改任何模板文件（D-06：不改现有模板）。
  </action>
  <verify>
    <automated>
# 1. 源断言 — @Component("dict") 存在
grep -c '@Component("dict")' zgbas-admin/src/main/java/com/spt/bas/web/config/TemplateHelperConfig.java

# 2. 源断言 — 委派 auth-sdk DictUtil（非 BsDictUtil）
grep -c 'com.spt.auth.sdk.cache.DictUtil' zgbas-admin/src/main/java/com/spt/bas/web/config/TemplateHelperConfig.java
# 期望 1（import 或全限定调用）
grep -c 'BsDictUtil' zgbas-admin/src/main/java/com/spt/bas/web/config/TemplateHelperConfig.java
# 期望 0（不可使用 BsDictUtil）

# 3. 源断言 — @Component("permission") 存在
grep -c '@Component("permission")' zgbas-admin/src/main/java/com/spt/bas/web/config/TemplateHelperConfig.java

# 4. 源断言 — 委派 ShiroUtil（非 spt-auth PermissionService）
grep -c 'ShiroUtil.isPermitted' zgbas-admin/src/main/java/com/spt/bas/web/config/TemplateHelperConfig.java
# 期望 >=1
grep -c 'framework.web.service.PermissionService\|com.spt.common.utils.SecurityUtils' zgbas-admin/src/main/java/com/spt/bas/web/config/TemplateHelperConfig.java
# 期望 0（不可使用 Spring Security API）

# 5. 源断言 — ShiroDialect @Bean 存在
grep -c 'ShiroDialect' zgbas-admin/src/main/java/com/spt/bas/web/config/TemplateHelperConfig.java
# 期望 >=2（方法返回类型 + new 表达式）

# 6. 编译验证 — admin 模块编译（JDK 8）
JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home /Users/alan/App/apache-maven-3.8.6/bin/mvn compile -pl zgbas-admin -am -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml -q
    </automated>
  </verify>
  <acceptance_criteria>
    <criterion type="source">TemplateHelperConfig.java 存在且包含 @Configuration 注解</criterion>
    <criterion type="source">文件包含 @Component("dict") 标注的类</criterion>
    <criterion type="source">DictTemplateHelper 导入或全限定引用 com.spt.auth.sdk.cache.DictUtil</criterion>
    <criterion type="source">DictTemplateHelper.getType 返回 List&lt;SysDictDataSdk&gt;</criterion>
    <criterion type="source">DictTemplateHelper.getLabel 遍历列表匹配 getDictValue() 返回 getDictLabel()</criterion>
    <criterion type="source">DictTemplateHelper.getLabel 对 null dictType 或 null dictValue 返回空串</criterion>
    <criterion type="source">文件不包含 BsDictUtil 引用（grep BsDictUtil 输出 0）</criterion>
    <criterion type="source">文件包含 @Component("permission") 标注的类</criterion>
    <criterion type="source">PermissionTemplateHelper.hasPermi 委派 ShiroUtil.isPermitted</criterion>
    <criterion type="source">PermissionTemplateHelper.hasPermi 对 null/empty 输入返回 false</criterion>
    <criterion type="source">文件不包含 com.spt.framework.web.service.PermissionService 或 com.spt.common.utils.SecurityUtils 引用</criterion>
    <criterion type="source">文件包含 @Bean 方法返回 at.pollux.thymeleaf.shiro.dialect.ShiroDialect</criterion>
    <criterion type="source">@Bean 方法体包含 new ShiroDialect()</criterion>
    <criterion type="cli">mvn compile -pl zgbas-admin -am 退出码 0</criterion>
  </acceptance_criteria>
  <done>TemplateHelperConfig.java 创建完成，三个 bean（@dict/@permission/ShiroDialect）编译通过</done>
</task>

<task type="auto">
  <name>Task 2: 向 admin pom.xml 添加 thymeleaf-extras-shiro:2.1.0 依赖</name>
  <files>zgbas-admin/pom.xml</files>
  <read_first>
    zgbas-admin/pom.xml
  </read_first>
  <action>
在 zgbas-admin/pom.xml 的 `<dependencies>` 块内添加 thymeleaf-extras-shiro 依赖（per D-05 + RESEARCH Standard Stack）。

**插入位置：** 紧接 `spring-boot-starter-thymeleaf` 依赖（line 39-42）之后、`spt-sign-client` 依赖（line 43-48）之前。放在 thymeleaf starter 附近表明用途关联。

**依赖声明（直接声明 version 2.1.0，不进 parent dependencyManagement — RESEARCH Anti-Pattern: admin-only 依赖）：**

groupId: `com.github.theborakompanioni`
artifactId: `thymeleaf-extras-shiro`
version: `2.1.0`

使用标准 Maven dependency 块格式，与 easyexcel/screw-core/html2image 同风格（直接 version，无 scope）。

不要修改 parent pom.xml 的 dependencyManagement（RESEARCH Anti-Pattern）。
不要修改其他依赖。
不要删除或重排序现有依赖。
  </action>
  <verify>
    <automated>
# 1. 源断言 — 依赖存在
grep -c 'thymeleaf-extras-shiro' zgbas-admin/pom.xml
# 期望 >=3（groupId 行 + artifactId 行 + 可能的 version 行）

# 2. 源断言 — groupId 正确
grep -c 'com.github.theborakompanioni' zgbas-admin/pom.xml
# 期望 1

# 3. 源断言 — version 2.1.0
grep -c '2.1.0' zgbas-admin/pom.xml | grep -v lastUpdated
# 期望 >=1（thymeleaf-extras-shiro 的 version 行）

# 4. 依赖解析验证（JDK 8）
JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home /Users/alan/App/apache-maven-3.8.6/bin/mvn dependency:resolve -pl zgbas-admin -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml -q -Dinclude=com.github.theborakompanioni:thymeleaf-extras-shiro:2.1.0
    </automated>
  </verify>
  <acceptance_criteria>
    <criterion type="source">zgbas-admin/pom.xml 包含 groupId com.github.theborakompanioni 的 dependency 块</criterion>
    <criterion type="source">artifactId 为 thymeleaf-extras-shiro</criterion>
    <criterion type="source">version 为 2.1.0</criterion>
    <criterion type="source">依赖块位于 spring-boot-starter-thymeleaf 之后</criterion>
    <criterion type="source">parent pom.xml 未被修改（无 thymeleaf-extras-shiro 进 dependencyManagement）</criterion>
    <criterion type="cli">mvn dependency:resolve 成功解析 thymeleaf-extras-shiro:2.1.0</criterion>
  </acceptance_criteria>
  <done>admin pom.xml 新增 thymeleaf-extras-shiro:2.1.0 依赖，Maven 依赖解析成功</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Template→Bean | Thymeleaf 模板通过 `${@dict.getType(...)}` / `${@permission.hasPermi(...)}` 调用 Spring Bean — 跨越 SSR 边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Elevation of Privilege | @permission bean | mitigate | hasPermi 委派 ShiroUtil.isPermitted → Subject.isPermitted → ShiroDbRealm 授权缓存；null/empty 输入返回 false（非默认放行） |
| T-02-02 | Tampering | @dict bean | accept | 字典数据来自 spt-auth DB 经认证的 IAuthOpenFacade HTTP 获取，非用户可编辑；模板用 th:text 自动转义 |
| T-02-03 | Elevation of Privilege | ShiroDialect bean | mitigate | ShiroDialect 处理 shiro:hasPermission 属性走 Subject.hasPermission() — 与 @permission bean 同源 Shiro 授权链，无旁路 |
| T-02-SC | Tampering | Maven 依赖安装 | mitigate | thymeleaf-extras-shiro 2.1.0 已在本地仓库验证（jar + pom + sha1），来源为 Maven Central + old zgbas 4.8.3 pom 确认；非 [SLOP]/[SUS] 包，无 blocking checkpoint |
</threat_model>

<verification>
整体验证（依赖 + bean 注册）：
- thymeleaf-extras-shiro:2.1.0 在 admin pom 中声明且可解析
- @Component("dict") / @Component("permission") / @Bean ShiroDialect 三个 bean 在 TemplateHelperConfig.java 中定义
- admin 模块编译零错
-（运行时验证在 02-03 端到端计划执行）
</verification>

<success_criteria>
- [ ] TemplateHelperConfig.java 包含 @Component("dict") 委派 auth-sdk DictUtil（非 BsDictUtil）
- [ ] TemplateHelperConfig.java 包含 @Component("permission") 委派 ShiroUtil（非 spt-auth PermissionService）
- [ ] TemplateHelperConfig.java 包含 @Bean ShiroDialect 方法
- [ ] zgbas-admin/pom.xml 新增 thymeleaf-extras-shiro:2.1.0 依赖
- [ ] parent pom.xml 未被修改
- [ ] mvn compile -pl zgbas-admin -am 退出码 0
</success_criteria>

<output>
Create `.planning/phases/02-quartz-log-ui-and-thymeleaf-support/02-01-SUMMARY.md` when done
</output>
