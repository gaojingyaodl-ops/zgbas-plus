---
phase: 02-quartz-log-ui-and-thymeleaf-support
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - zgbas-admin/src/main/resources/templates/monitor/jobLog/jobLog.html
  - zgbas-quartz/src/main/java/com/spt/quartz/controller/SysJobLogController.java
autonomous: true
requirements:
  - QTZ-05

must_haves:
  truths:
    - "用户访问 /monitor/jobLog 返回调度日志列表页面（非 404/500/NPE）"
    - "页面搜索栏的任务组名下拉框有 sys_job_group 字典数据（非空）"
    - "页面搜索栏的执行状态下拉框有 sys_common_status 字典数据（非空）"
    - "用户勾选记录点击删除按钮，选中日志从列表和数据库中移除"
    - "用户点击清空按钮，所有调度日志被清空"
    - "用户点击导出按钮，浏览器下载 xlsx 文件"
    - "SysJobLogController.getInfo 的路径变量名与 @PathVariable 参数名一致（不再 400）"
  artifacts:
    - path: "zgbas-admin/src/main/resources/templates/monitor/jobLog/jobLog.html"
      provides: "调度日志列表页 — 搜索/删除/清空/导出 + bootstrap-table"
      contains: "monitor/jobLog"
    - path: "zgbas-quartz/src/main/java/com/spt/quartz/controller/SysJobLogController.java"
      provides: "getInfo 路径变量 bug 修复"
      contains: "@GetMapping"
  key_links:
    - from: "jobLog.html $.table.init options.url"
      to: "SysJobLogController.@GetMapping(\"/list\")"
      via: "AJAX GET /monitor/jobLog/list"
      pattern: "prefix \\+ \"/list\""
    - from: "jobLog.html removeAllJobLogs() / removeJobLog()"
      to: "SysJobLogController.@DeleteMapping(\"/{jobLogIds}\")"
      via: "$.ajax DELETE prefix/{ids}"
      pattern: "type.*delete.*prefix"
    - from: "jobLog.html cleanJobLogs()"
      to: "SysJobLogController.@DeleteMapping(\"/clean\")"
      via: "$.ajax DELETE prefix/clean"
      pattern: "type.*delete.*clean"
    - from: "jobLog.html $.table.exportExcel()"
      to: "SysJobLogController.@PostMapping(\"/export\")"
      via: "AJAX POST prefix/export"
      pattern: "exportUrl.*export"
    - from: "jobLog.html @dict.getType('sys_job_group')"
      to: "TemplateHelperConfig.DictTemplateHelper.getType()"
      via: "Thymeleaf ${@dict.getType(...)} → DictUtil.getListByCategory()"
      pattern: "@dict.getType"
---

<objective>
创建调度日志列表页面 `templates/monitor/jobLog/jobLog.html`（QTZ-05），并修复 SysJobLogController.getInfo 的路径变量 bug（RESEARCH Pitfall 3）。

Purpose: SysJobLogController 的 5 个 REST 路由已就绪，但没有前端页面。用户无法浏览、检索、删除、清空或导出调度日志。同时 getInfo 方法有一个预存 bug：`@GetMapping("/{configId}")` 与 `@PathVariable Long jobLogId` 不匹配，调用时抛 MissingPathVariableException。

Output: 新建 jobLog.html（镜像 operlog.html 风格，per D-01/D-02）；修复 SysJobLogController line 61 的 `/{configId}` → `/{jobLogId}`。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-quartz-log-ui-and-thymeleaf-support/02-CONTEXT.md
@.planning/phases/02-quartz-log-ui-and-thymeleaf-support/02-RESEARCH.md
@.planning/phases/02-quartz-log-ui-and-thymeleaf-support/02-01-SUMMARY.md
@zgbas-quartz/src/main/java/com/spt/quartz/controller/SysJobLogController.java
@zgbas-quartz/src/main/java/com/spt/quartz/domain/SysJobLog.java
@zgbas-admin/src/main/resources/templates/monitor/operlog/operlog.html
@zgbas-admin/src/main/resources/templates/monitor/job/job.html

<interfaces>
### SysJobLogController 目标路由（@RestController @RequestMapping("/monitor/jobLog")）

来源：`zgbas-quartz/src/main/java/com/spt/quartz/controller/SysJobLogController.java`

| 功能 | HTTP | 路径 | 参数 | 权限 |
|------|------|------|------|------|
| 列表 | GET | /monitor/jobLog/list | SysJobLog（form bind） | monitor:job:list |
| 导出 | POST | /monitor/jobLog/export | SysJobLog（form bind）→ HttpServletResponse xlsx | monitor:job:export |
| 详情 | GET | /monitor/jobLog/{jobLogId} | @PathVariable Long jobLogId | monitor:job:query |
| 删除 | DELETE | /monitor/jobLog/{jobLogIds} | @PathVariable Long[] jobLogIds | monitor:job:remove |
| 清空 | DELETE | /monitor/jobLog/clean | 无 | monitor:job:remove |

**BUG（Pitfall 3）：** line 61 `@GetMapping(value = "/{configId}")` 与 line 62 `@PathVariable Long jobLogId` 不匹配。修复后 line 61 应为 `@GetMapping(value = "/{jobLogId}")`。

### SysJobLog 域字段（9 字段 + BaseEntity）

来源：`zgbas-quartz/src/main/java/com/spt/quartz/domain/SysJobLog.java`

| 字段 | 类型 | @Excel | 列标题 |
|------|------|--------|--------|
| jobLogId | Long | 日志序号 | 日志编号 |
| jobName | String | 任务名称 | 任务名称 |
| jobGroup | String | 任务组名 | 任务组名 |
| invokeTarget | String | 调用目标字符串 | 调用目标字符串 |
| jobMessage | String | 日志信息 | 日志信息 |
| status | String | 0=正常,1=失败 | 执行状态 |
| exceptionInfo | String | 异常信息 | 异常信息 |
| startTime | Date | — | 开始时间 |
| stopTime | Date | — | 停止时间 |
| createTime | Date（继承 BaseEntity） | — | 执行时间 |

### operlog.html 模式（镜像目标，per D-01/D-02）

来源：`zgbas-admin/src/main/resources/templates/monitor/operlog/operlog.html`

结构：search-collapse form → toolbar btn-group（删除/清空/导出，shiro:hasPermission 控制）→ bootstrap-table → script（var datas = [[${@dict.getType(...)}]]; var prefix = ctx + "..."; $.table.init(options) with columns + queryParams + searchPre/resetPre）。

### ry-ui.js $.operate.removeAll / clean 行为（关键约束）

来源：`zgbas-admin/src/main/resources/static/ruoyi/js/ry-ui.js` line 1140-1160

```
removeAll: function() { ... $.operate.submit(url, "post", "json", data); }  // line 1150 — POST + {ids: rows.join()}
clean: function() { ... $.operate.submit(url, "post", "json", ""); }       // line 1158 — POST
```

**问题：** operlog.html 用 `$.operate.removeAll()` 和 `$.operate.clean()` 发 POST，但 operlog 的 Controller 用 `@PostMapping("/remove")` 和 `@PostMapping("/clean")` 匹配 POST。SysJobLogController 用 `@DeleteMapping("/{jobLogIds}")` 和 `@DeleteMapping("/clean")` — 不匹配 POST。

**解决方案（per Phase 1 job.html 同模式）：** jobLog.html 不使用 `$.operate.removeAll()` / `$.operate.clean()`，改用自定义 `removeAllJobLogs()` / `cleanJobLogs()` 函数发送 `$.ajax DELETE`（RESEARCH Open Question 1 已验证）。

### Phase 1 job.html 参考模式（自定义 DELETE 函数）

来源：`zgbas-admin/src/main/resources/templates/monitor/job/job.html`（Phase 1 01-03 输出）

job.html 的 `removeJob(jobId)` 和 `removeAllJobs()` 函数发送 `$.ajax DELETE` 到 `prefix + "/" + id`，匹配 `@DeleteMapping("/{jobIds}")`。jobLog.html 复用此模式，适配 jobLogId 字段名。

### 字典类型（RESEARCH Code Examples）

- `sys_job_group` — 任务组名下拉框（值如 DEFAULT/SYSTEM）
- `sys_common_status` — 执行状态下拉框（0=成功/1=失败，值与 status 字段一致）

### TemplateHelperConfig bean（来自 02-01，运行时依赖）

- `@Component("dict")` → `getType(String)` 返回 `List<SysDictDataSdk>`（dictLabel/dictValue）
- `@Component("permission")` → `hasPermi(String)` 返回 boolean
- `@Bean ShiroDialect` → 处理 `shiro:hasPermission` 属性

jobLog.html 在运行时渲染时需要这三个 bean 已注册（02-01 Wave 1 产物）。本计划编译期无文件冲突（02-01 改 pom + 新建 config java；本计划新建 html + 改 quartz controller java）。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 jobLog.html 调度日志列表页</name>
  <files>zgbas-admin/src/main/resources/templates/monitor/jobLog/jobLog.html</files>
  <read_first>
    zgbas-admin/src/main/resources/templates/monitor/operlog/operlog.html
    zgbas-admin/src/main/resources/templates/monitor/job/job.html
    zgbas-quartz/src/main/java/com/spt/quartz/domain/SysJobLog.java
    zgbas-quartz/src/main/java/com/spt/quartz/controller/SysJobLogController.java
    zgbas-admin/src/main/resources/static/ruoyi/js/ry-ui.js
  </read_first>
  <action>
创建新文件 `zgbas-admin/src/main/resources/templates/monitor/jobLog/jobLog.html`，镜像 operlog.html 的完整监控页结构（per D-01），适配 SysJobLog 字段和 /monitor/jobLog 路由（per D-02）。

**HTML 头部（per D-01 operlog 风格）：**
- `<!DOCTYPE html>` + `<html lang="zh" xmlns:th="http://www.thymeleaf.org" xmlns:shiro="http://www.pollix.at/thymeleaf/shiro">`
- `<th:block th:include="include :: header('调度日志列表')" />`
- `<th:block th:include="include :: bootstrap-select-css" />`

**搜索栏 search-collapse（per D-01/D-02，字段适配 SysJobLog）：**
form id="jobLog-form" 内 div.select-list ul：
- 任务名称：`<input type="text" name="jobName"/>`（对应 SysJobLog.jobName）
- 任务组名：`<select name="jobGroup" th:with="type=${@dict.getType('sys_job_group')}">` + option 循环 `${type}` 用 `th:text="${dict.dictLabel}"` `th:value="${dict.dictValue}"`（per D-03 委派 auth-sdk DictUtil，字段 dictLabel/dictValue）
- 执行状态：`<select name="status" th:with="type=${@dict.getType('sys_common_status')}">` 同上 option 循环
- 执行时间：`<input type="text" class="time-input" id="startTime" name="params[beginTime]"/>` + `<span>-</span>` + `<input ... id="endTime" name="params[endTime]"/>`（对应 BaseEntity.params map，operlog 同模式）
- 搜索按钮 `onclick="searchPre()"` + 清空按钮 `onclick="resetPre()"`

**工具栏 toolbar btn-group-sm（per D-01，shiro:hasPermission per D-05）：**
- 删除（批量）：`<a class="btn btn-danger multiple disabled" onclick="removeAllJobLogs()" shiro:hasPermission="monitor:job:remove">` 图标 fa-remove + 文字"删除"
- 清空：`<a class="btn btn-danger" onclick="cleanJobLogs()" shiro:hasPermission="monitor:job:remove">` 图标 fa-trash + 文字"清空"
- 导出：`<a class="btn btn-warning" onclick="$.table.exportExcel()" shiro:hasPermission="monitor:job:export">` 图标 fa-download + 文字"导出"

**bootstrap-table 容器：**
`<div class="col-sm-12 select-table table-striped"><table id="bootstrap-table"></table></div>`

**页脚：**
`<th:block th:include="include :: footer" />` + `<th:block th:include="include :: bootstrap-select-js" />`

**script th:inline="javascript"（核心逻辑，per D-02 operlog 模式 + RESEARCH Code Examples）：**

变量声明：
- `var datas = [[${@dict.getType('sys_job_group')}]];`（行内 Thymeleaf 渲染字典数据给 JS formatter 用）
- `var prefix = ctx + "monitor/jobLog";`（路由前缀）

`$(function() { var options = { ... }; $.table.init(options); });`

options 对象字段：
- `url: prefix + "/list"`（匹配 @GetMapping("/list")）
- `cleanUrl: prefix + "/clean"`（匹配 @DeleteMapping("/clean")）
- `removeUrl: prefix`（用于自定义函数拼接路径，非 $.operate.remove 调用）
- `exportUrl: prefix + "/export"`（匹配 @PostMapping("/export")，$.table.exportExcel 发 POST）
- `queryParams: queryParams`
- `sortName: "createTime"`，`sortOrder: "desc"`
- `modalName: "调度日志"`
- `escape: true`，`showPageGo: true`，`rememberSelected: true`

columns 数组（字段适配 SysJobLog，per RESEARCH Code Examples）：
- `{ field: 'state', checkbox: true }`
- `{ field: 'jobLogId', title: '日志编号' }`
- `{ field: 'jobName', title: '任务名称' }`
- `{ field: 'jobGroup', title: '任务组名', align: 'center', formatter: function(value, row, index) { return $.table.selectDictLabel(datas, value); } }`（用 sys_job_group 字典格式化）
- `{ field: 'invokeTarget', title: '调用目标字符串' }`
- `{ field: 'jobMessage', title: '日志信息' }`
- `{ field: 'status', title: '执行状态', align: 'center', formatter: function(value, row, index) { if (value == '0') return '<span class="badge badge-primary">成功</span>'; else if (value == '1') return '<span class="badge badge-danger">失败</span>'; } }`（硬编码 badge，匹配 SysJobLog @Excel readConverterExp="0=正常,1=失败"）
- `{ field: 'createTime', title: '执行时间', sortable: true }`（继承自 BaseEntity）

辅助函数：
- `function queryParams(params) { var search = $.table.queryParams(params); return search; }`
- `function searchPre() { $.table.search('jobLog-form', 'bootstrap-table'); }`
- `function resetPre() { $("#jobLog-form")[0].reset(); $.table.search('jobLog-form', 'bootstrap-table'); }`

**自定义删除/清空函数（per RESEARCH Open Question 1 验证结果 — $.operate.removeAll/clean 发 POST，不匹配 @DeleteMapping）：**

`function removeJobLog(jobLogId)`：`$.modal.confirm` 确认后发送 `$.ajax DELETE` 到 `prefix + "/" + jobLogId`，匹配 `@DeleteMapping("/{jobLogIds}")`。dataType: "json"。beforeSend: `$.modal.loading(...)`。success: 检查 `result.code == 0` 则 `$.modal.msgSuccess(result.msg)` + `$.table.refresh()`，否则 `$.modal.alertError(result.msg)`；最后 `$.modal.closeLoading()`。（复用 Phase 1 job.html removeJob 模式）

`function removeAllJobLogs()`：`$.table.set()` 获取选中行 `$.table.selectFirstColumns()`。rows.length == 0 时 `$.modal.alertWarning("请至少选择一条记录")`。确认后发送 `$.ajax DELETE` 到 `prefix + "/" + rows.join(",")`，匹配 `@DeleteMapping("/{jobLogIds}")` Long[] jobLogIds = [id1, id2, ...]。success 逻辑同 removeJobLog。

`function cleanJobLogs()`：`$.table.set()`。`$.modal.confirm` 确认后发送 `$.ajax DELETE` 到 `prefix + "/clean"`，匹配 `@DeleteMapping("/clean")`。type: "delete"。success 逻辑同上。

不要添加 detailUrl 或 detail 按钮（RESEARCH Pitfall 6：SysJobLogController 无 view-returning route，D-01 不要求 detail）。
不要使用 `$.operate.removeAll()` 或 `$.operate.clean()`（它们发 POST，不匹配 @DeleteMapping — RESEARCH Open Question 1）。
不要修改 operlog.html 或其他现有模板（D-06：不改现有模板）。
  </action>
  <verify>
    <automated>
# 1. 源断言 — 文件存在且包含关键结构
test -f zgbas-admin/src/main/resources/templates/monitor/jobLog/jobLog.html && echo "EXISTS"
grep -c 'monitor/jobLog' zgbas-admin/src/main/resources/templates/monitor/jobLog/jobLog.html
# 期望 >=1（prefix 定义）

# 2. 源断言 — @dict 使用（per D-03）
grep -c "@dict.getType('sys_job_group')" zgbas-admin/src/main/resources/templates/monitor/jobLog/jobLog.html
# 期望 >=1
grep -c "@dict.getType('sys_common_status')" zgbas-admin/src/main/resources/templates/monitor/jobLog/jobLog.html
# 期望 >=1
grep -c 'dict.dictLabel\|dict.dictValue' zgbas-admin/src/main/resources/templates/monitor/jobLog/jobLog.html
# 期望 >=2

# 3. 源断言 — shiro:hasPermission 使用（per D-05）
grep -c 'shiro:hasPermission' zgbas-admin/src/main/resources/templates/monitor/jobLog/jobLog.html
# 期望 >=3（删除/清空/导出）

# 4. 源断言 — 自定义 DELETE 函数（非 $.operate.removeAll/clean）
grep -c 'function removeAllJobLogs' zgbas-admin/src/main/resources/templates/monitor/jobLog/jobLog.html
# 期望 1
grep -c 'function cleanJobLogs' zgbas-admin/src/main/resources/templates/monitor/jobLog/jobLog.html
# 期望 1
grep -c 'function removeJobLog' zgbas-admin/src/main/resources/templates/monitor/jobLog/jobLog.html
# 期望 1
grep -c '"delete"' zgbas-admin/src/main/resources/templates/monitor/jobLog/jobLog.html
# 期望 >=3（三个函数各一）

# 5. 源断言 — 不使用 $.operate.removeAll / $.operate.clean
grep -c '\$.operate.removeAll\b\|\$.operate.clean\b' zgbas-admin/src/main/resources/templates/monitor/jobLog/jobLog.html
# 期望 0

# 6. 源断言 — 路由对齐
grep -c 'prefix + "/list"' zgbas-admin/src/main/resources/templates/monitor/jobLog/jobLog.html
# 期望 >=1
grep -c 'prefix + "/export"' zgbas-admin/src/main/resources/templates/monitor/jobLog/jobLog.html
# 期望 >=1
grep -c 'prefix + "/clean"' zgbas-admin/src/main/resources/templates/monitor/jobLog/jobLog.html
# 期望 >=1

# 7. 源断言 — bootstrap-table 列含 SysJobLog 字段
grep -c "field: 'jobLogId'\|field: 'jobName'\|field: 'jobGroup'\|field: 'invokeTarget'\|field: 'jobMessage'\|field: 'status'\|field: 'createTime'" zgbas-admin/src/main/resources/templates/monitor/jobLog/jobLog.html
# 期望 >=7
    </automated>
  </verify>
  <acceptance_criteria>
    <criterion type="source">jobLog.html 存在于 templates/monitor/jobLog/ 目录</criterion>
    <criterion type="source">html 标签包含 xmlns:th 和 xmlns:shiro 命名空间声明</criterion>
    <criterion type="source">搜索栏包含 jobName 文本输入、jobGroup 和 status 字典下拉框（th:with type=${@dict.getType(...)}）</criterion>
    <criterion type="source">字典 option 使用 th:text="${dict.dictLabel}" 和 th:value="${dict.dictValue}"</criterion>
    <criterion type="source">工具栏包含删除、清空、导出三个按钮，均有 shiro:hasPermission 属性</criterion>
    <criterion type="source">script 定义 var prefix = ctx + "monitor/jobLog"</criterion>
    <criterion type="source">$.table.init options.url 为 prefix + "/list"</criterion>
    <criterion type="source">options.cleanUrl 为 prefix + "/clean"</criterion>
    <criterion type="source">options.exportUrl 为 prefix + "/export"</criterion>
    <criterion type="source">options.removeUrl 为 prefix（非 prefix + "/remove"）</criterion>
    <criterion type="source">columns 包含 jobLogId/jobName/jobGroup/invokeTarget/jobMessage/status/createTime 七个字段</criterion>
    <criterion type="source">jobGroup 列 formatter 调用 $.table.selectDictLabel(datas, value)</criterion>
    <criterion type="source">status 列 formatter 用 badge 硬编码 0=成功/1=失败</criterion>
    <criterion type="source">定义 removeJobLog(jobLogId) 函数发送 $.ajax DELETE prefix/jobLogId</criterion>
    <criterion type="source">定义 removeAllJobLogs() 函数发送 $.ajax DELETE prefix/id1,id2,...</criterion>
    <criterion type="source">定义 cleanJobLogs() 函数发送 $.ajax DELETE prefix/clean</criterion>
    <criterion type="source">不调用 $.operate.removeAll() 或 $.operate.clean()</criterion>
    <criterion type="source">不包含 detailUrl 配置</criterion>
  </acceptance_criteria>
  <done>jobLog.html 创建完成，镜像 operlog.html 结构，字段/路由/权限适配 SysJobLog，删除/清空用自定义 DELETE 函数</done>
</task>

<task type="auto">
  <name>Task 2: 修复 SysJobLogController.getInfo 路径变量 bug</name>
  <files>zgbas-quartz/src/main/java/com/spt/quartz/controller/SysJobLogController.java</files>
  <read_first>
    zgbas-quartz/src/main/java/com/spt/quartz/controller/SysJobLogController.java
  </read_first>
  <action>
修复 SysJobLogController.java line 61 的预存路径变量 bug（RESEARCH Pitfall 3）。

**当前代码（line 60-63，buggy）：**
- line 61: `@GetMapping(value = "/{configId}")`
- line 62: `public AjaxResult getInfo(@PathVariable Long jobLogId) {`

**问题：** URL 模板变量名 `configId` 与 @PathVariable 参数名 `jobLogId` 不匹配。Spring MVC 在 `@PathVariable` 未显式指定 value 时按参数名绑定 URL 模板变量，找不到 `jobLogId` 模板变量 → 抛 `MissingPathVariableException`（400/500）。

**修复（per RESEARCH Pitfall 3 — one-line fix）：**
将 line 61 的 `@GetMapping(value = "/{configId}")` 改为 `@GetMapping(value = "/{jobLogId}")`。

使 URL 模板变量名 `jobLogId` 与 @PathVariable 参数名 `jobLogId` 一致。Spring MVC 成功绑定 Long jobLogId。

不要修改其他行。不要修改其他方法（list/export/remove/clean）。不要添加新方法。不要修改类级注解或 import。
  </action>
  <verify>
    <automated>
# 1. 源断言 — 路径变量名已修复
grep -c '@GetMapping(value = "/{jobLogId}")' zgbas-quartz/src/main/java/com/spt/quartz/controller/SysJobLogController.java
# 期望 1

# 2. 源断言 — 旧 buggy 路径已消失
grep -c '@GetMapping(value = "/{configId}")' zgbas-quartz/src/main/java/com/spt/quartz/controller/SysJobLogController.java
# 期望 0

# 3. 源断言 — @PathVariable 参数名仍为 jobLogId（未改动）
grep -c '@PathVariable Long jobLogId' zgbas-quartz/src/main/java/com/spt/quartz/controller/SysJobLogController.java
# 期望 1

# 4. 编译验证
JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home /Users/alan/App/apache-maven-3.8.6/bin/mvn compile -pl zgbas-quartz -am -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml -q
    </automated>
  </verify>
  <acceptance_criteria>
    <criterion type="source">SysJobLogController.java line 61 为 @GetMapping(value = "/{jobLogId}")</criterion>
    <criterion type="source">文件不包含 @GetMapping(value = "/{configId}")</criterion>
    <criterion type="source">@PathVariable Long jobLogId 参数声明未改动</criterion>
    <criterion type="source">list/export/remove/clean 方法未被修改</criterion>
    <criterion type="cli">mvn compile -pl zgbas-quartz -am 退出码 0</criterion>
  </acceptance_criteria>
  <done>SysJobLogController.getInfo 路径变量名与 @PathVariable 参数名一致，调用不再抛 MissingPathVariableException</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser→API | AJAX DELETE/POST from jobLog.html to /monitor/jobLog/* routes — user-controlled ids path param |
| Template→Bean | jobLog.html renders via @dict/@permission/ShiroDialect (02-01 beans) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | jobLog.html removeJobLog/removeAllJobLogs ids 参数 | mitigate | Long[] jobLogIds 由 Spring MVC 类型转换约束，非数字输入抛 TypeMismatchException 而非注入；@RequiresPermissions("monitor:job:remove") 守卫路由 |
| T-02-05 | Elevation of Privilege | jobLog.html shiro:hasPermission 按钮显隐 | mitigate | shiro:hasPermission 走 ShiroDialect → Subject.hasPermission()（02-01 ShiroDialect bean）；路由层 @RequiresPermissions 双重守卫 |
| T-02-06 | Information Disclosure | jobLog.html 导出 xlsx | accept | @RequiresPermissions("monitor:job:export") 守卫；ExcelUtil 用 SysJobLog @Excel 注解控制列，无敏感字段 |
| T-02-07 | Tampering | SysJobLogController.getInfo 路径变量 | mitigate | 修复后 jobLogId 为 Long 类型，Spring MVC 类型转换约束非法输入 |
</threat_model>

<verification>
整体验证（页面 + controller）：
- jobLog.html 存在且包含搜索/工具栏/bootstrap-table/自定义 DELETE 函数
- jobLog.html 使用 @dict/@permission/shiro:hasPermission（依赖 02-01 bean）
- SysJobLogController.getInfo 路径变量名修复
- quartz 模块编译零错
-（端到端运行时验证在 02-03 计划执行）
</verification>

<success_criteria>
- [ ] jobLog.html 创建于 templates/monitor/jobLog/ 目录
- [ ] jobLog.html 镜像 operlog.html 结构（搜索栏 + 工具栏 + bootstrap-table + script）
- [ ] jobLog.html 字段适配 SysJobLog（jobLogId/jobName/jobGroup/invokeTarget/jobMessage/status/createTime）
- [ ] jobLog.html 路由对齐 SysJobLogController（list/export/clean + 自定义 DELETE remove）
- [ ] jobLog.html 不使用 $.operate.removeAll/$.operate.clean（改用自定义 DELETE 函数）
- [ ] jobLog.html 使用 @dict/@permission/shiro:hasPermission（依赖 02-01 bean）
- [ ] SysJobLogController.getInfo 路径变量 @GetMapping("/{jobLogId}") 与 @PathVariable Long jobLogId 一致
- [ ] mvn compile -pl zgbas-quartz -am 退出码 0
</success_criteria>

<output>
Create `.planning/phases/02-quartz-log-ui-and-thymeleaf-support/02-02-SUMMARY.md` when done
</output>
