---
phase: 02-infrastructure
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - zgbas-common/src/main/java/com/spt/tools/core/**
  - zgbas-common/src/main/java/com/spt/tools/data/**
  - zgbas-common/src/main/java/com/spt/tools/http/**
  - zgbas-common/src/main/java/com/spt/tools/file/**
  - zgbas-common/pom.xml
autonomous: true
requirements: [INLINE-01, INLINE-02]

must_haves:
  truths:
    - "spt-tools-core (70 source files) is inlined verbatim into zgbas-common preserving package com.spt.tools.core.*"
    - "spt-tools-data/http/file (40 source files) are inlined verbatim preserving com.spt.tools.{data,http,file}.*"
    - "Each layer compiles green before the next layer is copied (compile-gate ordering, Phase 1 gotcha-cascade lesson)"
    - "No source file is renamed or refactored — packages match the source tree byte-for-byte (D-P2-07)"
  artifacts:
    - path: "zgbas-common/src/main/java/com/spt/tools/core/"
      provides: "spt-tools-core verbatim (ToolsCoreConfig, constants, exceptions, utils — 70 files)"
    - path: "zgbas-common/src/main/java/com/spt/tools/data/"
      provides: "DataSourceCreator + DataSourceConfig + DataEntity (prefix-agnostic Druid creator)"
    - path: "zgbas-common/src/main/java/com/spt/tools/http/"
      provides: "FeignConfig + TokenUtil + HttpClient utils (brings spring-cloud-starter-openfeign usage)"
    - path: "zgbas-common/src/main/java/com/spt/tools/file/"
      provides: "ExcelUtil + file utils (poi/easyexcel/zxing)"
  key_links:
    - from: "zgbas-common/src/main/java/com/spt/tools/data/util/DataSourceCreator.java"
      to: "zgbas-framework ZgbasDataSourceConfig (Wave 4)"
      via: "createDataSource(config) call — the single DataSource factory both ORMs will share"
      pattern: "DataSourceCreator\\.createDataSource"
    - from: "zgbas-common/src/main/java/com/spt/tools/http/config/FeignConfig.java"
      to: "spring-cloud-starter-openfeign (transitive)"
      via: "Feign configuration bean for cfca sign client"
      pattern: "FeignConfig"
---

<objective>
Inline spt-tools layers 1 and 2 (core alone, then data/http/file) verbatim into zgbas-common. This delivers INLINE-01 (core, the dependency root) and INLINE-02 (data/http/file), with a compile gate between each layer to prevent the 1200+ reference gotcha cascade documented in Phase 1.

Purpose: spt-tools-core is the foundation every other inlined module depends on. spt-tools-data provides DataSourceCreator (the single DataSource factory both ORMs will consume in Wave 4). spt-tools-http provides FeignConfig (needed by the cfca sign client). These must land before the jpa/web/mybatis/shiro/aop/config layers (Plan 03).

Output: ~110 new source files under zgbas-common/src/main/java/com/spt/tools/{core,data,http,file}/, compile-gated green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-infrastructure/02-CONTEXT.md
@.planning/phases/02-infrastructure/02-RESEARCH.md
@.planning/phases/02-infrastructure/02-PATTERNS.md
@.planning/phases/02-infrastructure/02-01-SUMMARY.md

**BUILD COMMAND PREFIX:** `JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml`

**Source tree (verbatim copy origin):** `/Users/alan/WorkSpace/IDEA/tools/spt-tools-{core,data,http,file}/src/main/java/com/spt/tools/`

<interfaces>
<!-- These are the load-bearing exports downstream plans consume. Executor copies verbatim — no modification. -->

From spt-tools-data (consumed by Wave 4 ZgbasDataSourceConfig):
- com.spt.tools.data.config.DataSourceConfig — POJO with 14 Druid pool fields, bound via @ConfigurationProperties(prefix="spring.datasource.druid") in framework
- com.spt.tools.data.util.DataSourceCreator.createDataSource(DataSourceConfig) — static factory, prefix-agnostic, returns configured DruidDataSource

From spt-tools-core (consumed by all other spt-tools modules + Phase 4 entities):
- com.spt.tools.core.* — constants, exceptions, utils (70 files, no internal spt-tools deps)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inline spt-tools-core (layer 1) — compile gate</name>
  <files>zgbas-common/src/main/java/com/spt/tools/core/**</files>
  <read_first>
    - /Users/alan/WorkSpace/IDEA/tools/spt-tools-core/src/main/java/com/spt/tools/core/ (list the directory tree — this is the verbatim copy origin, 70 .java files)
    - /Users/alan/WorkSpace/IDEA/tools/spt-tools-core/pom.xml (confirm the 3rd-party deps core needs — Plan 01 already declared them in common/pom.xml)
    - zgbas-common/pom.xml (confirm deps from Plan 01 are present)
    - .planning/phases/02-infrastructure/02-RESEARCH.md §"Pattern 1: Layered Inline Compile-Gate" lines 383-424 (the layer order + cross-module dep map)
  </read_first>
  <action>
    Copy the entire directory tree `/Users/alan/WorkSpace/IDEA/tools/spt-tools-core/src/main/java/com/spt/tools/core/` into `/Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-common/src/main/java/com/spt/tools/core/` preserving package structure and all 70 .java files verbatim (D-P2-07 — zero renames, zero refactors, zero import edits). The existing Phase 1 placeholder `com/spt/tools/core/PackageMarker.java` will be superseded by the real core package; if it duplicates an existing class name, delete the PackageMarker. Do not modify any copied file.

    Run the compile gate: `mvn -pl zgbas-common -am compile`. If the gate fails with `[ERROR]` lines, the cause is a missing 3rd-party dependency not yet declared in zgbas-common/pom.xml — add the missing `<dependency>` (version resolved from root pom dependencyManagement) and recompile. Do NOT modify the copied spt-tools source; fix only the pom. Locale is zh_CN so read `^\[ERROR]` lines only (Pitfall 6). Repeat until 0 `[ERROR]`.
  </action>
  <verify>
    <automated>cd /Users/alan/WorkSpace/IDEA/zgbas-plus && JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml -pl zgbas-common -am compile 2>&1 | tee /tmp/p2-core.log | tail -3; echo "ERROR_COUNT=$(grep -c '^\[ERROR\]' /tmp/p2-core.log)"; echo "FILE_COUNT=$(find zgbas-common/src/main/java/com/spt/tools/core -name '*.java' | wc -l)"</automated>
  </verify>
  <done>
    - 70 .java files (±2 for PackageMarker resolution) exist under zgbas-common/src/main/java/com/spt/tools/core/ (FILE_COUNT >= 69).
    - `mvn -pl zgbas-common -am compile` exits BUILD SUCCESS with ERROR_COUNT == 0.
    - Package declarations in all copied files are `com.spt.tools.core.*` (no renames — D-P2-07).
  </done>
</task>

<task type="auto">
  <name>Task 2: Inline spt-tools-data/http/file (layer 2) — compile gate</name>
  <files>zgbas-common/src/main/java/com/spt/tools/data/**, zgbas-common/src/main/java/com/spt/tools/http/**, zgbas-common/src/main/java/com/spt/tools/file/**, zgbas-common/pom.xml</files>
  <read_first>
    - /Users/alan/WorkSpace/IDEA/tools/spt-tools-data/src/main/java/com/spt/tools/data/ (17 files — DataSourceCreator, DataSourceConfig, DataEntity)
    - /Users/alan/WorkSpace/IDEA/tools/spt-tools-http/src/main/java/com/spt/tools/http/ (15 files — FeignConfig, TokenUtil, http utils)
    - /Users/alan/WorkSpace/IDEA/tools/spt-tools-file/src/main/java/com/spt/tools/file/ (8 files — ExcelUtil, file utils)
    - The 3 source module poms (spt-tools-{data,http,file}/pom.xml) for their 3rd-party deps
    - zgbas-common/pom.xml (confirm deps; may need additions if gate fails)
  </read_first>
  <action>
    Copy the 3 directory trees verbatim (D-P2-07) into zgbas-common/src/main/java/com/spt/tools/:
    - data/ (17 files — brings DataSourceConfig, DataSourceCreator, DataEntity; depends on core only)
    - http/ (15 files — brings FeignConfig, TokenUtil; depends on core only; uses spring-cloud-starter-openfeign + httpclient + jjwt)
    - file/ (8 files — brings ExcelUtil etc; depends on core only; uses poi 4.1.2 + easyexcel + fastjson + zxing)

    Run the compile gate: `mvn -pl zgbas-common -am compile`. If `[ERROR]` lines appear, add missing 3rd-party deps to zgbas-common/pom.xml (not to the copied source). Expected possible missing deps: jjwt (io.jsonwebtoken:jjwt — version inherited from Spring Boot management per RESEARCH A6; if unresolved, pin explicitly), httpcore. Fix pom only, never the spt-tools source. Repeat until ERROR_COUNT == 0.

    These 3 modules are the direct children of core (RESEARCH cross-module dep map: data/http/file all depend on core only). Compile together after core is green.
  </action>
  <verify>
    <automated>cd /Users/alan/WorkSpace/IDEA/zgbas-plus && JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml -pl zgbas-common -am compile 2>&1 | tee /tmp/p2-l2.log | tail -3; echo "ERROR_COUNT=$(grep -c '^\[ERROR\]' /tmp/p2-l2.log)"; echo "DATA_COUNT=$(find zgbas-common/src/main/java/com/spt/tools/data -name '*.java' | wc -l)"; echo "HTTP_COUNT=$(find zgbas-common/src/main/java/com/spt/tools/http -name '*.java' | wc -l)"; echo "FILE_COUNT=$(find zgbas-common/src/main/java/com/spt/tools/file -name '*.java' | wc -l)"</automated>
  </verify>
  <done>
    - data/ has 17 files, http/ has 15 files, file/ has 8 files (±1) under zgbas-common.
    - `mvn -pl zgbas-common -am compile` BUILD SUCCESS, ERROR_COUNT == 0.
    - DataSourceCreator.java present and unmodified (grep for `createDataSource` confirms the factory method exists).
    - FeignConfig.java present and unmodified.
    - All packages are com.spt.tools.{data,http,file}.* verbatim (D-P2-07).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source project → zgbas-common | Verbatim copy from /Users/alan/WorkSpace/IDEA/tools (trusted local source tree, the migration origin) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-P2-02-copy | Tampering | Verbatim source copy integrity | accept | Source tree is the trusted migration origin (same codebase, branch feat-系统重构v5.0). No untrusted input. Compile gate validates integrity. |
| T-P2-02-deps | Tampering | Missing dependency → build failure | mitigate | Compile gate between layers surfaces missing deps immediately; fix pom only (never source). Phase 1 gotcha-cascade lesson applied. |
</threat_model>

<verification>
- Layer 1 (core): `mvn -pl zgbas-common -am compile` 0 `[ERROR]`, 70 files present.
- Layer 2 (data/http/file): `mvn -pl zgbas-common -am compile` 0 `[ERROR]`, 40 files present.
- Total common spt-tools files: ~110 (core 70 + data 17 + http 15 + file 8).
- DataSourceCreator + FeignConfig present and unmodified.
</verification>

<success_criteria>
spt-tools-core (INLINE-01) and spt-tools-data/http/file (INLINE-02) are inlined verbatim into zgbas-common with compile gates green between layers. The foundation for Plan 03 (jpa/web/mybatis/shiro/aop/config) is ready.
</success_criteria>

<output>
Create `.planning/phases/02-infrastructure/02-02-SUMMARY.md` when done
</output>
