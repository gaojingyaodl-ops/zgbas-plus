---
phase: 02-infrastructure
plan: 05
type: execute
wave: 4
depends_on: [02-03]
files_modified:
  - zgbas-system/src/main/java/com/spt/bas/client/entity/**
  - zgbas-system/src/main/java/com/spt/bas/server/dao/**
  - zgbas-system/src/main/java/com/spt/bas/system/dao/SampleMapper.java
  - zgbas-system/src/main/resources/mybatis/mappers/SampleMapper.xml
autonomous: true
requirements: [PERSIST-01, PERSIST-03, PERSIST-04]

must_haves:
  truths:
    - "239 basClient entities (com.spt.bas.client.entity.*) are bulk-copied verbatim into zgbas-system and compile against IdEntity (PERSIST-01)"
    - "240 basServer Dao (com.spt.bas.server.dao.*) are bulk-copied verbatim and compile against BaseDao (PERSIST-01)"
    - "Entity/Dao packages are com.spt.bas.client.entity.* and com.spt.bas.server.dao.* — UNCHANGED (zero renames, else 1226+ import cascade in Phase 4)"
    - "basWx/purchase entities (~11) are excluded (D-P2-05, v2 deferred)"
    - "A trivial sample Mapper (select count(*) from t_api_external_his) proves mybatis-plus queries through the same DataSource as JPA (PERSIST-03)"
    - "The @EntityListeners audit chain compiles intact across all 239 entities (PERSIST-04)"
  artifacts:
    - path: "zgbas-system/src/main/java/com/spt/bas/client/entity/"
      provides: "239 @Entity classes extending IdEntity — the JPA entity layer (PERSIST-01)"
      min_lines: 1
    - path: "zgbas-system/src/main/java/com/spt/bas/server/dao/"
      provides: "240 Dao interfaces extending BaseDao<T> — the JPA data-access layer (PERSIST-01)"
    - path: "zgbas-system/src/main/java/com/spt/bas/system/dao/SampleMapper.java"
      provides: "@MyBatisDao trivial Mapper proving dual-ORM same-source query (PERSIST-03)"
      contains: "countAll"
    - path: "zgbas-system/src/main/resources/mybatis/mappers/SampleMapper.xml"
      provides: "mybatis XML for SampleMapper.countAll — select count(*) from t_api_external_his"
      contains: "t_api_external_his"
  key_links:
    - from: "zgbas-system entities (com.spt.bas.client.entity.*)"
      to: "com.spt.tools.jpa.vo.IdEntity (inlined in Plan 03)"
      via: "every entity extends IdEntity — verbatim package must match or @EntityScan fails"
      pattern: "extends IdEntity"
    - from: "zgbas-system Dao (com.spt.bas.server.dao.*)"
      to: "com.spt.tools.jpa.dao.BaseDao (inlined in Plan 03)"
      via: "every Dao extends BaseDao<T> — @EnableJpaRepositories basePackages in Plan 06 targets this package"
      pattern: "extends BaseDao"
    - from: "SampleMapper.xml"
      to: "t_api_external_his table (backs ApiExternalHis @Entity)"
      via: "count(*) query — dual-ORM proof"
      pattern: "t_api_external_his"
---

<objective>
Bulk-copy the entire JPA data layer (239 entities + 240 Dao) verbatim into zgbas-system and create a trivial sample Mapper proving dual-ORM same-source query. This delivers PERSIST-01 (entity+Dao migration), PERSIST-03 (mybatis sample Mapper proof), and PERSIST-04 (audit chain compiles across all entities).

Purpose: The entities and Dao are the data-layer foundation for Phase 4 business migration. They are mechanical verbatim copies (D-P2-01 — "搬运而非重造"). Package names MUST stay com.spt.bas.client.entity.* and com.spt.bas.server.dao.* unchanged, because Phase 4 will bulk-copy business code that imports these packages (1226+ references) — any rename cascades. The sample Mapper proves mybatis-plus can query through the same Druid DataSource as JPA (the PERSIST-03 success criterion).

Output: ~479 bulk-copied source files + 1 sample Mapper java + 1 sample Mapper XML in zgbas-system.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-infrastructure/02-CONTEXT.md
@.planning/phases/02-infrastructure/02-RESEARCH.md
@.planning/phases/02-infrastructure/02-PATTERNS.md
@.planning/phases/02-infrastructure/02-03-SUMMARY.md

**BUILD COMMAND PREFIX:** `JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml`

**Entity source (verbatim copy origin):** `/Users/alan/WorkSpace/IDEA/zgbas/basCore/basClient/src/main/java/com/spt/bas/client/entity/`
**Dao source (verbatim copy origin):** `/Users/alan/WorkSpace/IDEA/zgbas/basCore/basServer/src/main/java/com/spt/bas/server/dao/`

<interfaces>
<!-- Inlined contracts from common (Plan 03) that entities/Dao compile against. -->

```java
// com.spt.tools.jpa.vo.IdEntity — @MappedSuperclass @EntityListeners(EntityListener.class)
// All 239 entities extend this. Audit fields (createdDate/updatedDate) auto-populated.
public abstract class IdEntity extends DataEntity { ... }

// com.spt.tools.jpa.dao.BaseDao — @NoRepositoryBean
// All 240 Dao extend this. Spring Data JPA provides CRUD.
@NoRepositoryBean
public interface BaseDao<T> extends PagingAndSortingRepository<T, Long>, JpaSpecificationExecutor<T> {}

// com.spt.tools.mybatis.annotation.MyBatisDao — @Component-meta-annotated
// SampleMapper uses this. @MapperScan(annotationClass=MyBatisDao.class) in Plan 04 picks it up.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bulk-copy 239 entities + 240 Dao verbatim into zgbas-system</name>
  <files>zgbas-system/src/main/java/com/spt/bas/client/entity/**, zgbas-system/src/main/java/com/spt/bas/server/dao/**</files>
  <read_first>
    - /Users/alan/WorkSpace/IDEA/zgbas/basCore/basClient/src/main/java/com/spt/bas/client/entity/ (entity source tree — 239 files, 234 with @Entity)
    - /Users/alan/WorkSpace/IDEA/zgbas/basCore/basServer/src/main/java/com/spt/bas/server/dao/ (Dao source tree — 240 files across subpackages)
    - /Users/alan/WorkSpace/IDEA/zgbas/basCore/basClient/src/main/java/com/spt/bas/client/entity/ApiExternalHis.java (sample entity — confirms extends IdEntity + @Table(name="t_api_external_his"), the table SampleMapper queries)
    - /Users/alan/WorkSpace/IDEA/zgbas/basCore/basServer/src/main/java/com/spt/bas/server/dao/BsDictTypeDao.java (sample Dao — confirms extends BaseDao<T> + @Query pattern)
    - .planning/phases/02-infrastructure/02-RESEARCH.md §"PERSIST-01" line 72 (actual counts: 234 @Entity / 239 entity files + 240 Dao)
    - .planning/phases/02-infrastructure/02-RESEARCH.md §"Anti-Patterns" line 448 (NEVER rename entity/Dao packages — cascades into 1226+ import edits in Phase 4)
    - zgbas-system/pom.xml (confirm it depends on common + framework — entities compile against IdEntity/BaseDao from common)
  </read_first>
  <action>
    Bulk-copy the entity directory tree: copy `/Users/alan/WorkSpace/IDEA/zgbas/basCore/basClient/src/main/java/com/spt/bas/client/entity/` recursively into `/Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-system/src/main/java/com/spt/bas/client/entity/`. This preserves package com.spt.bas.client.entity.* verbatim (D-P2-01/05 — zero renames, zero refactors). All 239 files including subpackages.

    Bulk-copy the Dao directory tree: copy `/Users/alan/WorkSpace/IDEA/zgbas/basCore/basServer/src/main/java/com/spt/bas/server/dao/` recursively into `/Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-system/src/main/java/com/spt/bas/server/dao/`. Preserves package com.spt.bas.server.dao.* verbatim. All 240 files including subpackages.

    EXCLUDE basWx/purchase entities (~11 files — D-P2-05, v2 deferred). These live in a DIFFERENT source path (basWx module, not basClient.entity), so they are naturally excluded by copying only the basClient.entity and basServer.dao directories.

    Run the compile gate: `mvn -pl zgbas-system -am compile`. Entities extend IdEntity (from common's com.spt.tools.jpa.vo) and Dao extend BaseDao (from common's com.spt.tools.jpa.dao) — both inlined in Plan 03. If [ERROR] lines appear, the likely cause is a @Query referencing an entity not yet copied, or a missing JPA import. Per RESEARCH assumption A3 (LOW risk — source already runs on Hibernate 5.4), most entities should compile clean. Fix only if a file references a basWx/purchase entity (exclude that file and note it for v2). Do NOT rename packages or refactor entities.

    The Phase 1 placeholder zgbas-system/src/main/java/com/spt/system/PackageMarker.java can remain (different package com.spt.system, no conflict with com.spt.bas.*).
  </action>
  <verify>
    <automated>cd /Users/alan/WorkSpace/IDEA/zgbas-plus && JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml -pl zgbas-system -am compile 2>&1 | tee /tmp/p2-entity.log | tail -5; echo "ERROR_COUNT=$(grep -c '^\[ERROR\]' /tmp/p2-entity.log)"; echo "ENTITY_COUNT=$(find zgbas-system/src/main/java/com/spt/bas/client/entity -name '*.java' | wc -l)"; echo "DAO_COUNT=$(find zgbas-system/src/main/java/com/spt/bas/server/dao -name '*.java' | wc -l)"; echo "IDEXTEND=$(grep -rl 'extends IdEntity' zgbas-system/src/main/java/com/spt/bas/client/entity/ | wc -l)"; echo "BASEDAOEXTEND=$(grep -rl 'extends BaseDao' zgbas-system/src/main/java/com/spt/bas/server/dao/ | wc -l)"</automated>
  </verify>
  <done>
    - ENTITY_COUNT == 239 (±2); DAO_COUNT == 240 (±2).
    - IDEXTEND >= 200 (most entities extend IdEntity — confirms PERSIST-01 inheritance chain + PERSIST-04 audit base).
    - BASEDAOEXTEND >= 200 (most Dao extend BaseDao).
    - `mvn -pl zgbas-system -am compile` BUILD SUCCESS, ERROR_COUNT == 0 (or documented exclusions for basWx-coupled files).
    - Package declarations are com.spt.bas.client.entity.* and com.spt.bas.server.dao.* (grep confirms no package renames — D-P2-01).
    - ApiExternalHis.java present with @Table(name="t_api_external_his") (the table SampleMapper will query).
  </done>
</task>

<task type="auto">
  <name>Task 2: Sample Mapper — dual-ORM same-source query proof (PERSIST-03)</name>
  <files>zgbas-system/src/main/java/com/spt/bas/system/dao/SampleMapper.java, zgbas-system/src/main/resources/mybatis/mappers/SampleMapper.xml</files>
  <read_first>
    - .planning/phases/02-infrastructure/02-RESEARCH.md §"C. Trivial Sample Mapper" lines 676-703 (the EXACT target code for SampleMapper.java + SampleMapper.xml)
    - .planning/phases/02-infrastructure/02-RESEARCH.md §"D-P2-04" line 24 (mybatis scope = infra + trivial sample Mapper)
    - /Users/alan/WorkSpace/IDEA/zgbas/basReport/reportServer/src/main/java/com/spt/bas/report/server/dao/RptInvoiceBillMapper.java (source Mapper analog — confirms @MyBatisDao interface shape)
    - zgbas-common/src/main/java/com/spt/tools/mybatis/annotation/MyBatisDao.java (confirm the annotation exists from Plan 03)
    - zgbas-framework/src/main/java/com/spt/framework/config/ZgbasMybatisConfig.java (confirm @MapperScan basePackages = "com.spt.bas.system.dao" from Plan 04 — this is where SampleMapper lives)
  </read_first>
  <action>
    Create SampleMapper.java at zgbas-system/src/main/java/com/spt/bas/system/dao/SampleMapper.java per RESEARCH §C lines 680-690. Package com.spt.bas.system.dao. Annotated @MyBatisDao (the inlined annotation from common — picked up by ZgbasMybatisConfig's @MapperScan basePackages="com.spt.bas.system.dao" + annotationClass=MyBatisDao.class). Single method: long countAll().

    Create SampleMapper.xml at zgbas-system/src/main/resources/mybatis/mappers/SampleMapper.xml per RESEARCH §C lines 694-702. Namespace com.spt.bas.system.dao.SampleMapper. One select: id="countAll" resultType="long" with SQL `select count(*) from t_api_external_his`. This table backs ApiExternalHis @Entity (verified in Task 1 — extends IdEntity + @Table(name="t_api_external_his")), proving both ORMs query the same schema through the same DataSource.

    The mapper XML location (resources/mybatis/mappers/) matches the mybatis-plus.mapper-locations=classpath:/mybatis/mappers/*Mapper.xml config that Plan 06 will set in application.yml.

    Compile gate: `mvn -pl zgbas-system -am compile`. The SampleMapper interface compiles against @MyBatisDao (from common). XML is a resource, not compiled.
  </action>
  <verify>
    <automated>cd /Users/alan/WorkSpace/IDEA/zgbas-plus && JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml -pl zgbas-system -am compile 2>&1 | tee /tmp/p2-mapper.log | tail -3; echo "ERROR_COUNT=$(grep -c '^\[ERROR\]' /tmp/p2-mapper.log)"; echo "MAPPER_JAVA=$(test -f zgbas-system/src/main/java/com/spt/bas/system/dao/SampleMapper.java && echo 1 || echo 0)"; echo "MAPPER_XML=$(test -f zgbas-system/src/main/resources/mybatis/mappers/SampleMapper.xml && echo 1 || echo 0)"; echo "MYBATISDAO=$(grep -c '@MyBatisDao' zgbas-system/src/main/java/com/spt/bas/system/dao/SampleMapper.java)"; echo "TABLE_REF=$(grep -c 't_api_external_his' zgbas-system/src/main/resources/mybatis/mappers/SampleMapper.xml)"</automated>
  </verify>
  <done>
    - SampleMapper.java exists (MAPPER_JAVA == 1) with @MyBatisDao annotation (MYBATISDAO >= 1) and countAll() method.
    - SampleMapper.xml exists (MAPPER_XML == 1) with namespace com.spt.bas.system.dao.SampleMapper and SQL referencing t_api_external_his (TABLE_REF >= 1).
    - `mvn -pl zgbas-system -am compile` BUILD SUCCESS, ERROR_COUNT == 0.
    - Package com.spt.bas.system.dao matches the @MapperScan basePackages in ZgbasMybatisConfig (Plan 04).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source DB schema → entity validation | ddl-auto=validate (Plan 06) will check entity @Table mappings against sptbasdb_pd at startup — drift surfaces as startup failure |
| Source project → zgbas-system | Verbatim copy of 479 data-layer files from trusted migration origin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-P2-05-copy | Tampering | Bulk-copy integrity (479 files) | accept | Trusted migration origin; compile gate validates all entities/Dao against IdEntity/BaseDao. RESEARCH A3: LOW risk — source runs on same Hibernate 5.4. |
| T-P2-05-persist04 | Repudiation | Audit field integrity (PERSIST-04) | mitigate | IdEntity @EntityListeners(EntityListener.class) preserved (Plan 03); all entities extend IdEntity → createdDate/updatedDate auto-populated. Compile gate confirms chain intact. Startup validate confirms mapping. |
| T-P2-05-exclude | Tampering | basWx/purchase entity leakage | mitigate | D-P2-05: only basClient.entity + basServer.dao directories copied; basWx is a separate source module, naturally excluded. Verified no basWx packages in copy. |
</threat_model>

<verification>
- Entity count: 239 files under com.spt.bas.client.entity (PERSIST-01).
- Dao count: 240 files under com.spt.bas.server.dao (PERSIST-01).
- `mvn -pl zgbas-system -am compile` 0 `[ERROR]` (PERSIST-01 + PERSIST-04 compile-level).
- SampleMapper.java + XML present, referencing t_api_external_his (PERSIST-03 proof).
- No basWx/purchase packages present (D-P2-05).
</verification>

<success_criteria>
239 entities + 240 Dao bulk-copied verbatim into zgbas-system with compile green (PERSIST-01). Audit chain (IdEntity + EntityListener) compiles across all entities (PERSIST-04). Trivial sample Mapper created proving mybatis-plus queries through the same DataSource (PERSIST-03). Data layer ready for Plan 06 boot wiring + startup validate.
</success_criteria>

<output>
Create `.planning/phases/02-infrastructure/02-05-SUMMARY.md` when done
</output>
