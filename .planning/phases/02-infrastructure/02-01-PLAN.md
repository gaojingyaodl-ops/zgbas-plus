---
phase: 02-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pom.xml
  - zgbas-common/pom.xml
  - zgbas-framework/pom.xml
  - zgbas-system/pom.xml
  - zgbas-admin/pom.xml
autonomous: true
requirements: [INLINE-04, EXT-01, EXT-02, EXT-03, EXT-04, INFRA-01]

must_haves:
  truths:
    - "Root pom declares pinned versions (D-P2-08) for every 3rd-party lib the 10 inlined spt-tools modules need"
    - "Root pom dependencyManagement imports spring-cloud-dependencies BOM 2020.0.5 and declares the 4 external SDK coords"
    - "No spt-tools-* artifact appears in any pom (INLINE-04) and no nacos artifact appears (INFRA-01)"
    - "Each module pom declares the deps its Wave 2-5 code needs so downstream compile gates stay green"
    - "mvn compile across all 6 reactor projects still exits BUILD SUCCESS with zero [ERROR]"
  artifacts:
    - path: "pom.xml"
      provides: "pinned <properties> + <dependencyManagement> (spring-cloud BOM + pinned 3rd-party + 4 external SDK coords)"
      contains: "spring-cloud-dependencies"
    - path: "zgbas-common/pom.xml"
      provides: "declares every pinned 3rd-party dep the 10 spt-tools modules import"
      contains: "hutool-all"
    - path: "zgbas-framework/pom.xml"
      provides: "declares auth-sdk / spt-push-sdk / spt-file-sdk + spring-data-jpa + mybatis-plus-boot-starter"
      contains: "auth-sdk"
    - path: "zgbas-admin/pom.xml"
      provides: "declares spt-sign-client dep (EXT-03)"
      contains: "spt-sign-client"
  key_links:
    - from: "zgbas-framework/pom.xml"
      to: "root pom dependencyManagement"
      via: "external SDK version resolution"
      pattern: "auth-sdk|spt-push-sdk|spt-file-sdk"
    - from: "zgbas-admin/pom.xml"
      to: "root pom dependencyManagement"
      via: "spt-sign-client version resolution"
      pattern: "spt-sign-client"
---

<objective>
Establish the POM foundation for all Phase 2 work: pin every 3rd-party version the inlined spt-tools modules need (D-P2-08), declare the 4 external HTTP SDK coordinates, and wire each module pom with the deps its downstream code requires. This is Wave 1 — every subsequent plan compiles against these declarations.

Purpose: Without pinned versions + module dep declarations, the Wave 2 spt-tools inline compile gates would fail on unmanaged transitive versions (Phase 1 "gotcha cascade" lesson). This plan makes the build deterministic before any source is copied.

Output: Updated root pom.xml (properties + dependencyManagement) + 4 module pom deltas. No new source files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-infrastructure/02-CONTEXT.md
@.planning/phases/02-infrastructure/02-RESEARCH.md
@.planning/phases/02-infrastructure/02-PATTERNS.md

**BUILD COMMAND PREFIX (JDK8 invariant — use verbatim on every mvn invocation):**
`JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml`

Locale is zh_CN → compile-error grep MUST be locale-independent: count `^\[ERROR\]` lines, never grep for "cannot find symbol" / "找不到符号" alone (Pitfall 6).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Root pom — pinned versions + dependencyManagement</name>
  <files>pom.xml</files>
  <read_first>
    - /Users/alan/WorkSpace/IDEA/zgbas-plus/pom.xml (current Phase 1 root — the file you are modifying)
    - .planning/phases/02-infrastructure/02-RESEARCH.md §"Installation (root pom.xml delta)" lines 150-214 (the EXACT properties + dependencyManagement block to reproduce)
    - /Users/alan/App/Repository/com/spt/spt-parent/2.5.3/spt-parent-2.5.3.pom (version source-of-truth for shiro/druid/poi/spring-cloud/guava)
    - /Users/alan/WorkSpace/IDEA/zgbas/bas-parent/pom.xml (version source for hutool/fastjson/spt-push-sdk/spt-file-sdk)
  </read_first>
  <action>
    Edit the existing root pom.xml (com.spt:zgbas-plus:1.0.0-SNAPSHOT, parent spring-boot-starter-parent:2.5.9). Add to the existing `<properties>` block the D-P2-08 pinned version properties exactly as listed in RESEARCH §152-172: spring-cloud.version=2020.0.5, hutool.version=5.5.9, fastjson.version=1.2.75, druid.version=1.2.8, shiro.version=1.8.0, mybatis-plus.version=3.1.2, mysql-connector.version=8.0.13, poi.version=4.1.2, guava.version=27.1-jre, commons-validator.version=1.4.0, commons-collections.version=3.2.2, commons-text.version=1.1, pinyin4j.version=2.5.1, easyexcel.version=1.1.2-beta4, zxing.version=3.3.0, commons-io.version=2.11.0. Keep java.version=1.8.

    Extend the existing `<dependencyManagement>` block (which already holds the 4 zgbas-* internal module entries) by appending: (a) spring-cloud-dependencies BOM 2020.0.5 pom import; (b) every pinned 3rd-party `<dependency>` entry from RESEARCH §174-211 (hutool-all, hutool-core, fastjson, druid, mybatis-plus-boot-starter, mybatis-plus-generator, shiro-spring, shiro-ehcache, shiro-cas, mysql-connector-java, poi, poi-ooxml, poi-ooxml-schemas, easyexcel, javase from com.google.zxing, commons-validator, commons-collections, commons-text, pinyin4j, guava); (c) the 4 external SDK coordinates (com.spt:auth-sdk:3.8.2-SNAPSHOT, com.spt.micoservice:spt-push-sdk:2.0.15-SNAPSHOT, com.spt.micoservice:spt-file-sdk:2.1.5-SNAPSHOT, com.spt:spt-sign-client:1.0.0-SNAPSHOT). Do NOT add any spt-tools-* artifact (INLINE-04) and do NOT add any nacos artifact (INFRA-01).
  </action>
  <verify>
    <automated>JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml -q help:effective-pom 2>&1 | tail -5</automated>
  </verify>
  <done>
    - pom.xml `<properties>` contains all 16 pinned version keys listed above (grep -c matches 16).
    - pom.xml `<dependencyManagement>` contains `spring-cloud-dependencies` (BOM import), `auth-sdk`, `spt-push-sdk`, `spt-file-sdk`, `spt-sign-client`.
    - `grep -c 'spt-tools' pom.xml` returns 0 (no spt-tools jar dep); `grep -c 'nacos' pom.xml` returns 0.
    - `mvn help:effective-pom` exits BUILD SUCCESS (pom is well-formed XML, all version refs resolve).
  </done>
</task>

<task type="auto">
  <name>Task 2: Module pom deltas — declare deps per module role</name>
  <files>zgbas-common/pom.xml, zgbas-framework/pom.xml, zgbas-system/pom.xml, zgbas-admin/pom.xml</files>
  <read_first>
    - The 4 module poms listed in <files> (current Phase 1 baseline — PackageMarker-only modules)
    - /Users/alan/WorkSpace/IDEA/tools/spt-tools-core/pom.xml, spt-tools-data/pom.xml, spt-tools-http/pom.xml, spt-tools-file/pom.xml, spt-tools-jpa/pom.xml, spt-tools-web/pom.xml, spt-tools-mybatis/pom.xml, spt-tools-shiro/pom.xml, spt-tools-aop/pom.xml (the exact 3rd-party deps each module declares — these define what common/pom.xml must provide)
    - .planning/phases/02-infrastructure/02-RESEARCH.md §"Standard Stack" lines 98-130 and §"Recommended Project Structure" lines 330-381 (which deps go in which module)
    - .planning/phases/02-infrastructure/02-PATTERNS.md §"Wave 0" lines 36-41 (spt-tools-config gotcha: drop kafka/redis)
  </read_first>
  <action>
    zgbas-common/pom.xml: add `<dependencies>` entries for every 3rd-party lib the 10 inlined spt-tools modules import (read each source module pom to compile the full list). The required set: hutool-all, fastjson, druid, mysql-connector-java, mybatis-plus-boot-starter, mybatis-plus-generator, freemarker (spt-tools-mybatis generator), shiro-spring, shiro-ehcache, shiro-cas, poi, poi-ooxml, poi-ooxml-schemas, easyexcel, com.google.zxing:javase, commons-validator, commons-collections, commons-text, pinyin4j, guava, commons-lang3, commons-io, plus the spring-boot starters the inlined code references (spring-boot-starter-web, spring-boot-starter-data-jpa, spring-boot-starter-aop, spring-boot-starter-jdbc), hibernate-ehcache, org.apache.httpcomponents:httpclient + httpcore, and org.springframework.cloud:spring-cloud-starter-openfeign. Declare lombok as optional. Do NOT add spt-tools-kafka or spt-tools-redis deps (D-P2-06: those modules are skipped; spt-tools-config's 2 source files only import core/http/jpa/shiro/aop — verified RESEARCH §424).

    zgbas-framework/pom.xml: add `<dependency>` entries for com.spt:auth-sdk, com.spt.micoservice:spt-push-sdk, com.spt.micoservice:spt-file-sdk (the 3 external SDK facades ZgbasExternalBeansConfig will @Bean-declare), plus org.springframework.boot:spring-boot-starter-data-jpa and com.baomidou:mybatis-plus-boot-starter (made explicit per RESEARCH §349 even though common provides them transitively — keeps framework self-documenting). It already depends on zgbas-common (Phase 1 D-08 topology).

    zgbas-admin/pom.xml: add `<dependency>` com.spt:spt-sign-client (EXT-03 — the cfca FeignClient jar with SignClientConfig). Do NOT explicitly declare spring-cloud-starter-openfeign (it arrives transitively via common's spt-tools-http inline — RESEARCH §451 anti-pattern). It already depends on all 4 internal modules.

    zgbas-system/pom.xml: no new `<dependency>` entries needed (it already depends on common + framework per Phase 1 D-08; mybatis-plus + spring-data-jpa arrive transitively from common). Verify this is sufficient for the entity/Dao/sample-Mapper compile in Wave 4.
  </action>
  <verify>
    <automated>JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml clean compile 2>&1 | tee /tmp/p2w1-build.log | tail -3; echo "ERROR_COUNT=$(grep -c '^\[ERROR\]' /tmp/p2w1-build.log)"; echo "NACOS_CHECK=$(JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml -pl zgbas-admin dependency:tree -Dincludes=com.alibaba.cloud:* 2>/dev/null | grep -c 'com.alibaba.cloud')"</automated>
  </verify>
  <done>
    - `mvn clean compile` across all 6 reactor projects exits BUILD SUCCESS with `grep -c '^\[ERROR\]'` == 0 (no compile errors — PackageMarker classes still compile with the new deps).
    - zgbas-common/pom.xml declares hutool-all, fastjson, druid, mybatis-plus-boot-starter, shiro-spring, poi, spring-boot-starter-data-jpa, spring-cloud-starter-openfeign (grep confirms each artifactId present).
    - zgbas-framework/pom.xml declares auth-sdk, spt-push-sdk, spt-file-sdk (grep -E 'auth-sdk|spt-push-sdk|spt-file-sdk' matches 3).
    - zgbas-admin/pom.xml declares spt-sign-client (grep matches).
    - `dependency:tree -Dincludes=com.alibaba.cloud:*` on zgbas-admin returns 0 (NACOS_CHECK=0 — INFRA-01 nacos absence verified).
    - `dependency:tree -Dincludes=com.spt.tools:spt-tools-*` on zgbas-admin returns 0 (INLINE-04 — no spt-tools jar).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maven nexus → build | Private nexus (zg_settings.xml) resolves 4 external SDK SNAPSHOTs + pinned public libs; no untrusted input crosses here in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-P2-SC-01 | Tampering | Maven dependency resolution (nexus + Maven Central) | accept | All pinned libs are established projects with jars verified on disk in /Users/alan/App/Repository (RESEARCH §Package Legitimacy Audit — slopcheck false positives, all corroborated by source poms + local jars). No checkpoint needed. |
| T-P2-01-nacos | Information Disclosure / Elevation | nacos discovery residue | mitigate | INFRA-01: verified absent from dependency:tree (NACOS_CHECK=0); skeleton never had nacos — fresh build. Phase 4 swap rule documented in RESEARCH Pitfall 8. |
| T-P2-01-secrets | Information Disclosure | pom.xml secrets | mitigate | No secrets in poms — secrets are env-var placeholders in application-*.yml (Plan 06, D-P2-13). POM only holds non-sensitive version coords + SDK artifact coordinates. |
</threat_model>

<verification>
- `mvn clean compile` BUILD SUCCESS, 0 `[ERROR]` lines (locale-independent grep).
- `dependency:tree -Dincludes=com.alibaba.cloud:*` empty (INFRA-01).
- `dependency:tree -Dincludes=com.spt.tools:spt-tools-*` empty (INLINE-04).
- Root pom has 16 pinned version properties + spring-cloud BOM + 4 SDK coords.
</verification>

<success_criteria>
Root pom pins all D-P2-08 versions; module poms declare every dep the downstream waves need; build stays green; nacos + spt-tools jars confirmed absent from the dependency graph. This unblocks Wave 2 (spt-tools inline compile gates).
</success_criteria>

<output>
Create `.planning/phases/02-infrastructure/02-01-SUMMARY.md` when done
</output>
