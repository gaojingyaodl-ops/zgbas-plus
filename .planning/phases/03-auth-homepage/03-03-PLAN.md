---
phase: 03-auth-homepage
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - zgbas-framework/src/main/java/com/spt/bas/web/ws/WebSocketConfig.java
  - zgbas-framework/src/main/java/com/spt/bas/web/ws/IndexWebSocketServer.java
  - zgbas-framework/src/main/java/com/spt/bas/web/ws/WebSocketServer.java
  - zgbas-framework/src/main/java/com/spt/bas/web/ws/po/Message.java
  - zgbas-framework/pom.xml
  - zgbas-admin/src/main/resources/templates/
  - zgbas-admin/src/main/resources/static/
autonomous: true
requirements: [AUTH-02]
tags: [websocket, templates, static-resources, bulk-copy]

must_haves:
  truths:
    - "WebSocketConfig + IndexWebSocketServer + WebSocketServer(stub) + Message 存在于 zgbas-framework"
    - "WebSocketServer.@OnOpen approve-count 块已 stub（IApproveWaitDealClient Phase-4 依赖注释掉）"
    - "zgbas-admin/resources/templates/ 包含源 web 全部 608 模板（含 login.html/index.html/admin/index.html）"
    - "zgbas-admin/resources/static/ 包含源 web 全量静态资源（390 JS + 352 CSS）"
    - "mvn compile framework+admin 零错误"
  artifacts:
    - path: "zgbas-framework/src/main/java/com/spt/bas/web/ws/WebSocketConfig.java"
      provides: "WebSocket 自动配置 + ServerEndpointExporter"
      contains: "EnableWebSocket"
    - path: "zgbas-framework/src/main/java/com/spt/bas/web/ws/WebSocketServer.java"
      provides: "WebSocket 端点 stub（@OnOpen approve-count 注释，Phase 4 接通）"
      contains: "ServerEndpoint"
    - path: "zgbas-admin/src/main/resources/templates/login.html"
      provides: "登录页模板"
    - path: "zgbas-admin/src/main/resources/templates/index.html"
      provides: "首页模板"
  key_links:
    - from: "WebSocketConfig.registerWebSocketHandlers()"
      to: "IndexWebSocketServer"
      via: "addHandler(new IndexWebSocketServer(), \"/fundSocket/\")"
      pattern: "IndexWebSocketServer"
    - from: "LoginController return 'login'"
      to: "templates/login.html"
      via: "Thymeleaf classpath resolution"
      pattern: "login\\.html"
---

<objective>
将源 zgbas/web 的 WebSocket 端点三件套照搬进 zgbas-framework（WebSocketServer 的 Phase-4 approve-count 依赖做 stub），并将源 web 全部前端资源（608 HTML 模板 + 742 JS/CSS）批量复制进 zgbas-admin/resources。

Purpose: AUTH-02（前端 UI 面照搬）+ D-P3-11（WebSocket 端点照搬，先连上闲置）+ D-P3-08（全量模板/静态资源搬运）。
Output: framework 内 4 WS Java 文件 + websocket starter 依赖；admin 内全量 templates/ + static/。
</objective>

<execution_context>
@/Users/alan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-auth-homepage/03-CONTEXT.md
@.planning/phases/03-auth-homepage/03-PATTERNS.md
@.planning/phases/03-auth-homepage/03-01-SUMMARY.md

**BUILD PREFIX:** `JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml`

**源资源根:** `/Users/alan/WorkSpace/IDEA/zgbas/web/src/main/resources/`（取 `src/`，不取 `target/classes`）

**⚠️ Wave 2 顺序约束 (WARNING 3):** 本 Plan 与 03-02 同属 Wave 2（depends_on 仅 03-01，无互相依赖），但两者编译命令均涉及 `zgbas-admin` 模块的 `target/` 目录。若 execute-phase 以并行模式运行 Wave 2，**两者必须串行执行**（先 03-02 再 03-03，或反之），否则 Maven `target/` 写入冲突 → 假性编译失败。"parallel-eligible" = 无逻辑依赖，非强制并发。
</context>

<tasks>

<task type="auto">
  <name>Task 1: 照搬 WebSocket 三件套 + stub @OnOpen + websocket starter</name>
  <files>
    zgbas-framework/src/main/java/com/spt/bas/web/ws/WebSocketConfig.java,
    zgbas-framework/src/main/java/com/spt/bas/web/ws/IndexWebSocketServer.java,
    zgbas-framework/src/main/java/com/spt/bas/web/ws/WebSocketServer.java,
    zgbas-framework/src/main/java/com/spt/bas/web/ws/po/Message.java,
    zgbas-framework/pom.xml
  </files>
  <read_first>
    - .planning/phases/03-auth-homepage/03-PATTERNS.md §WebSocket trio
    - 源文件:
      - /Users/alan/WorkSpace/IDEA/zgbas/web/src/main/java/com/spt/bas/web/ws/WebSocketConfig.java
      - /Users/alan/WorkSpace/IDEA/zgbas/web/src/main/java/com/spt/bas/web/ws/IndexWebSocketServer.java
      - /Users/alan/WorkSpace/IDEA/zgbas/web/src/main/java/com/spt/bas/web/ws/WebSocketServer.java
      - /Users/alan/WorkSpace/IDEA/zgbas/web/src/main/java/com/spt/bas/web/ws/po/Message.java
    - 当前 framework pom: /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-framework/pom.xml (Plan 01 已加 ruoyi-common)
  </read_first>
  <action>
    **D-P3-11: WebSocket 端点照搬进 framework，WS 先连上闲置，推送业务发送方留 Phase 4。**

    1. **WebSocketConfig.java**（com.spt.bas.web.ws）— 干净照搬。@Configuration @EnableWebSocket，注册 IndexWebSocketServer at /fundSocket/，导出 ServerEndpointExporter bean。复制到 `zgbas-framework/src/main/java/com/spt/bas/web/ws/WebSocketConfig.java`。

    2. **IndexWebSocketServer.java**（com.spt.bas.web.ws）— 干净照搬。@Component @ServerEndpoint("/indexWebSocket/{userId}")，自包含（仅依赖 fastjson + Message POJO）。复制到 framework 同包。

    3. **Message.java**（com.spt.bas.web.ws.po）— 干净照搬。简单 POJO（from/to/text/date/countReadFlg/countCompleteFlg + fastjson @JSONField）。复制到 `zgbas-framework/src/main/java/com/spt/bas/web/ws/po/Message.java`。

    4. **WebSocketServer.java**（com.spt.bas.web.ws）— **照搬 + stub 修改**。@Component @ServerEndpoint("/webSocket/{username}")。源 @OnOpen（第 91-115 行）调用 `iApproveWaitDealClient.findPageWaitDealCount()`（Phase 4 FeignClient 契约，framework 不依赖 system 无法编译）。按 D-P3-11 stub:
       - 注释掉 3 个 import: `IApproveWaitDealClient`、`ApproveWaitDeal`（entity）、`ApproveWaitSearchVo`（vo）— 这些是 system/com.spt.bas.client.* 类型，framework 不可见
       - 注释掉 `private static IApproveWaitDealClient iApproveWaitDealClient;` 字段
       - 注释掉 `@Autowired public void setChatService(IApproveWaitDealClient...)` setter
       - 注释掉 @OnOpen 内 approve-count 块（从 `ApproveWaitSearchVo searchVo = new ApproveWaitSearchVo();` 到 `msg.setCountCompleteFlg(countCompleteFlg);`）
       - 保留 @OnOpen 的基础部分: sessionPools.put + addOnlineCount + 构建 Message（date/to/text）+ sendInfo
       - 每处注释加 `// Phase 4 stub: uncomment when IApproveWaitDealClient impl is ported`
       - 保留 authOpenFacade 字段 + setter（SysDeptSdk + IAuthOpenFacade 类型在 auth-sdk jar，framework 可见；保留不动即使 @OnOpen 不再用 — 编译不报错）
       其余方法（sendMessage/sendInfo/broadcast/onClose/onMessage/onError）照搬不变。

    5. **websocket starter 依赖** — 在 `zgbas-framework/pom.xml` 的 `<dependencies>` 段添加 `spring-boot-starter-websocket`（org.springframework.boot，版本由 starter-parent 管理）。runtime 通过 admin boot 解析。

    6. **编译绿灯** — `mvn compile -pl zgbas-framework,zgbas-admin -am`。迭代至零错。
  </action>
  <verify>
    <automated>
cd /Users/alan/WorkSpace/IDEA/zgbas-plus && \
echo "WS_CONFIG=$(test -f zgbas-framework/src/main/java/com/spt/bas/web/ws/WebSocketConfig.java && echo EXISTS)" && \
echo "INDEX_WS=$(test -f zgbas-framework/src/main/java/com/spt/bas/web/ws/IndexWebSocketServer.java && echo EXISTS)" && \
echo "WS_SERVER=$(test -f zgbas-framework/src/main/java/com/spt/bas/web/ws/WebSocketServer.java && echo EXISTS)" && \
echo "MESSAGE=$(test -f zgbas-framework/src/main/java/com/spt/bas/web/ws/po/Message.java && echo EXISTS)" && \
echo "WS_POM=$(grep -c 'websocket' zgbas-framework/pom.xml)" && \
echo "WS_STUB=$(grep -c 'Phase 4 stub' zgbas-framework/src/main/java/com/spt/bas/web/ws/WebSocketServer.java)" && \
echo "WS_NO_APPROVE=$(grep -v '^ *//' zgbas-framework/src/main/java/com/spt/bas/web/ws/WebSocketServer.java | grep -c 'findPageWaitDealCount')" && \
JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml -pl zgbas-framework,zgbas-admin -am compile 2>&1 | tee /tmp/p3w2b-compile.log | tail -5 && \
echo "ERROR_COUNT=$(grep -cE '^\[ERROR\]|cannot find symbol|找不到符号' /tmp/p3w2b-compile.log)"
    </automated>
  </verify>
  <acceptance_criteria>
    - WebSocketConfig.java + IndexWebSocketServer.java + WebSocketServer.java + Message.java 均存在于 framework/com/spt/bas/web/ws/
    - zgbas-framework/pom.xml grep 'websocket' 返回 ≥1
    - WebSocketServer.java 非注释行中 grep 'findPageWaitDealCount' 返回 0（approve-count stub 验证）
    - WebSocketServer.java grep 'Phase 4 stub' 返回 ≥1（stub 标注验证）
    - mvn compile framework+admin BUILD SUCCESS 且 ERROR_COUNT == 0
  </acceptance_criteria>
  <done>
    WebSocket 三件套照搬就位；WebSocketServer @OnOpen approve-count stub（Phase 4 依赖注释掉）；websocket starter 依赖就位；编译零错。
  </done>
</task>

<task type="auto">
  <name>Task 2: 批量复制全量前端资源（608 模板 + 742 静态）</name>
  <files>
    zgbas-admin/src/main/resources/templates/,
    zgbas-admin/src/main/resources/static/
  </files>
  <read_first>
    - .planning/phases/03-auth-homepage/03-PATTERNS.md §Bulk Resources（corrected counts: 608 templates / 390 JS / 352 CSS）
    - .planning/phases/03-auth-homepage/03-CONTEXT.md D-P3-08（全量照搬，取 src/ 不取 target/classes）
  </read_first>
  <action>
    **D-P3-08: 批量复制源 web 全部 HTML 模板 + 全部静态资源进 zgbas-admin/resources。**

    1. **复制模板目录** — 整目录递归复制:
       `cp -R /Users/alan/WorkSpace/IDEA/zgbas/web/src/main/resources/templates/ /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-admin/src/main/resources/templates/`
       包含 login.html / index.html / index-topnav.html / main.html / admin/index.html / common/(layout/fragment) 等全部 608 文件。
       **取 `src/main/resources`，不取 `target/classes`（D-P3-08，避免编译副本）。**

    2. **复制静态资源目录** — 整目录递归复制:
       `cp -R /Users/alan/WorkSpace/IDEA/zgbas/web/src/main/resources/static/ /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-admin/src/main/resources/static/`
       包含 390 JS + 352 CSS + 图片/字体等。模板内 `/static/...` 引用路径不变（无需重写，D-P3-08）。

    3. **验证关键文件存在** — login.html / index.html / admin/index.html 必须存在于 templates/。模板不在编译期校验（D-P3-09），mvn compile 基线不受影响。
  </action>
  <verify>
    <automated>
cd /Users/alan/WorkSpace/IDEA/zgbas-plus && \
echo "TEMPLATE_COUNT=$(find zgbas-admin/src/main/resources/templates -type f | wc -l)" && \
echo "JS_COUNT=$(find zgbas-admin/src/main/resources/static -name '*.js' -type f | wc -l)" && \
echo "CSS_COUNT=$(find zgbas-admin/src/main/resources/static -name '*.css' -type f | wc -l)" && \
echo "LOGIN_HTML=$(test -f zgbas-admin/src/main/resources/templates/login.html && echo EXISTS)" && \
echo "INDEX_HTML=$(test -f zgbas-admin/src/main/resources/templates/index.html && echo EXISTS)" && \
echo "ADMIN_INDEX=$(test -f zgbas-admin/src/main/resources/templates/admin/index.html && echo EXISTS)"
    </automated>
  </verify>
  <acceptance_criteria>
    - `find templates -type f | wc -l` ≥ 600（源 608，允许少量偏差）
    - `find static -name '*.js' | wc -l` ≥ 380（源 390）
    - `find static -name '*.css' | wc -l` ≥ 340（源 352）
    - templates/login.html 存在
    - templates/index.html 存在
    - templates/admin/index.html 存在
  </acceptance_criteria>
  <done>
    全量前端资源（608 模板 + 742 JS/CSS）批量复制到 admin/resources；关键模板文件存在。
  </done>
</task>

</tasks>

<threat_model>
## 信任边界

| 边界 | 描述 |
|------|------|
| 客户端→/webSocket/{username} | WebSocket 端点（需认证，Shiro user 过滤器覆盖 /**) |
| 客户端→/indexWebSocket/{userId} | WebSocket 端点（同上） |
| 浏览器→/static/** | 静态资源（公开，Spring MVC 默认处理） |

## STRIDE 威胁登记

| 威胁 ID | 类别 | 组件 | 处置 | 缓解计划 |
|---------|------|------|------|---------|
| T-03-08 | 拒绝服务(DoS) | WebSocket 端点无连接数限制 | **接受风险** | 照搬源行为（D-P3-11，源无连接数限制）。Phase 4 接通推送业务时评估。ConcurrentHashMap sessionPools 提供基础隔离。 |
| T-03-09 | 信息泄露 | 静态资源含前端配置 | **接受风险** | 照搬源全量前端（D-P3-08）。模板/JS/CSS 为公开 UI 资源，不含服务端密钥。服务端密钥走环境变量（Phase 2 D-P2-13）。 |
</threat_model>

<verification>
- [x] WebSocket 4 文件（Config + 2 Server + Message）存在于 framework
- [x] WebSocketServer @OnOpen approve-count stub（Phase 4 依赖注释掉）
- [x] websocket starter 依赖添加到 framework pom
- [x] 全量 608 模板复制到 admin/resources/templates/
- [x] 全量 742 JS/CSS 复制到 admin/resources/static/
- [x] 关键模板 login.html/index.html/admin/index.html 存在
- [x] mvn compile framework+admin 零错误
</verification>

<success_criteria>
- WebSocket 端点骨架就位（闲置连接，推送业务 Phase 4 接通）
- 全量前端资源照搬进 admin（Phase 4 业务 Controller 可直接引用模板）
- 编译零错（模板不在编译期校验）
</success_criteria>

<output>
Create `.planning/phases/03-auth-homepage/03-03-SUMMARY.md` when done
</output>
