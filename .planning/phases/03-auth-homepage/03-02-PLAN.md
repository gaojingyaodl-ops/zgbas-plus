---
phase: 03-auth-homepage
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - zgbas-admin/src/main/java/com/spt/bas/web/controller/LoginController.java
  - zgbas-admin/src/main/java/com/spt/bas/web/controller/IndexController.java
  - zgbas-admin/src/main/java/com/spt/bas/web/open/apply/UserOpenController.java
  - zgbas-admin/src/main/java/com/spt/bas/web/util/CookieUtils.java
  - zgbas-admin/src/main/java/com/spt/bas/web/util/DateUtils.java
  - zgbas-admin/src/main/java/com/spt/bas/web/util/EasyTreeUtil2.java
  - zgbas-admin/src/main/java/com/spt/bas/web/util/WebParamUtils.java
  - zgbas-system/src/main/java/com/spt/bas/client/remote/IPmProcessClient.java
  - zgbas-system/src/main/java/com/spt/bas/client/remote/IApproveWaitDealClient.java
  - zgbas-admin/pom.xml
autonomous: true
requirements: [AUTH-01, AUTH-02]
tags: [controller, login, index, sso, thymeleaf, stub]

must_haves:
  truths:
    - "LoginController (/login GET 渲染 login.html) 存在于 zgbas-admin"
    - "UserOpenController (/open/user/ssoLogin + resSsoLogin SSO 入口) 存在于 zgbas-admin"
    - "IndexController (/index 首页+菜单) 存在于 zgbas-admin，Phase-4 缺失契约为 required=false stub"
    - "/index handler 经 authOpenFacade.findAllMenu/findUserById 渲染菜单（业务数据调用降级 null/空）"
    - "mvn compile admin 零错误"
  artifacts:
    - path: "zgbas-admin/src/main/java/com/spt/bas/web/controller/LoginController.java"
      provides: "登录页 Controller（GET/POST /login）"
      contains: "RequestMapping(value = \"/login\")"
    - path: "zgbas-admin/src/main/java/com/spt/bas/web/controller/IndexController.java"
      provides: "首页+菜单 Controller（stub，业务数据降级）"
      contains: "Autowired(required = false)"
    - path: "zgbas-admin/src/main/java/com/spt/bas/web/open/apply/UserOpenController.java"
      provides: "SSO 单点登录入口"
      contains: "RequestMapping(value = \"/open/user\")"
    - path: "zgbas-admin/src/main/java/com/spt/bas/web/util/WebParamUtils.java"
      provides: "WebParamUtils stub（Phase 4 替换真实实现）"
  key_links:
    - from: "LoginController.login()"
      to: "templates/login.html"
      via: "Thymeleaf view name 'login'"
      pattern: "return \"login\""
    - from: "IndexController.index()"
      to: "authOpenFacade.findAllMenu/findUserById"
      via: "@Autowired IAuthOpenFacade HTTP"
      pattern: "authOpenFacade\\.(findAllMenu|findUserById)"
    - from: "UserOpenController.ssoLogin()"
      to: "SecurityUtils.getSubject().login(SsoUsernamePasswordToken)"
      via: "Shiro SSO login"
      pattern: "SecurityUtils\\.getSubject\\(\\)\\.login"
---

<objective>
将源 zgbas/web 的登录/首页/SSO Controller 照搬进 zgbas-admin，配套 Thymeleaf + 所需 Web 工具类。IndexController 因缺失 Phase-4 FeignClient 契约（IPmProcessClient/IApproveWaitDealClient），按用户决策做 stub-port（required=false + null 守卫），使 `/index` 能渲染菜单而业务数据调用降级。

Purpose: AUTH-01（登录接口照搬）+ AUTH-02（首页+动态菜单照搬）。LoginController/UserOpenController 为干净照搬；IndexController 为 stub-port（D-P3-10 接受业务页裸 404）。
Output: admin 内 3 Controller + 4 Web 工具类 + 2 stub 契约接口 + Thymeleaf 依赖。
</objective>

<execution_context>
@/Users/alan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-auth-homepage/03-CONTEXT.md
@.planning/phases/03-auth-homepage/03-PATTERNS.md
@.planning/phases/03-auth-homepage/03-01-SUMMARY.md

**BUILD PREFIX:** `JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml`

**跨模块依赖拓扑说明:** com.spt.bas.web.util 包在 Phase 3 被拆分到两个模块 — framework 内 ServletUtils/Convert/StringUtils/CharsetKit（Plan 01 已搬，ShiroDbRealm 编译依赖）；admin 内 CookieUtils/DateUtils/EasyTreeUtil2/WebParamUtils（本 Plan 搬，因 EasyTreeUtil2 依赖 system 实体类型，framework 不依赖 system 故不能放 framework）。运行时 admin→framework 类路径合并，同包解析跨 JAR 有效。

**⚠️ Wave 2 顺序约束 (WARNING 3):** 本 Plan 与 03-03 同属 Wave 2（depends_on 仅 03-01，无互相依赖），但两者编译命令均涉及 `zgbas-admin` 模块的 `target/` 目录（03-02 Task 2 跑 `mvn compile -pl zgbas-admin -am`，03-03 Task 1 跑 `mvn compile -pl zgbas-framework,zgbas-admin -am`）。"parallel-eligible" ≠ 强制并发——若 execute-phase 以并行模式运行 Wave 2，**两者必须串行执行**（先 03-02 再 03-03，或反之），否则 Maven `target/` 写入冲突会导致假性编译失败。

<interfaces>
<!-- Plan 01 已就位（framework 内）-->
From zgbas-framework/src/main/java/com/spt/bas/web/shiro/ShiroUtil.java:
- public static Long getCurrAppId()
- public static Long getCurrentUserId()  // inherited from base
- public static boolean isLogin()  // inherited from base
- public static Long getEnterpriseId()
- public static boolean isPermitted(String permission)
- public static final String APPID, ENTERPRISEID, INDUSTRY, DEPTID, DEPTABBR
- public static String appCd  // inherited static field from base

From zgbas-framework/src/main/java/com/spt/bas/web/shiro/SsoUsernamePasswordToken.java:
- extends UsernamePasswordToken, fields: appCode, ssoLogin

From auth-sdk jar (Phase 2 @Bean):
- IAuthOpenFacade: login(), findUserById(), findUserByLoginName(), findAllMenu(), findRoleByUserId(), findEnterpriseById(), findAppByCode(), findDeptByUserId()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 干净照搬 LoginController + UserOpenController + Web 工具类 + Thymeleaf</name>
  <files>
    zgbas-admin/src/main/java/com/spt/bas/web/controller/LoginController.java,
    zgbas-admin/src/main/java/com/spt/bas/web/open/apply/UserOpenController.java,
    zgbas-admin/src/main/java/com/spt/bas/web/util/CookieUtils.java,
    zgbas-admin/src/main/java/com/spt/bas/web/util/DateUtils.java,
    zgbas-admin/src/main/java/com/spt/bas/web/util/EasyTreeUtil2.java,
    zgbas-admin/pom.xml
  </files>
  <read_first>
    - .planning/phases/03-auth-homepage/03-PATTERNS.md §LoginController, §UserOpenController, §ShiroPropConfig
    - 源文件（照搬来源）:
      - /Users/alan/WorkSpace/IDEA/zgbas/web/src/main/java/com/spt/bas/web/controller/LoginController.java
      - /Users/alan/WorkSpace/IDEA/zgbas/web/src/main/java/com/spt/bas/web/open/apply/UserOpenController.java
      - /Users/alan/WorkSpace/IDEA/zgbas/web/src/main/java/com/spt/bas/web/util/CookieUtils.java
      - /Users/alan/WorkSpace/IDEA/zgbas/web/src/main/java/com/spt/bas/web/util/DateUtils.java
      - /Users/alan/WorkSpace/IDEA/zgbas/web/src/main/java/com/spt/bas/web/util/EasyTreeUtil2.java
    - 当前 admin pom: /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-admin/pom.xml
    - 源 web pom thymeleaf 声明: /Users/alan/WorkSpace/IDEA/zgbas/web/pom.xml (第 54-58 行)
  </read_first>
  <action>
    **照搬保包名（D-P2-07）。所有文件 1:1 复制，不改包名不改代码。**

    1. **LoginController**（com.spt.bas.web.controller）— 最干净的照搬。将源文件复制到 `zgbas-admin/src/main/java/com/spt/bas/web/controller/LoginController.java`。仅依赖 `ShiroUtil.isLogin()`（Plan 01 已在 framework）。GET /login 返回 Thymeleaf view "login"；POST /login 失败重渲染。真正登录 POST 由已内联 MyFormAuthenticationFilter 拦截。

    2. **UserOpenController**（com.spt.bas.web.open.apply）— SSO 入口干净照搬。将源文件复制到 `zgbas-admin/src/main/java/com/spt/bas/web/open/apply/UserOpenController.java`。依赖 IAuthOpenFacade（Phase 2 bean）+ ShiroUtil + SsoUsernamePasswordToken（Plan 01 已在 framework）。@Value("${zgBas.secret}") 读 dev 配置（Plan 01 Task 2 已加）。

    3. **Web 工具类 3 文件**（com.spt.bas.web.util）— 复制到 `zgbas-admin/src/main/java/com/spt/bas/web/util/`:
       - `CookieUtils.java` — 干净（仅 javax.servlet + java.net）
       - `DateUtils.java` — 干净（仅 commons-lang3 + java.lang.management）
       - `EasyTreeUtil2.java` — 依赖 auth-sdk 实体 + system 实体（BasManual/BsDictData/PmProcess）+ common 工具。admin → system → common 全链路可见，干净编译。

    4. **Thymeleaf 依赖** — 在 `zgbas-admin/pom.xml` 的 `<dependencies>` 段添加:
       `spring-boot-starter-thymeleaf`（org.springframework.boot，版本由 starter-parent 管理）。模板不在编译期校验，不影响 mvn compile 基线（D-P3-09）。
  </action>
  <verify>
    <automated>
cd /Users/alan/WorkSpace/IDEA/zgbas-plus && \
echo "LOGIN=$(test -f zgbas-admin/src/main/java/com/spt/bas/web/controller/LoginController.java && echo EXISTS)" && \
echo "SSO=$(test -f zgbas-admin/src/main/java/com/spt/bas/web/open/apply/UserOpenController.java && echo EXISTS)" && \
echo "COOKIE=$(test -f zgbas-admin/src/main/java/com/spt/bas/web/util/CookieUtils.java && echo EXISTS)" && \
echo "DATE_UT=$(test -f zgbas-admin/src/main/java/com/spt/bas/web/util/DateUtils.java && echo EXISTS)" && \
echo "EASY_TREE=$(test -f zgbas-admin/src/main/java/com/spt/bas/web/util/EasyTreeUtil2.java && echo EXISTS)" && \
echo "THEMELEAF=$(grep -c 'thymeleaf' zgbas-admin/pom.xml)"
    </automated>
  </verify>
  <acceptance_criteria>
    - LoginController.java 存在且包含 'RequestMapping(value = "/login")'
    - UserOpenController.java 存在且包含 'RequestMapping(value = "/open/user")'
    - CookieUtils.java + DateUtils.java + EasyTreeUtil2.java 存在于 admin/com/spt/bas/web/util/
    - zgbas-admin/pom.xml grep 'thymeleaf' 返回 ≥1
  </acceptance_criteria>
  <done>
    LoginController + UserOpenController + 3 Web 工具类 + Thymeleaf 依赖就位。
  </done>
</task>

<task type="auto">
  <name>Task 2: IndexController stub-port + stub 契约接口 + WebParamUtils stub + 编译绿灯</name>
  <files>
    zgbas-admin/src/main/java/com/spt/bas/web/controller/IndexController.java,
    zgbas-admin/src/main/java/com/spt/bas/web/util/WebParamUtils.java,
    zgbas-system/src/main/java/com/spt/bas/client/remote/IPmProcessClient.java,
    zgbas-system/src/main/java/com/spt/bas/client/remote/IApproveWaitDealClient.java
  </files>
  <read_first>
    - .planning/phases/03-auth-homepage/03-PATTERNS.md §IndexController (COMPILE BLOCKERS 表 + planner options)
    - .planning/phases/03-auth-homepage/03-CONTEXT.md §deferred (MyIndexController → Phase 5 决策)
    - 源文件: /Users/alan/WorkSpace/IDEA/zgbas/web/src/main/java/com/spt/bas/web/controller/IndexController.java (412 行，完整阅读)
    - 源文件: /Users/alan/WorkSpace/IDEA/zgbas/web/src/main/java/com/spt/bas/web/util/WebParamUtils.java (理解其方法签名)
  </read_first>
  <action>
    **用户决策（2026-07-16 确认）: IndexController stub-port，MyIndexController 延后 Phase 5。**

    1. **创建 stub 契约接口**（zgbas-system，com.spt.bas.client.remote 包 — 与现有 IBsCompanyOurClient/IBsDictClient 同位）。这些是临时 stub，Phase 4 用真实 spt-bas-client 接口覆盖。仅包含 IndexController 调用的方法:
       - `IPmProcessClient.java`: `List<PmProcess> findAccess(PmProcessSearchVo searchVo);`（PmProcess/PmProcessSearchVo 在 system，import com.spt.pm.entity.PmProcess + com.spt.pm.vo.PmProcessSearchVo）
       - `IApproveWaitDealClient.java`: `Long getUserWaitDealNum(ApproveWaitSearchVo vo);`（ApproveWaitSearchVo 在 system，import com.spt.bas.client.vo.ApproveWaitSearchVo）
       每个文件顶部加注释: `// STUB — Phase 4 ports the real interface from spt-bas-client. Temporary for Phase 3 IndexController compilation.`

    2. **创建 WebParamUtils stub**（zgbas-admin，com.spt.bas.web.util 包）。真实 WebParamUtils 依赖 ~15 个未迁移的 remote client，无法照搬。创建最小 stub 仅含 IndexController 调用的 2 个方法:
       - `String formatterWaitDealNum(Long num)` — 返回 `num == null ? "0" : String.valueOf(num)`
       - `BsCompanyDcsx queryFundCompany()` — 返回 null（Phase 4 替换）
       不加 @Component（IndexController 用 @Autowired(required=false)，无 bean 时为 null → null 守卫跳过）。
       顶部注释: `// STUB — Phase 4 ports the real WebParamUtils. Methods return empty/null per D-P3-10.`

    3. **照搬 IndexController 并做 stub 修改** — 将源文件复制到 `zgbas-admin/src/main/java/com/spt/bas/web/controller/IndexController.java`，然后做以下修改:
       - 字段 `@Autowired IPmProcessClient processClient` → 改为 `@Autowired(required = false)`
       - 字段 `@Autowired IApproveWaitDealClient waitDealClient` → 改为 `@Autowired(required = false)`
       - 字段 `@Resource WebParamUtils webParamUtils` → 改为 `@Autowired(required = false)`（@Resource 不支持 required，必须换注解）
       - `index()` 方法: waitDealClient.getUserWaitDealNum 调用加 `if (waitDealClient != null)` 守卫，else 分支 mmap.put("waitDealNum", "0"); webParamUtils 调用加 `if (webParamUtils != null)` 守卫
       - `index2()` 方法: processClient.findAccess 调用加 `if (processClient != null)` 守卫，else 返回空 list
       - `queryMenu()` 方法: processClient.findAccess 调用加 `if (processClient != null)` 守卫; 无 processClient 时菜单树仍从 authOpenFacade.findAllMenu 构建（仅缺业务流程/企管流程子菜单分组）
       其余代码（findAllMenu/findUserById/queryMenu 树构建/权限判断/换肤/锁屏等）照搬不变。

    4. **编译绿灯** — `mvn compile -pl zgbas-admin -am`。迭代 gotcha 级联至零错。预期 gotcha: EasyTreeUtil2 可能引用未迁移的 system 类型 → 检查并补搬；ruoyi-common 类（AjaxResult/ShiroConstants 等）→ Plan 01 的 framework pom ruoyi-common 传递依赖覆盖。
  </action>
  <verify>
    <automated>
cd /Users/alan/WorkSpace/IDEA/zgbas-plus && \
echo "INDEX=$(test -f zgbas-admin/src/main/java/com/spt/bas/web/controller/IndexController.java && echo EXISTS)" && \
echo "REQUIRED_FALSE=$(grep -c 'required = false\|required=false' zgbas-admin/src/main/java/com/spt/bas/web/controller/IndexController.java)" && \
echo "STUB_PM=$(test -f zgbas-system/src/main/java/com/spt/bas/client/remote/IPmProcessClient.java && echo EXISTS)" && \
echo "STUB_APPROVE=$(test -f zgbas-system/src/main/java/com/spt/bas/client/remote/IApproveWaitDealClient.java && echo EXISTS)" && \
echo "STUB_WEBPARAM=$(test -f zgbas-admin/src/main/java/com/spt/bas/web/util/WebParamUtils.java && echo EXISTS)" && \
echo "MY_INDEX_NOT_PORTED=$(test ! -f zgbas-admin/src/main/java/com/spt/bas/web/controller/MyIndexController.java && echo DEFERRED)" && \
JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml -pl zgbas-admin -am compile 2>&1 | tee /tmp/p3w2a-compile.log | tail -5 && \
echo "ERROR_COUNT=$(grep -cE '^\[ERROR\]|cannot find symbol|找不到符号' /tmp/p3w2a-compile.log)"
    </automated>
  </verify>
  <acceptance_criteria>
    - IndexController.java 存在且 grep 'required = false|required=false' 返回 ≥3（3 个 stub 字段）
    - IPmProcessClient.java + IApproveWaitDealClient.java 存在于 system/com/spt/bas/client/remote/，各含 STUB 注释
    - WebParamUtils.java stub 存在于 admin/com/spt/bas/web/util/
    - MyIndexController.java 不存在（确认延后 Phase 5）
    - mvn compile -pl zgbas-admin -am BUILD SUCCESS 且 ERROR_COUNT == 0
    - IndexController.java grep 'authOpenFacade.findAllMenu|authOpenFacade.findUserById' 返回 ≥1（菜单渲染链路验证）
  </acceptance_criteria>
  <done>
    IndexController stub-port 就位（required=false + null 守卫）；2 stub 契约接口 + WebParamUtils stub 就位；admin 编译零错。MyIndexController 确认未搬运（延后 Phase 5）。
  </done>
</task>

</tasks>

<threat_model>
## 信任边界

| 边界 | 描述 |
|------|------|
| 用户→/login | 登录页 GET（公开），POST 由 Shiro authc 拦截 |
| 用户→/index | 首页（需认证，Shiro user 过滤器） |
| 用户→/open/user/ssoLogin | SSO 入口（公开，token 验证） |

## STRIDE 威胁登记

| 威胁 ID | 类别 | 组件 | 处置 | 缓解计划 |
|---------|------|------|------|---------|
| T-03-05 | 欺骗(Spoofing) | IndexController stub required=false | **接受风险** | 缺失 Phase-4 客户端使业务数据调用返回 null/空（D-P3-10 接受裸 404/空数据）。菜单渲染不受影响（authOpenFacade 独立可用）。 |
| T-03-06 | 篡改(Tampering) | stub 契约接口 IPmProcessClient/IApproveWaitDealClient | **接受风险** | 临时 stub 接口，仅含 IndexController 调用的方法签名，无实现。Phase 4 用真实接口覆盖。无运行时 bean（required=false），不暴露攻击面。 |
| T-03-07 | 欺骗(Spoofing) | UserOpenController SSO token 验证 | **接受风险** | SSO ticket = MD5(secretKey+user+date)，弱加密照搬（D-P3-12）。zgBas.secret 走 dev 明文/prod 环境变量占位（D-P3-07 pattern）。 |
</threat_model>

<verification>
- [x] LoginController + UserOpenController 干净照搬就位
- [x] IndexController stub-port 就位（required=false × 3 + null 守卫）
- [x] 2 stub 契约接口 + WebParamUtils stub 就位
- [x] MyIndexController 确认延后 Phase 5（不存在）
- [x] CookieUtils + DateUtils + EasyTreeUtil2 就位
- [x] Thymeleaf 依赖添加到 admin pom
- [x] mvn compile admin 零错误
</verification>

<success_criteria>
- 登录/首页/SSO Controller 照搬进 admin，包名保源
- IndexController stub-port 使 /index 渲染菜单（authOpenFacade 链路），业务数据降级
- Thymeleaf 就位使模板可解析
- 编译零错
</success_criteria>

<output>
Create `.planning/phases/03-auth-homepage/03-02-SUMMARY.md` when done
</output>
