---
phase: 03-auth-homepage
plan: 04
type: execute
wave: 3
depends_on: [03-02, 03-03]
files_modified:
  - zgbas-admin/src/main/resources/application-prod.yml
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04]
tags: [config, verification, startup, prod-profile]

must_haves:
  truths:
    - "application-prod.yml 包含 shiro.prop.mock-password / app-cd / zgBas.secret 的 \${ENV:} 占位"
    - "单进程启动成功（Shiro securityManager/shiroFilter bean 接线 + 无启动异常）"
    - "/login 端点返回 200 或 302 redirect（Thymeleaf 渲染 login.html）"
    - "/index 端点返回 200 或 302 redirect（IndexController stub 渲染或重定向）"
    - "/open/user/ssoLogin 端点返回 200 或 302（SSO 入口可达）"
    - "Mock 后门保留（D-P3-06）；prod 默认关闭（占位空值，D-P3-07）"
  artifacts:
    - path: "zgbas-admin/src/main/resources/application-prod.yml"
      provides: "prod profile 配置占位（mockPassword/secret/appCd/env-var）"
      contains: "ZGBAS_MOCK_PASSWORD"
  key_links:
    - from: "ZgbasApplication boot"
      to: "ToolsShiroConfig (securityManager/shiroFilter beans)"
      via: "exclude removed → auto-config engages"
      pattern: "shiroFilter|securityManager"
    - from: "ShiroService.initSection"
      to: "authOpenFacade.findAllMenu HTTP → external spt-auth"
      via: "filter chain dynamic menu"
      pattern: "findAllMenu"
---

<objective>
完成 Phase 3 配置外置（prod profile 占位）并执行启动验证（D-P3-13）：确认单进程启动成功、Shiro bean 全接线、关键端点可达。不强求真实登录通过、不拉起真实 spt-auth（IAuthOpenFacade 是 HTTP bean 启动期懒注入）。

Purpose: D-P3-13 启动验证为主（同 Phase 2 D-P2-03）。覆盖 AUTH-01..04 的可观测验收：Shiro 链路激活 + 端点可达。
Output: prod profile 占位完成 + 启动验证通过（3 条 ROADMAP 成功标准达成）。
</objective>

<execution_context>
@/Users/alan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-auth-homepage/03-CONTEXT.md
@.planning/phases/03-auth-homepage/03-PATTERNS.md
@.planning/phases/03-auth-homepage/03-01-SUMMARY.md
@.planning/phases/03-auth-homepage/03-02-SUMMARY.md
@.planning/phases/03-auth-homepage/03-03-SUMMARY.md

**BUILD PREFIX:** `JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml`

**启动前提:** 需要 `export DB_PASSWORD=xxx` + `export SPT_APP_SECRET=xxx`（Phase 2 D-P2-13 环境变量外置）。如未设置，dev profile 启动会因占位符解析失败报错 — 这是预期行为（非 bug）。
</context>

<tasks>

<task type="auto">
  <name>Task 1: prod profile 配置占位完成</name>
  <files>
    zgbas-admin/src/main/resources/application-prod.yml
  </files>
  <read_first>
    - /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-admin/src/main/resources/application-prod.yml (当前完整文件)
    - /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-admin/src/main/resources/application-dev.yml (Plan 01 Task 2 已加 dev 键)
    - 源配置值: /Users/alan/WorkSpace/IDEA/zgbas/web/src/main/resources/application-prod.properties (prod mock-password 被注释=关闭)
    - .planning/phases/03-auth-homepage/03-CONTEXT.md D-P3-07 (dev 明文 + prod 占位模式)
  </read_first>
  <action>
    **D-P3-07/D-P3-12: prod profile 走 \${ENV:} 占位（空默认 = 功能关闭），沿用 Phase 2 D-P2-13/D-P2-14 外置模式。**

    在 `application-prod.yml` 末尾追加（与 dev 对称但全占位）:
    ```
    # Phase 3 auth config — prod env-var placeholders (D-P3-07/D-P3-12)
    shiro:
      prop:
        mock-password: ${ZGBAS_MOCK_PASSWORD:}   # D-P3-07: empty default = mock backdoor OFF in prod
        app-cd: ${ZGBAS_APP_CD:}                 # D-P3-02: appCode via env-var
    zgBas:
      secret: ${ZGBAS_SECRET:}                   # D-P3-12: SSO secret via env-var
    ```

    说明:
    - `mock-password` 空 = 后门关闭（isMockLogin 检查 mockPwd.equals(null) = false）。源 prod 也注释掉了 mock-password（=关闭），行为一致。
    - `app-cd` / `secret` 空占位，prod 靠环境变量注入。
    - dev 键已在 Plan 01 Task 2 添加（明文照搬旧值），此 Task 仅补 prod。
  </action>
  <verify>
    <automated>
cd /Users/alan/WorkSpace/IDEA/zgbas-plus && \
echo "PROD_MOCK=$(grep -c 'ZGBAS_MOCK_PASSWORD' zgbas-admin/src/main/resources/application-prod.yml)" && \
echo "PROD_APPCD=$(grep -c 'ZGBAS_APP_CD' zgbas-admin/src/main/resources/application-prod.yml)" && \
echo "PROD_SECRET=$(grep -c 'ZGBAS_SECRET' zgbas-admin/src/main/resources/application-prod.yml)"
    </automated>
  </verify>
  <acceptance_criteria>
    - application-prod.yml grep 'ZGBAS_MOCK_PASSWORD' 返回 ≥1
    - application-prod.yml grep 'ZGBAS_APP_CD' 返回 ≥1
    - application-prod.yml grep 'ZGBAS_SECRET' 返回 ≥1
    - ZGBAS_MOCK_PASSWORD 占位含空默认 `:` （确认后门 prod 默认关闭）
  </acceptance_criteria>
  <done>
    prod profile 3 个 auth 配置键走环境变量占位，mock 后门 prod 默认关闭。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 启动验证（D-P3-13）</name>
  <what-built>
    Phase 3 全部代码就位: Shiro 链路（framework）+ 登录/首页/SSO Controller（admin）+ WebSocket 端点（framework）+ 全量前端资源（admin/resources）+ dev/prod 配置。本 Task 验证单进程启动成功 + Shiro bean 接线 + 端点可达。
  </what-built>
  <read_first>
    - .planning/phases/03-auth-homepage/03-CONTEXT.md D-P3-13（验收策略: 启动验证为主）
    - /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-admin/src/main/resources/application-dev.yml (确认 dev 键就位)
  </read_first>
  <action>
    **D-P3-13: 启动验证为主，不强求真实登录通过，不拉起真实 spt-auth。**

    1. **全模块编译验证** — 确认所有模块零错:
       `JAVA_HOME=...mvn... clean compile`（全 5 模块）
       期望: BUILD SUCCESS, `grep -cE '^\[ERROR\]|cannot find symbol|找不到符号'` == 0

    2. **启动 dev profile** — 需先设置环境变量:
       ```
       export DB_PASSWORD=<local dev DB password>
       export SPT_APP_SECRET=<local dev auth secret>
       ```
       然后启动:
       `JAVA_HOME=...mvn... -pl zgbas-admin spring-boot:run -Dspring-boot.run.profiles=dev 2>&1 | tee /tmp/p3-startup.log`
       或者: 先 `mvn -pl zgbas-admin -am package -DskipTests`，然后 `java -jar zgbas-admin/target/*.jar --spring.profiles.active=dev`

    3. **验证启动日志确认 Shiro bean 接线** — 检查 /tmp/p3-startup.log:
       - `grep -i 'shiroFilter\|securityManager\|ShiroDbRealm' /tmp/p3-startup.log`（Shiro bean 存在）
       - `grep -i 'ToolsShiroConfig\|EnableToolsShiroConfig' /tmp/p3-startup.log`（自动配置激活）
       - `grep -i 'Started ZgbasApplication' /tmp/p3-startup.log`（启动完成）
       - `grep -iE 'ERROR|Exception.*startup|Failed to' /tmp/p3-startup.log`（无致命错误）

    4. **端点可达性验证** — 启动后（端口 8080，D-P2-16），用 curl 测试:
       - `curl -s -o /dev/null -w '%{http_code}' http://localhost:8080/login`（期望 200 或 302）
       - `curl -s -o /dev/null -w '%{http_code}' http://localhost:8080/index`（期望 200 或 302 redirect to /login — 未登录时 Shiro authc 拦截重定向）
       - `curl -s -o /dev/null -w '%{http_code}' http://localhost:8080/open/user/ssoLogin`（期望 200 或 302）
       注意: 未登录时 /index 被 Shiro `/**=user` 重定向到 /login 是正确行为（302）。

    5. **接受预期降级行为** — 以下为预期（非 bug，D-P3-10/D-P3-13）:
       - /index 页面加载但业务数据区域为空/null（stub）
       - 菜单点业务页 → HTML 能开但数据接口裸 404（Phase 4 Controller 未迁）
       - IAuthOpenFacade HTTP 调外部 spt-auth 可能超时（不拉起真实 spt-auth）— ShiroService.initSection try-catch 兜底，不阻塞启动
  </action>
  <how-to-verify>
    1. 运行全模块 `mvn clean compile`，确认 BUILD SUCCESS 零错
    2. 设置 DB_PASSWORD + SPT_APP_SECRET 环境变量
    3. 启动 dev profile: `mvn -pl zgbas-admin spring-boot:run -Dspring-boot.run.profiles=dev`
    4. 看到日志 "Started ZgbasApplication" = 启动成功
    5. `curl -s -o /dev/null -w '%{http_code}' http://localhost:8080/login` → 200
    6. `curl -s -o /dev/null -w '%{http_code}' http://localhost:8080/index` → 200 或 302
    7. `curl -s -o /dev/null -w '%{http_code}' http://localhost:8080/open/user/ssoLogin` → 200 或 302
    8. 启动日志无致命 ERROR/Exception
  </how-to-verify>
  <verify>
    <automated>
cd /Users/alan/WorkSpace/IDEA/zgbas-plus && \
JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml clean compile 2>&1 | tee /tmp/p3-final-compile.log | tail -3 && \
echo "COMPILE_ERRORS=$(grep -cE '^\[ERROR\]|cannot find symbol|找不到符号' /tmp/p3-final-compile.log)" && \
echo "EXCLUDE_CHECK=$(grep -v '^ *//' zgbas-admin/src/main/java/com/spt/ZgbasApplication.java | grep -v '^ *\*' | grep -c 'ToolsShiroConfig')" && \
echo "REALM_CHECK=$(test -f zgbas-framework/src/main/java/com/spt/bas/web/shiro/ShiroDbRealm.java && echo EXISTS)" && \
echo "TEMPLATE_COUNT=$(find zgbas-admin/src/main/resources/templates -type f | wc -l)" && \
echo "LOGIN_CTRL=$(test -f zgbas-admin/src/main/java/com/spt/bas/web/controller/LoginController.java && echo EXISTS)" && \
echo "INDEX_CTRL=$(test -f zgbas-admin/src/main/java/com/spt/bas/web/controller/IndexController.java && echo EXISTS)" && \
echo "SSO_CTRL=$(test -f zgbas-admin/src/main/java/com/spt/bas/web/open/apply/UserOpenController.java && echo EXISTS)" && \
echo "WS_CONFIG=$(test -f zgbas-framework/src/main/java/com/spt/bas/web/ws/WebSocketConfig.java && echo EXISTS)"
    </automated>
  </verify>
  <acceptance_criteria>
    - 全模块 `mvn clean compile` BUILD SUCCESS，ERROR_COUNT == 0
    - ZgbasApplication excludeFilters 不含 ToolsShiroConfig
    - ShiroDbRealm.java + LoginController.java + IndexController.java + UserOpenController.java + WebSocketConfig.java 均存在
    - templates/ 文件数 ≥ 600
    - [人工验证] dev profile 启动成功（"Started ZgbasApplication" 日志出现）
    - [人工验证] /login 端点返回 200
    - [人工验证] /index 端点返回 200 或 302（Shiro 重定向）
    - [人工验证] /open/user/ssoLogin 端点返回 200 或 302
    - [人工验证] 启动日志无致命 ERROR
  </acceptance_criteria>
  <done>
    全模块编译零错；dev profile 单进程启动成功；/login · /index · /open/user/ssoLogin 端点可达；Shiro bean 接线确认。
  </done>
  <resume-signal>Type "approved" if startup + endpoints verified, or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## 信任边界

| 边界 | 描述 |
|------|------|
| 启动期→配置 | 环境变量注入敏感配置（DB 密码/SSO secret/mockPassword） |
| dev vs prod | dev 保明文默认值（行为等价）；prod 全占位（安全外置） |

## STRIDE 威胁登记

| 威胁 ID | 类别 | 组件 | 处置 | 缓解计划 |
|---------|------|------|------|---------|
| T-03-10 | 信息泄露 | application-dev.yml mockPassword=123456 明文 | **接受风险** | D-P3-07 有意保留 dev 明文（行为等价照搬旧项目）。dev profile 仅本地开发，不入 prod 部署。prod profile 使用 `${ZGBAS_MOCK_PASSWORD:}` 占位（空=后门关闭）。 |
| T-03-11 | 欺骗(Spoofing) | Mock 后门 dev 默认开启 | **接受风险** | D-P3-06 有意保留（行为等价优先，PROJECT.md 已列技术债）。dev 后门用于模拟任意用户登录测试；prod 占位空值=关闭。CR-01 轮换生产库密码仍为跨阶段延迟债。 |
| T-03-12 | 信息泄露 | zgBas.secret dev 明文 | **接受风险** | 照搬源 dev 值（D-P3-12）。prod 走 `${ZGBAS_SECRET:}` 占位。SSO ticket 弱加密（MD5）是照搬行为。 |
</threat_model>

<verification>
Phase 3 全部成功标准达成验证:

**ROADMAP §Phase 3 Success Criteria:**
1. [x] 登录接口照搬旧项目，Shiro session+cookie（非 JWT）认证链路可用 — ShiroDbRealm + MyFormAuthenticationFilter + LoginController 就位
2. [x] 登录成功后首页 + 动态菜单正常加载 — IndexController stub（findAllMenu via authOpenFacade）+ 全量模板就位
3. [x] Shiro 链路迁入 zgbas-framework，过滤器链配置正确 — Realm/Service/Util/ShiroFilter 在 framework，exclude 移除

**D-P3-13 启动验证:**
- [x] 全模块 mvn compile 零错
- [x] ToolsShiroConfig exclude 移除
- [x] ShiroDbRealm/ShiroService 接线
- [x] /login · /index · /open/user/ssoLogin 端点可达
- [x] Mock 后门保留（dev on / prod off）
- [x] 全量模板/静态资源就位
</verification>

<success_criteria>
- prod profile 配置占位完成（mock 后门 prod 默认关闭）
- 单进程 dev profile 启动成功
- 3 条 ROADMAP 成功标准全部达成
- Phase 3 可观测验收通过（启动验证为主，真实 e2e 留 Phase 7）
</success_criteria>

<output>
Create `.planning/phases/03-auth-homepage/03-04-SUMMARY.md` when done
</output>
