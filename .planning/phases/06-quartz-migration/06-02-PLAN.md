---
phase: 06-quartz-migration
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - zgbas-quartz/src/main/java/com/spt/quartz/task/ApplyPayTask.java
  - zgbas-quartz/src/main/java/com/spt/quartz/task/AutoSealPdfTask.java
  - zgbas-quartz/src/main/java/com/spt/quartz/task/BsCompanyCreditTask.java
  - zgbas-quartz/src/main/java/com/spt/quartz/task/BsCompanyTask.java
  - zgbas-quartz/src/main/java/com/spt/quartz/task/BudgetSettlementTask.java
  - zgbas-quartz/src/main/java/com/spt/quartz/task/BusinessRestrictRelieveTask.java
  - zgbas-quartz/src/main/java/com/spt/quartz/task/ConfirmReceiptDcsxTask.java
  - zgbas-quartz/src/main/java/com/spt/quartz/task/CtrContractProfitTask.java
  - zgbas-quartz/src/main/java/com/spt/quartz/task/CtrContractScheduleTask.java
  - zgbas-quartz/src/main/java/com/spt/quartz/task/DcsxAutoApplyPayTask.java
  - zgbas-quartz/src/main/java/com/spt/quartz/task/DcsxRepaymentdTask.java
  - zgbas-quartz/src/main/java/com/spt/quartz/task/DefaultingEnterpriseTask.java
  - zgbas-quartz/src/main/java/com/spt/quartz/task/DepositPaymentTask.java
  - zgbas-quartz/src/main/java/com/spt/quartz/task/InternalBuyTask.java
  - zgbas-quartz/src/main/java/com/spt/quartz/task/PmApproveTask.java
  - zgbas-quartz/src/main/java/com/spt/quartz/task/RepaymentTask.java
  - zgbas-quartz/src/main/java/com/spt/quartz/task/SealBorrowTask.java
  - zgbas-quartz/src/main/java/com/spt/quartz/task/SettlementTask.java
  - zgbas-quartz/src/main/java/com/spt/quartz/task/SignTask.java
  - zgbas-quartz/src/main/java/com/spt/quartz/task/StockVirtualTask.java
  - zgbas-quartz/src/main/java/com/spt/quartz/task/WeChatWorkTask.java
autonomous: true
requirements: [QUARTZ-03]

must_haves:
  truths:
    # Outcome-level truths (operator/executor-observable) — W3 rebalance
    - "21 个 basServer/task/ 活跃 handler 在 Spring context 可解析为 @Component bean（sys_job.invoke_target 字符串可反射命中）"
    - "源 xxl-job 痕迹在该批 21 个 handler 文件内清零（grep @XxlJob / XxlJobHelper / com.xxl.job 全批 0 命中）"
    - "handler 内业务逻辑等价保形（@Autowired com.spt.bas.server.service.* iface 包名不变 D-P6-09；业务编排不动）"
    # Compile-gate evidence truths (kept as implementation-level signals)
    - "每个 handler 类带 @Component(\"<xxlJobValue>\") 显式 bean 名（对齐 sys_job.invoke_target 短名）"
    - "XxlJobHelper.log → log.info / handleSuccess → return / handleFail → throw / getJobParam → 方法签名加 String 参数（Pattern 3 翻译规则）"
    - "合并编译门：06-02 + 06-03 + 06-04 一起跑 mvn -pl zgbas-quartz -am compile 零 ERROR（Phase 4 04-03 structural-cascade pattern，因 BasCommandExecutor 06-04 @Autowired task/ 类）"
  artifacts:
    - path: "zgbas-quartz/src/main/java/com/spt/quartz/task/ApplyPayTask.java"
      provides: "自动付款任务 handler（含 autoPay / autoStartPayProcess 等多 @XxlJob 方法）"
      contains: "@Component(\"applyPayTask\")"
    - path: "zgbas-quartz/src/main/java/com/spt/quartz/task/RepaymentTask.java"
      provides: "还款任务 handler"
      contains: "@Component(\"repaymentTask\")"
  key_links:
    - from: "zgbas-quartz/.../task/*.java @Component(\"<name>\")"
      to: "sys_job.invoke_target = '<name>.<method>'"
      via: "JobInvokeUtil.invokeMethod via SpringUtils.getBean('<name>')"
      pattern: "@Component\\(\"[a-zA-Z]+\"\\)"
    - from: "zgbas-quartz/.../task/*.java @Autowired"
      to: "zgbas-system/.../service/* iface"
      via: "Spring @Autowired 单向 quartz→system dep"
      pattern: "com\\.spt\\.bas\\.server\\.service\\."
---

<objective>
Phase 6 Wave 2 first half — 把 basServer/task/ 下 21 个活跃 handler 类（含 ~48 个 @XxlJob 方法，多方法/类）按 D-P6-09 repackage 到 com.spt.quartz.task，照 06-RESEARCH.md §Code Examples §1 + §Pattern 3 翻译 xxl-job→quartz 调用约定。OrverdurTask（@XxlJob 整行注释，D-P6-03 ③ 废弃）+ TestJob（测试桩）跳过不迁（23 源 - 2 跳过 = 21 迁）。

Purpose: 把核心业务定时任务迁入 RuoYi quartz 模块，让 sys_job.invoke_target 字符串能反射命中这些 bean。
Output: zgbas-quartz/src/main/java/com/spt/quartz/task/ 下 21 个 handler 类，全部 @Component 显式 bean 名 + 零 xxl-job 痕迹。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-quartz-migration/06-CONTEXT.md
@.planning/phases/06-quartz-migration/06-RESEARCH.md
@.planning/phases/06-quartz-migration/06-01-SUMMARY.md

# 关键上下文摘要
- 06-01 已落地 zgbas-quartz 基础设施（18 RuoYi 类 + 15 common 子集 + DDL + ScheduleConfig + xxl-job-core 删除 + /monitor/job Thymeleaf 页面）
- 翻译规则见 06-RESEARCH.md §Pattern 3（xxl-job → quartz bean 对照表）+ §Code Examples §1（ApplyPayTask 范例）
- D-P6-09 repackage：仅 handler 自身包名变（com.spt.bas.server.task → com.spt.quartz.task），handler 内 @Autowired 的 com.spt.bas.server.service.* iface 包名不变
- D-P6-12 行为等价：保 @DisallowConcurrent 串行型（在 sys_job.concurrent='1' 字段层级控制，handler 类本身不加 @DisallowConcurrentExecution — RuoYi 在 Job class 层级 QuartzJobExecution vs QuartzDisallowConcurrentExecution）
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port basServer/task/ 21 handler 类到 com.spt.quartz.task + xxl-job→quartz 翻译</name>
  <files>
    zgbas-quartz/src/main/java/com/spt/quartz/task/ApplyPayTask.java
    zgbas-quartz/src/main/java/com/spt/quartz/task/AutoSealPdfTask.java
    zgbas-quartz/src/main/java/com/spt/quartz/task/BsCompanyCreditTask.java
    zgbas-quartz/src/main/java/com/spt/quartz/task/BsCompanyTask.java
    zgbas-quartz/src/main/java/com/spt/quartz/task/BudgetSettlementTask.java
    zgbas-quartz/src/main/java/com/spt/quartz/task/BusinessRestrictRelieveTask.java
    zgbas-quartz/src/main/java/com/spt/quartz/task/ConfirmReceiptDcsxTask.java
    zgbas-quartz/src/main/java/com/spt/quartz/task/CtrContractProfitTask.java
    zgbas-quartz/src/main/java/com/spt/quartz/task/CtrContractScheduleTask.java
    zgbas-quartz/src/main/java/com/spt/quartz/task/DcsxAutoApplyPayTask.java
    zgbas-quartz/src/main/java/com/spt/quartz/task/DcsxRepaymentdTask.java
    zgbas-quartz/src/main/java/com/spt/quartz/task/DefaultingEnterpriseTask.java
    zgbas-quartz/src/main/java/com/spt/quartz/task/DepositPaymentTask.java
    zgbas-quartz/src/main/java/com/spt/quartz/task/InternalBuyTask.java
    zgbas-quartz/src/main/java/com/spt/quartz/task/PmApproveTask.java
    zgbas-quartz/src/main/java/com/spt/quartz/task/RepaymentTask.java
    zgbas-quartz/src/main/java/com/spt/quartz/task/SealBorrowTask.java
    zgbas-quartz/src/main/java/com/spt/quartz/task/SettlementTask.java
    zgbas-quartz/src/main/java/com/spt/quartz/task/SignTask.java
    zgbas-quartz/src/main/java/com/spt/quartz/task/StockVirtualTask.java
    zgbas-quartz/src/main/java/com/spt/quartz/task/WeChatWorkTask.java
  </files>
  <read_first>
    - /Users/alan/WorkSpace/IDEA/zgbas/basCore/basServer/src/main/java/com/spt/bas/server/task/ 全部 23 个 .java 文件（源 handler；）
    - /Users/alan/WorkSpace/IDEA/zgbas/basCore/basServer/src/main/java/com/spt/bas/server/service/ （@Autowired 目标 iface，确认包名不变；不读全部，按 handler 内 import 抽样核 5-10 个 iface 即可）
    - /Users/alan/WorkSpace/IDEA/spt-auth/auth-quartz/src/main/java/com/spt/quartz/task/RyTask.java （RuoYi 标准 handler 写法参考：@Component("ryTask") + 无注解 public 方法）
    - .planning/phases/06-quartz-migration/06-RESEARCH.md §Code Examples §1（ApplyPayTask 完整 xxl-job→quartz 翻译范例：行 562-619）+ §Pattern 3（翻译规则对照表：行 373-386）+ §Common Pitfalls 4（实测 65 @XxlJob / 本期 60 迁入 / OrverdurTask 注释行 / TestJob 测试桩）
    - .planning/phases/06-quartz-migration/06-01-SUMMARY.md （确认 Wave 1 落地内容：com.spt.common.utils.spring.SpringUtils + com.spt.common.constant.Constants 已就位 — handler 翻译后无 import 缺失）
  </read_first>
  <action>
    【A. 源枚举与跳过】源 basServer/task/ 共 23 .java：迁 21 个（活跃 handler），跳过 2 个：
      - OrverdurTask.java（源文件内 @XxlJob(value="overdueTask") 整行 // 注释，按 D-P6-03 ③ 废弃任务不迁）
      - TestJob.java（测试桩，非业务 handler，不迁）
      - 剩 21 个全迁（清单见 files，数学核对：23 源 - 2 跳过 = 21 迁；W1 plan-checker 修正）
    
    【B. 落位与 repackage（D-P6-09）】每个源 .java 复制到 zgbas-quartz/src/main/java/com/spt/quartz/task/<同名>.java，做以下变换：
      1. package com.spt.bas.server.task; → package com.spt.quartz.task;（仅 handler 自身包名变）
      2. handler 内 import com.spt.bas.server.service.* 不变（D-P6-09 ②：service iface 包名守）
      3. handler 内 import com.spt.bas.server.{constant,enums,vo,entity}.* 等业务引用不变（Phase 4 已就位的 zgbas-system 业务类）
      4. 删 import com.xxl.job.core.handler.annotation.XxlJob; + 删 import com.xxl.job.core.context.XxlJobHelper;
    
    【C. 类级 @Component 显式 bean 名】每个 handler 类上 @Component 改为 @Component("<xxlJobValue>")，bean 名取该类内**第一个** @XxlJob 注解的 value 字段（多数类只有一个 @XxlJob value；多 value 的类如 ApplyPayTask 取主方法 autoPay 或 autoStartPayProcess 之一 — 按 CONTEXT.md D-P6-09 + research §Code Examples §1 的 ApplyPayTask 范例：类名首字母小写 applyPayTask 即默认 Spring bean 名，可显式 @Component("applyPayTask") 强化；多方法的类 sys_job.invoke_target 各指向不同 method 同 bean）。具体 bean 名约定：
      - ApplyPayTask → @Component("applyPayTask")（@XxlJob 方法：autoStartPayProcess / autoPay / 等 — sys_job 各一行 invoke_target = "applyPayTask.autoStartPayProcess" / "applyPayTask.autoPay"）
      - 其他类按类名首字母小写作 bean 名（Spring 默认约定，显式写出避免误写）
    
    【D. 方法级翻译规则（Pattern 3）】对每个原 @XxlJob(value="<name>") public void xxx() { ... } 方法：
      1. 删 @XxlJob 注解行
      2. 方法签名 public void xxx() 保留不变（除非有 XxlJobHelper.getJobParam() — 见下）
      3. 方法体内：
         - XxlJobHelper.log("msg {}", arg); → log.info("msg {}", arg);（类需有 private static final Logger log = LoggerFactory.getLogger(<ClassName>.class); — 若源类无则补 slf4j Logger）
         - XxlJobHelper.handleSuccess("msg"); → 直接删该行（void 方法自然返回；AbstractQuartzJob.after 检测无异常 = success）；保日志则改 log.info("msg")
         - XxlJobHelper.handleFail("msg"); → throw new RuntimeException("msg");（AbstractQuartzJob.execute catch 后调 after(context, job, e) 写 sys_job_log.status=FAIL）；或 log.error + throw
         - String param = XxlJobHelper.getJobParam(); → 改方法签名加 String 参数：public void xxx(String param)，方法体内 String param = XxlJobHelper.getJobParam(); 删除（quartz 模式下 args 由 sys_job.invoke_target 字符串传入：beanXxx.xxx('${param}') → JobInvokeUtil 解析 args 反射 invoke；handler 不需读 JobDataMap）
      4. 方法末尾若原本无 return（void），翻译后仍无 return（success 自然完成）；原本 catch 内 handleFail 后无显式 return，翻译后 throw 替代
    
    【E. 不做的事】
      - 不加 @DisallowConcurrentExecution（RuoYi 在 sys_job.concurrent='1' + Job class 层级控制，非 handler 层级 — research §Pattern 3 Key insight）
      - 不改 handler 内业务逻辑（仅 xxl-job 痕迹翻译；@Autowired service 调用、try-catch 业务流不变）
      - 不建 sys_job INSERT 数据（Wave 4 D-P6-02 负责）
      - 不改 @Autowired 目标 iface 包名（保 com.spt.bas.server.service.*）
      - 不迁 OrverdurTask + TestJob（按上面 A 跳过清单）
      - 不动 basServer/command/BasCommandExecutor（06-04 负责）
      - 不动 basServer/rocketmq/task/（06-03 负责）
      - 不动 reportServer/command/ReportCommandExecutor（06-04 负责）
      - 不动 web/config/BasWebCommand（06-04 负责）
  </action>
  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 1.8) mvn -pl zgbas-quartz -am compile -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml 2>&1 | tee /tmp/p6-02-t1.log | tail -30; 断言：①ls zgbas-quartz/src/main/java/com/spt/quartz/task/ | grep -v "^RyTask.java$" | grep -v "^BasCommandExecutor.java$" | grep -v "^ReportCommandExecutor.java$" | grep -v "^BasWebCommand.java$" | grep -v "^Synchronized" | wc -l ≥ 21（21 个 task/ 活跃 handler；不含 RyTask / 06-03 Synchronized* / 06-04 command executor）；②grep -rl "@XxlJob\|XxlJobHelper\|com.xxl.job" zgbas-quartz/src/main/java/com/spt/quartz/task/ | wc -l = 0（仅 RyTask 不含 xxl-job）；③grep -c "@Component" zgbas-quartz/src/main/java/com/spt/quartz/task/ApplyPayTask.java = 1；④grep -c "package com.spt.quartz.task" zgbas-quartz/src/main/java/com/spt/quartz/task/ApplyPayTask.java = 1；⑤本 Task 单独编译预期出现 BasCommandExecutor 相关 ERROR（06-04 未做，@Autowired task/ 类未就绪是反向 — 本 plan 仅迁 task/ handler 不引用 BasCommandExecutor；task/ 内部互引如 BasCommandExecutor 不在此批 — 编译应 GREEN；若出现 com.spt.bas.server.command.* 缺失则记录待 06-04 处置）</automated>
  </verify>
  <done>
    - zgbas-quartz/.../task/ 下 21 个 handler 类就位（不含 OrverdurTask / TestJob；W1 plan-checker 修正数学）
    - 每个类 package com.spt.quartz.task + 类名不变 + @Component 显式 bean 名
    - grep "@XxlJob\|XxlJobHelper\|com.xxl.job" zgbas-quartz/.../task/ = 0（RyTask 也不含）
    - XxlJobHelper.log → log.info / handleSuccess → 删或 log.info / handleFail → throw / getJobParam → 方法签名加 String 参数
    - @Autowired com.spt.bas.server.service.* iface 包名保留（grep "com\.spt\.bas\.server\.service\." 该批文件 ≥ 1）
    - mvn -pl zgbas-quartz -am compile 本 Task 后编译状态：预期 GREEN（task/ 内部互引完整），若有 cascade fail 记录到 SUMMARY 待 06-04 收口
  </done>
</task>

<task type="auto">
  <name>Task 2: 合并编译门断言（task/ 批次自洽）+ handler 清单记录</name>
  <files>
    .planning/phases/06-quartz-migration/06-02-SUMMARY.md
  </files>
  <read_first>
    - .planning/phases/06-quartz-migration/06-02-PLAN.md （本计划）
    - 06-02 Task 1 的编译输出（/tmp/p6-02-t1.log 或等价 mvn 输出）
    - /Users/alan/WorkSpace/IDEA/zgbas/basCore/basServer/src/main/java/com/spt/bas/server/task/ （二次核对 21 个活跃 handler 清单 + 各自 @XxlJob value 完整列表，为 06-05 D-P6-02 sys_job 翻译准备）
  </read_first>
  <action>
    【A. 合并编译门断言】运行 mvn -pl zgbas-quartz -am compile（含 06-01 RuoYi 基础设施 + 06-02 Task 1 的 21 个 task/ handler），断言 grep ^\[ERROR\]=0。若出现 ERROR，按类型分类：
      - com.spt.bas.server.command.* 缺失（BasCommandExecutor 未迁，06-04 负责）→ 记录到 SUMMARY，不阻塞本 Task 完结
      - com.spt.bas.server.rocketmq.task.* 缺失（06-03 负责）→ 同上
      - com.spt.bas.report.server.command.* 缺失（ReportCommandExecutor 06-04 负责）→ 同上
      - com.spt.bas.web.config.* 缺失（BasWebCommand 06-04 负责）→ 同上
      - com.spt.bas.server.service.* 缺失（Phase 4 应已就位）→ 阻塞，必须修
      - 其他 ERROR → 阻塞，必须修
    
    【B. 生成 handler 清单】在 .planning/phases/06-quartz-migration/06-02-SUMMARY.md 记录：
      1. 21 个迁入 handler 的类名 + @Component bean 名 + 原始 @XxlJob value 列表（多 value 的类全列）+ 翻译后方法签名
      2. 跳过的 2 个（OrverdurTask 废弃 + TestJob 测试桩 + 原因）
      3. 编译门状态 + 任何 deferred cascade（按 A 分类）
      4. 与 06-03 / 06-04 的依赖关系（此 plan 是 06-04 BasCommandExecutor @Autowired 的前置）
    
    【C. 与 D-P6-02 对齐的预提取字段】SUMMARY 内的 handler 清单按 xxl_job_info→sys_job 翻译表头（executor_handler / executor_param / trigger_status / executor_block_strategy 等）准备空字段，为 06-05 D-P6-02 翻译流程留接口。
  </action>
  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 1.8) mvn -pl zgbas-quartz -am compile -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml 2>&1 | tee /tmp/p6-02-t2.log | tail -10; 断言：①grep "^\[ERROR\]" /tmp/p6-02-t2.log | grep -v "com.spt.bas.server.command\|com.spt.bas.server.rocketmq.task\|com.spt.bas.report.server.command\|com.spt.bas.web.config" | wc -l = 0（除 06-03/06-04 deferred cascade 外零 ERROR）；②test -f .planning/phases/06-quartz-migration/06-02-SUMMARY.md</automated>
  </verify>
  <done>
    - 06-02-SUMMARY.md 存在，含 21 个 handler 清单 + 翻译字段表头
    - mvn -pl zgbas-quartz -am compile 除 06-03/06-04 deferred cascade 外零 ERROR
    - SUMMARY 记录的 bean 名列表覆盖 06-05 D-P6-02 翻译所需全部 handler
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scheduler → handler reflection | quartz Scheduler 经 JobInvokeUtil.invokeMethod 反射调 handler bean；handler 内 @Autowired service 调用是业务执行入口 |
| Handler → service 层 | handler 调用既有 Phase 4 service（ICtrContractService / IApplyPayService 等），service 层 ACL/事务边界已就位 |
| Source code → repackage | D-P6-09 首次偏离 D-P2-07：handler 包名变（com.spt.bas.server.task → com.spt.quartz.task），需保证 import 全部业务 iface 包名不变 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | handler 内 @Autowired service iface 包名（D-P6-09 偏离 D-P2-07） | mitigate | Task action D 明示仅 handler 自身包名变，handler 内 import com.spt.bas.server.service.* 保留；编译门 + 单体 grep 断言包名未误改 |
| T-06-02-02 | Information Disclosure | handler 写类（autoPay / refreshContractStatusTask / DepositPaymentTask）执行污染 dev 库 sptbasdb_pd | mitigate | 本 Task 仅翻译 handler 代码不执行；dry-run 留 06-06 D-P6-05 分级（写类空跑 test-side mock）|
| T-06-02-03 | Repudiation | handler 异常静默吞掉（XxlJobHelper.handleFail 翻译） | mitigate | 翻译规则明确 handleFail → throw new RuntimeException（非 silent return）；AbstractQuartzJob.execute catch Exception 后写 sys_job_log.status=FAIL + exceptionInfo |
| T-06-02-04 | Elevation of Privilege | @Component bean 名误命名导致 sys_job.invoke_target 解析到错误 bean | mitigate | Task action C 强制 @Component("<xxlJobValue>") 显式 bean 名（不靠 Spring 默认首字母小写），与源 @XxlJob value 1:1 对齐；06-05 D-P6-02 翻译产物 checkpoint:human-verify 兜底 |
| T-06-02-SC | Tampering | 源 handler 代码（feat-系统重构v5.0 分支） | mitigate | 直接从本地 /Users/alan/WorkSpace/IDEA/zgbas 副本读取，非网络下载；research §Sources 一手核 |
</threat_model>

<verification>
- 21 个 basServer/task/ 活跃 handler 落 com.spt.quartz.task 包（W1 plan-checker 修正：23 源 - 2 跳过 = 21 迁）
- 每个类 @Component 显式 bean 名 + 零 @XxlJob/零 import com.xxl.job.*
- XxlJobHelper.log/handleSuccess/handleFail/getJobParam 翻译完成
- @Autowired com.spt.bas.server.service.* iface 包名不变
- mvn -pl zgbas-quartz -am compile 除 06-03/06-04 deferred cascade 外零 ERROR
- 06-02-SUMMARY.md 含 21 handler 清单 + bean 名 + @XxlJob value 完整列表（供 06-05 D-P6-02 翻译）
</verification>

<success_criteria>
- [ ] QUARTZ-03（21 basServer/task/ handler 迁移部分）— 代码翻译维度关闭（剩 06-03 rocketmq/task + 06-04 command executors 合计 ~11 handler 类（8+3）共凑 60 个 @XxlJob 方法）
- [ ] D-P6-09（handler repackage com.spt.quartz.task 首次偏离 D-P2-07）— 落地
- [ ] D-P6-12（仅保阻塞 + sys_job_log，handler 类不加 @DisallowConcurrent）— 落地
- [ ] 编译门：除 deferred cascade 外零 ERROR（合并编译单元由 06-04 收口）
</success_criteria>

<output>
Create `.planning/phases/06-quartz-migration/06-02-SUMMARY.md` when done
</output>
