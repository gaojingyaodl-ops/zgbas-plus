---
phase: 06-quartz-migration
plan: 06
type: execute
wave: 6
depends_on: [06-05]
files_modified:
  - zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java
  - .planning/phases/06-quartz-migration/06-06-SUMMARY.md
autonomous: true
requirements: [QUARTZ-01, QUARTZ-02, QUARTZ-03, QUARTZ-04]

must_haves:
  truths:
    # Outcome-level truths (operator/test-observable) — W3 rebalance
    - "单体启动到 @PostConstruct 不抛（D-P6-06 启动期强 fail-fast：全量 sys_job cron 全解析 + invoke_target bean.method 全存在 — 翻译错即启动失败）"
    - "≥ 1 个只读/幂等 handler 可手动触发并写 sys_job_log（D-P6-04 sampling + D-P6-05 只读真跑分支 — QUARTZ-04 手动触发 UX 真验证）"
    - "≥ 1 个写类 handler 经 test-side mock 空跑（D-P6-05 写类空跑：bean 解析 + JobDataMap 参数传递 + slf4j 日志 — dev 库不污染）"
    - "/monitor/job 可视化运维台经浏览器可达 + 至少 1 个手动触发 dry-run 经 UI 触发不抛（D-P6-10 LOCKED option a + QUARTZ-04 UX）"
    - "全 reactor mvn test BUILD SUCCESS（沿用 Phase 5 D-P5-08 非 hermetic 契约，明文密钥无需 export）"
    # Compile-gate evidence truths (kept as implementation-level signals)
    - "Scheduler bean 在 Spring context 可解析（QuartzScheduleConfig SchedulerFactoryBean 注入成功）"
    - "SysJobMapper.selectJobAll() 可执行（sys_job / sys_job_log 表已 apply 到 sptbasdb_pd + @MyBatisDao + @MapperScan 覆盖）"
    - "60 handler + executeCommand 子命令 + RyTask demo 全部 bean 在 Spring context 含（context.containsBean via @Component 显式 bean 名 — D-P6-04 全量 bean 解析维度）"
  artifacts:
    - path: "zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java"
      provides: "扩 4 个 quartz probe/proof：quartzBeanResolution_probe + quartzTablesExist_probe + schedulerLoadAllJobs_proof + sampleQuartzJobDryRun_proof"
      contains: "quartzBeanResolution_probe"
  key_links:
    - from: "ZgbasApplicationTest.schedulerLoadAllJobs_proof"
      to: "SysJobServiceImpl.@PostConstruct init()"
      via: "Spring context 启动加载全量 sys_job 触发 Scheduler.scheduleJob"
      pattern: "schedulerLoadAllJobs_proof"
    - from: "ZgbasApplicationTest.sampleQuartzJobDryRun_proof"
      to: "sysJobService.run(jobId)"
      via: "D-P6-04 sampling：手动触发 1 个只读 handler + 断言 sys_job_log 新增 status=SUCCESS"
      pattern: "sampleQuartzJobDryRun_proof"
---

<objective>
Phase 6 Wave 6 — 最终验证：扩展 ZgbasApplicationTest 加 4 个 quartz probe/proof（quartzBeanResolution_probe + quartzTablesExist_probe + schedulerLoadAllJobs_proof + sampleQuartzJobDryRun_proof），落地 D-P6-06 启动期强 Scheduler fail-fast（cron 全解析 + invoke_target bean.method 全存在）+ D-P6-04 抽样 dry-run（D-P6-05 只读真跑 + 写类空跑分级），关闭 QUARTZ-01/02/03/04 全部启动/运行验证维度。

Purpose: 以自动化测试兜住整个 Phase 6 的运行期风险（cron 翻译错 / bean 缺失 / Scheduler 配置错 / D-P6-11 双入口冲突），对齐 Phase 5 D-P5-08 非 hermetic 启动验证模式（明文密钥无需 export）。
Output: ZgbasApplicationTest 扩 4 probe/proof + 全 reactor mvn test 绿 + 06-06-SUMMARY 收口 Phase 6 全部成功标准。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-quartz-migration/06-CONTEXT.md
@.planning/phases/06-quartz-migration/06-RESEARCH.md
@.planning/phases/06-quartz-migration/06-01-SUMMARY.md
@.planning/phases/06-quartz-migration/06-02-SUMMARY.md
@.planning/phases/06-quartz-migration/06-03-SUMMARY.md
@.planning/phases/06-quartz-migration/06-04-SUMMARY.md
@.planning/phases/06-quartz-migration/06-05-SUMMARY.md

# 关键上下文摘要
- Phase 5 D-P5-08 非 hermetic 契约沿用（dev profile 明文密钥无需 export DB_PASSWORD/SPT_APP_SECRET）
- D-P6-04 全量 bean 解析 + 抽样执行：3-5 代表性 handler dry-run
- D-P6-05 分级：只读/幂等类真跑（如 BsCompanyTask.updateCompanyGrey / WeChatWorkTask 报表刷新类）；写类（autoPay / refreshContractStatusTask / OrverdurTask / DepositPaymentTask）空跑（Mockito mock service 依赖）
- D-P6-06 启动期强 fail-fast：SysJobServiceImpl.@PostConstruct init() 加载全量 sys_job cron 全解析 + trigger 下次触发时间可算 + invoke_target bean.method 全存在
- 06-05 sys_job_data.sql 应已 apply 到 sptbasdb_pd（前置条件；若未 apply 由用户/运维先执行）
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩 ZgbasApplicationTest 加 4 个 quartz probe/proof（D-P6-06 fail-fast + D-P6-04 sampling）</name>
  <files>
    zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java
  </files>
  <read_first>
    - /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java （Phase 5 完成态：25 test，含 reportApiPathPrefixWiring_probe + sampleReportQuery_proof 等模式参考）
    - /Users/alan/WorkSpace/IDEA/spt-auth/auth-quartz/src/main/java/com/spt/quartz/service/impl/SysJobServiceImpl.java 行 37-50 （@PostConstruct init() — scheduler.clear() + 遍历 selectJobAll + ScheduleUtils.createScheduleJob，理解 D-P6-06 fail-fast 触发点）
    - /Users/alan/WorkSpace/IDEA/spt-auth/auth-quartz/src/main/java/com/spt/quartz/util/ScheduleUtils.java （createScheduleJob 内 cron 解析 + trigger 构建 + scheduler.scheduleJob — 翻译错则此处抛 SchedulerException）
    - /Users/alan/WorkSpace/IDEA/spt-auth/auth-quartz/src/main/java/com/spt/quartz/util/JobInvokeUtil.java （invokeMethod 解析 invoke_target 字符串 → SpringUtils.getBean(beanName) → 反射 invoke；bean 不存在抛 NoSuchBeanDefinitionException）
    - /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-quartz/src/main/java/com/spt/quartz/task/RyTask.java （D-P6-04 dry-run 候选：ryTask.ryNoParams 是 ry_20210908.sql:584 已 INSERT 的 demo 任务，最安全的 dry-run 目标）
    - .planning/phases/06-quartz-migration/06-05-SUMMARY.md （核对 sys_job_data.sql 已 apply 到 sptbasdb_pd；若未 apply 本 Task 前置失败，需提示用户先 apply）
    - .planning/phases/06-quartz-migration/06-RESEARCH.md §Validation Architecture（测试框架 + Phase Requirements → Test Map 表行 793-804 + Wave 0 Gaps 行 806-814）
  </read_first>
  <action>
    【A. 扩 ZgbasApplicationTest】在现有类（Phase 5 D-P5-08 模式：@SpringBootTest + 非 hermetic + 明文密钥无需 export）追加 4 个 quartz probe/proof 方法。所有方法保 Phase 5 既有的 @Disabled 默认 + 手动启用约定（除了启动期自动加载的 schedulerLoadAllJobs_proof 应作为默认 @Test 跑 — 因为它是 D-P6-06 fail-fast 强验证，每次启动验证应自动跑）：
    
    【B. quartzBeanResolution_probe】默认启用（@Test 不 @Disabled）— 启动期 Spring context 加载完后断言：
      - context.containsBean("scheduler")（或 context.getBean(Scheduler.class) != null）— Scheduler bean 可解析
      - context.containsBean("sysJobService") + context.containsBean("sysJobLogService")
      - 抽 10+ 代表性 handler bean：context.containsBean("applyPayTask") / context.containsBean("repaymentTask") / context.containsBean("synchronizedCtrContractTask") / context.containsBean("basCommandExecutor") / context.containsBean("reportCommandExecutor") / context.containsBean("basWebCommand") / context.containsBean("ryTask")（从 06-02/03/04 SUMMARY bean 名清单抽取跨域代表性 10+）
    
    【C. quartzTablesExist_probe】默认 @Disabled（需 DB apply 前置，且与 schedulerLoadAllJobs_proof 冗余）— 手动启用时断言：
      - 注入 SysJobMapper / SysJobLogMapper（@Autowired）
      - sysJobMapper.selectJobAll() 不抛 BindingException（验证 @MyBatisDao + @MapperScan + XML 路径对齐 + sys_job 表存在）
      - sysJobLogMapper 选若干行（验证 sys_job_log 表存在）
      - 断言 selectJobAll().size() 与 sys_job_data.sql 的 INSERT 行数一致（约 60+55+3=118；与 06-05 Task 2 行数预算对齐）
    
    【D. schedulerLoadAllJobs_proof】默认启用（@Test 不 @Disabled）— 这是 D-P6-06 fail-fast 强验证：
      - 启动期 SysJobServiceImpl.@PostConstruct init() 自动跑：scheduler.clear() + 遍历 selectJobAll + 对每条 sys_job 调 ScheduleUtils.createScheduleJob
      - 若任何 cron_expression 解析失败（CronExpression.isValidExpression 返回 false 或 CronScheduleBuilder.cronSchedule 抛 IllegalArgumentException）→ Spring context 启动失败（fail-fast）
      - 若任何 invoke_target bean 不存在（SpringUtils.getBean 抛 NoSuchBeanDefinitionException）→ 启动失败
      - 本 proof 只需断言 "Spring context 启动成功到 @PostConstruct 之后" — 即 SpringBootTest 启动到这一步本身即证明全部 sys_job 加载通过（启动失败 SpringBootTest 会抛 IllegalStateException）
      - 加显式断言：scheduler != null + ((scheduler.getJobKeys(GroupMatcher.anyGroup())).size() == selectJobAll().size())（Scheduler 中注册的 job 数 = sys_job 表行数）
    
    【E. sampleQuartzJobDryRun_proof】默认 @Disabled（手动启用 — D-P6-04 sampling + D-P6-05 分级）：
      - **只读真跑分支**（D-P6-05）：
        - 注入 ISysJobService（@Autowired sysJobService）
        - 选 1-3 个只读/幂等代表性 handler：ryTask.ryNoParams（最安全 demo）/ BsCompanyTask.updateCompanyGrey（业务只读刷新）/ 1 个 report refresh 类
        - 调 sysJobService.run(jobId)（对应 sys_job 表的 job_id）
        - 断言 sys_job_log 新增一行 status=SUCCESS（注入 SysJobLogMapper 查最近一行）
      - **写类空跑分支**（D-P6-05）：
        - @MockBean 或 Mockito mock 掉 handler 的 service 依赖（如 IApplyPayService / ICtrContractService）
        - 调 sysJobService.run(jobId) 触发写类 handler（如 applyPayTask.autoPay）
        - 验证 mock service 收到调用（verify mock.times(1)）— 证明 bean 解析 + JobDataMap 参数传递 + handler 方法被反射调成功
        - 验证 sys_job_log 写入（AbstractQuartzJob.after）
        - **不污染 dev 库**：因 service 被 mock，真实业务副作用短路
    
    【F. 不做的事】①不重写 Phase 3-5 既有 probe（仅扩 4 个新方法）；②不强制 sys_job_data.sql 已 apply 到 prod（仅 dev 库前置，prod 由运维）；③不做全量 60 handler dry-run（D-P6-04 抽样 3-5 个）；④不引入 testcontainers/H2（沿用 Phase 5 非 hermetic 真库 dev）
  </action>
  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 1.8) mvn -pl zgbas-admin -am test -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml -Dtest=ZgbasApplicationTest 2>&1 | tee /tmp/p6-06-t1.log | tail -30; 断言：①grep -c "quartzBeanResolution_probe\|quartzTablesExist_probe\|schedulerLoadAllJobs_proof\|sampleQuartzJobDryRun_proof" zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java = 4；②grep "Tests run:" /tmp/p6-06-t1.log 显示 quartzBeanResolution_probe + schedulerLoadAllJobs_proof 默认启用且 PASS（若 sys_job_data 未 apply 到 dev 库则 proof 会失败 — 这是 D-P6-06 fail-fast 设计预期，需前置完成 DB apply）；③写类空跑分支的 mock 用 Mockito（不污染 dev 库）</automated>
  </verify>
  <done>
    - ZgbasApplicationTest 含 4 个新 quartz probe/proof 方法
    - quartzBeanResolution_probe + schedulerLoadAllJobs_proof 默认启用且 PASS（依赖 sys_job_data.sql 已 apply）
    - quartzTablesExist_probe + sampleQuartzJobDryRun_proof 默认 @Disabled（手动启用）
    - schedulerLoadAllJobs_proof 含 Scheduler 中 job 数 = sys_job 表行数断言（D-P6-06 fail-fast 强验证）
    - sampleQuartzJobDryRun_proof 含只读真跑分支（ryTask.ryNoParams 等）+ 写类空跑分支（Mockito mock）
    - mvn -pl zgbas-admin -am test -Dtest=ZgbasApplicationTest 绿（除 @Disabled 外）
  </done>
</task>

<task type="auto">
  <name>Task 2: 全 reactor mvn test 绿 + Phase 6 收口 SUMMARY</name>
  <files>
    .planning/phases/06-quartz-migration/06-06-SUMMARY.md
  </files>
  <read_first>
    - 06-06 Task 1 测试输出 /tmp/p6-06-t1.log
    - .planning/ROADMAP.md §Phase 6 Success Criteria 4 条
    - .planning/phases/06-quartz-migration/06-01-SUMMARY.md ~ 06-05-SUMMARY.md （全部前序 SUMMARY — 收口依据）
    - .planning/REQUIREMENTS.md QUARTZ-01/02/03/04 + INFRA-03 完成态判定
  </read_first>
  <action>
    【A. 全 reactor 测试门】JAVA_HOME=Corretto 1.8 mvn test（全 reactor，含 zgbas-quartz + zgbas-system + zgbas-admin + framework + common）；预期：
      - zgbas-quartz 模块（若独立有测试则跑，无则仅编译）
      - zgbas-admin ZgbasApplicationTest 含 Phase 3-5 既有 25 test + Phase 6 新增 4 probe/proof（其中 2 默认启用 + 2 @Disabled）
      - 全 reactor BUILD SUCCESS
    
    【B. Phase 6 4 条成功标准逐项核验】在 06-06-SUMMARY.md 记录每条成功标准的关闭证据：
      1. zgbas-quartz 模块就位 + RuoYi quartz 整模块复制 + ScheduleConfig 落 QuartzScheduleConfig + sys_job/sys_job_log 表建好 → 引用 06-01 SUMMARY + 06-05 sys_job_data apply 状态
      2. 60 @XxlJob handler 迁移为 quartz bean（XxlJobHelper.log→slf4j / handleSuccess/Fail→return/异常 / getJobParam→JobDataMap）→ 引用 06-02/03/04 SUMMARY + grep 全单体零 xxl-job 痕迹
      3. 任务记录初始化（cron / bean / method 翻译为 sys_job 数据）+ 支持手动触发与传参 → 引用 06-05 sys_job_data.sql + sampleQuartzJobDryRun_proof
      4. 至少 1 个迁移后的任务可手动触发 + 传参 dry-run 通过 + xxl-job 依赖与 executor 配置完全移除 → 引用 sampleQuartzJobDryRun_proof PASS + INFRA-03 grep = 0
    
    【C. 5 个研究标定阻塞收口记录】SUMMARY 内记录每个阻塞的最终处置：
      - Pitfall 1（ScheduleConfig 注释）→ 06-01 QuartzScheduleConfig uncomment+rename
      - Pitfall 2（17 com.spt.common.* 缺失）→ 06-01 15 类子集本地化
      - Pitfall 3（JOB_WHITELIST_STR）→ 06-01 改 {"com.spt"}
      - Pitfall 4（@PreAuthorize 与 Shiro 冲突）→ 06-01 剥除
      - Pitfall 5（Thymeleaf 不可达）→ D-P6-10 LOCKED option (a) 保可视化 UI：06-01 Task 4 建 /monitor/job Thymeleaf（job/add/edit.html 仿 operlog shell）+ Task 5 external spt-auth sys_menu INSERT checkpoint:human-blocked
      - Pitfall 6（executeCommand 3 同名）→ 06-04 bean 名 + 06-05 sys_job 拆 ~55 行
      - Pitfall 7（mapper-locations 路径）→ 06-01 XML 落 mybatis/mappers/
      - Pitfall 8（XxlJobHelper.log 非 handler）→ 06-01 CtrContractProfitServiceImpl 翻译
      - Pitfall 9（BasCommandExecutor @Autowired 4 task）→ 06-04 import 改 com.spt.quartz.task.*
    
    【D. D-P6-01..12 全 12 决策落地证据】逐项记录到 SUMMARY，附对应 plan/task 引用。
    
    【E. Phase 7 衔接】SUMMARY 末尾记录 Phase 7 ALIGN-01/02 的输入：
      - 单服务启动 → 登录 → 首页 → 核心业务 → 报表 → 定时任务 全链路 e2e 验证（含 /monitor/job API 或直调 sysJobService.run）
      - 关键业务流程与旧系统 zgbas 行为等价（含 cron 节奏 + 任务执行结果回归对照 — 写类真实回归留 P7）
      - 已知 deferred：xxl-job admin 服务退役（运维事务）/ 真实全量任务回归（P7 写类真实回归）
      - 已交付（非 deferred）：/monitor/job 可视化 UI（D-P6-10 option a，06-01 Task 4）
  </action>
  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 1.8) mvn test -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml 2>&1 | tee /tmp/p6-06-t2.log | tail -10; 断言：①grep "BUILD SUCCESS" /tmp/p6-06-t2.log 出现 1 次；②grep "Tests run:" /tmp/p6-06-t2.log 显示 ZgbasApplicationTest 全部 PASS（除 @Disabled 4 个：Phase 3-5 旧 4 + Phase 6 新 2）；③test -f .planning/phases/06-quartz-migration/06-06-SUMMARY.md；④grep -c "QUARTZ-01\|QUARTZ-02\|QUARTZ-03\|QUARTZ-04\|INFRA-03" .planning/phases/06-quartz-migration/06-06-SUMMARY.md ≥ 5（5 需求全部核验记录）</automated>
  </verify>
  <done>
    - 全 reactor mvn test BUILD SUCCESS
    - ZgbasApplicationTest 全 PASS（除 @Disabled proof）
    - 06-06-SUMMARY.md 存在，含 4 条成功标准逐项关闭证据 + 5 阻塞 + 12 决策落地记录 + Phase 7 衔接
    - QUARTZ-01/02/03/04 + INFRA-03 全 5 需求核验通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → Spring context | ZgbasApplicationTest @SpringBootTest 启动完整 context，触发 @PostConstruct init() — 这是 D-P6-06 fail-fast 的执行点 |
| Test → dev DB sptbasdb_pd | sampleQuartzJobDryRun_proof 只读真跑分支连真 dev 库（非 hermetic，Phase 5 D-P5-08 同契约）；写类空跑分支用 Mockito mock 不污染 |
| Test → scheduler.triggerJob | sampleQuartzJobDryRun_proof 调 sysJobService.run(jobId) 触发 quartz Scheduler.run → JobInvokeUtil.invokeMethod 反射 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | Repudiation | 启动期 cron 翻译错未被 fail-fast 暴露 | mitigate | schedulerLoadAllJobs_proof 默认启用 @Test（每次 mvn test 跑）；SpringBootTest 启动失败即证明；quartz CronScheduleBuilder.cronSchedule 抛 IllegalArgumentException 直接传播 |
| T-06-06-02 | Information Disclosure | 写类 handler dry-run 污染 dev 库 sptbasdb_pd | mitigate | D-P6-05 写类空跑分支用 @MockBean / Mockito mock 掉 service 依赖；不调真实 service.method；副作用短路；只读真跑分支选 ryTask.ryNoParams / BsCompanyTask.updateCompanyGrey 等幂等安全目标 |
| T-06-06-03 | DoS | sampleQuartzJobDryRun_proof 多次触发拖垮 dev 库 | mitigate | 默认 @Disabled（手动启用）；选 ryTask.ryNoParams（最轻量 demo）+ 1-2 个业务只读 handler（D-P6-04 sampling 上限 3-5 个）|
| T-06-06-04 | Tampering | 非 hermetic 测试契约依赖明文密钥 dev profile | accept | Phase 4 D-P4-04 option 3 决定保留明文密钥；Phase 5 D-P5-08 沿用；本 plan 沿用同一契约（无需 export DB_PASSWORD/SPT_APP_SECRET）；research §Environment Availability 行 485 确认 dev 47.104.15.98:3306 明文可达 |
| T-06-06-05 | Elevation of Privilege | test 范围调 sysJobService.run 暴露 admin 写接口 | accept | 测试代码非生产暴露面；run(jobId) 是 RuoYi SysJobController.run 的服务层入口，admin ShiroFilter 已认证才可达（生产）；测试环境 dev 不强制认证 |
| T-06-06-SC | Tampering | 无新增 maven 依赖（Mockito 已就位） | mitigate | research §Standard Stack Supporting 表：Mockito 已就位 zgbas-admin/test；无新增包 |
</threat_model>

<verification>
- ZgbasApplicationTest 含 4 新 quartz probe/proof 方法
- quartzBeanResolution_probe + schedulerLoadAllJobs_proof 默认启用 PASS（依赖 sys_job_data.sql 已 apply）
- quartzTablesExist_probe + sampleQuartzJobDryRun_proof 默认 @Disabled 手动启用
- D-P6-06 fail-fast 通过（SpringBootTest 启动到 @PostConstruct 即证全 sys_job cron 解析 + bean 存在）
- D-P6-04 sampling：sampleQuartzJobDryRun_proof 含只读真跑 + 写类空跑双分支
- D-P6-05 分级：只读真跑选 ryTask.ryNoParams / BsCompanyTask.updateCompanyGrey 等幂等；写类空跑用 Mockito mock
- 全 reactor mvn test BUILD SUCCESS
- 06-06-SUMMARY 收口 4 成功标准 + 5 阻塞 + 12 决策 + Phase 7 衔接
</verification>

<success_criteria>
- [ ] QUARTZ-01（zgbas-quartz + 整模块复制 + ScheduleConfig）— 启动维度关闭（schedulerBeanResolution_probe + schedulerLoadAllJobs_proof PASS）
- [ ] QUARTZ-02（sys_job / sys_job_log / QRTZ_* 表）— 启动维度关闭（quartzTablesExist_probe 手动启用 PASS）
- [ ] QUARTZ-03（60 handler 迁 quartz bean）— 启动维度关闭（quartzBeanResolution_probe 10+ bean containsBean PASS）
- [ ] QUARTZ-04（sys_job 数据初始化 + 手动触发传参）— 运行维度关闭（sampleQuartzJobDryRun_proof 双分支 PASS）
- [ ] D-P6-04（全量 bean 解析 + 抽样 dry-run）— 落地
- [ ] D-P6-05（只读真跑 + 写类空跑分级）— 落地
- [ ] D-P6-06（启动期强 Scheduler fail-fast）— 落地（schedulerLoadAllJobs_proof 默认启用）
- [ ] Phase 6 全 5 需求 + 4 成功标准关闭；可进入 Phase 7 ALIGN-01/02 行为对齐验证
</success_criteria>

<output>
Create `.planning/phases/06-quartz-migration/06-06-SUMMARY.md` when done
</output>
