---
phase: 06-quartz-migration
plan: 04
type: execute
wave: 4
depends_on: [06-02, 06-03]
files_modified:
  - zgbas-quartz/src/main/java/com/spt/quartz/task/BasCommandExecutor.java
  - zgbas-quartz/src/main/java/com/spt/quartz/task/ReportCommandExecutor.java
  - zgbas-quartz/src/main/java/com/spt/quartz/task/BasWebCommand.java
  - zgbas-admin/src/main/java/com/spt/bas/server/api/MQApi.java
autonomous: true
requirements: [QUARTZ-03, QUARTZ-04]

must_haves:
  truths:
    - "3 个 command executor（BasCommandExecutor + ReportCommandExecutor + BasWebCommand）迁入 com.spt.quartz.task 包"
    - "每个类带 @Component(\"<commandExecutorBeanName>\") 显式 bean 名"
    - "零 @XxlJob 注解、零 import com.xxl.job.*、零 XxlJobHelper.getJobParam fallback"
    - "executeCommand(String commandline) 方法签名保留 + 方法体内 XxlJobHelper.getJobParam() fallback 删除（args 直接由 sys_job.invoke_target 传入 method）"
    - "MQApi HTTP 端点保留（D-P6-11 前端零改），内部 @Autowired Synchronized*Task 改为 @Autowired service iface + 直调 service.method（按 06-03-SUMMARY 映射表决策的 1:1 直调或 1:N service 加组合方法）"
    - "合并编译门：mvn -pl zgbas-admin -am compile 全 reactor 零 ERROR（关闭 QUARTZ-03 编译维度 — 60 handler 全迁完成）"
  artifacts:
    - path: "zgbas-quartz/src/main/java/com/spt/quartz/task/BasCommandExecutor.java"
      provides: "BasCommand command executor（~50 子命令分支：updateBudgetSettlement / reContract / 等；@Autowired 4 个 task 类 ApplyPayTask/BudgetSettlementTask/CtrContractScheduleTask/DcsxAutoApplyPayTask 已 06-02 就位）"
      contains: "@Component(\"basCommandExecutor\")"
    - path: "zgbas-quartz/src/main/java/com/spt/quartz/task/ReportCommandExecutor.java"
      provides: "Report command executor（cache + pushWeChatWorkLeaderboard 2 子命令）"
      contains: "@Component(\"reportCommandExecutor\")"
    - path: "zgbas-quartz/src/main/java/com/spt/quartz/task/BasWebCommand.java"
      provides: "Web command executor（clean + cache + fundSocket 3 子命令）"
      contains: "@Component(\"basWebCommand\")"
    - path: "zgbas-admin/src/main/java/com/spt/bas/server/api/MQApi.java"
      provides: "D-P6-11 改造：HTTP 端点保留，内部 Synchronized*Task 调用改直调 service"
      contains: "ICtrContractService"
  key_links:
    - from: "zgbas-quartz/.../task/BasCommandExecutor.java @Autowired"
      to: "zgbas-quartz/.../task/{ApplyPayTask,BudgetSettlementTask,CtrContractScheduleTask,DcsxAutoApplyPayTask}.java"
      via: "同模块 com.spt.quartz.task.* import"
      pattern: "import com\\.spt\\.quartz\\.task\\.(ApplyPayTask|BudgetSettlementTask|CtrContractScheduleTask|DcsxAutoApplyPayTask)"
    - from: "zgbas-admin/.../api/MQApi.java @Autowired"
      to: "zgbas-system/.../service/* iface"
      via: "D-P6-11 直调 service（不经 Synchronized*Task handler）"
      pattern: "com\\.spt\\.bas\\.server\\.service\\.I[A-Z][a-zA-Z]+Service"
---

<objective>
Phase 6 Wave 4 — 把 3 个 command executor（BasCommandExecutor / ReportCommandExecutor / BasWebCommand）迁入 com.spt.quartz.task 并完成 xxl-job→quartz 翻译，关闭 60 handler 迁移（QUARTZ-03 编译维度收口）；同步完成 D-P6-11 MQApi HTTP 同步触发门面改造（HTTP 端点保留 + 内部 Synchronized*Task 调用改直调 service iface，前端零改）。

Purpose: 把 60 handler 的最后 3 个（command executors 是最复杂一批 — 每个 executor 的 executeCommand 方法含多子命令分支）迁完，让 sys_job.invoke_target 的 bean 反射链完整；同时完成 D-P6-11 同步触发门面与定时调度的双入口共存。
Output: 3 个 command executor + 改造后的 MQApi；全 reactor 编译门 GREEN（关闭 QUARTZ-03 编译维度）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-quartz-migration/06-CONTEXT.md
@.planning/phases/06-quartz-migration/06-RESEARCH.md
@.planning/phases/06-quartz-migration/06-01-SUMMARY.md
@.planning/phases/06-quartz-migration/06-02-SUMMARY.md
@.planning/phases/06-quartz-migration/06-03-SUMMARY.md

# 关键上下文摘要
- 06-02 + 06-03 已迁 28 个 handler（task/ 20 + rocketmq/task/ 8），剩 3 个 command executors 在本 plan
- 06-03-SUMMARY 含 Synchronized*Task 内部依赖映射表（本 plan Task 3 MQApi 改造的输入）
- D-P6-11 决策：HTTP 端点保留前端零改，内部 @Autowired Synchronized*Task 改 @Autowired service iface + 直调
- D-P6-07/D-P6-09：command executors 落 zgbas-quartz com.spt.quartz.task（与 handler 同模块，便于运维定位）
- Pitfall 6：executeCommand 3 个 command executors 共用 @XxlJob value "executeCommand"，sys_job 翻译必须按 bean 名 + commandline 子命令拆 ~55 行（在 06-05 D-P6-02 处理，本 plan 不建 sys_job）
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port BasCommandExecutor + ReportCommandExecutor + BasWebCommand 到 com.spt.quartz.task</name>
  <files>
    zgbas-quartz/src/main/java/com/spt/quartz/task/BasCommandExecutor.java
    zgbas-quartz/src/main/java/com/spt/quartz/task/ReportCommandExecutor.java
    zgbas-quartz/src/main/java/com/spt/quartz/task/BasWebCommand.java
  </files>
  <read_first>
    - /Users/alan/WorkSpace/IDEA/zgbas/basCore/basServer/src/main/java/com/spt/bas/server/command/BasCommandExecutor.java （源：@XxlJob("executeCommand") boolean executeCommand(String commandline) — 行 130-381，~50 子命令分支；@Autowired 4 个 task 类：ApplyPayTask / BudgetSettlementTask / CtrContractScheduleTask / DcsxAutoApplyPayTask）
    - /Users/alan/WorkSpace/IDEA/zgbas/basReport/reportServer/src/main/java/com/spt/bas/report/server/command/ReportCommandExecutor.java （源：2 子命令 cache + pushWeChatWorkLeaderboard）
    - /Users/alan/WorkSpace/IDEA/zgbas/web/src/main/java/com/spt/bas/web/config/BasWebCommand.java （源：3 子命令 clean + cache + fundSocket）
    - /Users/alan/WorkSpace/IDEA/zgbas/basCore/basServer/src/main/java/com/spt/bas/server/command/package-info.java （理解 ICommand 接口契约 — BasCommandExecutor implements ICommand；command/ 包本身是否迁入由 ICommand 是否在 basServer.command 定义决定）
    - .planning/phases/06-quartz-migration/06-RESEARCH.md §Code Examples §2（BasCommandExecutor 完整翻译范例：行 632-677）+ §Pitfall 6（executeCommand 3 同名 handler）+ §Pitfall 9（BasCommandExecutor @Autowired handler 类）
    - .planning/phases/06-quartz-migration/06-02-SUMMARY.md （4 个 @Autowired 目标 task 类 — ApplyPayTask/BudgetSettlementTask/CtrContractScheduleTask/DcsxAutoApplyPayTask — 已在 zgbas-quartz/.../task/ 就位，本 plan BasCommandExecutor import 直接命中）
    - /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-system/src/main/java/com/spt/bas/server/command/ （核：单体 Phase 4 是否已迁 ICommand 接口；若 ICommand 在 com.spt.bas.server.command 包，本 plan 不需迁 ICommand，BasCommandExecutor 跨模块 import 即可；若 ICommand 未迁则需评估迁移或本地化）
  </read_first>
  <action>
    【A. 前置核】先核单体 zgbas-system 是否已有 com.spt.bas.server.command.ICommand 接口（Phase 4 04-03 infra 照搬时是否含 command/ICommand.java）。若已有，BasCommandExecutor implements ICommand 的 import 保留 com.spt.bas.server.command.ICommand（保包名）；若无，ICommand 需在 06-04 内本地化或从 source 迁入 zgbas-system（research 未明确，本 plan Task 1 第一步核实并视情况补迁 ICommand 接口到 zgbas-system/.../command/ICommand.java 保包名 — 该补迁属 Phase 4 04-03 遗漏收口，记入 SUMMARY）。
    
    【B. BasCommandExecutor 落位 + repackage】源 basServer/command/BasCommandExecutor.java → zgbas-quartz/src/main/java/com/spt/quartz/task/BasCommandExecutor.java，变换：
      1. package com.spt.bas.server.command; → package com.spt.quartz.task;（D-P6-09 偏离 — 仅 handler 落 quartz 模块）
      2. 类上 @Component → @Component("basCommandExecutor")（显式 bean 名，与 sys_job.invoke_target = "basCommandExecutor.executeCommand('xxx')" 对齐）
      3. 删 import com.xxl.job.core.handler.annotation.XxlJob; + import com.xxl.job.core.context.XxlJobHelper;
      4. import com.spt.bas.server.command.ICommand; 保留（若 ICommand 在 zgbas-system 同包）— 或改 import com.spt.bas.server.command.ICommand 跨模块取（research 推荐：ICommand 留 zgbas-system 同包）
      5. import com.spt.bas.server.task.{ApplyPayTask, BudgetSettlementTask, CtrContractScheduleTask, DcsxAutoApplyPayTask}; → 改为 import com.spt.quartz.task.{ApplyPayTask, BudgetSettlementTask, CtrContractScheduleTask, DcsxAutoApplyPayTask};（Pitfall 9：4 个 task 类已在 06-02 迁入同模块同包）
      6. 方法级翻译（Pitfall 9 + §Code Examples §2）：
         - @XxlJob(value = "executeCommand") public boolean executeCommand(String commandline) → 删注解，方法签名 public boolean executeCommand(String commandline) 保留
         - 方法体内 String jobParam = XxlJobHelper.getJobParam(); if (StringUtils.isNotBlank(jobParam)) { commandline = jobParam; } 整段**删除**（Pitfall 9：quartz 模式下 commandline 直接由 sys_job.invoke_target = "basCommandExecutor.executeCommand('updateBudgetSettlement')" 字符串 args 传入方法参数；JobInvokeUtil 反射 invoke(this, "updateBudgetSettlement") → commandline = "updateBudgetSettlement"；源 getJobParam fallback 是 xxl-job 机制独有）
         - 方法体剩余 commandline.startsWith("updateBudgetSettlement") 等子命令分支保留不动（业务逻辑）
         - 任何 XxlJobHelper.log → log.info（补 slf4j Logger 若源类无）
         - 任何 XxlJobHelper.handleSuccess/handleFail → return true/false 或 throw
      7. ICommand 接口契约：executeCommand 返回 boolean（true 成功 / false 失败），翻译后行为保留（return true/false 不变；失败想触发 sys_job_log.status=FAIL 则 throw 而非 return false — D-P6-12 决策保留 sys_job_log，翻译时若需失败可观测，handleFail 改 throw RuntimeException 替代 return false；源若 handleFail 后 return true，则保 return true 不改业务行为，sys_job_log 记 SUCCESS）
    
    【C. ReportCommandExecutor 落位】源 basReport/reportServer/.../command/ReportCommandExecutor.java → zgbas-quartz/.../task/ReportCommandExecutor.java，同 B 规则变换：
      1. package com.spt.bas.report.server.command; → package com.spt.quartz.task;
      2. @Component("reportCommandExecutor")
      3. 删 @XxlJob + XxlJobHelper import + getJobParam fallback
      4. @Autowired 业务 service iface 包名保留（com.spt.bas.report.* 或 com.spt.bas.server.service.* — 按源 import 实际）
      5. 子命令分支（cache + pushWeChatWorkLeaderboard）业务逻辑保留
    
    【D. BasWebCommand 落位】源 web/.../config/BasWebCommand.java → zgbas-quartz/.../task/BasWebCommand.java，同 B 规则变换：
      1. package com.spt.bas.web.config; → package com.spt.quartz.task;
      2. @Component("basWebCommand")
      3. 删 @XxlJob + XxlJobHelper import + getJobParam fallback
      4. @Autowired 业务 iface 包名保留
      5. 子命令分支（clean + cache + fundSocket）业务逻辑保留
    
    【E. 不做的事】①不建 sys_job INSERT 数据（executeCommand ~55 行拆分在 06-05 D-P6-02）；②不动 MQApi.java（Task 3 负责）；③不动 RyTask（06-01 已就位）；④不补 ICommand 接口到 zgbas-quartz（若需补则补到 zgbas-system 同包，保 Phase 4 业务接口集中）
  </action>
  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 1.8) mvn -pl zgbas-quartz -am compile -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml 2>&1 | tee /tmp/p6-04-t1.log | tail -20; 断言：①test -f zgbas-quartz/src/main/java/com/spt/quartz/task/BasCommandExecutor.java && test -f zgbas-quartz/src/main/java/com/spt/quartz/task/ReportCommandExecutor.java && test -f zgbas-quartz/src/main/java/com/spt/quartz/task/BasWebCommand.java；②grep -c "@XxlJob\|XxlJobHelper\|com.xxl.job" zgbas-quartz/src/main/java/com/spt/quartz/task/BasCommandExecutor.java = 0；③grep -c "XxlJobHelper.getJobParam" zgbas-quartz/src/main/java/com/spt/quartz/task/BasCommandExecutor.java = 0（fallback 删除）；④grep "@Component" zgbas-quartz/src/main/java/com/spt/quartz/task/{BasCommandExecutor,ReportCommandExecutor,BasWebCommand}.java | wc -l = 3；⑤grep "^\[ERROR\]" /tmp/p6-04-t1.log | wc -l = 0（60 handler 全迁完成，zgbas-quartz 模块自洽）</automated>
  </verify>
  <done>
    - 3 个 command executor 文件落 com.spt.quartz.task
    - 每个 @Component 显式 bean 名（basCommandExecutor / reportCommandExecutor / basWebCommand）
    - 零 @XxlJob + 零 XxlJobHelper.getJobParam fallback + 零 import com.xxl.job.*
    - executeCommand(String commandline) 方法签名保留 + 业务子命令分支保留
    - 4 个 @Autowired task 类（BasCommandExecutor）import 改 com.spt.quartz.task.* 命中 06-02 已迁类
    - mvn -pl zgbas-quartz -am compile 零 ERROR（zgbas-quartz 模块自洽，60 handler 全迁）
  </done>
</task>

<task type="auto">
  <name>Task 2: D-P6-11 改造 — MQApi HTTP 端点保留 + 内部 Synchronized*Task 改直调 service iface</name>
  <files>
    zgbas-admin/src/main/java/com/spt/bas/server/api/MQApi.java
  </files>
  <read_first>
    - /Users/alan/WorkSpace/IDEA/zgbas/basCore/basServer/src/main/java/com/spt/bas/server/api/MQApi.java （源：@RestController @RequestMapping("/mq/api") + ~7 业务 GET + 1 GET/testSendMessage；每个端点 @Autowired 一个 Synchronized*Task bean 调其业务方法如 synchronizedAllCtrContract）
    - .planning/phases/06-quartz-migration/06-03-SUMMARY.md （8 个 Synchronized*Task 内部依赖映射表：1:1 直调 vs 1:N 加组合方法 — 本 Task 的直接决策依据）
    - .planning/phases/06-quartz-migration/06-RESEARCH.md §Pattern 6（D-P6-11 改造范例：行 414-434）+ §Code Examples §3（MQApi 翻译范例：行 686-718）+ §Open Questions Q2（依赖分析粒度）
    - /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-admin/src/main/java/com/spt/bas/server/api/ （核：Phase 4 04-05 是否已迁 MQApi.java 到 admin — research §Pattern 6 说"deferred 到本期"，若 admin/api/ 已有 MQApi 则本 Task 是修改而非新建；若无则从 source 照搬 + 改造）
    - /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-system/src/main/java/com/spt/bas/server/service/ （核：service iface 列表，确认 ICtrContractService.synchronizedAllCtrContract 等目标方法是否已在 iface 声明 — 若 1:1 直调目标方法未在 iface 声明则需在 iface + impl 加方法，本 Task 一并处置）
  </read_first>
  <action>
    【A. 落位确认】检查 zgbas-admin/.../api/MQApi.java 是否已存在（Phase 4 04-05 basServer api 照搬时是否含 MQApi）：
      - 若已存在：本 Task 是修改（删 @Autowired Synchronized*Task + 改 @Autowired service iface + 改方法体内调用）
      - 若不存在：从 source basServer/api/MQApi.java 复制到 zgbas-admin/.../api/MQApi.java（保包名 com.spt.bas.server.api，D-P2-07），再做改造
    
    【B. HTTP 端点契约保留（D-P6-11 前端零改）】保留 @RestController @RequestMapping("/mq/api") + 所有 @GetMapping 路径不变（/ctrContractTask /applyMatchTask 等 ~7 业务端点 + 1 /testSendMessage）。前端原调用 URL + 参数零改。
    
    【C. 内部改造 — 按 06-03-SUMMARY 映射表】对每个 @GetMapping 端点：
      1. 删 @Autowired Synchronized*Task handler 字段（如 @Autowired private SynchronizedCtrContractTask ctrContractTask;）
      2. 加 @Autowired service iface 字段（如 @Autowired private ICtrContractService ctrContractService;）
      3. 方法体内 ctrContractTask.synchronizedAllCtrContract() → ctrContractService.synchronizedAllCtrContract()
      4. **1:1 case**（Synchronized*Task 业务方法 1:1 对应 service.method 已存在）：直接替换调用，service iface 不需改
      5. **1:N case**（Synchronized*Task 方法体调多 service 或含编排逻辑）：在目标 service iface（如 ICtrContractService）加一个组合方法（如 void synchronizedAllCtrContractBatch();）+ 在 service impl 照搬原 Synchronized*Task 方法体业务编排（多 service 调用 + 数据加工）；MQApi 调该组合方法。**注意**：此场景需在 zgbas-system/.../service/iface/I<Domain>Service.java + .../service/impl/<Domain>ServiceImpl.java 加方法（保包名 D-P2-07），该 service 改动属本 Task 范围
    
    【D. 不改的事】①HTTP 端点路径 + HTTP 方法 + 入参/出参契约（前端零改）；②不删 Synchronized*Task handler 类（06-03 已迁，作为 sys_job 定时调度入口独立存在）；③不动其他 admin @RestController（仅 MQApi 一文件）；④不引入 quartz @DisallowConcurrentExecution 到 MQApi（HTTP 同步触发语义本就串行，单请求单线程）
    
    【E. 1:1 / 1:N 决策落地】Task action 内必须按 06-03-SUMMARY 映射表逐个端点标注决策结果（1:1 直调 / 1:N 加组合方法），并在 SUMMARY 记录每个 service.method 的下沉路径。
  </action>
  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 1.8) mvn -pl zgbas-admin -am compile -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml 2>&1 | tee /tmp/p6-04-t2.log | tail -15; 断言：①test -f zgbas-admin/src/main/java/com/spt/bas/server/api/MQApi.java；②grep -c "@RestController" zgbas-admin/src/main/java/com/spt/bas/server/api/MQApi.java = 1；③grep -c "@RequestMapping.*\"/mq/api\"" zgbas-admin/src/main/java/com/spt/bas/server/api/MQApi.java = 1（HTTP 端点保留）；④grep -c "@Autowired.*Synchronized.*Task\|Synchronized.*Task " zgbas-admin/src/main/java/com/spt/bas/server/api/MQApi.java = 0（D-P6-11 改造后不再 @Autowired Synchronized*Task handler）；⑤grep -c "com.spt.bas.server.service.I" zgbas-admin/src/main/java/com/spt/bas/server/api/MQApi.java ≥ 1（@Autowired service iface）；⑥grep "^\[ERROR\]" /tmp/p6-04-t2.log | wc -l = 0（全 reactor 编译门 GREEN，关闭 QUARTZ-03 编译维度）</automated>
  </verify>
  <done>
    - MQApi.java 在 zgbas-admin/.../api/ 就位（保包名 com.spt.bas.server.api，D-P2-07）
    - @RestController + @RequestMapping("/mq/api") + 所有 @GetMapping 路径保留
    - 零 @Autowired Synchronized*Task；@Autowired 改为 service iface
    - 方法体内 handler 调用改 service.method 直调
    - 1:N case service iface + impl 加组合方法（保包名 + 照搬原 Synchronized*Task 方法体业务编排）
    - mvn -pl zgbas-admin -am compile 全 reactor 零 ERROR（QUARTZ-03 编译维度关闭：60 handler 全迁 + MQApi 改造完成）
  </done>
</task>

<task type="auto">
  <name>Task 3: 合并编译门 + 06-04 SUMMARY（command executors 清单 + executeCommand 拆分预提取）</name>
  <files>
    .planning/phases/06-quartz-migration/06-04-SUMMARY.md
  </files>
  <read_first>
    - /Users/alan/WorkSpace/IDEA/zgbas/basCore/basServer/src/main/java/com/spt/bas/server/command/BasCommandExecutor.java 行 130-381 （抽取 ~50 个子命令分支名 — updateBudgetSettlement / reContract / 等，为 06-05 D-P6-02 sys_job 拆分预提取）
    - /Users/alan/WorkSpace/IDEA/zgbas/basReport/reportServer/src/main/java/com/spt/bas/report/server/command/ReportCommandExecutor.java （2 子命令：cache + pushWeChatWorkLeaderboard）
    - /Users/alan/WorkSpace/IDEA/zgbas/web/src/main/java/com/spt/bas/web/config/BasWebCommand.java （3 子命令：clean + cache + fundSocket）
    - 06-04 Task 1+2 编译输出（/tmp/p6-04-t1.log + /tmp/p6-04-t2.log）
  </read_first>
  <action>
    【A. 全 reactor 编译门】运行 mvn -pl zgbas-admin -am compile（含全部 5 模块：common/framework/system/quartz/admin），断言 grep ^\[ERROR\]=0（关闭 QUARTZ-03 编译维度 — 60 handler 全迁 + MQApi 改造完成 + xxl-job 痕迹清零的全 reactor 终态断言）。
    
    【B. SUMMARY 内容】.planning/phases/06-quartz-migration/06-04-SUMMARY.md 记录：
      1. 3 个 command executor 落位 + bean 名 + 原始 @XxlJob value（"executeCommand" 共用）
      2. **executeCommand 子命令完整清单**（为 06-05 D-P6-02 sys_job 拆分准备）：
         - BasCommandExecutor: ~50 个子命令分支（updateBudgetSettlement / reContract / 等从源方法体 commandline.startsWith("xxx") / .indexOf("xxx") 抽取）
         - ReportCommandExecutor: 2 子命令（cache + pushWeChatWorkLeaderboard）
         - BasWebCommand: 3 子命令（clean + cache + fundSocket）
         - PurchaseCommand 不迁（v2，D-P6-08）
      3. 每个 command executor 的 sys_job 行数预算（Pitfall 6：~55 行；executeCommand 按 bean 名 + commandline args 拆，每子命令一行 invoke_target）
      4. MQApi D-P6-11 改造结果：每个 HTTP 端点的"原 Synchronized*Task → 直调 service.method"映射 + 1:1/1:N 决策 + 1:N 时新加的 service 组合方法签名
      5. 06-04 与 06-05 D-P6-02 的接口（sys_job INSERT 翻译输入已就位）
    
    【C. 全 reactor xxl-job 痕迹最终核】grep -rl "com\.xxl\.job\|XxlJobHelper\|@XxlJob\|XxlJobSpringExecutor" 单体（排除 .planning/.claude/target）= 0（INFRA-03 + QUARTZ-03 双重关闭断言）。
  </action>
  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 1.8) mvn -pl zgbas-admin -am compile -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml 2>&1 | tee /tmp/p6-04-t3.log | tail -5; 断言：①grep "^\[ERROR\]" /tmp/p6-04-t3.log | wc -l = 0；②grep -rl "com\.xxl\.job\|XxlJobHelper\|@XxlJob\|XxlJobSpringExecutor" /Users/alan/WorkSpace/IDEA/zgbas-plus 2>/dev/null --include="*.java" --include="*.yml" --include="*.properties" --include="*.xml" | grep -v "/target/" | grep -v "/.planning/" | grep -v "/.claude/" | wc -l = 0；③test -f .planning/phases/06-quartz-migration/06-04-SUMMARY.md；④grep -c "executeCommand\|basCommandExecutor\|reportCommandExecutor\|basWebCommand" .planning/phases/06-quartz-migration/06-04-SUMMARY.md ≥ 4</automated>
  </verify>
  <done>
    - 全 reactor mvn -pl zgbas-admin -am compile 零 ERROR（QUARTZ-03 编译维度关闭）
    - 全单体 grep xxl-job 残留 = 0
    - 06-04-SUMMARY.md 存在，含 3 command executor + executeCommand 子命令完整清单 + MQApi D-P6-11 改造映射
    - SUMMARY 为 06-05 D-P6-02 sys_job 翻译提供完整输入（~55 executeCommand 行 + 28 task/Synchronized* handler 行 + 1 RyTask demo 行 + 60-65 总行预算）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scheduler → command executor reflection | sys_job.invoke_target = "basCommandExecutor.executeCommand('xxx')" 反射调；commandline 字符串经方法 arg 传入，方法体内 ~50 子命令分支路由 |
| HTTP → MQApi → service | 前端 HTTP 调 MQApi 端点（同步），MQApi 内部直调 service（不经 handler，不经 Scheduler）；与 sys_job cron 入口并存（双入口同 service）|
| commandline 字符串注入风险 | executeCommand(String commandline) 的 commandline 来自 sys_job.invoke_target 字符串 args，恶意 sys_job 数据可注入任意子命令字符串 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Elevation of Privilege | executeCommand(String commandline) 反射入口 + ~50 子命令分支 | mitigate | commandline 来自 sys_job.invoke_target（admin 已认证 + Constants.JOB_WHITELIST_STR={"com.spt"} + RuoYi invokeTarget 黑名单）；子命令分支是固定 if-else 路由（非任意 method 反射），commandline 仅作业务参数非方法选择器；admin 写 sys_job 需经 Shiro 认证 |
| T-06-04-02 | Tampering | D-P6-11 双入口并发（sys_job cron + MQApi HTTP 同一业务方法）| accept | 源 xxl-job 同样有双入口；service 层 @Transactional 边界已就位；D-P6-11 决策保留双入口语义保行为等价 |
| T-06-04-03 | Elevation of Privilege | MQApi 剥 @PreAuthorize 后 admin endpoint 权限粒度 | accept | 同 T-06-01-02；ShiroFilter 已认证才可达；monitor:* 粒度丢失接受 |
| T-06-04-04 | Tampering | ICommand 接口跨模块（zgbas-system ICommand + zgbas-quartz BasCommandExecutor implements）| mitigate | Task action A 前置核实 ICommand 落位（zgbas-system 保包名优先，避免循环依赖 quartz→system→quartz）；记录到 SUMMARY |
| T-06-04-05 | Repudiation | 1:N service 组合方法异常静默吞 | mitigate | service impl 内 try-catch 不静默；@Transactional rollback + 抛出上层；AbstractQuartzJob 经 sys_job 入口时写 sys_job_log.status=FAIL；MQApi 入口由 admin BasicErrorController 统一异常处理 |
| T-06-04-SC | Tampering | Maven + 源代码 | mitigate | 本地源副本 + research §Sources 一手核；无新增 maven 包 |
</threat_model>

<verification>
- 3 个 command executor 落 com.spt.quartz.task + @Component bean 名 + 零 xxl-job
- executeCommand(String commandline) 方法签名 + 子命令分支业务逻辑保留
- MQApi.java 在 zgbas-admin/.../api/ 保包名 + HTTP 端点契约保留 + 内部直调 service iface
- 1:N service 组合方法已加到 iface + impl（保包名 D-P2-07）
- mvn -pl zgbas-admin -am compile 全 reactor 零 ERROR（QUARTZ-03 编译维度关闭）
- 全单体 grep xxl-job = 0
- 06-04-SUMMARY.md 含 executeCommand 子命令完整清单（06-05 D-P6-02 翻译输入）
</verification>

<success_criteria>
- [ ] QUARTZ-03（60 handler 全迁：06-02 task/ 20 + 06-03 rocketmq/task/ 8 + 06-04 command executors 3 = 31 类含 ~60 个 @XxlJob 方法）— 编译维度关闭
- [ ] QUARTZ-04（command executor 落位 + sys_job 手动触发基础设施就位 — 真实 sys_job 数据在 06-05）
- [ ] D-P6-07/D-P6-09（command executors 落 zgbas-quartz com.spt.quartz.task）— 落地
- [ ] D-P6-11（MQApi HTTP 端点保留 + 内部直调 service iface）— 落地
- [ ] Pitfall 6（executeCommand 3 同名 handler）— Bean 名拆分落地；sys_job 拆 ~55 行在 06-05
- [ ] Pitfall 9（BasCommandExecutor @Autowired 4 task 类 import）— 落地（改 com.spt.quartz.task.*）
</success_criteria>

<output>
Create `.planning/phases/06-quartz-migration/06-04-SUMMARY.md` when done
</output>
