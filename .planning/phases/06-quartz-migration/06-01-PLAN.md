---
phase: 06-quartz-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - zgbas-quartz/pom.xml
  - zgbas-quartz/src/main/java/com/spt/quartz/config/QuartzScheduleConfig.java
  - zgbas-quartz/src/main/java/com/spt/quartz/controller/SysJobController.java
  - zgbas-quartz/src/main/java/com/spt/quartz/controller/SysJobLogController.java
  - zgbas-quartz/src/main/java/com/spt/quartz/domain/SysJob.java
  - zgbas-quartz/src/main/java/com/spt/quartz/domain/SysJobLog.java
  - zgbas-quartz/src/main/java/com/spt/quartz/mapper/SysJobMapper.java
  - zgbas-quartz/src/main/java/com/spt/quartz/mapper/SysJobLogMapper.java
  - zgbas-quartz/src/main/java/com/spt/quartz/service/ISysJobService.java
  - zgbas-quartz/src/main/java/com/spt/quartz/service/ISysJobLogService.java
  - zgbas-quartz/src/main/java/com/spt/quartz/service/impl/SysJobServiceImpl.java
  - zgbas-quartz/src/main/java/com/spt/quartz/service/impl/SysJobLogServiceImpl.java
  - zgbas-quartz/src/main/java/com/spt/quartz/task/RyTask.java
  - zgbas-quartz/src/main/java/com/spt/quartz/util/AbstractQuartzJob.java
  - zgbas-quartz/src/main/java/com/spt/quartz/util/CronUtils.java
  - zgbas-quartz/src/main/java/com/spt/quartz/util/JobInvokeUtil.java
  - zgbas-quartz/src/main/java/com/spt/quartz/util/QuartzDisallowConcurrentExecution.java
  - zgbas-quartz/src/main/java/com/spt/quartz/util/QuartzJobExecution.java
  - zgbas-quartz/src/main/java/com/spt/quartz/util/ScheduleUtils.java
  - zgbas-quartz/src/main/resources/mybatis/mappers/SysJobMapper.xml
  - zgbas-quartz/src/main/resources/mybatis/mappers/SysJobLogMapper.xml
  - zgbas-quartz/src/main/resources/sql/quartz.sql
  - zgbas-quartz/src/main/resources/sql/sys_job.sql
  - zgbas-framework/src/main/java/com/spt/framework/config/ZgbasMybatisConfig.java
  - zgbas-system/pom.xml
  - zgbas-system/src/main/java/com/spt/bas/server/service/impl/CtrContractProfitServiceImpl.java
  - zgbas-admin/src/main/resources/templates/monitor/job/job.html
  - zgbas-admin/src/main/resources/templates/monitor/job/add.html
  - zgbas-admin/src/main/resources/templates/monitor/job/edit.html
  - .planning/phases/06-quartz-migration/06-01-MENU-INSERT.sql
autonomous: false
requirements: [QUARTZ-01, QUARTZ-02, QUARTZ-04, INFRA-03]

must_haves:
  truths:
    # Outcome-level truths (operator/admin-observable) — W3 rebalance
    - "zgbas-quartz 模块自洽编译通过 + Spring context 启动期可解析 Scheduler bean（为 D-P6-06 fail-fast 提供前置）"
    - "admin/monitor/job 可视化管理台可登录后访问：列表/新增/编辑/启停/手动触发/查日志（D-P6-10 LOCKED intent）"
    - "admin/monitor/job 写接口（add/edit/changeStatus/run/remove）仅 monitor:job:* 权限用户可访问（@RequiresPermissions Shiro 注解补偿剥 @PreAuthorize 后的粒度丢失）"
    - "xxl-job 痕迹在单体全模块清零（含 pom/yml/properties/xml/java）— INFRA-03 关闭"
    - "Quartz invoke_target 反射仅命中 com.spt.* bean（白名单 + RuoYi 黑名单双控）"
    - "sys_job/sys_job_log/11 张 QRTZ_* DDL 文件可由运维 apply 到目标库（QUARTZ-02 文件维度关闭）"
    # Compile-gate evidence truths (kept as implementation-level signals)
    - "Constants.JOB_WHITELIST_STR = {\"com.spt\"}（覆盖所有 com.spt.quartz.task.* bean）"
    - "SysJobController/SysJobLogController 零 @PreAuthorize 注解 + 写方法均带 @RequiresPermissions(\"monitor:job:*\")"
    - "ZgbasMybatisConfig @MapperScan basePackages 含 com.spt.quartz.mapper（mapper 扫描可命中）"
  artifacts:
    - path: "zgbas-quartz/src/main/java/com/spt/quartz/config/QuartzScheduleConfig.java"
      provides: "RuoYi quartz SchedulerFactoryBean（@Primary DataSource + quartz properties）"
      contains: "SchedulerFactoryBean"
    - path: "zgbas-quartz/src/main/java/com/spt/common/constant/Constants.java"
      provides: "JOB_WHITELIST_STR = {\"com.spt\"}"
      contains: "com.spt"
    - path: "zgbas-quartz/src/main/resources/sql/sys_job.sql"
      provides: "sys_job + sys_job_log DDL（不含 QRTZ_*）"
      contains: "create table sys_job"
    - path: "zgbas-quartz/src/main/resources/sql/quartz.sql"
      provides: "11 张 QRTZ_* 表 DDL（照搬 spt-auth）"
      contains: "QRTZ_"
    - path: "zgbas-admin/src/main/resources/templates/monitor/job/job.html"
      provides: "定时任务可视化管理台列表页（D-P6-10：增删改查/启停/手动触发/传参/查日志）"
      contains: "monitor/job"
    - path: "zgbas-admin/src/main/resources/templates/monitor/job/add.html"
      provides: "新增任务表单页（含 invoke_target 字段，Thymeleaf 转义防 XSS）"
      contains: "invoke_target"
  key_links:
    - from: "zgbas-framework/src/main/java/com/spt/framework/config/ZgbasMybatisConfig.java"
      to: "com.spt.quartz.mapper.SysJobMapper / SysJobLogMapper"
      via: "@MapperScan basePackages 加 com.spt.quartz.mapper"
      pattern: "com\\.spt\\.quartz\\.mapper"
    - from: "zgbas-quartz/.../util/ScheduleUtils.whiteList()"
      to: "com.spt.quartz.task.* bean invokeTarget"
      via: "Constants.JOB_WHITELIST_STR containsAnyIgnoreCase"
      pattern: "\\{\"com\\.spt\"\\}"
    - from: "zgbas-admin/.../templates/monitor/job/job.html"
      to: "zgbas-quartz/.../controller/SysJobController @RequestMapping(/monitor/job)"
      via: "Thymeleaf 表单 action + AJAX GET /monitor/job/list|/run|/changeStatus"
      pattern: "th:action.*monitor/job|@GetMapping.*\"/list\""
---

<objective>
Phase 6 Wave 1 — Foundation：把 spt-auth/auth-quartz 整模块（18 类 + 2 XML）迁入 zgbas-quartz，处理 5 个研究标定的"非预期阻塞"（ScheduleConfig 整类注释 / 17 个 com.spt.common.* 缺失 / Constants 白名单硬编码 com.ruoyi / @PreAuthorize 与 Shiro 冲突 / DDL 实际在 ry_20210908.sql 非 quartz.sql），完成 INFRA-03 删除 xxl-job-core + 翻译 CtrContractProfitServiceImpl 的 XxlJobHelper.log，落地 D-P6-10 LOCKED 可视化运维台（/monitor/job Thymeleaf 页面 + 外部 spt-auth sys_menu 菜单接入），让 zgbas-quartz 自洽编译通过、Scheduler bean 可解析、xxl-job 痕迹清零、admin 可视化任务管理台就位。

Purpose: 为 Wave 2/3 的 60 个 handler 迁移和 Wave 4 的 sys_job 数据装载提供自洽的 RuoYi quartz 基础设施 + 可视化运维入口。
Output: zgbas-quartz 模块从空骨架（仅 PackageMarker）填充为 18 类 + 2 XML + 2 SQL 的完整 RuoYi quartz 基础设施；单体 reactor 编译零 ERROR，xxl-job-core 完全移除；/monitor/job 可视化管理台 3 个 Thymeleaf 页面就位；外部 spt-auth sys_menu 菜单接入 SQL 落 .planning 待用户/运维 apply。

**Scope note (W4 — plan-checker acknowledged optional):** 本计划共 ~45 文件，超出 2-3 task / ~42 file 经验阈值。但绝大多数是**逐字照搬 spt-auth/auth-quartz 副本**（低认知负荷，无决策点）— Task 1 的 18 RuoYi 类 + 2 XML + 2 SQL + Task 2 的 15 common 类，本质是 cp 操作 + 少量 import/package 改写。Phase 4 04-03 已立 bulk-copy 先例（合并编译单元吸 PM88）。拆分为 06-01a/b 增加重编号 churn 且 common 子集无独立 compile 价值（必须与 RuoYi 类同批编译），故保持单 plan。Task 4 (Thymeleaf) 是唯一新增创意工作（~3 文件），独立成 task 控制负荷。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-quartz-migration/06-CONTEXT.md
@.planning/phases/06-quartz-migration/06-RESEARCH.md

# 关键上下文摘要（详见 06-RESEARCH.md §Common Pitfalls 1-4 + §Architecture Patterns 4-5 + §Open Questions Q1/Q3 RESOLVED）
- 5 个非预期阻塞已研究标定，本计划按 Pitfall 1-4 + INFRA-03 分头处置
- **D-P6-10 LOCKED（option a 用户 2026-07-18 决议保可视化 UI）**：Pitfall 5 不再走"降级 API-only"，本计划 Task 4 建 /monitor/job Thymeleaf 页面 + Task 5 外部 spt-auth sys_menu INSERT checkpoint:human-blocked
- zgbas-quartz 模块骨架已就位（pom.xml 已声明 quartz→common/framework/system 单向依赖，src 仅有 PackageMarker.java 占位）
- ZgbasApplication @ComponentScan("com.spt") 已覆盖 com.spt.quartz.*，无需改 boot 类
- xxl-job 残留仅 2 处：zgbas-system/pom.xml:93-135 + CtrContractProfitServiceImpl.java:16,172,182
- zgbas-admin/src/main/resources/templates/monitor/operlog/{operlog,detail}.html 已就位 — Task 4 Thymeleaf 页面参照此 shell（admin 布局 + JS helpers + layout dialect），不整模块照搬 RuoYi 模板
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port auth-quartz 18 类 + 2 XML + 2 SQL + 5 阻塞适配（QuartzScheduleConfig / 白名单 / 剥 @PreAuthorize 补 @RequiresPermissions / pom 依赖）</name>
  <files>
    zgbas-quartz/pom.xml
    zgbas-quartz/src/main/java/com/spt/quartz/config/QuartzScheduleConfig.java
    zgbas-quartz/src/main/java/com/spt/quartz/controller/SysJobController.java
    zgbas-quartz/src/main/java/com/spt/quartz/controller/SysJobLogController.java
    zgbas-quartz/src/main/java/com/spt/quartz/domain/SysJob.java
    zgbas-quartz/src/main/java/com/spt/quartz/domain/SysJobLog.java
    zgbas-quartz/src/main/java/com/spt/quartz/mapper/SysJobMapper.java
    zgbas-quartz/src/main/java/com/spt/quartz/mapper/SysJobLogMapper.java
    zgbas-quartz/src/main/java/com/spt/quartz/service/ISysJobService.java
    zgbas-quartz/src/main/java/com/spt/quartz/service/ISysJobLogService.java
    zgbas-quartz/src/main/java/com/spt/quartz/service/impl/SysJobServiceImpl.java
    zgbas-quartz/src/main/java/com/spt/quartz/service/impl/SysJobLogServiceImpl.java
    zgbas-quartz/src/main/java/com/spt/quartz/task/RyTask.java
    zgbas-quartz/src/main/java/com/spt/quartz/util/AbstractQuartzJob.java
    zgbas-quartz/src/main/java/com/spt/quartz/util/CronUtils.java
    zgbas-quartz/src/main/java/com/spt/quartz/util/JobInvokeUtil.java
    zgbas-quartz/src/main/java/com/spt/quartz/util/QuartzDisallowConcurrentExecution.java
    zgbas-quartz/src/main/java/com/spt/quartz/util/QuartzJobExecution.java
    zgbas-quartz/src/main/java/com/spt/quartz/util/ScheduleUtils.java
    zgbas-quartz/src/main/resources/mybatis/mappers/SysJobMapper.xml
    zgbas-quartz/src/main/resources/mybatis/mappers/SysJobLogMapper.xml
    zgbas-quartz/src/main/resources/sql/quartz.sql
    zgbas-quartz/src/main/resources/sql/sys_job.sql
  </files>
  <read_first>
    - /Users/alan/WorkSpace/IDEA/spt-auth/auth-quartz/src/main/java/com/spt/quartz/config/ScheduleConfig.java （Pitfall 1：58 行整类被 // 注释，uncomment 全文作 QuartzScheduleConfig 起点）
    - /Users/alan/WorkSpace/IDEA/spt-auth/auth-quartz/src/main/java/com/spt/quartz/controller/SysJobController.java （Pitfall 4：~9 处 @PreAuthorize("@ss.hasPermi('monitor:job:*')") 待剥 + 补 Shiro @RequiresPermissions）
    - /Users/alan/WorkSpace/IDEA/spt-auth/auth-quartz/src/main/java/com/spt/quartz/controller/SysJobLogController.java （Pitfall 4：~5 处 @PreAuthorize 待剥 + 补 Shiro @RequiresPermissions）
    - /Users/alan/WorkSpace/IDEA/spt-auth/auth-quartz/src/main/java/com/spt/quartz/util/ScheduleUtils.java （定位 whiteList + 含AnyIgnoreCase 调用点，验证 Constants 引用）
    - /Users/alan/WorkSpace/IDEA/spt-auth/auth-quartz/src/main/java/com/spt/quartz/service/impl/SysJobServiceImpl.java （@PostConstruct init() — D-P6-06 fail-fast 入口）
    - /Users/alan/WorkSpace/IDEA/spt-auth/auth-quartz/pom.xml （照搬 quartz + spring-context-support 依赖声明的 excluding c3p0 写法）
    - /Users/alan/WorkSpace/IDEA/spt-auth/auth-quartz/src/main/resources/mapper/quartz/SysJobMapper.xml （XML 路径源，目标改 mybatis/mappers/）
    - /Users/alan/WorkSpace/IDEA/spt-auth/auth-quartz/src/main/resources/mapper/quartz/SysJobLogMapper.xml
    - /Users/alan/WorkSpace/IDEA/spt-auth/sql/quartz.sql （11 张 QRTZ_* 表 DDL，照搬）
    - /Users/alan/WorkSpace/IDEA/spt-auth/sql/ry_20210908.sql 行 567-606 （Pitfall 4 DDL 实际位置：sys_job + sys_job_log 表定义，抽出到独立 sys_job.sql）
    - /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-quartz/pom.xml （当前骨架，已有 quartz→common/framework/system 依赖）
    - /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-admin/src/main/java/com/spt/bas/server/controller/ 或同层 SysOperlogController.java （核 Shiro @RequiresPermissions 注解的既有使用模式 + import 路径，确保本计划加的注解风格一致）
    - .planning/phases/06-quartz-migration/06-RESEARCH.md §Common Pitfalls 1-4 + §Architecture Patterns 1/4/5 + §Standard Stack + §Security Domain V4 (Pitfall 4 — @RequiresPermissions 补偿控制已记录)
  </read_first>
  <action>
    按 RuoYi auth-quartz 整模块复制 + 5 阻塞适配：

    【A. pom.xml 增依赖】在 zgbas-quartz/pom.xml 加 quartz + spring-context-support（Spring Boot 2.5.9 BOM 托管，无需显式 version）：
      - org.quartz-scheduler:quartz（exclusion: com.mchange:c3p0，与 Druid 冲突，spt-auth/auth-quartz/pom.xml 同 exclude 写法照搬）
      - org.springframework:spring-context-support
    保留既有 zgbas-common/framework/system 依赖不动（D-P6-07 quartz→system 单向）。

    【B. 整模块 18 类照搬】从 spt-auth/auth-quartz/src/main/java/com/spt/quartz/ 复制到 zgbas-quartz 同包路径，照搬清单（保包名 com.spt.quartz，D-P2-07）：
      - config/ScheduleConfig.java（见下方 C 改造）
      - controller/{SysJobController, SysJobLogController}.java（见下方 D 改造）
      - domain/{SysJob, SysJobLog}.java
      - mapper/{SysJobMapper, SysJobLogMapper}.java（见 Task 2 加 @MyBatisDao）
      - service/ISysJobService.java / ISysJobLogService.java + impl/{SysJobServiceImpl, SysJobLogServiceImpl}.java
      - task/RyTask.java（保留作 sys_job 数据 demo）
      - util/{AbstractQuartzJob, CronUtils, JobInvokeUtil, QuartzDisallowConcurrentExecution, QuartzJobExecution, ScheduleUtils}.java

    【C. Pitfall 1 处置 — ScheduleConfig 整类注释】源文件 58 行整类被 // 注释。处置：
      1. 把源 ScheduleConfig.java 全文 uncomment（去掉每行行首 //）
      2. 改类名 ScheduleConfig → QuartzScheduleConfig（Pattern 5：避免与 zgbas-system 既有 com.spt.bas.server.config.ScheduleConfig 默认 bean 名 scheduleConfig 冲突；保包名 com.spt.quartz.config 不变，仅类名偏 1 字符量级为 QuartzScheduleConfig）
      3. 源首行注释 //package com.ruoyi.quartz.config; 改为 package com.spt.quartz.config;（其余 import 不变：org.springframework.context.annotation.Bean/Configuration + org.springframework.scheduling.quartz.SchedulerFactoryBean + javax.sql.DataSource + java.util.Properties）
      4. 保留 @Bean schedulerFactoryBean(DataSource dataSource) 方法体原样（含 Properties quartzProps：org.quartz.scheduler.instanceName=AutoScheduler / instanceId=AUTO / threadPool.class=SimpleThreadPool / threadCount=20 / threadPriority=5 / jobStore.class=RAMJobStore 或 DataSource JobStore — 照源文件，spt-auth 默认走 org.quartz.jobStore.class=org.quartz.simpl.RAMJobStore；本计划照搬 spt-auth 实现，不擅自改 JobStore 类型）
      落地：zgbas-quartz/src/main/java/com/spt/quartz/config/QuartzScheduleConfig.java

    【D. Pitfall 4 处置 — 剥 @PreAuthorize 补 @RequiresPermissions（W5 补偿控制）】SysJobController + SysJobLogController 共 ~14 处 @PreAuthorize("@ss.hasPermi('monitor:job:*')") 全部删注解 + 删 import org.springframework.security.access.prepost.PreAuthorize（单体用 Shiro，Phase 3 链路接管认证；spring-security 不在 classpath，注解静默失效或编译错）。**同步补 Shiro @RequiresPermissions 注解**（research §Security Domain V4 Pitfall 4 已标定的补偿控制；ASVS L1 V4.1.1 要求方法级权限粒度，剥除 @PreAuthorize 后写接口暴露给任意已认证用户，覆盖 monitor:job:* 权限串）：
      - import org.apache.shiro.authz.annotation.RequiresPermissions;（确认 Phase 3 已有此依赖，若 zgbas-quartz 需显式声明则加到 zgbas-quartz/pom.xml，通常 Shiro auth jar 已被 admin/framework 间接传递）
      - SysJobController 写方法补：
        - list(...) → @RequiresPermissions("monitor:job:list")
        - add/save(...) → @RequiresPermissions("monitor:job:add")
        - edit/update(...) → @RequiresPermissions("monitor:job:edit")
        - remove(...) → @RequiresPermissions("monitor:job:remove")
        - changeStatus(...) → @RequiresPermissions("monitor:job:changeStatus")
        - run(...) → @RequiresPermissions("monitor:job:run")
      - SysJobLogController：
        - list(...) → @RequiresPermissions("monitor:job:list")
        - detail/remove/clean(...) → @RequiresPermissions("monitor:job:remove") / @RequiresPermissions("monitor:job:clean")
      - **注解串与 spt-auth Shiro 既有权限命名约定对齐**（参照 zgbas-admin 既有 SysOperlogController / SysLogininforController 的 @RequiresPermissions 使用模式，保持权限串格式一致：monitor:<resource>:<action>）
      此变更使剥除 @PreAuthorize 后的权限粒度不丢失（threat_model T-06-01-02 disposition 从 accept 升级为 mitigated）。

    【E. SQL DDL 落资源目录】在 zgbas-quartz/src/main/resources/sql/ 建：
      - quartz.sql：照搬 spt-auth/sql/quartz.sql 全文（11 张 QRTZ_* 表）
      - sys_job.sql：从 spt-auth/sql/ry_20210908.sql 行 567-606 抽出 sys_job + sys_job_log 表 DDL + 3 行 RyTask demo INSERT（ryTask.ryNoParams / ryParams / ryMultipleParams，cronExpression 0/10、0/15、0/20 * * * * ?）— 这是 Pitfall 4 DDL 实际位置纠正
      两文件后续由用户/运维手工 apply 到 sptbasdb_pd（D-P2-02 ddl-auto=none 不自动建表）；本计划只落文件不执行 DDL。

    【F. XML 路径对齐】Pitfall 7：把 spt-auth/auth-quartz/src/main/resources/mapper/quartz/{SysJobMapper, SysJobLogMapper}.xml 复制到 zgbas-quartz/src/main/resources/mybatis/mappers/（非 mapper/quartz/）— 对齐 mybatis-plus.mapper-locations: classpath:/mybatis/mappers/*Mapper.xml 通配。namespace 保留 com.spt.quartz.mapper.SysJobMapper / SysJobLogMapper。

    【G. 不做的事】本 Task 不做：①本地化 17 个 com.spt.common.* 类（Task 2）；②改 @MapperScan（Task 2）；③剥 xxl-job-core（Task 3）；④任何 handler 迁移（Wave 2/3）；⑤建 Thymeleaf 模板（Task 4）；⑥外部 spt-auth sys_menu INSERT（Task 5 checkpoint:human-blocked）。
  </action>
  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 1.8) mvn -pl zgbas-quartz -am compile -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml 2>&1 | tee /tmp/p6-01-t1.log | grep -E "^\[ERROR\]|^\[INFO\] BUILD" | head -30; 测试断言：①grep "^\[ERROR\]" /tmp/p6-01-t1.log | grep -v "^$" | wc -l 表面计数（注意可能因 Task 2 未做而 fail com.spt.common.* import — 此 Task 单独编译预期 FAIL on com.spt.common.* 缺失，由 Task 2 修复后整体转绿，符合 Phase 4 04-03 "合并编译单元" pattern）；②grep -c "QuartzScheduleConfig" zgbas-quartz/src/main/java/com/spt/quartz/config/QuartzScheduleConfig.java ≥ 1；③grep -c "@PreAuthorize" zgbas-quartz/src/main/java/com/spt/quartz/controller/SysJobController.java = 0；④grep -c "@PreAuthorize" zgbas-quartz/src/main/java/com/spt/quartz/controller/SysJobLogController.java = 0；⑤grep -E "quartz-scheduler|spring-context-support" zgbas-quartz/pom.xml | wc -l = 2；⑥grep -c "@RequiresPermissions" zgbas-quartz/src/main/java/com/spt/quartz/controller/SysJobController.java ≥ 5（W5 补偿控制：list/add/edit/remove/run 等写方法至少 5 处带 Shiro 注解）；⑦grep -c "@RequiresPermissions" zgbas-quartz/src/main/java/com/spt/quartz/controller/SysJobLogController.java ≥ 1</automated>
  </verify>
  <done>
    - 文件存在：zgbas-quartz 下 18 个 .java（含 QuartzScheduleConfig 非 ScheduleConfig 命名）+ mybatis/mappers/ 下 2 XML + sql/ 下 2 SQL
    - grep "@PreAuthorize" 两个 controller 文件 0 命中
    - grep "@RequiresPermissions" SysJobController ≥ 5 处 / SysJobLogController ≥ 1 处（W5 补偿控制就位）
    - grep "QuartzScheduleConfig"（新类名）出现 ≥ 1；grep "ScheduleConfig"（老 RuoYi 类名）QuartzScheduleConfig 之外 0 命中
    - zgbas-quartz/pom.xml 含 quartz-scheduler + spring-context-support 两条依赖
    - sys_job.sql 含 "create table sys_job" + "create table sys_job_log"；quartz.sql 含 "QRTZ_"
    - 编译预期 partial failure（com.spt.common.* 缺失，由 Task 2 收口），记录失败 grep 输出供 Task 2 验证
  </done>
</task>

<task type="auto">
  <name>Task 2: 本地化 17 个 com.spt.common.* 子集 + 加 @MyBatisDao + 放宽 @MapperScan + 改 Constants 白名单</name>
  <files>
    zgbas-quartz/src/main/java/com/spt/common/constant/Constants.java
    zgbas-quartz/src/main/java/com/spt/common/constant/ScheduleConstants.java
    zgbas-quartz/src/main/java/com/spt/common/exception/job/TaskException.java
    zgbas-quartz/src/main/java/com/spt/common/core/controller/BaseController.java
    zgbas-quartz/src/main/java/com/spt/common/core/domain/AjaxResult.java
    zgbas-quartz/src/main/java/com/spt/common/core/domain/BaseEntity.java
    zgbas-quartz/src/main/java/com/spt/common/core/page/TableDataInfo.java
    zgbas-quartz/src/main/java/com/spt/common/enums/BusinessType.java
    zgbas-quartz/src/main/java/com/spt/common/annotation/Excel.java
    zgbas-quartz/src/main/java/com/spt/common/annotation/Log.java
    zgbas-quartz/src/main/java/com/spt/common/utils/StringUtils.java
    zgbas-quartz/src/main/java/com/spt/common/utils/ExceptionUtil.java
    zgbas-quartz/src/main/java/com/spt/common/utils/bean/BeanUtils.java
    zgbas-quartz/src/main/java/com/spt/common/utils/spring/SpringUtils.java
    zgbas-quartz/src/main/java/com/spt/common/utils/poi/ExcelUtil.java
    zgbas-quartz/src/main/java/com/spt/quartz/mapper/SysJobMapper.java
    zgbas-quartz/src/main/java/com/spt/quartz/mapper/SysJobLogMapper.java
    zgbas-framework/src/main/java/com/spt/framework/config/ZgbasMybatisConfig.java
  </files>
  <read_first>
    - /Users/alan/WorkSpace/IDEA/spt-auth/auth-common/src/main/java/com/spt/common/constant/Constants.java （Pitfall 2/3：JOB_WHITELIST_STR = {"com.ruoyi"} 硬编码源，照搬后改 com.spt）
    - /Users/alan/WorkSpace/IDEA/spt-auth/auth-common/src/main/java/com/spt/common/constant/ScheduleConstants.java （照搬：TASK_PROPERTIES / TASK_CLASS_NAME / MISFIRE_* / Status 枚举）
    - /Users/alan/WorkSpace/IDEA/spt-auth/auth-common/src/main/java/com/spt/common/exception/job/TaskException.java
    - /Users/alan/WorkSpace/IDEA/spt-auth/auth-common/src/main/java/com/spt/common/core/controller/BaseController.java （仅照搬 SysJobController/SysJobLogController 实际用到的方法：startPage / getDataTable / toAjax / error / getUsername 等；多余方法可剥）
    - /Users/alan/WorkSpace/IDEA/spt-auth/auth-common/src/main/java/com/spt/common/core/domain/{AjaxResult,BaseEntity}.java
    - /Users/alan/WorkSpace/IDEA/spt-auth/auth-common/src/main/java/com/spt/common/core/page/TableDataInfo.java
    - /Users/alan/WorkSpace/IDEA/spt-auth/auth-common/src/main/java/com/spt/common/enums/BusinessType.java
    - /Users/alan/WorkSpace/IDEA/spt-auth/auth-common/src/main/java/com/spt/common/annotation/{Excel,Log}.java
    - /Users/alan/WorkSpace/IDEA/spt-auth/auth-common/src/main/java/com/spt/common/utils/StringUtils.java
    - /Users/alan/WorkSpace/IDEA/spt-auth/auth-common/src/main/java/com/spt/common/utils/ExceptionUtil.java
    - /Users/alan/WorkSpace/IDEA/spt-auth/auth-common/src/main/java/com/spt/common/utils/bean/BeanUtils.java （注意：RuoYi BeanUtils 含 copyBeanProp 自定义方法，被 AbstractQuartzJob 用；不可替换为 Spring BeanUtils）
    - /Users/alan/WorkSpace/IDEA/spt-auth/auth-common/src/main/java/com/spt/common/utils/spring/SpringUtils.java （ApplicationContextAware 实现，JobInvokeUtil 通过 SpringUtils.getBean(beanName) 反射，必须本地化）
    - /Users/alan/WorkSpace/IDEA/spt-auth/auth-common/src/main/java/com/spt/common/utils/poi/ExcelUtil.java （export 用，可后期剥；本计划先照搬最小化 SysJobController.export / SysJobLogController.export 的依赖链）
    - /Users/alan/WorkSpace/IDEA/spt-auth/auth-quartz/src/main/java/com/spt/quartz/util/AbstractQuartzJob.java （定位 BeanUtils.copyBeanProp 调用点 + SpringUtils 使用点）
    - /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-framework/src/main/java/com/spt/framework/config/ZgbasMybatisConfig.java （当前 @MapperScan basePackages 数组 + annotationClass=MyBatisDao.class 过滤条件）
    - .planning/phases/06-quartz-migration/06-RESEARCH.md §Architecture Patterns 4（双 ORM 单 DataSource mapper 复用）+ §Common Pitfalls 2/3/7
  </read_first>
  <action>
    【A. 本地化 15 类到 zgbas-quartz/src/main/java/com/spt/common/】（Pitfall 2：单体无 auth-common，必须本地化）按源 spt-auth/auth-common/ 对应路径照搬以下 15 类（D-P2-07 保包名 com.spt.common.*，落 zgbas-quartz 模块；quartz→common 的 com.spt.common.* 引用编译期命中本模块的本地化副本，无需改 import）：
      - constant/Constants.java + ScheduleConstants.java
      - exception/job/TaskException.java
      - core/controller/BaseController.java（精简版：仅保留 SysJobController/SysJobLogController 实际用到的 startPage/getDataTable/toAjax/error/getUsername 等方法，多余 RuoYi SysUser-base 方法剥除避免引入新 transitive deps）
      - core/domain/{AjaxResult, BaseEntity}.java
      - core/page/TableDataInfo.java
      - enums/BusinessType.java
      - annotation/{Excel, Log}.java
      - utils/{StringUtils, ExceptionUtil}.java
      - utils/bean/BeanUtils.java（保 RuoYi 自定义 copyBeanProp 方法，不替换为 Spring/Apache）
      - utils/spring/SpringUtils.java（ApplicationContextAware，必须本地化）
      - utils/poi/ExcelUtil.java

    【B. Pitfall 3 处置 — Constants.JOB_WHITELIST_STR】在本地化的 Constants.java 中把
        public static final String[] JOB_WHITELIST_STR = {"com.ruoyi"};
      改为
        public static final String[] JOB_WHITELIST_STR = {"com.spt"};
      理由：本期 60 handler 落 com.spt.quartz.task.* bean，package 前缀 com.spt；ScheduleUtils.whiteList(invokeTarget) 调 StringUtils.containsAnyIgnoreCase 校验包名，不改则 SysJobController.add/edit 对任何 com.spt.quartz.task.* invokeTarget 返回"目标字符串不在白名单内"。

    【C. Pitfall 2/7 处置 — SysJobMapper/SysJobLogMapper 加 @MyBatisDao + 放宽 @MapperScan】
      1. 在 zgbas-quartz/.../mapper/SysJobMapper.java + SysJobLogMapper.java 接口上加 @MyBatisDao 注解（import com.spt.tools.mybatis.annotation.MyBatisDao；Phase 2/5 标记约定 — @MapperScan(annotationClass = MyBatisDao.class) 是 ZgbasMybatisConfig 的过滤条件，不加注解接口不被扫描，启动期 SysJobServiceImpl @Autowired SysJobMapper 抛 NoSuchBeanDefinitionException）
      2. 编辑 zgbas-framework/.../ZgbasMybatisConfig.java：@MapperScan basePackages 数组追加 "com.spt.quartz.mapper" 第三元素（Pattern 4：与 com.spt.bas.system.dao / com.spt.bas.report.server.dao 并列；annotationClass=MyBatisDao.class 过滤条件不变；XML 路径已在 Task 1 落 mybatis/mappers/ 对齐 mapper-locations glob，无需改 application.yml）。

    【D. 不做的事】①不内联 auth-common 全量（仅本地化 15 类子集，避免引入 SysUser/SysRole 等 ~150 transitive deps）；②不剥 ExcelUtil / @Excel / @Log（先保 SysJobController.export 编译通过，export 功能可后期剥除）；③不改 ZgbasApplication（@ComponentScan 已覆盖 com.spt.*）；④不改 application.yml（mybatis-plus.mapper-locations glob 已通配 classpath:/mybatis/mappers/*Mapper.xml）。
  </action>
  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 1.8) mvn -pl zgbas-quartz -am compile -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml 2>&1 | tee /tmp/p6-01-t2.log | tail -20; 断言：①grep "^\[ERROR\]" /tmp/p6-01-t2.log | wc -l = 0（合并 Task 1+2 后 zgbas-quartz 自洽编译通过）；②grep -c 'JOB_WHITELIST_STR = {"com.spt"}' zgbas-quartz/src/main/java/com/spt/common/constant/Constants.java = 1；③grep -c "@MyBatisDao" zgbas-quartz/src/main/java/com/spt/quartz/mapper/SysJobMapper.java = 1；④grep -c "@MyBatisDao" zgbas-quartz/src/main/java/com/spt/quartz/mapper/SysJobLogMapper.java = 1；⑤grep -A 5 "@MapperScan" zgbas-framework/src/main/java/com/spt/framework/config/ZgbasMybatisConfig.java | grep -c "com.spt.quartz.mapper" = 1</automated>
  </verify>
  <done>
    - zgbas-quartz/src/main/java/com/spt/common/ 下 15 个 .java 文件就位
    - Constants.java 含且仅含 JOB_WHITELIST_STR = {"com.spt"}（非 com.ruoyi）
    - SysJobMapper.java + SysJobLogMapper.java 各有 1 个 @MyBatisDao 注解
    - ZgbasMybatisConfig @MapperScan basePackages 含 com.spt.quartz.mapper
    - mvn -pl zgbas-quartz -am compile 零 ERROR（Task 1 + Task 2 合并编译门 GREEN，关闭 QUARTZ-01 编译断言）
  </done>
</task>

<task type="auto">
  <name>Task 3: INFRA-03 — 删 xxl-job-core pom 依赖 + 翻译 CtrContractProfitServiceImpl 的 XxlJobHelper.log + reactor 全量编译门 GREEN</name>
  <files>
    zgbas-system/pom.xml
    zgbas-system/src/main/java/com/spt/bas/server/service/impl/CtrContractProfitServiceImpl.java
  </files>
  <read_first>
    - /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-system/pom.xml 行 90-140 （xxl-job-core:2.3.0 依赖声明位置，Phase 4 04-04 Rule 3 临时加；本期 INFRA-03 删除）
    - /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-system/src/main/java/com/spt/bas/server/service/impl/CtrContractProfitServiceImpl.java 行 1-30, 165-190 （Pitfall 8：行 16 import com.xxl.job.core.context.XxlJobHelper + 行 172/182 XxlJobHelper.log 用法；非 handler，是普通 service impl 内的诊断日志）
    - .planning/phases/06-quartz-migration/06-RESEARCH.md §Common Pitfalls 8（XxlJobHelper 翻译规则）+ §Runtime State Inventory
  </read_first>
  <action>
    【A. 删 xxl-job-core pom 依赖】编辑 zgbas-system/pom.xml，删除整个 xxl-job-core 依赖块（含注释 ~ 行 90-140 范围内的 <dependency>...<artifactId>xxl-job-core</artifactId>...</dependency>）。保留其他 basServer pom 依赖（pdfbox / report-client / purchase-client / spring-cloud-alibaba-commons / nacos-common 等照搬不动）。

    【B. 翻译 CtrContractProfitServiceImpl XxlJobHelper.log】在 zgbas-system/.../CtrContractProfitServiceImpl.java：
      - 行 16：删 import com.xxl.job.core.context.XxlJobHelper;
      - 行 172 XxlJobHelper.log("refreshProfitData approveNo:{}", approveNo); → 改为 log.info("refreshProfitData approveNo:{}", approveNo);（相邻行已有 log.info 同参签名 slf4j Logger，直接复用，符合 CONTEXT.md Discretion "直接删 XxlJobHelper.log 行最干净"，本计划选 log.info 替换保日志语义）
      - 行 182 XxlJobHelper.log("refreshProfitData 上下游合同更新条数:{},中游合同更新条数:{}", changeContractList.size(), changeDCSXList.size()); → 改为 log.info("refreshProfitData 上下游合同更新条数:{},中游合同更新条数:{}", changeContractList.size(), changeDCSXList.size());
      确认该类已有 private static final Logger log = LoggerFactory.getLogger(...); 否则补 slf4j Logger 声明（Pitfall 8：相邻行已用 log.info，声明必已存在）。

    【C. reactor 全量编译门】跑 mvn -pl zgbas-admin -am compile（reactor 全模块，含 zgbas-quartz + zgbas-system 改动 + 下游 zgbas-admin），grep ^\[ERROR\] locale 无关零错；这是 INFRA-03 删除后的全 reactor 编译断言（关闭 INFRA-03 + QUARTZ-01 编译维度）。

    【D. 不做的事】①不删 ~/.m2/repository/com/xuxueli/xxl-job-core/ 本地 jar（无副作用，自然保留）；②不改 application.yml xxl.job.* keys（research 实测单体从未落地，无需删）；③不删 XxlJobSpringExecutor bean 配置（research 实测单体无此 bean）；④不动其他 service impl（仅 CtrContractProfitServiceImpl 一处 XxlJobHelper 用法，grep 全单体 .java 已确认）。
  </action>
  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 1.8) mvn -pl zgbas-admin -am compile -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml 2>&1 | tee /tmp/p6-01-t3.log | tail -20; 断言：①grep "^\[ERROR\]" /tmp/p6-01-t3.log | wc -l = 0；②grep -c "xxl-job-core" zgbas-system/pom.xml = 0；③grep -c "com.xxl.job\|XxlJobHelper" zgbas-system/src/main/java/com/spt/bas/server/service/impl/CtrContractProfitServiceImpl.java = 0；④全单体 grep -rl "com\.xxl\.job\|XxlJobHelper\|@XxlJob\|XxlJobSpringExecutor" --include="*.java" --include="*.yml" --include="*.properties" --include="*.xml" /Users/alan/WorkSpace/IDEA/zgbas-plus 2>/dev/null | grep -v "/target/" | grep -v "/.planning/" | grep -v "/.claude/" | wc -l = 0（INFRA-03 全单体 xxl-job 痕迹清零）</automated>
  </verify>
  <done>
    - zgbas-system/pom.xml grep "xxl-job-core" = 0
    - CtrContractProfitServiceImpl grep "com.xxl.job\|XxlJobHelper" = 0（行 16 import 删 + 行 172/182 改 log.info）
    - mvn -pl zgbas-admin -am compile grep ^\[ERROR\]=0（全 reactor 编译门 GREEN，关闭 QUARTZ-01 + INFRA-03 编译维度）
    - 全单体 grep xxl-job 残留（含 yml/properties/xml）= 0（除 .planning/.claude/target）
  </done>
</task>

<task type="auto">
  <name>Task 4: D-P6-10 可视化运维台 — 建 /monitor/job Thymeleaf 页面（参照 operlog shell，不整搬 RuoYi 模板）</name>
  <files>
    zgbas-admin/src/main/resources/templates/monitor/job/job.html
    zgbas-admin/src/main/resources/templates/monitor/job/add.html
    zgbas-admin/src/main/resources/templates/monitor/job/edit.html
  </files>
  <read_first>
    - /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-admin/src/main/resources/templates/monitor/operlog/operlog.html （**主要 shell 参照**：admin 布局 dialect + header + 左侧菜单 + JS helpers（ajaxSubmit / tableLoad / modalShow 等）+ Shiro principal 取用户名模式；照搬其 layout/shell 结构，仅换内容域为 sys_job 字段）
    - /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-admin/src/main/resources/templates/monitor/operlog/detail.html （次要参照：详情页/弹窗模式；add/edit 沿用其 form 布局风格）
    - /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-quartz/src/main/java/com/spt/quartz/controller/SysJobController.java （Task 1 已就位 — 页面要 wire 的端点契约：@RequestMapping("/monitor/job") + @GetMapping(/list /add /edit /run /changeStatus) + @PostMapping(/add /edit /remove /run) + 返回路径前缀 "monitor/job/"）
    - /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-quartz/src/main/java/com/spt/quartz/domain/SysJob.java （字段列表：jobId / jobName / jobGroup / invokeTarget / cronExpression / misfirePolicy / concurrent / status / createBy / remark — 表格列 + 表单字段照此）
    - /Users/alan/WorkSpace/IDEA/spt-auth/auth-quartz/src/main/resources/templates/monitor/job/job.html （**仅作字段/列参考**，不整搬 — spt-auth 用的 RuoYi 模板 shell 与 zgbas-plus operlog shell 不同；从 spt-auth 学字段渲染方式 + AJAX URL，shell/layout 用 zgbas-plus operlog 风格）
    - /Users/alan/WorkSpace/IDEA/spt-auth/auth-quartz/src/main/resources/templates/monitor/job/add.html
    - /Users/alan/WorkSpace/IDEA/spt-auth/auth-quartz/src/main/resources/templates/monitor/job/edit.html
    - .planning/phases/06-quartz-migration/06-CONTEXT.md D-P6-10 （LOCKED 决策：保可视化 UI；用户 2026-07-18 option a 决议）
    - .planning/phases/06-quartz-migration/06-RESEARCH.md §Common Pitfalls 5（D-P6-10 UI 引入风险）+ §Open Questions Q1/Q3 RESOLVED（菜单接入 checkpoint:human-blocked 在 Task 5）
  </read_first>
  <action>
    【A. 目录 + 3 文件】在 zgbas-admin/src/main/resources/templates/monitor/job/ 建 3 个 .html（admin Thymeleaf layout dialect 解析；SpringBoot 2.5.9 默认 ViewResolver 会命中 controller 返回的 "monitor/job/job" 字符串 → classpath:/templates/monitor/job/job.html）：
      - job.html（列表页）：admin shell 照搬 operlog/operlog.html（layout/head/header/aside 同源），主体为 sys_job 列表表格 + 操作列按钮（新增/修改/删除/状态切换/执行一次）
      - add.html（新增表单）：照搬 operlog/detail.html 的 form 风格，含 sys_job 全字段输入控件（jobName / jobGroup / invokeTarget / cronExpression / misfirePolicy select / concurrent radio / status radio）
      - edit.html（编辑表单）：与 add.html 同结构 + th:object="${job}" 预填 + hidden jobId

    【B. shell 复用 — 不要整搬 RuoYi 模板】operlog/operlog.html 已是 zgbas-plus 的 admin shell 风格（layout dialect / 头部菜单 / JS helpers），**本 Task 保留 operlog 的 shell 不变，仅替换 main 内容域为 sys_job 列表**。不引入 RuoYi spt-auth 完整模板（其依赖的 RuoYi 前端 header/aside table 封装可能与 zgbas-plus 不一致）。关键差异：
      - AJAX 调 SysJobController 端点（/monitor/job/list 等），非 operlog 的 /monitor/operlog/list
      - 列字段对应 SysJob entity（jobName / invokeTarget / cronExpression / status 等），非 operlog 字段
      - "执行一次"按钮调 POST /monitor/job/run?jobId=xxx（D-P6-10 手动触发 UX）

    【C. XSS 防护 — invoke_target 字段转义】sys_job.invoke_target 是用户可控字符串（admin 录入），在 add.html / edit.html 渲染时必须用 Thymeleaf 的 th:value + 自然转义（th:text / [[${}]] 默认 HTML escape）。**禁止** th:utext（unescape）渲染 invoke_target；表单 input value 用 th:value="${job.invokeTarget}"（自动转义）；列表页 job.html 的 invokeTarget 列用 th:text（非 th:utext）。理由：恶意 invoke_target 含 <script> 经表单回显到页面会触发 XSS（虽然保存时经 RuoYi invokeTarget 黑名单部分拦截，但渲染层独立做转义是 defense-in-depth；threat_model T-06-01-06 记录）。

    【D. 与 Task 1 SysJobController 端点对齐】页面 AJAX / form action 必须命中 Task 1 已就位的 SysJobController @RequestMapping：
      - GET /monitor/job → 返回 "monitor/job/job"（列表页入口）
      - GET /monitor/job/add → 返回 "monitor/job/add"
      - GET /monitor/job/edit/{jobId} → 返回 "monitor/job/edit"
      - POST /monitor/job/list → AJAX 返回 TableDataInfo（列表数据）
      - POST /monitor/job/add / /edit / /remove / /run → AJAX 返回 AjaxResult
      端点路径与 SysJobController 实际 @RequestMapping 对齐（执行时从 read_first 的 SysJobController.java 实际抽取路径，不臆造）。

    【E. 不做的事】①不整搬 spt-auth/auth-quartz/templates/monitor/job/ 全部资源（spt-auth 用 RuoYi 前端依赖如 ruoyi.js / table.js 等，zgbas-plus 未必有 — 用 zgbas-plus operlog 既有的 JS helpers 替代）；②不接 sys_job_log 列表页（SysJobLogController 端点保留作 API，可视化日志页留 Phase 7 或运维按需扩展 — 本期保 QUARTZ-04 手动触发 UX 核心，详情日志 API 已可用）；③不动 admin 的 WebMvcConfig / 视图解析器（SpringBoot 默认 ViewResolver 已覆盖 templates/ 目录）；④不做菜单接入（Task 5 checkpoint:human-blocked）。
  </action>
  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 1.8) mvn -pl zgbas-admin -am compile -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml 2>&1 | tee /tmp/p6-01-t4.log | tail -10; 断言：①test -f zgbas-admin/src/main/resources/templates/monitor/job/job.html && test -f zgbas-admin/src/main/resources/templates/monitor/job/add.html && test -f zgbas-admin/src/main/resources/templates/monitor/job/edit.html；②grep -c "monitor/job" zgbas-admin/src/main/resources/templates/monitor/job/job.html ≥ 1（页面引用 monitor/job 端点）；③grep -c "invokeTarget" zgbas-admin/src/main/resources/templates/monitor/job/add.html ≥ 1 && grep -c "invokeTarget" zgbas-admin/src/main/resources/templates/monitor/job/edit.html ≥ 1（表单含 invoke_target 字段）；④grep -c "th:utext" zgbas-admin/src/main/resources/templates/monitor/job/*.html = 0（**XSS 防护**：禁用 unescape 渲染 invoke_target）；⑤grep -c "th:value.*invokeTarget\|th:text.*invokeTarget" zgbas-admin/src/main/resources/templates/monitor/job/*.html ≥ 2（自动转义方式渲染）；⑥grep -E "执行一次|/run|changeStatus" zgbas-admin/src/main/resources/templates/monitor/job/job.html | wc -l ≥ 1（D-P6-10 手动触发 UX 就位）</automated>
  </verify>
  <done>
    - 3 个 Thymeleaf .html 文件就位 templates/monitor/job/
    - shell 沿用 operlog 风格（admin layout / header / aside / JS helpers 一致），未整搬 RuoYi 模板
    - 表单字段对齐 SysJob entity（jobName / invokeTarget / cronExpression / status 等）
    - AJAX / form action 端点对齐 Task 1 SysJobController @RequestMapping
    - grep "th:utext" 该批 3 文件 = 0（invoke_target 字段强制 HTML 转义，XSS 防护就位）
    - 列表页含"执行一次"按钮（POST /monitor/job/run?jobId=xxx — D-P6-10 手动触发 UX）
    - mvn -pl zgbas-admin -am compile 全 reactor 零 ERROR（Task 4 仅加 .html 不改 .java，预期不影响编译；HTML 不参与编译但模板路径在 admin classpath 内可见）
  </done>
</task>

<task type="checkpoint:human-blocked" gate="blocking">
  <name>Task 5: D-P6-10 菜单接入 — 外部 spt-auth sys_menu INSERT（operational task，不在代码 PR 内）</name>
  <files>
    .planning/phases/06-quartz-migration/06-01-MENU-INSERT.sql
  </files>
  <read_first>
    - /Users/alan/WorkSpace/IDEA/spt-auth/sql/ry_20210908.sql 行 41-53 （RuoYi sys_menu 表 schema + 示例 INSERT 格式：menu_name / parent_id / order_num / url / menu_type / visible / perms 等字段）
    - /Users/alan/WorkSpace/IDEA/spt-auth/sql/ry_20210908.sql 行 95-110 （menu_name='定时任务' 的 RuoYi 默认菜单行，照搬字段值参照；url 字段格式 "/monitor/job"）
    - .planning/phases/06-quartz-migration/06-CONTEXT.md D-P6-10 （"菜单接入 — zgbas 用动态菜单（经 auth-sdk 从外部 spt-auth 取），RuoYi /monitor/job 菜单项需接入"）
    - .planning/phases/06-quartz-migration/06-RESEARCH.md §Open Questions Q3 RESOLVED（"外部 spt-auth sys_menu INSERT 一行 ('定时任务','2','2','job','monitor/job/index',...) 作 checkpoint:human-blocked"）
    - /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-admin/src/main/java/.../ShiroConfig.java 或等价 filter chain 配置 （确认 /monitor/job** 路径已被 ShiroFilter 覆盖为已认证才可达，非 anon；参照既有 /monitor/operlog 的 filterChain 模式）
  </read_first>
  <action>
    【A. 生成菜单 INSERT SQL】在 .planning/phases/06-quartz-migration/06-01-MENU-INSERT.sql 生成一行 INSERT INTO sys_menu 语句（外部 spt-auth sys_menu 表 schema）：
      - 字段值（按 RuoYi 标准 + D-P6-10 + research Q3 RESOLVED 记录的 ('定时任务','2','2','job','monitor/job/index',...)）：
        - menu_name = '定时任务'
        - parent_id = <monitor 父菜单 id>（执行时查外部 spt-auth sys_menu 表 menu_name='系统监控' 的 menu_id 作 parent，或与 operlog 同 parent）
        - order_num = 同级排序号（与 operlog / logininfor 同级，取 operlog.order_num + 1 或 = 6 之类）
        - url = 'monitor/job' 或 '/monitor/job'（与 SysJobController @RequestMapping("/monitor/job") + 视图解析前缀对齐；**不是 'monitor/job/index'** — research Q3 推荐值 'monitor/job/index' 对应 RuoYi index.html，本期 Task 4 用 job.html 列表页，url 改 'monitor/job' 让 controller GET /monitor/job 返回 "monitor/job/job" 视图）
        - target = 'menuItem' （RuoYi 默认）
        - menu_type = 'C' （菜单项）
        - visible = '0' （显示）
        - perms = 'monitor:job:list'（与 Task 1 SysJobController list 方法 @RequiresPermissions("monitor:job:list") 对齐 — 菜单显示权限与 list 权限同串）
        - icon = 'fa fa-tasks' 或 RuoYi 标准 job icon
      - 额外生成子按钮 INSERT（可选，参照 operlog 模式）：monitor:job:add / :edit / :remove / :run / :changeStatus 五个 button type='F'（按钮）— **可选**，RuoYi sys_menu 通常列表页权限 1 行 + 按钮权限 N 行；本期保底 list 权限 1 行，按钮行记入 SQL 注释由用户决定是否 apply。

    【B. 阻塞条件 — 用户/运维 apply SQL 到外部 spt-auth sys_menu 表】本 Task 阻塞至：
      1. 用户/运维把 06-01-MENU-INSERT.sql 在外部 spt-auth DB 执行（INSERT INTO sys_menu ...）
      2. 用户/运维重启或刷新 zgbas-plus（让 auth-sdk 重新拉取 sys_menu 缓存，或等下次 session 自然刷新 — 取决于 auth-sdk 缓存策略，与 Phase 3 登录链路 D-P3-03 cache init 同机制）
      3. 用户在浏览器登录 zgbas-plus → 左侧菜单"系统监控"下出现"定时任务"菜单项
      4. 用户点击"定时任务" → 跳转 /monitor/job → 渲染 Task 4 的 job.html 列表页

    【C. 可选 fallback — 直链验证】若用户不想立即 apply sys_menu（如外部 spt-auth DB 维护窗口未到），可选直链验证：浏览器直接访问 http://<admin-host>/monitor/job（绕过菜单），ShiroFilter 已认证 session 下应直接渲染 job.html 列表页。这是 fallback 验证 Task 4 Thymeleaf 页面 + Task 1 SysJobController 端点通的；菜单 INSERT 仍需 apply 才算 D-P6-10 完整落地。

    【D. 范围说明】本 Task 严格按 D-P6-10 LOCKED + research Q3 RESOLVED 处理菜单接入。sys_menu 表不在本仓库（外部 spt-auth），故是 operational task（非代码 PR）；但 SQL 文件落 .planning/ 留审计 + 可重放。verify 是 checkpoint acknowledged（用户明示菜单可见或选 fallback 直链）。
  </action>
  <verify>
    <automated>test -f .planning/phases/06-quartz-migration/06-01-MENU-INSERT.sql && grep -c "INSERT INTO sys_menu" .planning/phases/06-quartz-migration/06-01-MENU-INSERT.sql ≥ 1（SQL 文件存在且含至少 1 行 INSERT）；菜单 apply 验证由 checkpoint:human-blocked 用户明示确认（浏览器登录后"定时任务"菜单可见 / 或 fallback 直链 /monitor/job 可渲染 Task 4 job.html）</automated>
  </verify>
  <done>
    - 06-01-MENU-INSERT.sql 文件落 .planning/，含至少 1 行 INSERT INTO sys_menu ('定时任务', ..., 'monitor/job', ..., 'monitor:job:list')
    - 用户在对话中明示以下任一即可解锁：
      - "menu-applied" — SQL 已 apply 到外部 spt-auth sys_menu + zgbas-plus 左侧菜单"定时任务"可见
      - "direct-link-verified" — 走 fallback C 直链 /monitor/job 可渲染 Task 4 job.html + 列表数据可加载
      - "menu-deferred-to-p7" — 用户明示菜单 apply 延期到 Phase 7 UAT，本期 D-P6-10 仅保代码侧（Thymeleaf 页面）就位，菜单接入留运维窗口处理（本 Task 完结但 caveat 记 SUMMARY）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dev/ops → sptbasdb_pd DDL | sql/ 下 DDL 文件需运维手工 apply 到 dev/prod 库（D-P2-02 ddl-auto=none）— 未经审计的 DDL 可引入约束/索引问题 |
| Admin UI / API → sys_job write | SysJobController.add/edit/changeStatus/run 暴露写接口；剥除 @PreAuthorize 后 admin endpoint 的细粒度权限粒度由 Shiro @RequiresPermissions 补偿 |
| Scheduler → bean reflection | quartz Scheduler 经 JobInvokeUtil.invokeMethod 反射调任意 sys_job.invoke_target 字符串指定的 bean.method；这是反射型 RCE 入口 |
| Dev → xxl-job-core 删除 | 删除依赖前若漏改 service impl（CtrContractProfitServiceImpl 之外）会编译失败或运行期 ClassNotFoundException |
| Admin browser → Thymeleaf render | /monitor/job Thymeleaf 页面回显 sys_job.invoke_target 用户可控字符串；未转义则 XSS（stored） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Elevation of Privilege | sys_job.invoke_target 反射调用（ScheduleUtils.createScheduleJob + JobInvokeUtil.invokeMethod） | mitigate | Constants.JOB_WHITELIST_STR = {"com.spt"} 限制可调用包名前缀；RuoYi 4 重防护（V5 Input Validation：cron isValid + invokeTarget 黑名单 rmi:/ldap:/http(s):/java.net.URL/javax.naming.InitialContext/org.yaml.snakeyaml/org.springframework/org.apache/com.ruoyi.common.utils.file + 白名单包名校验）；admin ShiroFilter 已认证才可达（Phase 3）|
| T-06-01-02 | Elevation of Privilege | SysJobController/SysJobLogController（剥除 @PreAuthorize 后 admin endpoints 权限粒度） | mitigated | Task 1 action D **补 Shiro @RequiresPermissions 注解**作补偿控制（W5）：SysJobController 写方法 list/add/edit/remove/changeStatus/run 各带对应 monitor:job:* 权限串；SysJobLogController list/remove/clean 同；粒度对齐剥 @PreAuthorize 前的 monitor:job:* 语义。权限串与 zgbas-plus 既有 SysOperlogController @RequiresPermissions 风格一致（参照 read_first）；Task 5 菜单 perms='monitor:job:list' 与 SysJobController list 方法 @RequiresPermissions 同串。ASVS L1 V4.1.1 方法级权限粒度覆盖。 |
| T-06-01-03 | Tampering | sql/quartz.sql + sql/sys_job.sql 未审计的 DDL apply | mitigate | DDL 文件本期只落资源目录不执行；Task verify 只断言文件存在 + 内容含 "create table sys_job" / "QRTZ_"；实际 apply 由 06-05 D-P6-02 human-verify 后用户/运维执行 |
| T-06-01-04 | Tampering | Constants.JOB_WHITELIST_STR 收紧粒度（com.spt vs com.spt.quartz.task） | accept | research §Open Questions Q5 决议用 {"com.spt"}：与 monolith 主包名一致 + 灵活；运维若担心可后期收紧到 {"com.spt.quartz.task"}；当前接受 com.spt 范围（仍受 RuoYi invokeTarget 黑名单 + admin 已认证双控） |
| T-06-01-05 | Repudiation | sys_job_log 自动记录（AbstractQuartzJob.after 写 status / exceptionInfo / start-stop time） | mitigate | 照搬不修改；V7 Logging 已覆盖；异常不会静默吞（execute catch Exception 后调 after(context, job, e)） |
| T-06-01-06 | Cross-Site Scripting (XSS) | Thymeleaf 页面渲染 sys_job.invoke_target（admin 可控字符串，含 <script> 时 stored XSS） | mitigate | Task 4 action C 强制：① add/edit/list 三个 .html 渲染 invoke_target 一律用 th:value / th:text（Thymeleaf 默认 HTML 转义）；② grep 断言 th:utext = 0（禁用 unescape）；defense-in-depth — 即便 invoke_target 黑名单漏过 <script>，渲染层独立转义阻断 XSS 执行 |
| T-06-01-07 | Elevation of Privilege | 菜单接入（外部 spt-auth sys_menu INSERT）误配 perms/url 字段 | mitigate | Task 5 SQL 文件落 .planning/ 可审计；perms 字段与 SysJobController list 方法 @RequiresPermissions 串对齐（monitor:job:list）；url 对齐 controller @RequestMapping；checkpoint:human-blocked 用户 apply 后浏览器验证菜单可见性 |
| T-06-01-SC | Tampering | Maven 包来源（quartz + spring-context-support） | mitigate | 均为 Spring Boot 2.5.9 BOM 托管（Maven Central 18+/14+ 年成熟包）；spt-auth/auth-quartz 同栈对照；不引入新可疑包（research §Package Legitimacy Audit） |
| T-06-06-SC | Tampering | RuoYi auth-quartz 18 类 + auth-common 15 类 + auth-quartz templates 来源（spt-auth 私服） | mitigate | 直接从 /Users/alan/WorkSpace/IDEA/spt-auth 本地副本复制（非网络下载），逐行经 research §Sources 一手核；不引入 maven coord 解析的不确定性 |
</threat_model>

<verification>
- mvn -pl zgbas-quartz -am compile 零 ERROR（合并 Task 1+2 编译门）
- mvn -pl zgbas-admin -am compile 零 ERROR（Task 3 reactor 全量编译门，关闭 QUARTZ-01 + INFRA-03 编译维度）
- zgbas-quartz 目录树含 18 .java 类（com.spt.quartz.*）+ 15 .java 类（com.spt.common.* 本地化子集）+ mybatis/mappers/ 下 2 XML + sql/ 下 2 SQL
- Constants.JOB_WHITELIST_STR = {"com.spt"}
- SysJobController/SysJobLogController 零 @PreAuthorize + 写方法带 @RequiresPermissions（W5 补偿控制）
- ZgbasMybatisConfig @MapperScan 含 com.spt.quartz.mapper
- 全单体 grep xxl-job（含 yml/properties/xml）= 0
- sys_job.sql 含 sys_job + sys_job_log DDL；quartz.sql 含 11 张 QRTZ_* 表
- zgbas-admin/src/main/resources/templates/monitor/job/ 下 3 个 .html（D-P6-10 可视化运维台）
- Thymeleaf 页面 grep th:utext = 0（invoke_target XSS 转义就位）
- 06-01-MENU-INSERT.sql 文件就位 + checkpoint:human-blocked 解锁（菜单可见 / 直链 fallback / 延期 P7）
- PackageMarker.java 可保留可删（编译无影响，本计划不动）
</verification>

<success_criteria>
- [ ] QUARTZ-01（zgbas-quartz + 整模块复制 + ScheduleConfig 落 QuartzScheduleConfig）— 编译维度关闭；启动维度（Scheduler bean 可解析）留 06-06 启动验证
- [ ] QUARTZ-02（sys_job/sys_job_log/QRTZ_* DDL 文件就位）— 文件维度关闭；DB apply 维度由 06-05 + 运维执行
- [ ] QUARTZ-04（D-P6-10 可视化运维台部分 — 手动触发 UX 落 Thymeleaf 页面）— Task 4 + Task 5 落地（页面 wire SysJobController run 端点 + 菜单接入 checkpoint）；运行维度 sys_job 数据 + dry-run 在 06-05 + 06-06
- [ ] INFRA-03（删 xxl-job-core + CtrContractProfitServiceImpl 翻译 + 全单体 xxl-job 痕迹清零）— 全维度关闭
- [ ] 5 个研究标定阻塞（Pitfall 1/2/3/4 + Pitfall 7）在本计划内处置；Pitfall 5（Thymeleaf）经 D-P6-10 LOCKED option a 处置（Task 4 建 /monitor/job 页面 + Task 5 外部 sys_menu 接入 checkpoint）
- [ ] W5 补偿控制：@RequiresPermissions Shiro 注解替代剥除的 @PreAuthorize，方法级权限粒度不丢失（threat_model T-06-01-02 accept → mitigated）
</success_criteria>

<output>
Create `.planning/phases/06-quartz-migration/06-01-SUMMARY.md` when done
</output>
