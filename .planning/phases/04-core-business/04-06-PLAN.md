---
phase: 04-core-business
plan: 06
type: execute
wave: 4
depends_on: [04-05]
files_modified:
  - zgbas-admin/src/main/java/com/spt/bas/web/controller/**
  - zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java
autonomous: true
requirements: [BIZ-02, BIZ-03]
tags: [bulk-copy, wave-4, bff-controller, stub-port, wr02-http-proof, final-verification]

must_haves:
  truths:
    - "Wave 4 BFF controller 照搬 267 文件到 zgbas-admin/com/spt/bas/web/controller/"
    - "252 个 BFF 的 @Autowired I*Client 经 D-P4-01 方案 A Feign 自回环解析（Feign proxy → 本进程 api 端点）"
    - "D-P4-02 stub-port：~5-15 无本地实现的契约（ApplyCompanyOnline/ApplyDeposit/ApplyPromoteVip/BsInvestigateInfo/CtrOutInLedger/RiskApply/SignFileApi + PM 域 13 簇默认 stub）改 @Autowired(required=false) + null 守卫"
    - "WR-02 验收：ZgbasApplicationTest 3-5 真实 I*Client 端点 HTTP reachability（200/3xx/401）+ BFF bean 抽样，@Disabled 全部移除并激活"
    - "全模块 mvn compile 零 ERROR + 全 ZgbasApplicationTest 绿（含 WR-02 扩展）"
    - "D-P4-01/02/03/04/05/06 决策逐项核验通过"
  artifacts:
    - path: "zgbas-admin/src/main/java/com/spt/bas/web/controller/"
      provides: "267 BFF controller（252 引用 I*Client，15 不引用）"
    - path: "zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java"
      provides: "WR-02 HTTP reachability + BFF bean 抽样 test（激活）"
  key_links:
    - from: "BFF @Autowired I*Client"
      to: "Feign proxy → localhost:8080 api 端点"
      via: "@EnableFeignClients(com.spt.bas.client.remote) + BasFeignPathConfig 剥离前缀"
      pattern: "@Autowired.*I[A-Z][a-zA-Z]*Client"
    - from: "BFF stub 字段（D-P4-02 契约）"
      to: "null 守卫 fallback（业务降级裸 404）"
      via: "@Autowired(required=false) + null check"
      pattern: "Autowired\\(required\\s*=\\s*false\\)"
    - from: "ZgbasApplicationTest basContractEndpointReachable_*"
      to: "restTemplate → /apply/brand/findAll 等（经 Shiro 链）"
      via: "TestRestTemplate over HTTP"
      pattern: "is2xxSuccessful\\(\\).*is3xxRedirection\\(\\)"
---

<objective>
Wave 4 BFF controller 照搬（267 文件到 zgbas-admin）+ D-P4-02 stub-port + 最终验收（WR-02 HTTP proof + 全 mvn compile + 决策逐项核验）。本 plan 是 Phase 4 收尾——落地 BIZ-02（BFF 迁入 admin）+ BIZ-03（同进程调用行为等价，由 WR-02 HTTP reachability 证明）。

Purpose: 让前端（Phase 3 已照搬的 608 模板 + 742 JS/CSS）经 BFF 访问到 Wave 3 业务 api；让 252 BFF 的 @Autowired I*Client 经 Feign 自回环解析（D-P4-01 方案 A）；让 ~5-15 无实现契约 stub 降级不阻塞启动（D-P4-02）；最终通过 WR-02 HTTP proof 证明端到端可达。
Output: 267 BFF net-new + ~5-15 字段 stub + ZgbasApplicationTest WR-02 激活。
</objective>

<execution_context>
@/Users/alan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-core-business/04-CONTEXT.md
@.planning/phases/04-core-business/04-RESEARCH.md
@.planning/phases/04-core-business/04-PATTERNS.md
@.planning/phases/04-core-business/04-VALIDATION.md
@.planning/phases/04-core-business/04-05-SUMMARY.md
@.planning/phases/03-auth-homepage/03-02-SUMMARY.md

**BUILD PREFIX:** `JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml`

**NON-HERMETIC 契约（D-P3-13 Option 4）：** `mvn test` 需先本地 `export DB_PASSWORD=<dev 密码> SPT_APP_SECRET=<dev secret>`；验收语义 "local-export = passing"。

<interfaces>
<!-- Phase 3 IndexController stub-port 蓝本（D-P4-02 复用） -->
From zgbas-admin/src/main/java/com/spt/bas/web/controller/IndexController.java L59-66 + L120-126:
- @Autowired(required = false) private IPmProcessClient processClient;
- @Autowired(required = false) private IApproveWaitDealClient waitDealClient;
- if (waitDealClient != null) { ... } else { mmap.put("waitDealNum", "0"); }
- List<PmProcess> processList = processClient != null ? processClient.findAccess(searchVo) : Collections.emptyList();

<!-- Wave 3 api + Wave 1 契约 + 04-01 接线（自回环闭环） -->
- Wave 3: zgbas-system/com/spt/bas/server/api/ (224 @RestController)
- Wave 1: zgbas-system/com/spt/bas/client/remote/ (238 I*Client 契约)
- 04-01: ZgbasApplication @EnableFeignClients(com.spt.bas.client.remote) + BasClientConfig bean + spt.bas.server.url + BasFeignPathConfig.RequestInterceptor

<!-- D-P4-02 stub 契约清单（RESEARCH §D-P4-02 Enumeration） -->
真实业务 stub（~7）：ApplyCompanyOnline / ApplyDeposit / ApplyPromoteVip / BsInvestigateInfo / CtrOutInLedger / RiskApply / SignFileApi
PM 域簇（13 默认 stub）：PmApplySet / PmApprove / PmApproveContents / PmApproveHistory / PmApprovePush / PmApproveStep / PmProcess / PmProcessAccess / PmProcessAutoStep / PmProcessCondition / PmProcessNode / PmProcessPush / PmProcessStep
命名变体匹配（~5，不算 stub，按真 api 走）：BaiduMap / LogisticsCompany×2 / MQ / ApplyDcsx
Phase 3 已 stub（保留）：IPmProcessClient / IApproveWaitDealClient —— 本期 Wave 1 Task 1 已升级为源真契约，IndexController 调用点自然激活

<!-- WR-02 验收扩展（04-01 Task 2 已建占位，本 plan 移除 @Disabled 激活） -->
From zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java（04-01 加的 4 个 @Disabled）:
- basContractEndpointReachable_applyBrand_findAll
- basContractEndpointReachable_ctrContract_findPage
- basContractEndpointReachable_stockDetail_findAll
- bffControllersRegistered_sample
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 4 BFF controller bulk 照搬（267 文件）</name>
  <files>
    zgbas-admin/src/main/java/com/spt/bas/web/controller/**
  </files>
  <read_first>
    - .planning/phases/04-core-business/04-PATTERNS.md §Wave 4（task-shape + stub 变体 + accept 数字）+ §Shared Patterns 1/2/6
    - .planning/phases/04-core-business/04-RESEARCH.md §Wave Composition Wave 4（267 BFF + 252 引用 I*Client）+ §D-P4-02 Stub-Degradation Enumeration（25 项分类 + 命名变体匹配）
    - .planning/phases/04-core-business/04-CONTEXT.md §decisions D-P4-02（stub 降级对齐 D-P3-10）
    - .planning/phases/03-auth-homepage/03-02-SUMMARY.md（Phase 3 IndexController stub-port 模式）
    - 源 BFF 目录: /Users/alan/WorkSpace/IDEA/zgbas/web/src/main/java/com/spt/bas/web/controller/（267 文件）
    - Phase 3 已就位的 LoginController / IndexController（避免覆盖）: /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-admin/src/main/java/com/spt/bas/web/controller/{LoginController,IndexController}.java
    - Phase 3 03-02-PLAN Task 1 controller 干净照搬蓝本: /Users/alan/WorkSpace/IDEA/zgbas-plus/.planning/phases/03-auth-homepage/03-02-PLAN.md
    - Phase 3 03-02-PLAN Task 2 IndexController stub-port 蓝本（D-P4-02 复用对象）
  </read_first>
  <action>
    照搬保包名（D-P2-07）：从源 `web/src/main/java/com/spt/bas/web/controller/` 递归 `cp -r` 到 `zgbas-admin/src/main/java/com/spt/bas/web/controller/`，保目录结构与包名。

    **覆盖保护规则**：
    - `LoginController.java` / `IndexController.java`：Phase 3 已照搬（03-02-PLAN），**不覆盖**（用 `cp -n` 或先列后排除）。
    - 其它 267 个 BFF 文件全部新照搬。

    依赖核查（编译期）：
    - BFF 引用 Wave 1 `I*Client`（Plan 04-02 已就位，238 契约）
    - BFF 引用 Phase 2 实体/vo（已就位）
    - BFF 引用 Phase 3 web 工具类（CookieUtils/DateUtils/EasyTreeUtil2/WebParamUtils，03-02 已就位）
    - BFF 引用 Phase 3 Shiro 工具类（ShiroUtil，03-01 已就位）
    - 0 个 BFF 直接引用 service 或 api 包（RESEARCH §Wave Composition Wave 4 实测）—— BFF 仅依赖契约接口

    本 Task 仅做照搬，**stub-port 留 Task 2 单独处理**——本 Task 后 compile 可能因 D-P4-02 ~5-15 无本地实现的契约在启动期 `NoSuchBeanDefinitionException` 报错（运行期，非编译期；compile gate 应能通过）。

    完成后跑 zgbas-admin compile gate。
  </action>
  <verify>
    <automated>
cd /Users/alan/WorkSpace/IDEA/zgbas-plus && \
echo "=== BFF 计数 ===" && \
echo "BFF_COUNT=$(find zgbas-admin/src/main/java/com/spt/bas/web/controller -name '*.java' | wc -l)" && \
echo "BFF_REFERENCES_ICLIENT=$(grep -rl 'I[A-Z][a-zA-Z]*Client' zgbas-admin/src/main/java/com/spt/bas/web/controller | wc -l)" && \
echo "=== Phase 3 资产保护 ===" && \
grep -c 'RequestMapping(value = "/login")' zgbas-admin/src/main/java/com/spt/bas/web/controller/LoginController.java && \
grep -c 'RequestMapping(value = "/index")' zgbas-admin/src/main/java/com/spt/bas/web/controller/IndexController.java && \
echo "=== 包名 verbatim 抽样 ===" && \
ls zgbas-admin/src/main/java/com/spt/bas/web/controller/*.java | head -3 | xargs -I{} head -1 {} && \
echo "=== compile gate (admin) ===" && \
JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home \
/Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml \
-pl zgbas-admin -am compile 2>&1 | tee /tmp/p4-06-t1.log | tail -10 && \
echo "ERROR_COUNT=$(grep -c '^\[ERROR\]' /tmp/p4-06-t1.log)" && \
echo "CANNOT_FIND=$(grep -cE 'cannot find symbol|找不到符号' /tmp/p4-06-t1.log)"
    </automated>
  </verify>
  <done>
    - BFF_COUNT == 269（267 新照搬 + Phase 3 已有 LoginController + IndexController）
    - BFF_REFERENCES_ICLIENT ≥ 252
    - LoginController / IndexController 未被覆盖（grep `"/login"` / `"/index"` 命中）
    - 包名 `com.spt.bas.web.controller.*` verbatim
    - ERROR_COUNT == 0 且 CANNOT_FIND == 0
  </done>
</task>

<task type="auto">
  <name>Task 2: D-P4-02 stub-port（~5-15 BFF 字段 @Autowired(required=false) + null 守卫）</name>
  <files>
    zgbas-admin/src/main/java/com/spt/bas/web/controller/**（仅改 ~5-15 个引用 D-P4-02 契约的 BFF 字段）
  </files>
  <read_first>
    - .planning/phases/04-core-business/04-RESEARCH.md §D-P4-02 Stub-Degradation Enumeration（25 项分类：~7 真业务 stub + 13 PM 域 + 5 命名变体 + 2 已存在）
    - .planning/phases/04-core-business/04-PATTERNS.md §D-P4-02 Stub-Port 升级（Wave 4 BFF 字段 stub 蓝本）
    - .planning/phases/03-auth-homepage/03-02-PLAN.md Task 2（IndexController stub-port 蓝本：@Autowired(required=false) + null 守卫）
    - Phase 3 IndexController stub 实现蓝本: /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-admin/src/main/java/com/spt/bas/web/controller/IndexController.java（L59-66 + L120-126 + L294-295）
  </read_first>
  <action>
    对 BFF controller 中引用 D-P4-02 无本地 api 实现的契约字段，改 `@Autowired(required=false)` + null 守卫（复用 Phase 3 D-P3-10 IndexController 模式）。

    **需 stub 的契约（RESEARCH §D-P4-02 Enumeration）**：
    (1) **真实业务 stub ~7**：ApplyCompanyOnline / ApplyDeposit / ApplyPromoteVip / BsInvestigateInfo / CtrOutInLedger / RiskApply / SignFileApi。grep 这些 Client 字段，改 `@Autowired(required=false)`，调用点加 null 守卫（参考 IndexController L120-126：`if (xxxClient != null) { ... } else { mmap.put(key, default); }`）。
    (2) **PM 域 13 簇默认 stub**：PmApplySet / PmApprove* / PmProcess*。grep 这些 Client 字段，同上 stub 处理。**注意**：若验收发现某 PM 实现在外部 spt-auth（grep 实测源 zgbas 找不到 implements），按 A4 假设仍走 stub（不阻塞启动），后续 Phase 7 ALIGN 验证时若需要再改外部 HTTP 接入。
    (3) **Phase 3 已 stub 升级**：IPmProcessClient / IApproveWaitDealClient 在 Wave 1 Task 1 已升级为源真 @FeignClient 契约，**保留 IndexController 现有 @Autowired(required=false)**（保险），调用点 null 守卫自然 fallback；实际 Wave 3 有真 api 实现（ApproveWaitDealApi 等），Feign 自回环激活。
    (4) **命名变体不算 stub**：BaiduMapClient / LogisticsCompanyConfig / LogisticsCompanyDetail / MQClient / ApplyDcsx —— 这些有对应真 api（命名变体匹配），按真 api 走，不 stub。

    **识别需 stub 的字段的方法**：跑 ZgbasApplicationTest（Task 3 闭环），启动期 `NoSuchBeanDefinitionException: No qualifying bean of type 'com.spt.bas.client.remote.IXxxClient'` 报错会暴露所有需 stub 的契约。对每个暴露的契约，grep 引用它的 BFF 字段，逐字段改 required=false + null 守卫。

    修改原则（最小侵入）：
    - 仅改字段注解（`@Autowired` → `@Autowired(required=false)`）+ 调用点加 null 守卫
    - 不删字段、不改类型、不改 import
    - 不动 BFF 的业务逻辑（仅 null fallback 到安全默认值）

    完成后跑 zgbas-admin compile gate。
  </action>
  <verify>
    <automated>
cd /Users/alan/WorkSpace/IDEA/zgbas-plus && \
echo "=== stub 字段计数 ===" && \
echo "STUB_FIELDS=$(grep -rn 'Autowired(required = false)\|Autowired(required=false)' zgbas-admin/src/main/java/com/spt/bas/web/controller | wc -l)" && \
echo "=== D-P4-02 真业务 stub 契约抽样 ===" && \
for c in ApplyCompanyOnline ApplyDeposit ApplyPromoteVip BsInvestigateInfo CtrOutInLedger RiskApply SignFile; do \
  echo "$c: $(grep -rl "I${c}Client" zgbas-admin/src/main/java/com/spt/bas/web/controller 2>/dev/null | wc -l) 个 BFF 引用"; \
done && \
echo "=== compile gate (admin) ===" && \
JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home \
/Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml \
-pl zgbas-admin -am compile 2>&1 | tee /tmp/p4-06-t2.log | tail -5 && \
echo "ERROR_COUNT=$(grep -c '^\[ERROR\]' /tmp/p4-06-t2.log)"
    </automated>
  </verify>
  <done>
    - STUB_FIELDS ≥ 5（至少 ~5-15 个 D-P4-02 契约字段标 required=false，含 PM 域簇）
    - 每个 stub 字段对应调用点有 null 守卫（grep `if (xxxClient != null)` 模式命中）
    - 真业务 stub 7 契约 + PM 域 13 簇中实际被 BFF 引用的全部 stub
    - 命名变体匹配契约（BaiduMap/Logistics×2/MQ/ApplyDcsx）未 stub（按真 api）
    - ERROR_COUNT == 0
  </done>
</task>

<task type="auto">
  <name>Task 3: 最终验收（WR-02 HTTP proof 激活 + 全 mvn compile + ZgbasApplicationTest 全绿 + 决策核验）</name>
  <files>
    zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java
  </files>
  <read_first>
    - .planning/phases/04-core-business/04-PATTERNS.md §WR-02 验收扩展（endpoint reachability + BFF bean 抽样断言风格）
    - .planning/phases/04-core-business/04-RESEARCH.md §D-P4-06 Verification Assets + §Code Examples Example 2（断言 is2xxSuccessful||is3xxRedirection||401）
    - .planning/phases/04-core-business/04-VALIDATION.md §Requirement → Test Map + §Validation Sign-Off
    - 04-01-SUMMARY（WR-02 占位测试位置与命名）
    - 当前 ZgbasApplicationTest: /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java（04-01 加的 4 @Disabled）
  </read_first>
  <action>
    Phase 4 最终验收三件事：

    (1) **激活 WR-02 测试**：移除 ZgbasApplicationTest 中 04-01 Task 2 加的 4 个 @Disabled 注解（basContractEndpointReachable_applyBrand_findAll / _ctrContract_findPage / _stockDetail_findAll / bffControllersRegistered_sample）。确认断言语义 `is2xxSuccessful || is3xxRedirection || statusCode==401`（Shiro 未登录可能返 401/302，证明端点已注册 + Spring MVC 经接口级 @GetMapping/@PostMapping over HTTP 可达）。
    - 若端点路径与 Wave 3 api 实际暴露路径不一致（如 path 前缀剥离失败），调整测试 URL 到正确路径（不改 api 源码，仅改测试 URL）。
    - BFF bean 抽样断言：选 3-5 个代表性 BFF bean（如 `applyBrandController` / `ctrContractController` / `stockDetailController`），断言 `context.containsBean(...)` 为 true。

    (2) **全模块 mvn compile + 全 ZgbasApplicationTest**（前置 export DB_PASSWORD + SPT_APP_SECRET）：
    ```
    JAVA_HOME=... mvn -am compile 2>&1 | tee /tmp/p4-final.log   # 全模块零 ERROR
    JAVA_HOME=... mvn -pl zgbas-admin -am test -Dtest=ZgbasApplicationTest  # 含 WR-02 全绿
    ```

    (3) **D-P4-01..06 逐项核验**（grep + 启动验证证据）：
    - D-P4-01 方案 A：grep ZgbasApplication `com.spt.bas.client.remote` ≥ 1 + BasFeignPathConfig 存在 + WR-02 HTTP reachability 绿
    - D-P4-01a path 前缀：WR-02 `basContractEndpointReachable_*` 200/3xx/401（证明 RequestInterceptor 剥离生效，自回环通）
    - D-P4-02 stub：grep STUB_FIELDS ≥ 5 + ZgbasApplicationTest 启动期不抛 NoSuchBeanDefinitionException
    - D-P4-03 wave 测序：5 wave 顺序执行（SUMMARY 时序确认）
    - D-P4-04 rocketmq：pom starter + 22 rocketmq 文件 + yml 占位 + ZgbasApplicationTest#contextLoads 绿（懒连接不阻塞 boot）
    - D-P4-05 ddl-auto=none：application-dev/prod.yml grep `ddl-auto: none` 命中（沿用 Phase 2）
    - D-P4-06 启动验证：ZgbasApplicationTest 全绿（含 WR-02）+ 全 mvn compile 零 ERROR

    在 SUMMARY 记录每项核验证据 + 实测计数（与 RESEARCH 预期数字对齐：238 remote / 224 api / 267 BFF / 22 rocketmq / 6 config）。
  </action>
  <verify>
    <automated>
export DB_PASSWORD="${DB_PASSWORD:?must export DB_PASSWORD}" && \
export SPT_APP_SECRET="${SPT_APP_SECRET:?must export SPT_APP_SECRET}" && \
cd /Users/alan/WorkSpace/IDEA/zgbas-plus && \
echo "=== 全模块 compile ===" && \
JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home \
/Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml \
-am compile 2>&1 | tee /tmp/p4-final-compile.log | tail -5 && \
echo "COMPILE_ERROR=$(grep -c '^\[ERROR\]' /tmp/p4-final-compile.log)" && \
echo "=== ZgbasApplicationTest 全套（含 WR-02 激活）===" && \
JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home \
/Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml \
-pl zgbas-admin -am test -Dtest=ZgbasApplicationTest 2>&1 | tee /tmp/p4-final-test.log | tail -20 && \
echo "TEST_ERROR=$(grep -c '^\[ERROR\]' /tmp/p4-final-test.log)" && \
echo "TEST_FAIL=$(grep -cE 'FAIL|FAILURE|APPLICATION FAILED TO START' /tmp/p4-final-test.log)" && \
echo "TESTS_RUN=$(grep -E 'Tests run: [0-9]+' /tmp/p4-final-test.log | tail -1)" && \
echo "=== @Disabled 应已移除（WR-02 激活）===" && \
echo "REMAINING_DISABLED=$(grep -c '@Disabled' zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java)" && \
echo "=== D-P4 决策核验 ===" && \
echo "D-P4-01 bas remote: $(grep -c 'com.spt.bas.client.remote' zgbas-admin/src/main/java/com/spt/ZgbasApplication.java)" && \
echo "D-P4-01a path filter: $(test -f zgbas-system/src/main/java/com/spt/bas/client/config/BasFeignPathConfig.java && echo OK)" && \
echo "D-P4-02 stub fields: $(grep -rc 'Autowired(required = false)\|Autowired(required=false)' zgbas-admin/src/main/java/com/spt/bas/web/controller | wc -l)" && \
echo "D-P4-04 starter: $(grep -c 'rocketmq-spring-boot-starter' zgbas-system/pom.xml)" && \
echo "D-P4-04 rocketmq files: $(find zgbas-system/src/main/java/com/spt/bas/server/rocketmq -name '*.java' | wc -l)" && \
echo "D-P4-04 xxl-job absent: $(grep -rl 'XxlJobSpringExecutor\|xxl-job' zgbas-system/src/main/java zgbas-admin/src/main/java zgbas-framework/src/main/java 2>/dev/null | wc -l)" && \
echo "D-P4-05 ddl-auto none: $(grep -r 'ddl-auto' zgbas-admin/src/main/resources/application-*.yml | head -3)"
    </automated>
  </verify>
  <done>
    - COMPILE_ERROR == 0（全模块零 ERROR）
    - TEST_ERROR == 0 且 TEST_FAIL == 0（ZgbasApplicationTest 全绿，含 WR-02 激活后）
    - REMAINING_DISABLED == 0（4 个 WR-02 占位测试 @Disabled 已全部移除，激活）
    - WR-02 HTTP reachability：3 个 basContractEndpointReachable_* test 全绿（200/3xx/401 任一）
    - BFF bean 抽样：bffControllersRegistered_sample 全绿（context.containsBean 命中）
    - D-P4-01 bas remote ≥ 1 + BasFeignPathConfig 文件存在
    - D-P4-02 stub fields ≥ 5
    - D-P4-04 starter ≥ 1 + rocketmq 文件 ≥ 22 + xxl-job 全模块 absent
    - D-P4-05 ddl-auto=none 命中
    - D-P4-01..06 逐项核验证据在 SUMMARY 记录
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 客户端 → BFF（267 controller） | 业务 BFF 端点暴露，运行期访问控制由 Phase 3 Shiro 链 `/**=user` + DB 动态链覆盖 |
| BFF → Feign proxy → 本进程 api | 252 处 @Autowired I*Client 经 Feign 自回环；仅 localhost，无跨进程跳 |
| BFF stub 字段 → null fallback | ~5-15 D-P4-02 契约无本地实现，业务降级裸 404（D-P3-10 模式） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-I | Info Disclosure | BFF 业务端点暴露 | mitigate | Phase 3 Shiro `/**=user` 链 + DB 动态权限链覆盖；BFF 照搬源 @RequestMapping 行为，访问控制随 controller 照搬生效 |
| T-04-06-S | Spoofing | BFF 未登录访问业务接口 | mitigate | Shiro 链强制 `/**=user`（Phase 3 已就位），未登录请求重定向 /login 或返 401 |
| T-04-06-T | Tampering | BFF 输入校验 | accept | 沿用源 @Valid + @RequestBody（V5 ASVS），照搬源 controller 不改校验逻辑 |
| T-04-06-D | DoS | stub 字段首次调用阻塞 | accept | stub 字段为 null（required=false），调用前 null 守卫直接返默认值，不发请求不阻塞 |
| T-04-06-E | EoP | PM 域 13 簇 stub 降级后绕过权限 | accept | stub 字段返默认值/null，不暴露业务数据；真实业务数据访问经 Wave 3 真 api + Shiro 链；后续 Phase 7 ALIGN 若发现 PM 需真接入则改外部 HTTP |
| T-04-06-R | Repudiation | WR-02 HTTP proof 不记录调用链 | accept | WR-02 仅证明端点可达，非业务审计；业务审计沿用 Phase 2 PERSIST-04 |

无 high 级威胁。Mitigate 项落地（Shiro 链 + @Valid 照搬）；Accept 项有充分理由（行为等价优先 + 后续 phase 接通）。
</threat_model>

<verification>
- 全模块 `mvn -am compile` 零 ERROR
- ZgbasApplicationTest 全绿（含 WR-02 激活的 4 个新 test：3 endpoint reachability + 1 BFF bean 抽样）
- @Disabled 已全部移除（WR-02 激活）
- D-P4-01..06 逐项核验证据齐全（grep + 启动验证）
- WR-02 HTTP reachability 证明 D-P4-01 方案 A 自回环接线成功（端点 200/3xx/401）
</verification>

<success_criteria>
- **BIZ-02 交付**：267 BFF controller 迁入 zgbas-admin，业务 HTTP 接口可访问（WR-02 reachability 证明）
- **BIZ-03 交付**：业务间原 Feign 改为同进程 Feign 自回环（D-P4-01 方案 A），行为等价（源即 Feign-over-HTTP，localhost 等价；WR-02 HTTP proof 通过）
- **D-P4-01..06 决策逐项核验**：方案 A 接线 + path 前缀剥离 + stub-port + wave 测序 + rocketmq INCLUDE + ddl-auto=none + 启动验证为主 + WR-02
- **Phase 4 三条成功标准达成**（ROADMAP §Phase 4）：
  1. 核心业务实体 + Service 迁入 zgbas-system，JPA 增删改查可用（BIZ-01，Plan 04-04/04-05 交付）
  2. 业务 Controller/BFF 迁入 zgbas-admin，HTTP 接口可访问（BIZ-02，本 Plan 交付）
  3. 业务间原 Feign 改为同进程调用（方案 A Feign 自回环），行为等价（BIZ-03，本 Plan WR-02 证明）
</success_criteria>

<output>
Create `.planning/phases/04-core-business/04-06-SUMMARY.md` when done.

**Phase 4 完成后**：可以进入 `/gsd:verify-phase 4` 做正式验证（nyquist_validation 已 true，VALIDATION.md 已就位）。
</output>
