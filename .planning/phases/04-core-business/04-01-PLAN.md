---
phase: 04-core-business
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - zgbas-admin/src/main/java/com/spt/ZgbasApplication.java
  - zgbas-system/src/main/java/com/spt/bas/client/config/BasClientConfig.java
  - zgbas-system/src/main/java/com/spt/bas/client/config/BasFeignPathConfig.java
  - zgbas-system/pom.xml
  - zgbas-admin/src/main/resources/application-dev.yml
  - zgbas-admin/src/main/resources/application-prod.yml
  - zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java
autonomous: true
requirements: [BIZ-03]
tags: [feign, self-loopback, wiring, rocketmq, wr02-scaffold, path-prefix]

must_haves:
  truths:
    - "ZgbasApplication @EnableFeignClients 同时扫 com.spt.sign.client.remote 与 com.spt.bas.client.remote"
    - "BasClientConfig 落位并产出名为 basServerConfig 的 LocalServerConfig bean，读 spt.bas.server.url"
    - "application-dev/prod.yml 配 spt.bas.server.url=http://localhost:8080 使 238 契约 SpEL 自回环到本进程 8080"
    - "rocketmq-spring-boot-starter:2.2.2 在 zgbas-system pom 声明；rocketmq.* 占位在 dev/prod yml"
    - "Feign 自回环 path 前缀剥离机制（BasFeignPathConfig.RequestInterceptor）作为 @Bean 注册"
    - "ZgbasApplicationTest 包含 WR-02 HTTP reachability 与 BFF bean 抽样的占位测试（@Disabled 或最小 stub 断言）"
    - "Phase 3 已验 ZgbasApplicationTest 14 test 仍绿（无回归）"
  artifacts:
    - path: "zgbas-admin/src/main/java/com/spt/ZgbasApplication.java"
      provides: "放宽 @EnableFeignClients basePackages 加 com.spt.bas.client.remote"
      contains: "com.spt.bas.client.remote"
    - path: "zgbas-system/src/main/java/com/spt/bas/client/config/BasClientConfig.java"
      provides: "basServerConfig bean 作 238 契约 SpEL 锚点"
      contains: "BasConstants.SERVER_BEAN_NAME"
    - path: "zgbas-system/src/main/java/com/spt/bas/client/config/BasFeignPathConfig.java"
      provides: "RequestInterceptor 剥离 spt-bas-server 前缀（D-P4-01a 首选方案）"
      contains: "RequestInterceptor"
    - path: "zgbas-system/pom.xml"
      provides: "rocketmq-spring-boot-starter:2.2.2 依赖"
      contains: "rocketmq-spring-boot-starter"
    - path: "zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java"
      provides: "WR-02 占位测试（端点 reachability + BFF bean 抽样）"
      contains: "basContractEndpointReachable"
  key_links:
    - from: "I*Client.url SpEL"
      to: "basServerConfig bean.url"
      via: "BasConstants.SERVER_URL = #{basServerConfig.url}"
      pattern: "\\#\\{basServerConfig\\.url\\}"
    - from: "BasFeignPathConfig.RequestInterceptor"
      to: "Feign proxy 发起请求的 URL"
      via: "template.uri() 剥离 spt-bas-server/ 前缀"
      pattern: "spt-bas-server"
    - from: "ZgbasApplication @EnableFeignClients"
      to: "238 bas 契约 Feign proxy 生成"
      via: "basePackages 包含 com.spt.bas.client.remote"
      pattern: "com\\.spt\\.bas\\.client\\.remote"
---

<objective>
Phase 4 前置接线（D-P4-01 方案 A Feign 自回环）+ 验收脚手架。此 plan 落地 238 bas 契约 Feign proxy 自回环到本进程 8080 的所有运行期锚点：放宽 @EnableFeignClients basePackages、产出 basServerConfig bean 供 SpEL 解析、配置 spt.bas.server.url 与 rocketmq.*、处理 D-P4-01a path 前缀（首选 RequestInterceptor 剥离 spt-bas-server/ 前缀，不设单体 context-path 以保 Phase 3 AUTH-03 根路径），并扩展 ZgbasApplicationTest 建立 WR-02 占位测试（后续 wave 填端点）。

Purpose: 为 Wave 1+ 提供契约编译/运行所需的接线基础；为 Wave 4 最终验收提供 WR-02 测试壳。无此 plan 则 Wave 4 BFF 启动直接 NoSuchBeanDefinitionException（Pitfall 1）。
Output: 1 pom 改 + 1 启动类注解放宽 + 2 新 @Configuration + 2 yml 改 + ZgbasApplicationTest 扩展。
</objective>

<execution_context>
@/Users/alan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-core-business/04-CONTEXT.md
@.planning/phases/04-core-business/04-RESEARCH.md
@.planning/phases/04-core-business/04-PATTERNS.md
@.planning/phases/04-core-business/04-VALIDATION.md

**BUILD PREFIX:** `JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml`

**NON-HERMETIC 契约（D-P3-13 Option 4）：** `mvn test` 需先本地 `export DB_PASSWORD=<dev 密码> SPT_APP_SECRET=<dev secret>`；dev profile 真连 47.104.15.98:3306/sptbasdb_pd + 真外部 spt-auth；验收语义 "local-export = passing"。

<interfaces>
<!-- D-P4-01 方案 A 接线所需的关键常量（来自源 BasConstants，Phase 2 已内联） -->
From zgbas-system/src/main/java/com/spt/bas/client/constant/BasConstants.java:
- String SERVER_NAME = "spt-bas-server"
- String SERVER_BEAN_NAME = "basServerConfig"
- String SERVER_URL = "#{" + SERVER_BEAN_NAME + ".url}"   // SpEL
- String SERVER_URL_KEY = "spt.bas.server.url"

<!-- Phase 2 已内联的 LocalServerConfig（BasClientConfig 依赖） -->
From zgbas-common/src/main/java/com/spt/tools/core/bean/LocalServerConfig.java:
- public void setUrlKey(String key)
- public String getUrl()  // 读 yml 对应 key

<!-- Phase 2 ZgbasApplication 收窄态（本 plan 放宽 basePackages） -->
From zgbas-admin/src/main/java/com/spt/ZgbasApplication.java L60-67:
- @EnableFeignClients(basePackages = "com.spt.sign.client.remote")
- excludeFilters 含 com.spt.tools.http.feign.FeignConfig + com.spt.sign.client.config.FeignConfig

<!-- Phase 2/3 ZgbasApplicationTest 当前 14 test，本 plan 扩展 -->
From zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java:
- @SpringBootTest(webEnvironment=RANDOM_PORT) + TestRestTemplate + @Autowired ApplicationContext context
- 现有 endpoint reachability 风格见 §PATTERNS.md WR-02 蓝本
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Feign 自回环接线 + D-P4-01a path 前缀处理 + pom/yml 配置</name>
  <files>
    zgbas-admin/src/main/java/com/spt/ZgbasApplication.java,
    zgbas-system/src/main/java/com/spt/bas/client/config/BasClientConfig.java,
    zgbas-system/src/main/java/com/spt/bas/client/config/BasFeignPathConfig.java,
    zgbas-system/pom.xml,
    zgbas-admin/src/main/resources/application-dev.yml,
    zgbas-admin/src/main/resources/application-prod.yml
  </files>
  <read_first>
    - .planning/phases/04-core-business/04-PATTERNS.md §D-P4-01 Feign 自回环 wiring + §Wave 1c BasClientConfig + §Shared Patterns 1/3/4
    - .planning/phases/04-core-business/04-RESEARCH.md §D-P4-01 Critical Finding + §Code Examples Example 1 + §D-P4-04 verdict + §Pitfall 4
    - .planning/phases/04-core-business/04-CONTEXT.md §decisions D-P4-01 / D-P4-01a / D-P4-04
    - 源 BasClientConfig（照搬目标）: /Users/alan/WorkSpace/IDEA/zgbas/basCore/basClient/src/main/java/com/spt/bas/client/config/BasClientConfig.java
    - 源 BasConstants: /Users/alan/WorkSpace/IDEA/zgbas/basCore/basClient/src/main/java/com/spt/bas/client/constant/BasConstants.java
    - 源 pom rocketmq version: /Users/alan/WorkSpace/IDEA/zgbas/basCore/pom.xml （第 25 行 `<rocketmq-version>2.2.2</rocketmq-version>`）
    - 源 dev/prod rocketmq 配置: /Users/alan/WorkSpace/IDEA/zgbas/basCore/basServer/src/main/resources/application-dev.properties（L22 name-server）+ application-prod.properties（L34-36 明文 AK/SK）
    - 当前单体启动类: /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-admin/src/main/java/com/spt/ZgbasApplication.java
    - 当前单体 system pom: /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-system/pom.xml
    - 当前单体 dev/prod yml: /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-admin/src/main/resources/application-{dev,prod}.yml
    - Phase 2 dedup 蓝本（ZgbasDataSourceConfig @Primary 击败模式）: /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-framework/src/main/java/com/spt/config/ZgbasDataSourceConfig.java
  </read_first>
  <action>
    一次性落地 D-P4-01 方案 A 全部接线要素，分 5 小步：

    (1) **BasClientConfig 照搬**：把源 `basClient/config/BasClientConfig.java`（19 行 @Configuration，产出 `@Bean(BasConstants.SERVER_BEAN_NAME)` 类型 `LocalServerConfig`，setUrlKey(SERVER_URL_KEY)）照搬到 `zgbas-system/src/main/java/com/spt/bas/client/config/BasClientConfig.java`，保包名 `com.spt.bas.client.config`（D-P2-07）。不加任何修改。此 bean 是 238 契约 SpEL `#{basServerConfig.url}` 的运行期锚点。

    (2) **ZgbasApplication 放宽 @EnableFeignClients**：把 `@EnableFeignClients(basePackages = "com.spt.sign.client.remote")` 改为 `@EnableFeignClients(basePackages = {"com.spt.sign.client.remote", "com.spt.bas.client.remote"})`。加注释 "Phase 4 D-P4-01 方案 A self-loopback (corrected 2026-07-17)"。**不修改 excludeFilters**（FeignConfig 仍排除，避免 spt-tools 全局 Feign config 与 Spring MVC 冲突）。

    (3) **BasFeignPathConfig 新建**（D-P4-01a 首选方案）：在 `zgbas-system/src/main/java/com/spt/bas/client/config/BasFeignPathConfig.java` 新建 `@Configuration` 类，`@Bean public RequestInterceptor basServerPathStripper()` 返回 lambda：检查 `template.url()`，若以 `spt-bas-server/` 或 `/spt-bas-server/` 开头则 `template.uri(<剥离前缀后的 url>)`（确保结果以 `/` 开头）。使用 `feign.RequestInterceptor` 与 `feign.RequestTemplate`（spring-cloud-starter-openfeign 2.5.x 兼容版自带，Phase 2 已在栈中）。注意：cfca 契约（com.spt.sign.client.remote）的 path 不以 `spt-bas-server/` 开头，因此此 interceptor 对 cfca 无副作用。**若此方案最终不可行**（Wave 4 验收暴露 404），在 SUMMARY 记录 fallback 探索（servlet URL rewrite filter / 单设 context-path + Shiro 链再验证），不擅自改 238 契约。

    (4) **pom 加 rocketmq starter**：在 `zgbas-system/pom.xml` 加 `<dependency><groupId>org.apache.rocketmq</groupId><artifactId>rocketmq-spring-boot-starter</artifactId><version>2.2.2</version></dependency>`（版本 verbatim 取自源 basCore/pom.xml，D-P2-08 锁旧版本原则）。

    (5) **yml 加 spt.bas.server.url + rocketmq.***：
    - `application-dev.yml`: 加 `spt.bas.server.url: http://localhost:8080`（自回环关键）+ `rocketmq.name-server: 47.104.66.178:9876`（照搬源 dev properties L22）+ `rocketmq.producer.group: contract_producer_group` + `rocketmq.producer.access-key: ${ROCKETMQ_AK:zgrocketmq}` + `rocketmq.producer.secret-key: ${ROCKETMQ_SK:zg12345678}`（dev 可默认值，便于本地启动）。
    - `application-prod.yml`: 同上但 `rocketmq.name-server: ${ROCKETMQ_NAMESERVER}`（无默认，强制 prod 外置）+ AK/SK 同 `${ROCKETMQ_AK}` / `${ROCKETMQ_SK}`（无默认，D-P2-13 prod 强外置）。

    完成后跑 compile gate + 现有 ZgbasApplicationTest（14 test 不应回归）。
  </action>
  <verify>
    <automated>
cd /Users/alan/WorkSpace/IDEA/zgbas-plus && \
echo "=== FeignClients basePackages 放宽 ===" && \
grep -c 'com.spt.bas.client.remote' zgbas-admin/src/main/java/com/spt/ZgbasApplication.java && \
echo "=== BasClientConfig 就位 ===" && \
test -f zgbas-system/src/main/java/com/spt/bas/client/config/BasClientConfig.java && \
grep -c 'BasConstants.SERVER_BEAN_NAME' zgbas-system/src/main/java/com/spt/bas/client/config/BasClientConfig.java && \
echo "=== BasFeignPathConfig 就位 ===" && \
grep -c 'RequestInterceptor' zgbas-system/src/main/java/com/spt/bas/client/config/BasFeignPathConfig.java && \
echo "=== rocketmq starter ===" && \
grep -c 'rocketmq-spring-boot-starter' zgbas-system/pom.xml && \
echo "=== yml spt.bas.server.url + rocketmq ===" && \
grep -c 'spt.bas.server.url\|http://localhost:8080' zgbas-admin/src/main/resources/application-dev.yml && \
grep -c 'ROCKETMQ_NAMESERVER\|ROCKETMQ_AK\|ROCKETMQ_SK' zgbas-admin/src/main/resources/application-prod.yml && \
echo "=== compile gate (全模块) ===" && \
JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home \
/Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml \
-am compile 2>&1 | tee /tmp/p4-01-t1.log | tail -5 && \
echo "ERROR_COUNT=$(grep -c '^\[ERROR\]' /tmp/p4-01-t1.log)" && \
echo "CANNOT_FIND=$(grep -cE 'cannot find symbol|找不到符号' /tmp/p4-01-t1.log)"
    </automated>
  </verify>
  <done>
    - ZgbasApplication.java grep `com.spt.bas.client.remote` ≥ 1
    - BasClientConfig.java 文件存在且含 `BasConstants.SERVER_BEAN_NAME`
    - BasFeignPathConfig.java 含 `RequestInterceptor` 类型 bean
    - zgbas-system/pom.xml 含 `rocketmq-spring-boot-starter` 且 version=2.2.2
    - application-dev.yml 含 `spt.bas.server.url: http://localhost:8080` + rocketmq.* 4 项
    - application-prod.yml 含 `${ROCKETMQ_NAMESERVER}` / `${ROCKETMQ_AK}` / `${ROCKETMQ_SK}` 3 占位
    - 全模块 `mvn -am compile` ERROR_COUNT=0 且 CANNOT_FIND=0
    - Phase 3 已验 14 test 不回归（Task 2 verify 闭环）
  </done>
</task>

<task type="auto">
  <name>Task 2: WR-02 验收脚手架（ZgbasApplicationTest 占位扩展）</name>
  <files>
    zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java
  </files>
  <read_first>
    - .planning/phases/04-core-business/04-PATTERNS.md §WR-02 验收扩展（endpoint reachability + BFF bean 抽样蓝本）
    - .planning/phases/04-core-business/04-RESEARCH.md §D-P4-06 Verification Assets + §Code Examples Example 2
    - .planning/phases/04-core-business/04-VALIDATION.md §Requirement → Test Map（BIZ-02/BIZ-03/WR-02 行）
    - 当前 ZgbasApplicationTest 全文: /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java（重点看 loginEndpointReachable / indexEndpointReachable / loginControllerRegistered 现有风格）
  </read_first>
  <action>
    扩展现有 ZgbasApplicationTest（14 @Test）新增 4 个占位测试方法，本 plan 先建测试壳，业务端点 / BFF bean 由 Wave 3/4 实装后自然激活（@Disabled 防 Wave 0 阶段失败）：

    (1) `basContractEndpointReachable_applyBrand_findAll`（BIZ-03 / WR-02）：@Disabled("Wave 3: ApplyBrandApi 落位后启用")。body 写 `restTemplate.postForEntity("/apply/brand/findAll", null, String.class)`，断言 `is2xxSuccessful || is3xxRedirection || statusCode==401`。代表合同域。

    (2) `basContractEndpointReachable_ctrContract_findPage`（BIZ-03 / WR-02）：@Disabled("Wave 3")。断言同上，端点 `/ctr/contract/findPage`（授信/合同）。

    (3) `basContractEndpointReachable_stockDetail_findAll`（BIZ-03 / WR-02）：@Disabled("Wave 3")。断言同上，端点 `/stock/detail/findAll`（库存）。

    (4) `bffControllersRegistered_sample`（BIZ-02）：@Disabled("Wave 4: BFF 落位后启用")。抽样 `context.containsBean("applyBrandController")` + 2-3 个其它代表性 BFF bean（如 ctrContractController / stockDetailController）。

    断言风格沿用现有 loginEndpointReachable：`assertThat(response.getStatusCode().is2xxSuccessful() || response.getStatusCode().is3xxRedirection() || response.getStatusCodeValue() == 401).isTrue();`（参考 PATTERNS.md §WR-02 蓝本）。

    所有 4 个 test 加 @Disabled 注释（org.junit.SkipIf 或 JUnit 5 @Disabled("reason")，匹配现有 test 框架）——确保 Wave 0 阶段 `mvn test` 不因业务端点未就位而失败，且 Wave 3/4 实装后仅需移除 @Disabled 即激活。

    跑 `mvn test -Dtest=ZgbasApplicationTest`（前置 export DB_PASSWORD + SPT_APP_SECRET）确认 14 旧 test 仍绿 + 4 新 @Disabled test 跳过。
  </action>
  <verify>
    <automated>
export DB_PASSWORD="${DB_PASSWORD:-dev_dummy}" && \
export SPT_APP_SECRET="${SPT_APP_SECRET:-dev_dummy}" && \
cd /Users/alan/WorkSpace/IDEA/zgbas-plus && \
echo "=== ZgbasApplicationTest 占位测试方法数 ===" && \
grep -cE 'void (basContractEndpointReachable|bffControllersRegistered)' zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java && \
echo "=== @Disabled 标注数 ===" && \
grep -c '@Disabled' zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java && \
echo "=== 启动验证（非 hermetic, local-export = passing）===" && \
JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home \
/Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml \
-pl zgbas-admin -am test -Dtest=ZgbasApplicationTest 2>&1 | tee /tmp/p4-01-t2.log | tail -15 && \
echo "ERROR_COUNT=$(grep -c '^\[ERROR\]' /tmp/p4-01-t2.log)" && \
echo "TEST_FAIL=$(grep -cE 'FAIL|FAILURE' /tmp/p4-01-t2.log)"
    </automated>
  </verify>
  <done>
    - ZgbasApplicationTest 含 4 个新测试方法（3 endpoint reachability + 1 BFF bean 抽样）
    - 4 个新方法全部 @Disabled（Wave 3/4 落位后移除）
    - 现有 14 test 全绿（无回归）
    - `mvn test -Dtest=ZgbasApplicationTest` ERROR_COUNT=0 + TEST_FAIL=0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Feign proxy → 本进程 8080 | 238 bas 契约经 Feign 自回环调用本进程 Spring MVC；仅 localhost，不跨进程 |
| 客户端 → BFF（Phase 3 Shiro 链已就位） | 本 plan 不新增端点，沿用 Phase 3 Shiro `/login`/`/index` 根路径链 |
| RocketMQ 配置 → broker | yml 占位外置 AK/SK；启动期不连 broker（懒连接） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-S | Spoofing | Feign 自回环调用 | accept | 仅 localhost:8080 自跳，不暴露新攻击面；同进程鉴权沿用 Phase 3 Shiro 链 |
| T-04-01-I | Info Disclosure | rocketmq 凭证 | mitigate | prod profile 走 `${ROCKETMQ_AK}` / `${ROCKETMQ_SK}` / `${ROCKETMQ_NAMESERVER}` 占位外置（D-P2-13），dev 默认值仅本地 |
| T-04-02-T | Tampering | BasFeignPathConfig URL rewrite | accept | interceptor 仅剥离硬编码 `spt-bas-server/` 前缀，不做其它 URL 变换；作用域限于本进程 |
| T-04-SC | Tampering | Maven installs（rocketmq-spring-boot-starter:2.2.2） | mitigate | 包来自 Apache 官方（github.com/apache/rocketmq-spring）+ 版本 verbatim 取自源 basCore/pom.xml；[ASSUMED] 标签（slopcheck 未装）；若 planner 复核发现可疑改 checkpoint:human-verify |
| T-04-01-D | DoS | Feign 自回环递归 | accept | Spring MVC 不接受 Feign proxy 触发的请求再次进入相同 FeignClient（proxy 走 HTTP，controller 走 bean），无递归路径 |

无 high 级威胁。所有 Mitigation 在本 plan task action 中已落地；Accept 项有充分理由（localhost / 硬编码前缀 / Apache 官方包）。
</threat_model>

<verification>
- 全模块 `mvn -am compile` 零 ERROR（grep `^\[ERROR\]` locale 无关）
- ZgbasApplicationTest 现有 14 test 全绿（无 Phase 3 回归）+ 4 新 @Disabled 占位测试存在
- pom/yml/ZgbasApplication/BasClientConfig/BasFeignPathConfig 5 处 grep 断言见 Task 1/2 verify
</verification>

<success_criteria>
- D-P4-01 方案 A 接线全要素就位（放宽 basePackages + basServerConfig bean + spt.bas.server.url + path 前缀剥离机制）
- D-P4-04 rocketmq starter 在 pom + rocketmq.* 在 dev/prod yml（占位外置）
- WR-02 占位测试建好（Wave 3/4 移除 @Disabled 后激活）
- Phase 3 AUTH-03 已验的 14 test 不回归（无 context-path 变更）
</success_criteria>

<output>
Create `.planning/phases/04-core-business/04-01-SUMMARY.md` when done.
</output>
