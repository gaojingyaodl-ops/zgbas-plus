---
plan_id: 04-01
phase: 4
wave: 1
title: pom 依赖声明 + application-dev.yml 配置
objective: 在 zgbas-system/pom.xml 声明 Redis 和 weixin-java-miniapp 依赖，在 application-dev.yml 补充 wx.miniapp 和 jwt.config 配置块
depends_on: []
files_modified:
  - zgbas-system/pom.xml
  - zgbas-admin/src/main/resources/application-dev.yml
autonomous: true
requirements_addressed:
  - WX-SERVICE-02
  - WX-SERVICE-03

must_haves:
  truths:
    - "zgbas-system/pom.xml 含 spring-boot-starter-data-redis（无版本，BOM 管理）"
    - "zgbas-system/pom.xml 含 weixin-java-miniapp:3.8.0（显式版本）"
    - "application-dev.yml 含 wx.miniapp.configs[0].* 配置块（appid/secret/token/aesKey/msgDataFormat）"
    - "application-dev.yml 含 jwt.config.key / jwt.config.ttl / jwt.config.remember 配置项"
  artifacts:
    - path: "zgbas-system/pom.xml"
      provides: "Redis + WeChat SDK 依赖"
      contains: "spring-boot-starter-data-redis"
    - path: "zgbas-admin/src/main/resources/application-dev.yml"
      provides: "WX SDK + JWT 运行时配置"
      contains: "wx.miniapp.configs"
  key_links:
    - from: "zgbas-system/pom.xml"
      to: "Spring Boot BOM"
      via: "无版本声明 spring-boot-starter-data-redis"
      pattern: "spring-boot-starter-data-redis"
    - from: "application-dev.yml"
      to: "WxMiniAppConfig.java"
      via: "@ConfigurationProperties(prefix = \"wx.miniapp\")"
      pattern: "wx\\.miniapp\\.configs"
---

<objective>
为 Phase 4 的 Java 类迁移做好前置：在 zgbas-system pom 中声明两个新依赖（Redis starter + WeChat miniapp SDK），在 application-dev.yml 中追加微信小程序配置块和 JWT 配置块。Wave 1 纯配置变更，不涉及 Java 源码，Wave 2 的类迁移可在此之后并行执行。

Purpose: 没有 pom 依赖，Wave 2 的 RedisConfig / WxConfiguration 无法编译；没有 yml 配置，WxMiniAppConfig @PostConstruct 初始化会抛 IllegalArgumentException。
Output: 更新后的 zgbas-system/pom.xml 和 application-dev.yml。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-sdk/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: zgbas-system/pom.xml — 追加 Redis 和 weixin-java-miniapp 依赖</name>
  <files>zgbas-system/pom.xml</files>
  <read_first>
    - zgbas-system/pom.xml — 读取现有 dependencies 块，找到合适插入位置（easyexcel/cglib 块之后，结束标签前）
    - .planning/phases/04-sdk/04-CONTEXT.md — D-03（Redis 声明规则）、D-11（miniapp 版本 3.8.0）
  </read_first>
  <action>
    在 zgbas-system/pom.xml 的 dependencies 块末尾（cglib 依赖之后，&lt;/dependencies&gt; 前）追加两个依赖：

    1. spring-boot-starter-data-redis：groupId=org.springframework.boot，artifactId=spring-boot-starter-data-redis，无 version 标签（由 spring-boot-starter-parent 2.5.9 BOM 管理，per D-03）。注释标注 "Phase 4 D-03: Redis starter, version managed by Spring Boot BOM"。

    2. weixin-java-miniapp：groupId=com.github.binarywang，artifactId=weixin-java-miniapp，version=3.8.0（显式版本，非 BOM，per D-11）。注释标注 "Phase 4 D-11: WeChat miniapp SDK, explicit version 3.8.0"。

    不修改已有依赖。不添加 &lt;dependencyManagement&gt; 条目。
  </action>
  <verify>
    <automated>JAVA_HOME=/Library/Java/JavaVirtualMachines/amazon-corretto-8.jdk/Contents/Home mvn dependency:resolve -pl zgbas-system --settings /Users/alan/App/apache-maven-3.8.6/zg_settings.xml -q 2>&amp;1 | grep -E "^\[ERROR\]" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - zgbas-system/pom.xml 包含 artifactId=spring-boot-starter-data-redis 且无 version 子标签
    - zgbas-system/pom.xml 包含 artifactId=weixin-java-miniapp 且 version=3.8.0
    - mvn dependency:resolve -pl zgbas-system 无 [ERROR] 行
  </acceptance_criteria>
  <done>两个依赖可被 Maven 解析，无 [ERROR]</done>
</task>

<task type="auto">
  <name>Task 2: application-dev.yml — 追加 wx.miniapp 和 jwt.config 配置块</name>
  <files>zgbas-admin/src/main/resources/application-dev.yml</files>
  <read_first>
    - zgbas-admin/src/main/resources/application-dev.yml — 读取现有内容，确认 spring.redis 块已存在（host/port/password），确认文件末尾当前最后一行（追加位置）
    - /Users/alan/WorkSpace/IDEA/zgbas/basWx/purchase-server/src/main/resources/application.properties — 参考 wx.miniapp.configs[0].* 和 jwt.config.* 原始值
    - .planning/phases/04-sdk/04-CONTEXT.md — D-01（Redis 已有，不重复写）、D-10（wx.miniapp 配置落点）、D-07 中说明 jwt.config 原始值
  </read_first>
  <action>
    在 application-dev.yml 文件末尾追加两个配置块（不修改已有任何 key）：

    块1 — wx.miniapp（per D-10）：
    - wx.miniapp.configs[0].appid=wxdfdcede72b54ff25
    - wx.miniapp.configs[0].secret=1a4310766e9f32b90ca55c0af688c17e
    - wx.miniapp.configs[0].token=123
    - wx.miniapp.configs[0].aesKey=123
    - wx.miniapp.configs[0].msgDataFormat=JSON
    YAML 格式：wx: miniapp: configs: 列表形式（使用 - 项），保持与现有 yml 缩进风格一致。
    头部注释：# Phase 4 D-10: WeChat miniapp SDK config（WxMiniAppConfig @ConfigurationProperties）

    块2 — jwt.config（per D-05/D-06 JwtConfig @ConfigurationProperties）：
    - jwt.config.key=sgcoding
    - jwt.config.ttl=600000
    - jwt.config.remember=604800000
    YAML 格式：jwt: config: key/ttl/remember。
    头部注释：# Phase 4: JWT config（JwtConfig @ConfigurationProperties prefix=jwt.config）

    注意：spring.redis.* 已存在于文件中（host: 47.104.15.98 / port: 6379 / password: zg123456 / database: 5），不重复添加。
  </action>
  <verify>
    <automated>grep -c "wx.miniapp\|wx:" /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-admin/src/main/resources/application-dev.yml</automated>
  </verify>
  <acceptance_criteria>
    - application-dev.yml 包含 appid: wxdfdcede72b54ff25 行
    - application-dev.yml 包含 jwt: 和 key: sgcoding 行
    - application-dev.yml 不含重复的 spring.redis.host 声明（已有的不变）
    - YAML 文件可被 snakeyaml 解析无异常（后续 mvn compile 会间接验证）
  </acceptance_criteria>
  <done>wx.miniapp.configs[0].appid 和 jwt.config.key 两个 key 均可在 yml 中 grep 到</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pom.xml → Maven Central/私服 | weixin-java-miniapp:3.8.0 从私服或中央仓库拉取 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | weixin-java-miniapp:3.8.0 jar | mitigate | 版本已固定 3.8.0，从 zg_settings.xml 指向的内网私服拉取，与 Maven checksum 校验一致 |
| T-04-02 | Information Disclosure | application-dev.yml 明文 wx.miniapp.secret | accept | Phase 4 明文密钥决策（用户选 option3，D-P4 明文密钥政策），dev 配置，非 prod |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | 本计划仅涉及 Maven pom 变更，无 npm/pip/cargo 操作 |
</threat_model>

<verification>
执行以下命令验证 Wave 1 完成：

```bash
JAVA_HOME=/Library/Java/JavaVirtualMachines/amazon-corretto-8.jdk/Contents/Home \
  mvn compile -pl zgbas-system \
  --settings /Users/alan/App/apache-maven-3.8.6/zg_settings.xml \
  2>&1 | grep -E "^\[ERROR\]"
```

期望：无 [ERROR] 行（此时 RedisConfig 等 Java 类还不存在，但 pom 依赖解析应成功）。
</verification>

<success_criteria>
1. zgbas-system/pom.xml 包含 spring-boot-starter-data-redis（无 version）+ weixin-java-miniapp:3.8.0
2. application-dev.yml 包含 wx.miniapp.configs[0].appid=wxdfdcede72b54ff25 + jwt.config.key=sgcoding
3. mvn dependency:resolve -pl zgbas-system 无 [ERROR]
</success_criteria>

<output>
Create `.planning/phases/04-sdk/04-01-SUMMARY.md` when done
</output>
