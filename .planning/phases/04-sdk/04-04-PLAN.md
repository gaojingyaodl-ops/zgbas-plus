---
plan_id: 04-04
phase: 4
wave: 3
title: JWT 认证体系迁入（JwtConfig + JwtUtil + JwtAuthenticationFilter + WxSecurityConfig + 辅助 stubs）
objective: 迁入 JwtConfig/JwtUtil/JwtAuthenticationFilter（改造去 @Component + CustomConfig），创建 WxSecurityConfig 用 FilterRegistrationBean 注册 /wx/* 路径，使 JwtConfig bean 可注册且 JWT filter 不干扰 Shiro session 路径
depends_on:
  - 04-01
  - 04-03
files_modified:
  - zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/config/JwtConfig.java
  - zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/util/JwtUtil.java
  - zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/config/JwtAuthenticationFilter.java
  - zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/config/WxSecurityConfig.java
  - zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/exception/SecurityException.java
  - zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/util/ResponseUtil.java
  - zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/util/UserContext.java
  - zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/vo/UserInfoVo.java
  - zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/service/impl/WxAccessTokenService.java
autonomous: true
requirements_addressed:
  - WX-SERVICE-03

must_haves:
  truths:
    - "JwtConfig bean 可通过 ApplicationContext.getBean(JwtConfig.class) 取到，无 BeanCreationException"
    - "JwtAuthenticationFilter 通过 FilterRegistrationBean 注册，urlPatterns 限定为 /wx/* /ewechat/* /axq/*"
    - "/login 和 /index 路径不经过 JwtAuthenticationFilter（Shiro session 路径不受干扰）"
    - "JwtUtil @Configuration bean 可创建（WxAccessTokenService stub @Component 已注册）"
  artifacts:
    - path: "zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/config/JwtConfig.java"
      provides: "JWT 配置属性绑定"
      contains: "@ConfigurationProperties(prefix = \"jwt.config\")"
    - path: "zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/config/WxSecurityConfig.java"
      provides: "FilterRegistrationBean 注册 JwtAuthenticationFilter"
      contains: "FilterRegistrationBean"
    - path: "zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/config/JwtAuthenticationFilter.java"
      provides: "JWT OncePerRequestFilter（已改造，无 @Component，checkIgnores() 直接返回 false）"
      contains: "extends OncePerRequestFilter"
    - path: "zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/service/impl/WxAccessTokenService.java"
      provides: "WxAccessToken service stub（Phase 5 替换为完整实现）"
      contains: "@Component"
  key_links:
    - from: "WxSecurityConfig.java"
      to: "JwtAuthenticationFilter.java"
      via: "new FilterRegistrationBean<>(new JwtAuthenticationFilter())"
      pattern: "FilterRegistrationBean"
    - from: "JwtUtil.java"
      to: "com.spt.tools.http.util.TokenUtil"
      via: "TokenUtil.parseJWT(jwt, jwtConfig.getKey()) / TokenUtil.createToken(...)"
      pattern: "TokenUtil"
    - from: "WxSecurityConfig.java"
      to: "Shiro filter chain"
      via: "FilterRegistrationBean urlPatterns 限定，不覆盖 /login /index（Shiro anon 不受干扰）"
      pattern: "urlPatterns.*wx"
---

<objective>
完成 Phase 4 JWT 认证体系迁入：JwtConfig（@ConfigurationProperties）、JwtUtil（委托 TokenUtil，无 API 改动）、JwtAuthenticationFilter（删除 @Component + CustomConfig + initIgnores/@PostConstruct，checkIgnores() 直接返回 false）、WxSecurityConfig（新建，用 FilterRegistrationBean 注册 filter 至 /wx/* /ewechat/* /axq/*）。同时创建辅助 stubs（SecurityException/ResponseUtil/UserContext/UserInfoVo/WxAccessTokenService），使 JwtUtil 和 JwtAuthenticationFilter 可完整编译和 bean 注册。

Purpose: JwtConfig bean + JwtAuthenticationFilter 注册是 Phase 4 成功标准 SC-3/SC-4 的核心；辅助 stubs 在 Phase 5 全量 service 迁入后替换为完整实现。
Output: JWT 体系全部可编译，JwtConfig bean 可注入，JwtAuthenticationFilter 通过 FilterRegistrationBean 限定路径。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-sdk/04-CONTEXT.md

<interfaces>
<!-- JwtAuthenticationFilter 改造要点（per D-06/D-07） -->
源文件位置：/Users/alan/WorkSpace/IDEA/zgbas/basWx/purchase-server/src/main/java/com/spt/bas/purchase/wx/server/config/JwtAuthenticationFilter.java

改造内容（以下均需从源码中删除）：
1. 删除类级别 @Component 注解
2. 删除字段：private static Set&lt;String&gt; ignores = new HashSet&lt;&gt;()
3. 删除字段：@Autowired private CustomConfig customConfig
4. 删除整个 initIgnores() 方法（含 @PostConstruct 注解）
5. 将 checkIgnores(HttpServletRequest request) 方法体改为直接 return false（方法签名保留）

保留内容（不改动）：
- @Slf4j 注解
- @Autowired JwtUtil jwtUtil
- @Autowired CompanyUserDao companyUserDao
- @Autowired UserDetailDao userDetailDao
- doFilterInternal() 完整方法体（含 JWT 解析 + UserContext.setUser + filterChain.doFilter 逻辑）

<!-- WxSecurityConfig 新文件规格（per D-06） -->
- package: com.spt.bas.purchase.wx.server.config
- 注解：@Configuration @EnableConfigurationProperties(JwtConfig.class)
- Bean 方法：FilterRegistrationBean&lt;JwtAuthenticationFilter&gt; jwtAuthenticationFilter(JwtUtil jwtUtil, ...)
  - new FilterRegistrationBean&lt;&gt;(new JwtAuthenticationFilter())
  - registration.addUrlPatterns("/wx/*", "/ewechat/*", "/axq/*")
  - registration.setOrder(1)
  - 返回 registration
- 注入 JwtUtil 并调用 ReflectionTestUtils 或直接通过 setter 注入（WxSecurityConfig 可直接 new JwtAuthenticationFilter() 后通过 @Autowired 让 Spring 注入其字段，因为 FilterRegistrationBean 不阻止 Spring 管理 filter 内部依赖）
  实现方式：直接 new FilterRegistrationBean&lt;&gt;(jwtAuthenticationFilter) 其中 JwtAuthenticationFilter 通过 @Autowired 参数注入即可（Spring 会先创建 JwtAuthenticationFilter 作为普通 bean，只要 WxSecurityConfig 把它作为方法参数接收）。

<!-- WxAccessTokenService stub 规格 -->
- package: com.spt.bas.purchase.wx.server.service.impl
- 注解：@Component @Transactional(readOnly = true)（per 源码）
- 照搬源码：extends BaseService&lt;WxAccessToken&gt;，implements IWxAccessTokenService
- IWxAccessTokenService 接口也需创建（package com.spt.bas.purchase.wx.server.service）
  包含：findByUserid(Long userId) / deleteByUserid(String userId)
- WxAccessTokenService 照搬源码（含 @Autowired WxAccessTokenDao + findByUserid/deleteByUserid 方法）
- BaseService 来自内联的 com.spt.tools.jpa.service.BaseService（zgbas-common 已内联）

<!-- 其他 stubs 来源文件 -->
- SecurityException：照搬 /Users/alan/WorkSpace/IDEA/zgbas/basWx/purchase-server/src/main/java/com/spt/bas/purchase/wx/server/exception/SecurityException.java
- ResponseUtil：照搬 /Users/alan/WorkSpace/IDEA/zgbas/basWx/purchase-server/src/main/java/com/spt/bas/purchase/wx/server/util/ResponseUtil.java（依赖 ApiResult.ofStatus，04-03 已创建）
- UserContext：照搬 /Users/alan/WorkSpace/IDEA/zgbas/basWx/purchase-server/src/main/java/com/spt/bas/purchase/wx/server/util/UserContext.java（ThreadLocal&lt;UserInfoVo&gt;）
- UserInfoVo：照搬 /Users/alan/WorkSpace/IDEA/zgbas/basWx/purchase-server/src/main/java/com/spt/bas/purchase/wx/server/vo/UserInfoVo.java（@Data @Builder）
  注意：UserInfoVo 依赖 io.swagger.annotations，处理同 ApiResult（如 swagger 不可编译则注释掉 swagger 行）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建辅助 stubs（SecurityException / ResponseUtil / UserContext / UserInfoVo / WxAccessTokenService + IWxAccessTokenService）</name>
  <files>
    zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/exception/SecurityException.java,
    zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/util/ResponseUtil.java,
    zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/util/UserContext.java,
    zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/vo/UserInfoVo.java,
    zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/service/IWxAccessTokenService.java,
    zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/service/impl/WxAccessTokenService.java
  </files>
  <read_first>
    - /Users/alan/WorkSpace/IDEA/zgbas/basWx/purchase-server/src/main/java/com/spt/bas/purchase/wx/server/exception/SecurityException.java
    - /Users/alan/WorkSpace/IDEA/zgbas/basWx/purchase-server/src/main/java/com/spt/bas/purchase/wx/server/util/ResponseUtil.java
    - /Users/alan/WorkSpace/IDEA/zgbas/basWx/purchase-server/src/main/java/com/spt/bas/purchase/wx/server/util/UserContext.java
    - /Users/alan/WorkSpace/IDEA/zgbas/basWx/purchase-server/src/main/java/com/spt/bas/purchase/wx/server/vo/UserInfoVo.java
    - /Users/alan/WorkSpace/IDEA/zgbas/basWx/purchase-server/src/main/java/com/spt/bas/purchase/wx/server/service/IWxAccessTokenService.java
    - /Users/alan/WorkSpace/IDEA/zgbas/basWx/purchase-server/src/main/java/com/spt/bas/purchase/wx/server/service/impl/WxAccessTokenService.java
    - zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/common/BaseException.java — 确认 04-03 已创建，SecurityException 继承它
    - zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/common/ApiResult.java — 确认 ofStatus 方法存在，ResponseUtil 依赖它
  </read_first>
  <action>
    在 zgbas-system 对应包路径下创建以下 6 个文件，每个文件首行注释加 "// Phase 4 stub — Phase 5 will overlay with complete source version"：

    1. exception/SecurityException.java — 照搬源码，extends BaseException，package com.spt.bas.purchase.wx.server.exception

    2. util/ResponseUtil.java — 照搬源码，package com.spt.bas.purchase.wx.server.util。
       依赖 ApiResult.ofStatus（04-03 Task 1 已创建）。
       依赖 Status（04-03 Task 1 已创建）。
       如果 io.swagger.annotations 无法解析，注释掉 swagger import 行。

    3. util/UserContext.java — 照搬源码，ThreadLocal&lt;UserInfoVo&gt;，package com.spt.bas.purchase.wx.server.util。
       依赖 UserInfoVo（本 Task 中创建）。
       源码还引用 CompanyVo（通过 getter 返回），如 CompanyVo 不存在则注释掉相关方法，保留 setUser/getUser/removeUser 核心方法。

    4. vo/UserInfoVo.java — 照搬源码，@Data @Builder，package com.spt.bas.purchase.wx.server.vo。
       如果 io.swagger.annotations 无法解析，注释掉 swagger 相关 import 和注解行，保留字段和 @Data @Builder。

    5. service/IWxAccessTokenService.java — 照搬源码，package com.spt.bas.purchase.wx.server.service。
       继承 IBaseService&lt;WxAccessToken&gt;（com.spt.tools.jpa.service.IBaseService，已内联到 zgbas-common）。
       方法：findByUserid(Long userId) 和 deleteByUserid(String userId)。

    6. service/impl/WxAccessTokenService.java — 照搬源码，@Component @Transactional(readOnly=true)，extends BaseService&lt;WxAccessToken&gt;（com.spt.tools.jpa.service.BaseService，已内联），implements IWxAccessTokenService。
       @Autowired WxAccessTokenDao（Phase 3 已迁入）。
       findByUserid 和 deleteByUserid 方法照搬实现。

    创建顺序：UserInfoVo → UserContext → SecurityException → ResponseUtil → IWxAccessTokenService → WxAccessTokenService。
  </action>
  <verify>
    <automated>ls /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/exception/ /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/vo/ /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/service/</automated>
  </verify>
  <acceptance_criteria>
    - exception/SecurityException.java 存在，package com.spt.bas.purchase.wx.server.exception
    - util/ResponseUtil.java 存在，含 renderJson(HttpServletResponse, Status, Object) 方法签名
    - util/UserContext.java 存在，含 setUser(UserInfoVo) 和 getUser() 方法
    - vo/UserInfoVo.java 存在，含 @Builder 注解，含 userId / phone / companyId 字段
    - service/IWxAccessTokenService.java 存在，含 findByUserid(Long) 方法
    - service/impl/WxAccessTokenService.java 存在，含 @Component 注解
  </acceptance_criteria>
  <done>6 个辅助 stub 文件全部创建完成</done>
</task>

<task type="auto">
  <name>Task 2: 创建 JwtConfig + JwtUtil + JwtAuthenticationFilter（改造）+ WxSecurityConfig，验证编译</name>
  <files>
    zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/config/JwtConfig.java,
    zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/util/JwtUtil.java,
    zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/config/JwtAuthenticationFilter.java,
    zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/config/WxSecurityConfig.java
  </files>
  <read_first>
    - /Users/alan/WorkSpace/IDEA/zgbas/basWx/purchase-server/src/main/java/com/spt/bas/purchase/wx/server/config/JwtConfig.java — 照搬
    - /Users/alan/WorkSpace/IDEA/zgbas/basWx/purchase-server/src/main/java/com/spt/bas/purchase/wx/server/util/JwtUtil.java — 照搬（委托 TokenUtil 部分已存在，per D-04/D-05）
    - /Users/alan/WorkSpace/IDEA/zgbas/basWx/purchase-server/src/main/java/com/spt/bas/purchase/wx/server/config/JwtAuthenticationFilter.java — 需按 D-06/D-07 改造
    - zgbas-common/src/main/java/com/spt/tools/http/util/TokenUtil.java — 确认 parseJWT/createToken 方法签名（JwtUtil 委托对象）
    - .planning/phases/04-sdk/04-CONTEXT.md — D-06（Filter 不加 @Component）、D-07（删 CustomConfig，checkIgnores return false）、D-08（Shiro /wx/**=anon 已就绪）
  </read_first>
  <action>
    创建 4 个文件：

    1. config/JwtConfig.java — 完整照搬源码，@Data @ConfigurationProperties(prefix = "jwt.config")，字段 key/ttl/remember。
       package com.spt.bas.purchase.wx.server.config。

    2. util/JwtUtil.java — 照搬源码，package com.spt.bas.purchase.wx.server.util。
       @EnableConfigurationProperties(JwtConfig.class) @Configuration @Slf4j。
       @Autowired JwtConfig jwtConfig + @Autowired WxAccessTokenService wxAccessTokenService（Task 1 已创建 stub）。
       所有方法（createJWT/parseJWT/getUsernameFromJWT/getUseridFromJWT/invildJWT/refreshJWT/getJwtFromRequest/getJwtFromRequestBody/getUseridFromRequest/getUseridFromJwt）完整照搬。
       import com.spt.tools.http.util.TokenUtil（TokenUtil.parseJWT/createToken 委托方法）。
       import io.jsonwebtoken.Claims 等 jjwt 0.7.0 API（通过 zgbas-common 内联传递可用）。

    3. config/JwtAuthenticationFilter.java — 照搬源码后执行以下精确改造（per D-06/D-07）：
       删除：类级别 @Component 注解
       删除：import org.springframework.stereotype.Component
       删除：字段 private static Set&lt;String&gt; ignores = new HashSet&lt;&gt;()（含 import java.util.HashSet 和 java.util.Set）
       删除：字段 @Autowired private CustomConfig customConfig（含 import 行）
       删除：整个 initIgnores() 方法体（含方法声明 @PostConstruct 和所有内容）
       删除：import javax.annotation.PostConstruct
       修改：checkIgnores(HttpServletRequest request) 方法体改为只含 return false；
       保留：@Slf4j + @Autowired JwtUtil + @Autowired CompanyUserDao + @Autowired UserDetailDao
       保留：doFilterInternal() 完整方法体不变

    4. config/WxSecurityConfig.java — 新建文件，package com.spt.bas.purchase.wx.server.config：
       注解：@Configuration @EnableConfigurationProperties(JwtConfig.class)
       Bean 方法签名：public FilterRegistrationBean&lt;JwtAuthenticationFilter&gt; jwtAuthenticationFilter(JwtAuthenticationFilter filter)
       方法体：创建 new FilterRegistrationBean&lt;&gt;(filter)，调用 addUrlPatterns("/wx/*", "/ewechat/*", "/axq/*")，setOrder(1)，返回 registration。
       @Autowired 参数 JwtAuthenticationFilter filter（JwtAuthenticationFilter 没有 @Component，须在此 @Bean 方法中注册它）。
       修正方案：改为在 WxSecurityConfig 中用 @Bean 方法声明 JwtAuthenticationFilter，然后用另一个 @Bean 方法注册 FilterRegistrationBean：
         @Bean public JwtAuthenticationFilter jwtAuthenticationFilter() { return new JwtAuthenticationFilter(); }
         @Bean public FilterRegistrationBean&lt;JwtAuthenticationFilter&gt; jwtFilterRegistration(JwtAuthenticationFilter filter) { ... addUrlPatterns("/wx/*","/ewechat/*","/axq/*"); setOrder(1); return registration; }
       (Spring 会注入 JwtUtil/CompanyUserDao/UserDetailDao 到 JwtAuthenticationFilter 的 @Autowired 字段，因为它是由 Spring 管理的 bean)

    创建完 4 个文件后，执行全量编译验证（-pl zgbas-system -am 确保 common/framework 也参与）。
  </action>
  <verify>
    <automated>JAVA_HOME=/Library/Java/JavaVirtualMachines/amazon-corretto-8.jdk/Contents/Home mvn compile -pl zgbas-system -am --settings /Users/alan/App/apache-maven-3.8.6/zg_settings.xml 2>&amp;1 | grep -E "^\[ERROR\]" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - config/JwtConfig.java 存在，含 @ConfigurationProperties(prefix = "jwt.config") 和 key/ttl/remember 字段
    - util/JwtUtil.java 存在，含 @Configuration 注解，含 parseJWT(String jwt) 方法
    - config/JwtAuthenticationFilter.java 不含 @Component 注解，不含 CustomConfig 字段，checkIgnores() 方法体只有 return false
    - config/WxSecurityConfig.java 存在，含 FilterRegistrationBean 和 addUrlPatterns("/wx/*") 调用
    - mvn compile -pl zgbas-system -am 输出无 [ERROR] 行
  </acceptance_criteria>
  <done>JWT 体系 4 个文件全部创建/改造完成，zgbas-system 含上游模块全量编译零错</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /wx/* → JwtAuthenticationFilter | 所有 WX 路径请求经过 JWT 验证 |
| JwtAuthenticationFilter → Shiro | JWT filter 先于 Shiro filter 执行（order=1 低数字先执行） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Spoofing | JwtAuthenticationFilter JWT 解析 | mitigate | TokenUtil.parseJWT 用 HS256 签名验证，key=sgcoding（dev 值）；prod 应外置 key |
| T-04-08 | Elevation of Privilege | FilterRegistrationBean 路径限定 | mitigate | urlPatterns 明确限定 /wx/* /ewechat/* /axq/*，Shiro session 路径（/login /index）不在 pattern 内，不受干扰（per D-08）|
| T-04-09 | Information Disclosure | jwt.config.key=sgcoding 明文 | accept | Phase 4 明文密钥政策（D-P4 option3），dev 配置 |
| T-04-10 | Denial of Service | JwtUtil @Autowired WxAccessTokenService stub | accept | stub 是真实 @Component bean，Spring context 可正常创建；stub 方法会真实查询 DB，Phase 5 替换为完整实现 |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | 本计划仅涉及 Java 源码，无包管理器操作 |
</threat_model>

<verification>
```bash
JAVA_HOME=/Library/Java/JavaVirtualMachines/amazon-corretto-8.jdk/Contents/Home \
  mvn compile -pl zgbas-system -am \
  --settings /Users/alan/App/apache-maven-3.8.6/zg_settings.xml \
  2>&1 | grep -E "^\[ERROR\]"
```
期望：无 [ERROR] 行。

追加检查：
```bash
grep -r "FilterRegistrationBean" \
  /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/config/WxSecurityConfig.java
```
期望：至少一行包含 FilterRegistrationBean。
</verification>

<success_criteria>
1. JwtConfig.java / JwtUtil.java / JwtAuthenticationFilter.java / WxSecurityConfig.java 全部存在
2. JwtAuthenticationFilter 无 @Component，无 CustomConfig，checkIgnores() 只 return false
3. WxSecurityConfig 用 FilterRegistrationBean 注册 /wx/* /ewechat/* /axq/* 三个路径
4. 辅助 stubs（SecurityException/ResponseUtil/UserContext/UserInfoVo/WxAccessTokenService）全部可编译
5. mvn compile -pl zgbas-system -am 零 [ERROR]
</success_criteria>

<output>
Create `.planning/phases/04-sdk/04-04-SUMMARY.md` when done
</output>
