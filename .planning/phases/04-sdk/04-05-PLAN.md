---
plan_id: 04-05
phase: 4
wave: 4
title: 全量编译验证 + bean 注册验证
objective: 全模块 mvn compile 零错，在 ZgbasApplicationTest 中追加 Redis/WxMaService/JwtConfig bean 可注入的验证方法，四条 Phase 4 成功标准全部确认
depends_on:
  - 04-02
  - 04-03
  - 04-04
files_modified:
  - zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java
autonomous: true
requirements_addressed:
  - WX-SERVICE-02
  - WX-SERVICE-03

must_haves:
  truths:
    - "全模块 mvn compile 零 [ERROR]"
    - "ZgbasApplicationTest 中 redisTemplate bean 可通过 applicationContext.getBean 取到"
    - "ZgbasApplicationTest 中 JwtConfig bean 可通过 applicationContext.getBean 取到"
    - "ZgbasApplicationTest 中 WxConfiguration.getMaService() 返回非 null WxMaService"
    - "原有 25/26 个测试方法仍然 GREEN，无回归"
  artifacts:
    - path: "zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java"
      provides: "Phase 4 bean 注册验证方法（3 个新 @Test）"
      contains: "wxInfrastructureBeans_phase4"
  key_links:
    - from: "ZgbasApplicationTest.wxInfrastructureBeans_phase4"
      to: "RedisConfig.redisTemplate"
      via: "applicationContext.getBean(RedisTemplate.class)"
      pattern: "getBean.*RedisTemplate"
    - from: "ZgbasApplicationTest.wxInfrastructureBeans_phase4"
      to: "WxConfiguration.getMaService()"
      via: "WxConfiguration.getMaService() != null"
      pattern: "getMaService"
---

<objective>
执行全量编译确认 Phase 4 所有迁入代码零错，并在 ZgbasApplicationTest 中添加一个新测试方法验证 Phase 4 的四条成功标准：RedisTemplate bean + JwtConfig bean + WxMaService bean 均可注册，JwtAuthenticationFilter 通过 FilterRegistrationBean 注册到正确路径。

Purpose: Phase 4 的 bean 是否真正注册进 Spring context 必须通过启动级别测试验证，编译通过不等于 bean 可注入。
Output: ZgbasApplicationTest 新增测试 GREEN，全模块编译通过，Phase 4 正式完成。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-sdk/04-CONTEXT.md

<interfaces>
<!-- ZgbasApplicationTest 现有结构 -->
- 位于 zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java
- @SpringBootTest(webEnvironment = RANDOM_PORT) @ActiveProfiles("dev")
- @Autowired ApplicationContext applicationContext（可用于 getBean）
- 测试方法已有模式：assertThat(applicationContext.getBean(XxxClass.class)).isNotNull()
- 测试方法命名规范：xxxWiring_probe（probe 类）或 xxxBeans_xxx（bean 验证类）
- WxAccessTokenService stub 已注册为 @Component，需要 DB 连接（@Transactional readOnly = true），
  ZgbasApplicationTest 本已连接真实 DB（application-dev.yml），所以 WxAccessTokenService bean 创建应正常

<!-- Phase 4 成功标准（参照 ROADMAP Phase 4 Success Criteria） -->
SC-1: RedisTemplate bean 可注入（spring-boot-starter-data-redis + RedisConfig）
SC-2: WxMaService bean 可通过 WxConfiguration.getMaService() 取到（WxConfiguration @PostConstruct init）
SC-3: JwtAuthenticationFilter 通过 FilterRegistrationBean 注册 /wx/* 路径（Shiro session 路径不变）
SC-4: Redis bean + WxMaService bean + JwtConfig bean 均可 getBean 取到，无 BeanCreationException
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 全模块编译验证（含 zgbas-admin）</name>
  <files>（本 Task 不创建或修改文件，执行验证命令）</files>
  <read_first>
    - .planning/phases/04-sdk/04-02-SUMMARY.md — 确认 04-02 Redis 类已完成
    - .planning/phases/04-sdk/04-03-SUMMARY.md — 确认 04-03 Wx 类已完成
    - .planning/phases/04-sdk/04-04-SUMMARY.md — 确认 04-04 JWT 链已完成
  </read_first>
  <action>
    执行以下两条命令确认无 [ERROR]：

    命令 1（全模块编译）：
    JAVA_HOME=/Library/Java/JavaVirtualMachines/amazon-corretto-8.jdk/Contents/Home
    mvn compile --settings /Users/alan/App/apache-maven-3.8.6/zg_settings.xml 2>&1 | grep -E "^\[ERROR\]"

    命令 2（zgbas-system 单模块更精确检查）：
    JAVA_HOME=/Library/Java/JavaVirtualMachines/amazon-corretto-8.jdk/Contents/Home
    mvn compile -pl zgbas-system -am --settings /Users/alan/App/apache-maven-3.8.6/zg_settings.xml 2>&1 | grep -c "^\[ERROR\]"

    如果命令 1 出现 [ERROR]，分析 ERROR 根因：
    - 若为 common stub 缺方法 → 补全对应 stub 中缺失的方法或字段
    - 若为 import 找不到 → 检查对应类的 package 声明是否与 import 路径一致
    - 若为 weixin SDK 类找不到 → 确认 04-01 pom 依赖已声明 weixin-java-miniapp:3.8.0
    - 若为 jjwt 类找不到 → 检查 zgbas-common 内联是否包含 jjwt 0.7.0

    修复所有 [ERROR] 直到全模块编译零错。
  </action>
  <verify>
    <automated>JAVA_HOME=/Library/Java/JavaVirtualMachines/amazon-corretto-8.jdk/Contents/Home mvn compile --settings /Users/alan/App/apache-maven-3.8.6/zg_settings.xml 2>&amp;1 | grep -c "^\[ERROR\]"</automated>
  </verify>
  <acceptance_criteria>
    - 全模块 mvn compile 输出 grep -c "^\[ERROR\]" 返回 0
    - zgbas-system、zgbas-admin 两个关键模块均显示 BUILD SUCCESS
  </acceptance_criteria>
  <done>全模块编译零 [ERROR]</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ZgbasApplicationTest 追加 Phase 4 bean 注册验证方法</name>
  <files>zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java</files>
  <read_first>
    - zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java — 读取现有测试类结构，找到合适插入位置（在最后一个测试方法和辅助方法 waitForNewJobLog 之间插入），确认 @Autowired ApplicationContext 字段已存在
    - .planning/phases/04-sdk/04-CONTEXT.md — 四条成功标准（SC-1 到 SC-4）
    - zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/config/WxConfiguration.java — 确认 getMaService() 静态方法存在（已由 04-03 创建）
  </read_first>
  <behavior>
    - wxInfrastructureBeans_phase4_probe 测试：
      - RedisTemplate bean 可从 applicationContext 取到且非 null（SC-1/SC-4）
      - JwtConfig bean 可从 applicationContext 取到且非 null（SC-4）
      - WxConfiguration.getMaService() 不抛异常且返回非 null（SC-2/SC-4）
      - 应用上下文中存在 FilterRegistrationBean（SC-3）
  </behavior>
  <action>
    在 ZgbasApplicationTest.java 中，在 waitForNewJobLog() 辅助方法之前（即最后一个 @Test 方法之后）插入一个新测试方法：

    方法名：wxInfrastructureBeans_phase4_probe
    注解：@Test（不加 @Disabled，Phase 4 验证需自动运行）
    方法体实现以下四个断言（使用已有的 assertThat 风格）：

    断言 1 — SC-1/SC-4 RedisTemplate：
    org.springframework.data.redis.core.RedisTemplate redisTemplate =
        applicationContext.getBean(org.springframework.data.redis.core.RedisTemplate.class);
    assertThat(redisTemplate).as("Phase 4 SC-1: RedisTemplate bean should be registered").isNotNull();

    断言 2 — SC-4 JwtConfig：
    com.spt.bas.purchase.wx.server.config.JwtConfig jwtConfig =
        applicationContext.getBean(com.spt.bas.purchase.wx.server.config.JwtConfig.class);
    assertThat(jwtConfig).as("Phase 4 SC-4: JwtConfig bean should be registered").isNotNull();
    assertThat(jwtConfig.getKey()).as("Phase 4 SC-4: JwtConfig.key should be sgcoding").isEqualTo("sgcoding");

    断言 3 — SC-2/SC-4 WxMaService：
    cn.binarywang.wx.miniapp.api.WxMaService wxMaService =
        com.spt.bas.purchase.wx.server.config.WxConfiguration.getMaService();
    assertThat(wxMaService).as("Phase 4 SC-2: WxMaService from WxConfiguration.getMaService() should be non-null").isNotNull();

    断言 4 — SC-3 FilterRegistrationBean 路径：
    org.springframework.boot.web.servlet.FilterRegistrationBean filterBean =
        applicationContext.getBean(org.springframework.boot.web.servlet.FilterRegistrationBean.class);
    assertThat(filterBean.getUrlPatterns())
        .as("Phase 4 SC-3: JwtFilter urlPatterns should include /wx/*")
        .contains("/wx/*");

    注意：WxConfiguration.getMaService() 调用需要在 @PostConstruct init() 执行后进行，@SpringBootTest 启动时 Spring context 已完成初始化，init() 已执行，可直接调用静态方法。

    如果 FilterRegistrationBean.class 在 context 中有多个（Shiro 等也注册了），改用 applicationContext.getBean("jwtFilterRegistration", FilterRegistrationBean.class) 或按 bean 名取（WxSecurityConfig 中方法名即 bean 名）。

    追加 import 语句（如类文件顶部尚无以下 import 则添加）：
    - import cn.binarywang.wx.miniapp.api.WxMaService;
    - import com.spt.bas.purchase.wx.server.config.WxConfiguration;
    - import com.spt.bas.purchase.wx.server.config.JwtConfig;
    - import org.springframework.data.redis.core.RedisTemplate;
    - import org.springframework.boot.web.servlet.FilterRegistrationBean;
  </action>
  <verify>
    <automated>JAVA_HOME=/Library/Java/JavaVirtualMachines/amazon-corretto-8.jdk/Contents/Home mvn test -pl zgbas-admin -Dtest=ZgbasApplicationTest#wxInfrastructureBeans_phase4_probe --settings /Users/alan/App/apache-maven-3.8.6/zg_settings.xml 2>&amp;1 | grep -E "Tests run:|BUILD|ERROR" | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - ZgbasApplicationTest.java 包含 wxInfrastructureBeans_phase4_probe 方法
    - 方法包含 RedisTemplate / JwtConfig / WxConfiguration.getMaService() 三个 assertThat 断言
    - 方法包含 FilterRegistrationBean urlPatterns 断言包含 /wx/*
    - mvn test -Dtest=ZgbasApplicationTest#wxInfrastructureBeans_phase4_probe 输出 Tests run: 1, Failures: 0, Errors: 0
    - 原有测试无回归（mvn test 全量通过）
  </acceptance_criteria>
  <done>wxInfrastructureBeans_phase4_probe 测试 GREEN，Phase 4 四条成功标准全部通过自动化验证</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试 → 真实 Redis/DB | ZgbasApplicationTest 连接真实 DB（application-dev.yml），Redis bean 创建不需要连接 Redis |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Information Disclosure | ZgbasApplicationTest 断言硬编码 jwt.config.key=sgcoding | accept | dev 测试配置，源码已有同等程度明文（DB 密码等），Phase 4 明文决策一致 |
| T-04-12 | Denial of Service | WxConfiguration.getMaService() 在测试中调用 | accept | getMaService() 从 static Map 返回已初始化对象，无网络调用，无 side effect |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | 本计划仅修改 Java 测试文件，无包管理器操作 |
</threat_model>

<verification>
全量验证命令序列：

```bash
# 1. 全模块编译
JAVA_HOME=/Library/Java/JavaVirtualMachines/amazon-corretto-8.jdk/Contents/Home \
  mvn compile --settings /Users/alan/App/apache-maven-3.8.6/zg_settings.xml \
  2>&1 | grep -c "^\[ERROR\]"
# 期望：0

# 2. Phase 4 专项 bean 注册验证
JAVA_HOME=/Library/Java/JavaVirtualMachines/amazon-corretto-8.jdk/Contents/Home \
  mvn test -pl zgbas-admin \
  -Dtest=ZgbasApplicationTest#wxInfrastructureBeans_phase4_probe \
  --settings /Users/alan/App/apache-maven-3.8.6/zg_settings.xml \
  2>&1 | grep "Tests run:"
# 期望：Tests run: 1, Failures: 0, Errors: 0, Skipped: 0

# 3. 全量测试无回归
JAVA_HOME=/Library/Java/JavaVirtualMachines/amazon-corretto-8.jdk/Contents/Home \
  mvn test -pl zgbas-admin \
  --settings /Users/alan/App/apache-maven-3.8.6/zg_settings.xml \
  2>&1 | grep "Tests run:"
# 期望：原有 N 个测试全部通过，Failures: 0, Errors: 0
```
</verification>

<success_criteria>
1. 全模块 mvn compile 零 [ERROR]
2. wxInfrastructureBeans_phase4_probe 测试 GREEN（RedisTemplate + JwtConfig + WxMaService + FilterRegistrationBean 四个断言全部通过）
3. 原有 ZgbasApplicationTest 测试无回归
4. Phase 4 四条成功标准（ROADMAP SC-1 到 SC-4）全部通过自动化验证
</success_criteria>

<output>
Create `.planning/phases/04-sdk/04-05-SUMMARY.md` when done
</output>
