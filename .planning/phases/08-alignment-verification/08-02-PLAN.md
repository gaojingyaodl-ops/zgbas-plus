---
plan_id: 08-02
phase: 8
wave: 2
title: 全 reactor 编译门 — mvn compile(全模块)+ test-compile(含新 probe)零 [ERROR](SC#1, WX-ALIGN-01)
objective: 执行全 reactor `JAVA_HOME=Corretto-1.8 mvn compile`(5 模块 admin/common/framework/quartz/system)零 [ERROR](SC#1, WX-ALIGN-01),并 `mvn test-compile -pl zgbas-admin -am` 证 08-01 新增的 wxEndpointsReachable_proof / wxPurchaseFeignSelfLoopbackWiring_probe 编译通过;编译期暴露的残余问题按 D-P8-01 最小修复回流。locale 无关 grep `cannot find symbol|找不到符号`(P5 gotcha)。
depends_on:
  - 08-01
files_modified:
  - (编译驱动,可能回流最小修复:08-01 probe 编译适配)
autonomous: true
requirements_addressed:
  - WX-ALIGN-01

must_haves:
  truths:
    - "JAVA_HOME=<Corretto-1.8> /Users/alan/App/apache-maven-3.8.6/bin/mvn compile -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml(全 reactor,无 -pl)退出码 0,locale 无关 grep `^\\[ERROR\\]` 与 `cannot find symbol|找不到符号` 均为零(SC#1, WX-ALIGN-01)"
    - "JAVA_HOME=<Corretto-1.8> mvn test-compile -pl zgbas-admin -am -s zg_settings.xml 退出码 0 —— 证 08-01 两新 probe 编译通过(新端点字符串 / I*Client 全限定名 / LocalServerConfig 类型解析无误)"
    - "编译必须带 -s zg_settings.xml(私服重定向)+ JAVA_HOME=Corretto-1.8(本机默认 JDK 21,记忆已记);locale 无关 grep 防中文 locale 漏判(P5 gotcha)"
    - "编译期暴露的残余问题按 D-P8-01 最小修复(改注释 / 补 import / 适配签名),不扩 scope、不动业务语义;每处标根因 + 行为等价"
    - "5 模块全部 BUILD SUCCESS(admin/common/framework/quartz/system),非仅 zgbas-admin(P7 仅证 zgbas-admin;本阶段复核全 reactor)"
  artifacts:
    - path: ".planning/phases/08-alignment-verification/08-02-SUMMARY.md"
      provides: "全 reactor 编译门结果 + test-compile(新 probe)通过证据"
      contains: "mvn compile"
  key_links:
    - from: "08-02 全 reactor 编译门绿"
      to: "08-03 mvn test 独立复跑(SC#2/3/4)"
      via: "编译前置通过"
      pattern: "mvn compile"
---

<objective>
Phase 8 第二波:全 reactor 编译门(SC#1, WX-ALIGN-01)。P7 已证 zgbas-admin BUILD SUCCESS;本阶段复核全 5 模块 reactor + 证 08-01 新 probe 编译,作为 SC#1 最终权威。

Purpose: SC#1 字面要求"全模块无任何 [ERROR]";且 08-03 `mvn test` 前置 = 编译全绿。本 plan 先 fail-fast 在编译层。
Output: 全 reactor + test-compile 零 ERROR;残余问题最小修复 inventory。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-alignment-verification/08-CONTEXT.md
@.planning/phases/08-alignment-verification/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 全 reactor mvn compile + zgbas-admin test-compile,迭代至零 ERROR</name>
  <files>(编译驱动:全 reactor 5 模块 + 可能的 08-01 probe 编译适配)</files>
  <read_first>
    - 08-01-PLAN.md(新 probe 代码,本 plan 证其编译)
    - 08-RESEARCH.md §4.1(编译命令 + locale 无关 grep)
    - mvn/settings 路径:/Users/alan/App/apache-maven-3.8.6 + zg_settings.xml;JDK:JAVA_HOME=Corretto-1.8(本机默认 JDK 21,每条 mvn 必前缀,记忆已记)
    - P5/P6/P7 既有 SUMMARY(编译门回流范式:locale 无关 grep / -am 防 .m2 stale / IDE 假性报红忽略)
  </read_first>
  <action>
    迭代执行两道编译命令直到零 ERROR:

    1. **全 reactor 主编译(SC#1):**
       ```bash
       JAVA_HOME=/Library/Java/JavaVirtualMachines/amazon-corretto-1.8.jdk/Contents/Home \
         /Users/alan/App/apache-maven-3.8.6/bin/mvn compile \
         -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml 2>&1 | tee /tmp/p8-compile.log
       ```
       (全 reactor,无 -pl;验证 admin/common/framework/quartz/system 5 模块全 BUILD SUCCESS)

    2. **test-compile(证 08-01 新 probe 编译):**
       ```bash
       JAVA_HOME=<Corretto-1.8> /Users/alan/App/apache-maven-3.8.6/bin/mvn test-compile \
         -pl zgbas-admin -am -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml 2>&1 | tee /tmp/p8-testcompile.log
       ```

    每轮:
    1. 解析 [ERROR]:locale 无关 grep `cannot find symbol|找不到符号|package .* does not exist|程序包.*不存在|不兼容的类型`(P5 gotcha)。
    2. 对编译期暴露的残余问题按 D-P8-01 最小修复(改注释 / 补 import / 适配签名),不扩 scope、不动业务语义;逐处记 SUMMARY(根因 → 最小 diff → 是否行为等价)。
    3. 重编译,直至两命令退出码 0、grep 零命中。
    4. IDE 单文件假性报红忽略(G-02),以 mvn 为权威。

    规则:① 仅最小修复使编译绿所必需;② 不改 08-01 probe 的判据/端点/断言语义(若 probe 编译错,修 import/全限定名,不改断言);③ CFCA / swagger 等已落依赖不动。
  </action>
  <verify>
    <automated>echo "=== full reactor compile ==="; grep -cE "^\[ERROR\]" /tmp/p8-compile.log; echo "EXIT_COMPILE=$(grep -c 'BUILD SUCCESS' /tmp/p8-compile.log)"; echo "=== symbol errors (locale-irrelevant) ==="; grep -cE "cannot find symbol|找不到符号|程序包.*不存在|package .* does not exist" /tmp/p8-compile.log /tmp/p8-testcompile.log; echo "=== test-compile 08-01 probes compiled ==="; grep -cE "BUILD SUCCESS" /tmp/p8-testcompile.log</automated>
  </verify>
  <acceptance_criteria>
    - `mvn compile`(全 reactor)退出码 0,`grep -cE "^\[ERROR\]" /tmp/p8-compile.log` = 0
    - `grep -cE "cannot find symbol|找不到符号" /tmp/p8-compile.log` = 0(locale 无关)
    - 5 模块全部 BUILD SUCCESS(admin/common/framework/quartz/system)
    - `mvn test-compile -pl zgbas-admin -am` 退出码 0(08-01 新 probe 编译通过)
    - 编译命令含 `-s zg_settings.xml` + `JAVA_HOME=Corretto-1.8`
    - SUMMARY 记录编译轮次 + 残余问题最小修复 inventory(根因 → diff → 行为等价),或「零残余问题直接绿」
  </acceptance_criteria>
  <done>全 reactor + test-compile 零 [ERROR],5 模块全 BUILD SUCCESS,新 probe 编译通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 编译验证 → 编译门通过 | 仅编译 + 最小适配(注释/import/签名),无业务语义变更,无外部网络调用 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02a | 可用性 | 不带 -s zg_settings.xml 致私服重定向失效 | mitigate | acceptance 锁命令含 -s zg_settings.xml |
| T-08-02b | 可用性 | JAVA_HOME 非 1.8 致 javax→jakarta 假性错 | mitigate | acceptance 锁 JAVA_HOME=Corretto-1.8(本机默认 JDK 21,记忆已记) |
| T-08-02c | Repudiation | locale 中文输出致 grep 漏判 | mitigate | locale 无关 grep `cannot find symbol|找不到符号`(P5 gotcha) |
</threat_model>

<verification>
本 plan 为 Phase 8 编译层最终权威(SC#1, WX-ALIGN-01)。通过后 SC#1 达成;runtime 启动 + reachability + 自回环 GREEN 由 08-03 产出。
```bash
test "$(grep -cE '^\[ERROR\]' /tmp/p8-compile.log)" -eq 0 && echo "SC1_COMPILE_GATE=GREEN"
```
</verification>

<success_criteria>
1. 全 reactor mvn compile 零 [ERROR](退出码 0,5 模块 BUILD SUCCESS,locale 无关 grep 零命中)
2. mvn test-compile -pl zgbas-admin -am 绿(08-01 新 probe 编译通过)
3. 编译命令含 -s zg_settings.xml + JAVA_HOME=Corretto-1.8
4. 残余问题按 D-P8-01 最小修复,inventory 记 SUMMARY
</success_criteria>

<output>
Create `.planning/phases/08-alignment-verification/08-02-SUMMARY.md` when done
</output>
