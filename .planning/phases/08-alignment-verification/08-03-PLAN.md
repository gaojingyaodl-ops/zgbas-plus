---
plan_id: 08-03
phase: 8
wave: 3
title: 独立复跑 mvn test + runtime fix-to-GREEN 回流(SC#2 WX-ALIGN-02 + SC#3/#4 evidence,D-P8-01/02)
objective: 用户本地独立复跑 `JAVA_HOME=Corretto-1.8 mvn test -pl zgbas-admin -am -Dtest=ZgbasApplicationTest`,证 always-on @Test 全绿(0 failures/0 errors + 启动日志无 FAILED/Exception),含 08-01 新增 wxEndpointsReachable_proof + wxPurchaseFeignSelfLoopbackWiring_probe 两 probe(SC#3/#4 evidence);runtime 暴露的非编译问题按 D-P8-01 最小修复回流;**GREEN 必须用户独立复跑证实**(D-P8-02 防假阳性,P6 executor 自报曾假绿)。dev DB sptbasdb_pd 可达为硬前置(非 hermetic)。
depends_on:
  - 08-02
files_modified:
  - (复跑驱动;runtime 最小修复可能触及:ZgbasApplication.java excludeFilter / config / bean wiring,逐处 D-P8-01 最小适配)
autonomous: false
requirements_addressed:
  - WX-ALIGN-02
  - WX-ALIGN-03

must_haves:
  truths:
    - "SC#2 判据 = 用户本地 `JAVA_HOME=Corretto-1.8 mvn test -pl zgbas-admin -am -s zg_settings.xml -Dtest=ZgbasApplicationTest` **独立复跑**(不靠 executor 自报),always-on @Test 全绿(`Tests run: N, Failures: 0, Errors: 0` + 启动日志无 `FAILED`/`Exception`/`NoSuchBeanDefinitionException`/`AmbiguousMappingException`)(WX-ALIGN-02, D-P8-02)"
    - "wxEndpointsReachable_proof + wxPurchaseFeignSelfLoopbackWiring_probe 两 always-on @Test 在复跑中 GREEN —— 4 端点 HTTP 非404(SC#3 evidence)+ purchaseWxServerConfig bean/I*Client proxy/url localhost:8080 三段断言(SC#4 evidence)(WX-ALIGN-03)"
    - "**防假阳性(强制,D-P8-02):** executor 自报 GREEN **不作为唯一证据**;SC#2/3/4 PASS 以**用户本地独立复跑**证实为准(P6 executor 自报 GREEN 曾内联泄漏密钥假绿)"
    - "**dev DB 前置(硬,非 hermetic):** 复跑前确认 sptbasdb_pd 可达 + sys_job 53 行(3 RyTask + 50 prod)+ 239 实体表存在;`application-dev.yml` 已明文 localhost:8080 + 明文密钥(D-P4)→ `export DB_PASSWORD/SPT_APP_SECRET` **不再需要**(修正 STATE.md line 83 旧表述)"
    - "runtime 暴露的非编译问题(swagger auto-config 副作用 / BasicErrorController 同名 bean / /wx/contract ambiguous / 自回环 SpEL / 循环依赖 / NPE)按 D-P8-01 最小修复(改注释 / 扩 excludeFilter / 补缺 bean / lazy-degradation),不扩 scope、不动业务语义;每处标根因 + 是否行为等价"
    - "@Disabled 真跑/写类按既有手动启用约定,不计入 SC#2 硬闸门(D-P8-02);仅 always-on @Test 计入"
    - "本 plan autonomous=false —— execute-phase 不自动盖章,checkpoint 让用户本地独立复跑(D-P8-02 防假阳性 + open-ended runtime 修复)"
  artifacts:
    - path: ".planning/phases/08-alignment-verification/08-03-SUMMARY.md"
      provides: "独立复跑结果 + runtime fix-to-GREEN inventory + SC#2/3/4 evidence + 用户独立复跑确认记录"
      contains: "mvn test"
  key_links:
    - from: "08-03 启动 GREEN + probe GREEN"
      to: "Phase 8 验收 → v1.2 milestone 验收通过(/gsd-complete-milestone)"
      via: "四层闸门全绿"
      pattern: "Tests run.*Failures: 0, Errors: 0"
---

<objective>
Phase 8 第三波(最终权威):独立复跑启动测试 + runtime fix-to-GREEN 回流。把 P5/P6/P7 显式留到 P8 的全部 runtime 证明收口 —— SC#2 启动 GREEN(WX-ALIGN-02)+ SC#3/#4 reachability/自回环 probe GREEN evidence(WX-ALIGN-03)。这是 Phase 8 存在的核心理由:四层闸门在 runtime 全绿,basWx 正式成为单体有机组成。

Purpose: SC#1(编译,08-02)已绿;本 plan 证 SC#2/3/4 在 runtime 绿。防假阳性是硬约束 —— executor 自报不算数,用户本地独立复跑为准(D-P8-02)。
Output: 独立复跑 GREEN 证据 + runtime 最小修复 inventory + 用户确认记录。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-alignment-verification/08-CONTEXT.md
@.planning/phases/08-alignment-verification/08-RESEARCH.md
@.planning/phases/07-bff-edge/07-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 确认 dev DB 前置 + 独立复跑 mvn test,triage runtime 失败</name>
  <files>(复跑驱动;本 task 不改代码,仅跑 + triage)</files>
  <read_first>
    - 08-RESEARCH.md §4.2(启动测试命令 + 防假阳性 + dev DB 前置)+ §5(runtime 修复预算 + watchlist)
    - 08-01-PLAN.md(新 probe:wxEndpointsReachable_proof + wxPurchaseFeignSelfLoopbackWiring_probe —— 本复跑证其 GREEN)
    - .planning/phases/07-bff-edge/07-VERIFICATION.md(§Deferred to Phase 8 runtime 5 项:启动无 AmbiguousMappingException/ConflictingBeanDefinitionException;/wx 非404;ContractController 透传链自回环;swagger auto-config 副作用;P8 回退预案)
    - .planning/phases/07-bff-edge/07-06-SUMMARY.md(P7 runtime gotcha:swagger 注解依赖 / BasicErrorController excludeFilter / ErrorResp.setTimestamp LocalDateTime)
    - zgbas-admin/src/main/resources/application-dev.yml(明文密钥 D-P4,无需 export)
    - mvn/settings/JDK:JAVA_HOME=Corretto-1.8 + /Users/alan/App/apache-maven-3.8.6 + zg_settings.xml
  </read_first>
  <action>
    **Step 0 — dev DB 前置确认(硬,非 hermetic):**
    复跑前确认(执行者本地 / 用户提供):
    1. dev DB `sptbasdb_pd` 可达(Druid datasource,application-dev.yml 配置)。
    2. `sys_job` 表 53 行(3 RyTask + 50 prod —— schedulerLoadAllJobs_proof 依赖)。
    3. 239 实体表存在(ddl-auto=none)。
    任一缺失 → STOP,报告前置缺失(非代码问题,不可最小修复绕过)。

    **Step 1 — 独立复跑(always-on @Test 全集):**
    ```bash
    JAVA_HOME=/Library/Java/JavaVirtualMachines/amazon-corretto-1.8.jdk/Contents.Home \
      /Users/alan/App/apache-maven-3.8.6/bin/mvn test \
      -pl zgbas-admin -am \
      -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml \
      -Dtest=ZgbasApplicationTest 2>&1 | tee /tmp/p8-test.log
    ```
    (无需 export DB_PASSWORD/SPT_APP_SECRET —— dev yml 明文 D-P4;但需 dev DB 可达)

    **Step 2 — triage runtime 失败:**
    解析 `/tmp/p8-test.log`:
    - 启动期失败(bean 装配 / AmbiguousMappingException / ConflictingBeanDefinitionException / SpEL / NPE)→ runtime 问题(D-P8-01 watchlist)。
    - always-on @Test 断言失败(wxEndpointsReachable_proof 真 404 / wxPurchaseFeignSelfLoopbackWiring_probe bean/SpEL 解析失败)→ 路由/wiring 问题。
    - 分类:① 可最小修复(D-P8-01)② 需用户决策(撞业务/DB)③ probe 端点需替换(Claude's Discretion)。
    **不在此 task 修代码** —— 仅 triage,把每个失败记入 SUMMARY(根因 + 类别 + 拟最小修复方向)。
  </action>
  <verify>
    <automated>echo "=== test result line ==="; grep -E "Tests run.*Failures.*Errors" /tmp/p8-test.log | tail -3; echo "=== startup failures ==="; grep -cE "FAILED|APPLICATION FAILED TO START|NoSuchBeanDefinitionException|AmbiguousMappingException|ConflictingBeanDefinitionException|SpelEvaluationException" /tmp/p8-test.log; echo "=== new probes mentioned ==="; grep -cE "wxEndpointsReachable_proof|wxPurchaseFeignSelfLoopbackWiring_probe" /tmp/p8-test.log</automated>
  </verify>
  <acceptance_criteria>
    - dev DB 前置确认(sptbasdb_pd 可达 / sys_job 53 行 / 239 表)—— 记 SUMMARY 或 STOP 报告缺失
    - `/tmp/p8-test.log` 产出(完整复跑日志)
    - SUMMARY 记录:always-on @Test 结果(绿/失败清单)+ 启动期异常清单 + 每项分类(可最小修复/需决策/probe 端点替换)
    - 若全绿 → 直接收口(Task 2 skip);若有失败 → 进入 Task 2 最小修复回流
  </acceptance_criteria>
  <done>dev DB 前置确认 + 独立复跑执行 + 失败 triage inventory 产出</done>
</task>

<task type="auto">
  <name>Task 2: runtime fix-to-GREEN 回流(D-P8-01 最小修复,迭代至 always-on @Test 全绿)</name>
  <files>(runtime 最小修复可能触及:zgbas-admin/src/main/java/com/spt/ZgbasApplication.java excludeFilter / config 类 / bean wiring;逐处 D-P8-01 最小适配)</files>
  <read_first>
    - Task 1 triage inventory(每项根因 + 拟修复方向)
    - 08-RESEARCH.md §5(runtime 修复预算 + watchlist:swagger auto-config / BasicErrorController excludeFilter / /wx/contract ambiguous 回退预案 / 自回环 SpEL / 循环依赖 / NPE)
    - .planning/phases/07-bff-edge/07-ROUTE-MATRIX.md §6(/wx/contract ambiguous 回退预案:report 侧 sub-path delta + IRptWxCtrContractClient Feign path 同步)
    - ZgbasApplication.java L94-104(5 个 BasicErrorController excludeFilter,assignable-type 模式)
    - P5/P6/P7 SUMMARY(最小修复回流范式:标根因 + 行为等价,对齐 P7-06 ErrorResp.setTimestamp LocalDateTime 适配)
  </read_first>
  <action>
    仅当 Task 1 有失败时执行。逐项按 D-P8-01 最小修复回流,迭代重跑 `mvn test -pl zgbas-admin -am -Dtest=ZgbasApplicationTest` 直至 always-on @Test 全绿:

    **watchlist 最小修复 playbook(按 08-RESEARCH §5 / 07-VERIFICATION deferred 5 项):**
    1. **swagger-bootstrap-ui 1.9.6 auto-config 副作用** → 最小适配(exclude auto-config / 补缺 bean),记根因;不动业务。
    2. **BasicErrorController 同名 bean 冲突** → 沿用 assignable-type excludeFilter(ZgbasApplication L99-104 模式)扩排除;记根因。
    3. **/wx/contract AmbiguousMappingException**(理论不应,前缀已隔离)→ 启用 07-ROUTE-MATRIX §6 回退预案:report 侧 RptCtrContractApi 10 重叠读方法 sub-path delta + IRptWxCtrContractClient Feign path 同步;记根因 + 行为等价(report 侧重叠方法无 web 消费者)。
    4. **WX 自回环 SpEL/bean 缺失**(purchaseWxServerConfig 未注册)→ 证 PurchaseWxClientConfig 被 com.spt ComponentScan 扫入;补 @Configuration 扫描 / @DependsOn 顺序;记根因。
    5. **循环依赖 / 缺 bean / NPE** → 最小适配(lazy-degradation / 补 @Lazy / 调 bean 顺序);记根因 + 行为等价。

    **修复边界(D-P8-01 强制):**
    - ✅ 仅修"使四层闸门 GREEN 所必需"的 runtime 问题(改注释 / 扩 excludeFilter / 补缺 bean / 适配签名 / lazy-degradation)
    - ✅ 每处标根因 + 是否行为等价
    - ❌ 不顺手根治(否决 ①-B:膨胀 / 撞 dev DB/业务 / 推迟 milestone)
    - ❌ 不"验证为主 defer 修复"(否决 ①-C)
    - 深层行为等价(端点业务正确性 / 多步流程 / WX 小程序端到端)→ 手动 UAT / v1.3,非本 task
    - probe 端点真 404(路径笔误)→ 替换为同族同 controller 确认端点(Claude's Discretion,D-P8-03)

    每轮重跑 + 重 triage,直至 always-on @Test `Failures: 0, Errors: 0` + 启动日志无 FAILED/Exception。
  </action>
  <verify>
    <automated>JAVA_HOME=/Library/Java/JavaVirtualMachines/amazon-corretto-1.8.jdk/Contents/Home /Users/alan/App/apache-maven-3.8.6/bin/mvn test -pl zgbas-admin -am -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml -Dtest=ZgbasApplicationTest 2>&1 | tee /tmp/p8-test-final.log; echo "EXIT=${PIPESTATUS[0]}"; echo "=== result ==="; grep -E "Tests run.*Failures.*Errors" /tmp/p8-test-final.log | tail -1; echo "=== startup exceptions ==="; grep -cE "FAILED|APPLICATION FAILED TO START|NoSuchBeanDefinitionException|AmbiguousMappingException|ConflictingBeanDefinitionException" /tmp/p8-test-final.log</automated>
  </verify>
  <acceptance_criteria>
    - `mvn test -pl zgbas-admin -am -Dtest=ZgbasApplicationTest` 退出码 0,`Tests run: N, Failures: 0, Errors: 0`
    - 启动日志无 `FAILED` / `APPLICATION FAILED TO START` / `NoSuchBeanDefinitionException` / `AmbiguousMappingException` / `ConflictingBeanDefinitionException`
    - wxEndpointsReachable_proof + wxPurchaseFeignSelfLoopbackWiring_probe 两新 probe 在结果中 GREEN(非 SKIPPED/非 @Disabled)
    - 每处 runtime 最小修复记 SUMMARY(根因 → 最小 diff → 是否行为等价),对齐 P7-06 适配前例
    - **用户本地独立复跑确认**(D-P8-02 防假阳性):SUMMARY 含用户独立复跑记录(executor 自报不作唯一证据)
    - @Disabled 真跑/写类不计入硬闸门(按既有手动启用约定)
  </acceptance_criteria>
  <done>always-on @Test 全绿(0 failures/0 errors),两新 probe GREEN,启动日志无异常,用户独立复跑确认</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime 启动 + always-on @Test 复跑 → 四层闸门 GREEN | 启动真实 dev DB + Druid + 53 sys_job + WX bean 装配;runtime 最小修复仅适配 wiring,无业务语义变更 |
| 防假阳性边界 | executor 自报 GREEN 不可信(P6 假绿教训);用户本地独立复跑为最终权威(D-P8-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03a | Repudiation | executor 自报 GREEN 假阳性(P6 教训) | mitigate | autonomous=false checkpoint + acceptance 锁"用户本地独立复跑确认"(D-P8-02) |
| T-08-03b | 可用性 | dev DB 不可达 → SC#2 无法复跑 | mitigate | Task 1 Step 0 硬前置确认;缺失即 STOP 报告(不绕过) |
| T-08-03c | Tampering | runtime 最小修复误改业务语义 | mitigate | D-P8-01 边界:仅适配 wiring,逐处标根因 + 行为等价;深层 defer v1.3 |
| T-08-03d | Info Disclosure | 复跑真实加载明文密钥(EweChat/JinXin/OCR,D-P4) | accepted | D-P4 明文策略锁定;复跑若暴露密钥相关问题登记;轮换本身留 todo rotate-leaked-prod-credentials(里程碑归档前处理,非验证 scope) |
</threat_model>

<verification>
本 plan 为 Phase 8 runtime 层最终权威(SC#2/3/4, WX-ALIGN-02/03)。通过后四层闸门全绿 → Phase 8 验收 → v1.2 milestone 验收通过(/gsd-complete-milestone)。
```bash
grep -E "Tests run.*Failures: 0, Errors: 0" /tmp/p8-test-final.log && echo "SC2_STARTUP_GATE=GREEN"
```
**最终判据 = 用户本地独立复跑证实(D-P8-02,防假阳性)。**
</verification>

<success_criteria>
1. always-on @Test 全绿(`Tests run: N, Failures: 0, Errors: 0`,启动日志无 FAILED/Exception)(WX-ALIGN-02)
2. wxEndpointsReachable_proof(4 端点非404)+ wxPurchaseFeignSelfLoopbackWiring_probe(bean/proxy/url 三段)两新 probe GREEN(WX-ALIGN-03,SC#3/#4 evidence)
3. dev DB 前置确认(sptbasdb_pd / 53 sys_job / 239 表)
4. runtime 最小修复按 D-P8-01 边界(标根因 + 行为等价,不扩 scope/不动业务)
5. **用户本地独立复跑确认**(D-P8-02 防假阳性,executor 自报不作唯一证据)
6. @Disabled 真跑/写类不计硬闸门
</success_criteria>

<output>
Create `.planning/phases/08-alignment-verification/08-03-SUMMARY.md` when done
</output>
