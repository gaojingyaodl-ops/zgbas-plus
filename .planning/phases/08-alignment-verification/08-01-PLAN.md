---
plan_id: 08-01
phase: 8
wave: 1
title: WX 端点 reachability probe + WX 自回环 bean-resolve probe + stale 注释清理(SC#3/#4 代码,D-P8-03/04)
objective: 在 ZgbasApplicationTest.java 新增两个 always-on @Test —— wxEndpointsReachable_proof(三族 + api 代表端点 HTTP 非404,D-P8-03→SC#3)与 wxPurchaseFeignSelfLoopbackWiring_probe(purchaseWxServerConfig bean + IWxUserDetailClient/ISaveTempClient proxy + url 含 localhost:8080,D-P8-04→SC#4),1:1 镜像既有 reportHttpReachability_proof / reportFeignSelfLoopbackWiring_probe;并清理 application-dev.yml:49-51 + ZgbasApplication.java:131-134 两处"basWx v2-deferred 404"stale 注释(P7 已迁 controller)。纯加性 + 注释适配,行为等价。
depends_on: []
files_modified:
  - zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java
  - zgbas-admin/src/main/resources/application-dev.yml
  - zgbas-admin/src/main/java/com/spt/ZgbasApplication.java
autonomous: true
requirements_addressed:
  - WX-ALIGN-03

must_haves:
  truths:
    - "ZgbasApplicationTest.java 含 `void wxEndpointsReachable_proof()` always-on @Test(无 @Disabled),对 4 个代表端点 POST 后断言 `getStatusCodeValue() != 404`(D-P8-03)"
    - "4 代表端点(grep 实测锁定):POST /wx/user/login(WxUserController)、POST /ewechat/buyEnquiry/getProductTree(BuyEnquiryController)、POST /axq/doSuccessContract(OtherController)、POST /purchase/user/saveApplyOnLineData(WxUserApi)—— 覆盖三族 + api 族 / 4 不同 controller"
    - "ZgbasApplicationTest.java 含 `void wxPurchaseFeignSelfLoopbackWiring_probe()` always-on @Test,三段断言:① context.getBean(\"purchaseWxServerConfig\", LocalServerConfig.class) 非 null;② context.getBean(IWxUserDetailClient.class) 与 context.getBean(ISaveTempClient.class) 两 Feign proxy 非 null;③ purchaseWxServerConfig.getUrl() contains \"localhost:8080\"(D-P8-04)"
    - "两个新 probe 复用既有注入字段 context(ApplicationContext)+ restTemplate(TestRestTemplate),无新框架 / 无新依赖 / 无新 @Autowired"
    - "application-dev.yml 注释(L49-51 附近)与 ZgbasApplication.java @EnableFeignClients WX 包注释(L131-134 附近)由 'basWx v2-deferred 404' 改为 'P7 已迁 WX controller,自回环非 404';url http://localhost:8080 与 @EnableFeignClients 包名 com.spt.bas.purchase.wx.client.remote 不动(D-P8-04/01 行为等价)"
    - "新 probe 落位延续既有 *Reachability_proof / *Wiring_probe 约定,区段注释标 D-P8-03/D-P8-04 + Phase 8(对齐 reportFeignSelfLoopbackWiring_probe 注释风格)"
  artifacts:
    - path: "zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java"
      provides: "wxEndpointsReachable_proof + wxPurchaseFeignSelfLoopbackWiring_probe 两个 always-on @Test"
      contains: "wxEndpointsReachable_proof"
  key_links:
    - from: "08-01 新 WX probe 代码"
      to: "08-02 test-compile 门(证 probe 编译)+ 08-03 mvn test 复跑(证 probe GREEN)"
      via: "ZgbasApplicationTest.java"
      pattern: "wxEndpointsReachable_proof"
---

<objective>
Phase 8 第一波(纯加性代码):把 P5/P6/P7 显式留到 P8 的两道 runtime 缺口补成 always-on fail-fast probe —— WX 三族端点 reachability(SC#3)+ WX 自回环 wiring(SC#4),并清理两处"basWx v2-deferred 404"stale 注释(P7 已迁 controller)。两 probe 1:1 镜像既有 reportHttpReachability_proof / reportFeignSelfLoopbackWiring_probe,无新框架。

Purpose: 为 08-03 的 `mvn test` 复跑提供 SC#3/#4 的可执行断言;stale 注释清理属 D-P8-01 最小修复范畴(注释适配,行为等价)。
Output: ZgbasApplicationTest.java 增 2 个 @Test + 2 处注释修正。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-alignment-verification/08-CONTEXT.md
@.planning/phases/08-alignment-verification/08-RESEARCH.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: 新增 wxEndpointsReachable_proof + wxPurchaseFeignSelfLoopbackWiring_probe(always-on @Test)</name>
  <files>zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java</files>
  <read_first>
    - zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java(重点 L295-344 feignSelfLoopbackWiring_probe + reportFeignSelfLoopbackWiring_probe 镜像源;L421-433 reportHttpReachability_proof 镜像源;L1-50 注入字段 context/restTemplate/import;L935 wxInfrastructureBeans_phase4_probe 既有 WX 区段)
    - 08-RESEARCH.md §1.1/§1.2(probe 1:1 镜像规范 + 代表端点表)+ §2(WX config 机制)
    - zgbas-system/src/main/java/com/spt/bas/purchase/wx/client/remote/IWxUserDetailClient.java + ISaveTempClient.java(确认 import 路径 com.spt.bas.purchase.wx.client.remote.*)
    - zgbas-system/src/main/java/com/spt/bas/purchase/wx/client/constant/PurchaseWxConstant.java(SERVER_BEAN_NAME=purchaseWxServerConfig 确认)
    - com.spt.tools.core.bean.LocalServerConfig(report probe 用同类型,确认 getUrl() 存在)
  </read_first>
  <action>
    在 ZgbasApplicationTest.java 既有 WX/probe 区段(紧邻 wxInfrastructureBeans_phase4_probe 或 reportFeignSelfLoopbackWiring_probe 之后)新增两个 always-on @Test 方法,无 @Disabled:

    1. **wxEndpointsReachable_proof()**(镜像 reportHttpReachability_proof L421-433):
       对 4 个代表端点各执行 `restTemplate.postForEntity(path, null, String.class)`,逐个 `assertThat(response.getStatusCodeValue()).as(...).isNotEqualTo(404)`:
       - `"/wx/user/login"`(注释:WxUserController @PostMapping("/login"),/wx 族)
       - `"/ewechat/buyEnquiry/getProductTree"`(注释:BuyEnquiryController,/ewechat 族)
       - `"/axq/doSuccessContract"`(注释:OtherController,/axq 族)
       - `"/purchase/user/saveApplyOnLineData"`(注释:WxUserApi,/purchase api 族)
       区段注释标 `// ---- Phase 8 (D-P8-03): WX three-family + purchase api reachability fail-fast proof ----` + 说明"判据 HTTP != 404(WX JwtFilter 未认证预期 401/错误信封,均非 404 = handler 命中)"。

    2. **wxPurchaseFeignSelfLoopbackWiring_probe()**(镜像 reportFeignSelfLoopbackWiring_probe L315-344,三段断言):
       区段注释标 `// ---- Phase 8 (D-P8-04): WX(purchase) Feign self-loopback fail-fast probe ----`。
       - (1) `assertThat(context.getBean("purchaseWxServerConfig", com.spt.tools.core.bean.LocalServerConfig.class)).as("purchaseWxServerConfig bean 解析(PurchaseWxClientConfig @Bean)").isNotNull();`
       - (2) `assertThat(context.getBean(com.spt.bas.purchase.wx.client.remote.IWxUserDetailClient.class)).as("IWxUserDetailClient Feign proxy 解析(@EnableFeignClients 含 WX 包 + SpEL #{purchaseWxServerConfig.url})").isNotNull();` + 同样断言 `ISaveTempClient.class`(注释:两 client 为最常调用,IWxUserDetailClient ~15 / ISaveTempClient ~5 service 引用)
       - (3) `com.spt.tools.core.bean.LocalServerConfig purchaseWxServerConfig = context.getBean("purchaseWxServerConfig", com.spt.tools.core.bean.LocalServerConfig.class); assertThat(purchaseWxServerConfig.getUrl()).as("spt.bas.purchaseWx.url 解析 localhost:8080 自回环").contains("localhost:8080");`

    规则:① 复用既有注入字段 `context`(ApplicationContext)与 `restTemplate`(TestRestTemplate),不加新 @Autowired;② WX I*Client 用全限定名(对齐 report probe 用 IRptFundReceivableStatisticsClient 全限定名风格),或补 import;③ 不改任何既有 @Test;④ 不 @Disabled(对齐 bas/report reachability/probe 前例属 wiring 级 always-on)。
  </action>
  <verify>
    <automated>grep -nE 'void wxEndpointsReachable_proof|void wxPurchaseFeignSelfLoopbackWiring_probe' zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java; echo "--- 4 endpoints present ---"; grep -cE '"/wx/user/login"|"/ewechat/buyEnquiry/getProductTree"|"/axq/doSuccessContract"|"/purchase/user/saveApplyOnLineData"' zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java; echo "--- self-loop 3 assertions ---"; grep -cE 'purchaseWxServerConfig|IWxUserDetailClient|ISaveTempClient' zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java; echo "--- no @Disabled on new probes (count @Disabled near them should be 0 for these two) ---"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'void wxEndpointsReachable_proof' ZgbasApplicationTest.java` = 1
    - `grep -cE 'void wxPurchaseFeignSelfLoopbackWiring_probe' ZgbasApplicationTest.java` = 1
    - wxEndpointsReachable_proof 含全部 4 代表端点路径字符串(`/wx/user/login` + `/ewechat/buyEnquiry/getProductTree` + `/axq/doSuccessContract` + `/purchase/user/saveApplyOnLineData`),逐个 `isNotEqualTo(404)`
    - wxPurchaseFeignSelfLoopbackWiring_probe 含三段:purchaseWxServerConfig bean + IWxUserDetailClient proxy + ISaveTempClient proxy + url contains localhost:8080
    - 两新方法均无 `@Disabled` 注解
    - 无新 @Autowired / 无新 import 框架依赖(复用 context + restTemplate)
    - 既有 @Test 方法体未被改动(diff 仅加性 + 2 方法)
  </acceptance_criteria>
  <done>两 always-on @Test 落位,4 端点 + 3 段自回环断言齐全,无 @Disabled</done>
</task>

<task type="execute">
  <name>Task 2: 清理 application-dev.yml + ZgbasApplication.java 两处"basWx v2-deferred 404"stale 注释(D-P8-04/01)</name>
  <files>zgbas-admin/src/main/resources/application-dev.yml, zgbas-admin/src/main/java/com/spt/ZgbasApplication.java</files>
  <read_first>
    - zgbas-admin/src/main/resources/application-dev.yml(L44-56 区段:report.url + bas.purchaseWx 注释块 + `bas.purchaseWx: url: http://localhost:8080`)
    - zgbas-admin/src/main/java/com/spt/ZgbasApplication.java(L90-140 区段:excludeFilters + @EnableFeignClients basePackages 含 com.spt.bas.purchase.wx.client.remote 注释块)
    - 08-RESEARCH.md §3(stale 注释清理规范:仅改注释文字,url 与包名不动,标根因 + 行为等价)
  </read_first>
  <action>
    仅改注释文字,行为等价(D-P8-01 最小修复范畴):

    1. **application-dev.yml**(L49-51 附近,bas.purchaseWx 注释块):把"basWx itself is v2-deferred ... proxies self-loop to localhost:8080 where no @RestController handles them → runtime calls 404 (D-P4-02 lazy-degradation)"改为反映 P7 已迁 controller 的事实,例如:"Phase 7 ported basWx BFF controllers to zgbas-admin (/wx/* /ewechat/* /axq/*); WX self-loop calls now reach real handlers (non-404). URL unchanged." 保留 `bas.purchaseWx: url: http://localhost:8080` 不动。

    2. **ZgbasApplication.java**(L131-134 附近,@EnableFeignClients 的 com.spt.bas.purchase.wx.client.remote 注释块):把"basWx itself is v2-deferred ... no @RestController impl exists → runtime calls 404 (D-P4-02 lazy-degradation for v2 contracts)"改为:"Phase 7 ported WX controllers; Feign self-loop now reaches real handlers (non-404). bean purchaseWxServerConfig + SpEL #{purchaseWxServerConfig.url} resolved at proxy creation." 保留 `@EnableFeignClients(basePackages = {... "com.spt.bas.purchase.wx.client.remote" ...})` 包名不动。

    规则:① 仅注释文字变更,非注释代码(yml key / Java 包名 / url 值)零改动;② 每处标"根因:P7 迁 controller,404 不再成立;行为等价"。
  </action>
  <verify>
    <automated>echo "--- dev yml url unchanged ---"; grep -nE 'bas.purchaseWx:' -A1 zgbas-admin/src/main/resources/application-dev.yml; echo "--- stale 404 wording gone from both files ---"; grep -nE 'v2-deferred.*404|no @RestController.*404' zgbas-admin/src/main/resources/application-dev.yml zgbas-admin/src/main/java/com/spt/ZgbasApplication.java || echo "STALE_GONE=true"; echo "--- @EnableFeignClients WX package still present ---"; grep -nE 'com.spt.bas.purchase.wx.client.remote' zgbas-admin/src/main/java/com/spt/ZgbasApplication.java</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'v2-deferred.*404|no @RestController.*404' application-dev.yml ZgbasApplication.java` = 0(stale 404 措辞已清)
    - application-dev.yml 仍含 `bas.purchaseWx:` + `url: http://localhost:8080`(url 不动)
    - ZgbasApplication.java 仍含 `com.spt.bas.purchase.wx.client.remote`(@EnableFeignClients 包名不动)
    - diff 仅注释行变更(无 yml key / Java 代码 / url 值改动)
    - 新注释提及"P7"/"Phase 7 ported" + "non-404"(反映 controller 已迁)
  </acceptance_criteria>
  <done>两处 stale 注释修正为"P7 已迁 controller,自回环非 404";url/包名零改动</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 加性 probe + 注释适配 → 四层闸门可执行 | 仅新增 fail-fast @Test + 注释文字;无业务语义变更,无外部网络/DB 写入(probe 走 TestRestTemplate 但判据仅 !=404,已在既有 reachability 前例验证) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01a | Tampering | probe 判据过宽(仅 !=404)可能掩盖路由缺失 | mitigate | 4 端点覆盖三族 + api 族 / 4 controller;handler 缺失即 404 fail-fast;自回环 probe 三段独立断言补强 |
| T-08-01b | Repudiation | 注释清理误删非注释代码 | mitigate | acceptance 锁 url / 包名零改动;diff 仅注释行 |
| T-08-01c | Info Disclosure | dev yml 已明文密钥(D-P4 决策),注释清理不触及密钥 | accepted | D-P4 明文策略锁定(覆盖安全铁律);注释仅改 404 措辞,不动密钥行 |
</threat_model>

<verification>
本 plan 为 SC#3/#4 代码层。probe 实际 GREEN 证据由 08-03 `mvn test` 独立复跑产出(D-P8-02);本 plan 只证代码落位正确。
```bash
test "$(grep -cE 'void wxEndpointsReachable_proof|void wxPurchaseFeignSelfLoopbackWiring_probe' zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java)" -ge 2 && echo "PROBES_PRESENT=true"
```
</verification>

<success_criteria>
1. wxEndpointsReachable_proof + wxPurchaseFeignSelfLoopbackWiring_probe 两 always-on @Test 落位(4 端点 + 3 段自回环断言齐全,无 @Disabled)
2. 两处"basWx v2-deferred 404"stale 注释修正(url / @EnableFeignClients 包名零改动)
3. 无新框架 / 无新 @Autowired(复用 context + restTemplate)
4. 既有 @Test 方法体未被改动
</success_criteria>

<output>
Create `.planning/phases/08-alignment-verification/08-01-SUMMARY.md` when done
</output>
