---
plan_id: 06-05
phase: 6
wave: 4
title: Service 迁入 V — UserService(->UserInfo)+ PurchaseCommand(@XxlJob scrub,保 ICommand)
objective: 迁入最后 2 类:UserService(extends BaseService<CompanyUser>,->IUserInfoService)与 PurchaseCommand(implements ICommand + @XxlJob scrub D-P6-02——删 xxl-job 注解/import,保留 ICommand 队列路径,定时触发延后 v1.3)
depends_on:
  - 06-04
files_modified:
  - zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/service/{IUserService.java,impl/UserService.java}
  - zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/command/PurchaseCommand.java
autonomous: true
requirements_addressed:
  - WX-SERVICE-01

must_haves:
  truths:
    - "IUserService(extends IBaseService<CompanyUser>)就位于 service/,UserService(extends BaseService<CompanyUser>)就位于 service/impl/"
    - "UserService @Autowired IUserInfoService(06-04)注入保留;getBaseDao() 返回 CompanyUser Dao(WX 包内 or 仅 BaseDao,以源实测为准)"
    - "PurchaseCommand 就位于 command/,@XxlJob 注解 + XxlJobHelper + com.xxl.job.* import 全部删除(D-P6-02),零 xxl-job 残留"
    - "PurchaseCommand 保留 implements ICommand + executeCommand(String) 方法体(axq/successContract/doSuccessDebtCertificate/cache/doReceiveGood/userInfoService 分支)+ @Component;ICommand 队列路径闭环(CommandExecutor 在 zgbas-common)"
  artifacts:
    - path: "zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/command/PurchaseCommand.java"
      provides: "采购命令 impl(@XxlJob scrub 后,ICommand 队列消费)"
      contains: "implements ICommand"
    - path: "zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/service/impl/UserService.java"
      provides: "WX 用户 CRUD impl"
      contains: "extends BaseService<CompanyUser>"
  key_links:
    - from: "command/PurchaseCommand(ICommand)"
      to: "ApplicationStartup CommandExecutor 队列(D-P5-09 已并入单体)"
      via: "implements ICommand + executeCommand(String)"
      pattern: "ICommand"
---

<objective>
Wave 4(拓扑 Level 4):迁入最后两个依赖 UserInfo/SuccessContract 的实现。UserService 闭环用户实体 CRUD;PurchaseCommand 是硬地雷——xxl-job 已删(v1.0 #10),@XxlJob 注解类不在 classpath,verbatim 迁会编译失败,必须 scrub(D-P6-02)。scrub 后保留 ICommand 队列路径(被 ApplicationStartup 消费),定时触发能力延后 v1.3 quartz gap-closure(与 SignContractTask 同批)。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-service/06-CONTEXT.md
@.planning/phases/06-service/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 迁入 UserService(IUserService + impl,->IUserInfoService)</name>
  <files>zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/service/{IUserService.java,impl/UserService.java}</files>
  <read_first>
    - 源 service/IUserService.java + impl/UserService.java
    - 06-RESEARCH.md §2a(UserService->IUserInfoService)+ §3(实体型;源 import 仅 BaseDao——executor 读源确认 Dao 用法,CompanyUserDao 在 WX 包内已就位)
    - 依赖:06-04 IUserInfoService 已落
    - enclave Dao:zgbas-system/.../wx/server/dao/CompanyUserDao.java(P3,若 UserService 用)
  </read_first>
  <action>
    verbatim 复制 IUserService -> service/,UserService -> service/impl/,包名保留。
    规则:① IUserService extends IBaseService<CompanyUser>,UserService extends BaseService<CompanyUser> implements IUserService;② getBaseDao() 返回源实测 Dao(若 CompanyUserDao 则 WX 包内无 re-point;若仅 BaseDao 则原样);③ @Autowired IUserInfoService(06-04)注入保留;④ 不改签名。
  </action>
  <verify>
    <automated>grep -c "extends BaseService<CompanyUser>" zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/service/impl/UserService.java; grep -c "IUserInfoService" zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/service/impl/UserService.java</automated>
  </verify>
  <acceptance_criteria>
    - IUserService.java(extends IBaseService<CompanyUser>)+ UserService.java 存在
    - UserService 含 `extends BaseService<CompanyUser>` 与 IUserInfoService
  </acceptance_criteria>
  <done>UserService 就位</done>
</task>

<task type="auto">
  <name>Task 2: 迁入 PurchaseCommand + @XxlJob scrub(D-P6-02 硬地雷)</name>
  <files>zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/command/PurchaseCommand.java</files>
  <read_first>
    - 源 command/PurchaseCommand.java(59 行,逐行读)
    - 06-RESEARCH.md §4(@XxlJob scrub 精确删行表:行 8/9/21/23-26)+ §D-P6-02
    - 06-CONTEXT.md §灰区②(D-P6-02 scrub @XxlJob 保 ICommand,定时触发延后 v1.3)
    - ICommand/CommandExecutor:zgbas-common/src/main/java/com/spt/tools/core/cmd/{ICommand,CommandExecutor}.java(单体已有)
    - ApplicationStartup:zgbas-system/.../bas/server/listener/ApplicationStartup.java(D-P5-09 已并入,等 PurchaseCommand 注册 CommandExecutor 队列)
  </read_first>
  <action>
    把 PurchaseCommand.java verbatim 复制到 enclave .../wx/server/command/,包名 com.spt.bas.purchase.wx.server.command 保留。然后做下列 scrub(D-P6-02):

    删除:
    - 行 8 `import com.xxl.job.core.context.XxlJobHelper;`
    - 行 9 `import com.xxl.job.core.handler.annotation.XxlJob;`
    - 行 21 `@XxlJob(value = "executeCommand")`
    - 行 23-26 `String jobParam = XxlJobHelper.getJobParam();` 及紧随的 `if(StringUtils.isNotBlank(jobParam)){ commandline = jobParam; }`(引用已删的 XxlJobHelper)

    保留(不动):
    - `import com.spt.tools.core.cmd.ICommand;`、`implements ICommand`、`@Component`
    - `public boolean executeCommand(String commandline) throws Exception` 方法体(axq / successContract / doSuccessDebtCertificate / cache / doReceiveGood / userInfoService 各分支 + LocalCacheManager.refreshAll() + ContractNoRequest + ISuccessContractService/IUserInfoService 调用)
    - `import org.apache.commons.lang3.StringUtils;`(cache 分支仍用 isNotBlank)

    规则:① 只删 xxl-job 残留,不动 ICommand 业务体(D-P6-02);② scrub 后文件零 `XxlJob`/`XxlJobHelper`/`com.xxl` 命中;③ 定时触发能力延后 v1.3(不接 RuoYi sys_job,不扩 scope)。
  </action>
  <verify>
    <automated>grep -cE "XxlJob|com\.xxl" zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/command/PurchaseCommand.java; grep -c "implements ICommand" zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/command/PurchaseCommand.java</automated>
  </verify>
  <acceptance_criteria>
    - PurchaseCommand.java 存在于 command/,首行包名正确
    - `grep -cE "XxlJob|com\.xxl"` 返回 0(零 xxl-job 残留)
    - 含 `implements ICommand` 与 `executeCommand(String`
    - LocalCacheManager / ContractNoRequest / successContract 分支保留(scrub 后 executeCommand 体完整)
  </acceptance_criteria>
  <done>PurchaseCommand scrub 完成,ICommand 队列路径闭环</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 源码 → zgbas-system enclave | verbatim 复制 + PurchaseCommand 受限 scrub(删 xxl-job 注解/import),包名保留 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05a | Tampering | UserService verbatim 保真 | mitigate | 与源对照;extends BaseService<CompanyUser> 保留 |
| T-06-05b | Tampering | PurchaseCommand scrub 完整性 | mitigate | acceptance_criteria 锁零 xxl-job + ICommand 体完整;grep 断言 |
| T-06-05-func | Repudiation | 定时触发能力丢失 | accept→defer | D-P6-02 锁定延后 v1.3 quartz gap-closure(与 SignContractTask 同批);ICommand 队列路径本阶段保 |
| T-06-05-SC | Tampering | npm/pip installs | accept | 纯 Java 源码编辑 |
</threat_model>

<verification>
本 plan 是最后一批源码迁入。落地后全部 19 impl + 18 interface + EweChatApi + PurchaseCommand(scrubbed)就位,可进入 06-06 编译门。plan 自检:
```bash
grep -rcE "XxlJob|com\.xxl" zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/ | grep -v ":0"   # 期望全 0(PurchaseCommand scrub 干净)
```
</verification>

<success_criteria>
1. IUserService + UserService 就位,@Autowired IUserInfoService 注入保留
2. PurchaseCommand @XxlJob scrub 完成(零 xxl-job 残留),ICommand 队列路径闭环
3. 定时触发能力延后 v1.3(STATE deferred 已列),本阶段不接 quartz
</success_criteria>

<output>
Create `.planning/phases/06-service/06-05-SUMMARY.md` when done
</output>
