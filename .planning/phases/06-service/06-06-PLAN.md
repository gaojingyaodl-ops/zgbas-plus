---
plan_id: 06-06
phase: 6
wave: 5
title: 编译门 — mvn compile -pl zgbas-system 零 [ERROR](SC#4 / D-P6-04)
objective: Phase 6 唯一验收门(D-P6-04 仅编译门)。运行 JAVA_HOME=Corretto-1.8 mvn compile -pl zgbas-system 零 [ERROR],并用静态 grep 断言三件事:① PurchaseCommand xxl-job scrub 干净;② 无 spt-tools 私服 jar 残余 import;③ 19 impl + 18 interface 结构就位
depends_on:
  - 06-01
  - 06-02
  - 06-03
  - 06-04
  - 06-05
files_modified: []
autonomous: true
requirements_addressed:
  - WX-SERVICE-01

must_haves:
  truths:
    - "JAVA_HOME=/Library/Java/JavaVirtualMachines/amazon-corretto-8.jdk/Contents/Home mvn compile -pl zgbas-system --settings /Users/alan/App/apache-maven-3.8.6/zg_settings.xml 零 [ERROR](locale 无关 grep)"
    - "zgbas-system 全包 com.spt.bas.purchase.wx.* 零 xxl-job 残留(PurchaseCommand scrub 干净)"
    - "WX service 零 purchase-client 私服 jar 残余 import(com.spt.tools.* 内联引用合法,不在排除列)"
    - "service/impl/ .java 数 ≥ 19(WxAccessToken 已在故全量 20),service/ I*Service.java ≥ 18"
  artifacts:
    - path: ".planning/phases/06-service/06-VERIFICATION.md"
      provides: "Phase 6 编译门验证报告(SC#4 结果)"
      contains: "status"
  key_links: []
---

<objective>
Phase 6 收口门。本 plan 无源码改动(纯验证)—— 跑 zgbas-system 编译 + 三组静态断言,确认 06-01..06-05 迁入的 19 impl + 18 interface + EweChatApi + PurchaseCommand(scrubbed)全部编译可达、依赖解析无残留。编译绿即 SC#4 达成、WX-SERVICE-01 达成(D-P6-04 仅编译门)。runtime/bean/启动验证全留 Phase 8。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-service/06-CONTEXT.md
@.planning/phases/06-service/06-RESEARCH.md
@.planning/phases/06-service/06-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 编译 zgbas-system 零 [ERROR](SC#4 主门)</name>
  <files>zgbas-system(pom-driven,无源码改动)</files>
  <read_first>
    - 06-VALIDATION.md §Test Infrastructure(quick command + locale gotcha)
    - memory project_zgbas-javac-locale(本机中文 locale 输出"找不到符号",grep 必须 locale 无关)
    - memory project_zgbas-plus-jdk8-mvn-prefix(每条 mvn 必须前缀 JAVA_HOME=Corretto 1.8)
  </read_first>
  <action>
    运行(必须在工程根):
    `JAVA_HOME=/Library/Java/JavaVirtualMachines/amazon-corretto-8.jdk/Contents/Home mvn compile -pl zgbas-system --settings /Users/alan/App/apache-maven-3.8.6/zg_settings.xml 2>&1 | tee /tmp/p6-compile.log`

    判定:locale 无关 grep `grep -E "^\[ERROR\]|cannot find symbol|找不到符号" /tmp/p6-compile.log` 必须为空。

    若有 [ERROR]:
    1. 优先判级联 gotcha(见 memory project_v13-compilefix-gotchas:gotcha 级联,修一处可能 unmask 下层,持续至 0)。
    2. 残留 I*Client 缺失:回查 06-RESEARCH §2b 已验清单,误报则补;真缺则记 SUMMARY 并判断是否 stub(非本 phase 目标则 @Disabled/最小 stub + 登记)。
    3. 残留 Dao 缺失/签名微差:按 D-P6-01 改 WX service 适配(不扩主域 Dao/Base)。
    4. CompanyIndustryService BsCompanyIndustryDao 方法缺失(R1):核对源用法 vs 主域 Dao;若主域 Dao 缺方法,在 WX service 内最小适配(不扩共享 Dao),记 SUMMARY。
    每次修复后重跑编译,直至零 [ERROR]。把最终编译尾行(BUILD SUCCESS)与 grep 结果写入 06-VERIFICATION.md。
  </action>
  <verify>
    <automated>JAVA_HOME=/Library/Java/JavaVirtualMachines/amazon-corretto-8.jdk/Contents/Home mvn compile -pl zgbas-system --settings /Users/alan/App/apache-maven-3.8.6/zg_settings.xml -q 2>&1 | grep -E "^\[ERROR\]|cannot find symbol|找不到符号" | head; echo "EXIT=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `mvn compile -pl zgbas-system` 输出含 `BUILD SUCCESS`
    - locale 无关 grep `[ERROR]/cannot find symbol/找不到符号` 返回空
    - 06-VERIFICATION.md 记录编译结果 + 任何修复级联
  </acceptance_criteria>
  <done>zgbas-system 编译零 [ERROR]</done>
</task>

<task type="auto">
  <name>Task 2: 静态断言(xxl-job scrub 干净 / 无私服 jar 残余 / 结构就位)</name>
  <files>(只读校验)</files>
  <read_first>
    - 06-RESEARCH.md §4(PurchaseCommand scrub 目标)+ §6(验证维度)
    - 06-VALIDATION.md §Per-Task Verification Map
  </read_first>
  <action>
    依次跑 3 组静态断言(全部必须通过):

    ① xxl-job scrub 干净:
    `grep -rE "XxlJob|com\.xxl" zgbas-system/src/main/java/com/spt/bas/purchase/wx/ | grep -v ":0" || true` → 期望空(PurchaseCommand 已 scrub)。

    ② 无 purchase-client/spt-tools 私服 jar 残余 import:
    `grep -rE "^import com\.spt\.(purchase\.wx\.)?client\." zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/service/ || true` → 期望空(注:`com.spt.tools.*` 内联引用合法,不在此排除;排除的是 purchase-client 私服 jar 路径)。

    ③ 结构就位:
    `ls zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/service/impl/*.java | wc -l` ≥ 19
    `ls zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/service/I*.java | wc -l` ≥ 18

    把三组结果写入 06-VERIFICATION.md。任一不符回对应 plan 修(① → 06-05;② → 06-01..05 对应类;③ → 对应 plan)。
  </action>
  <verify>
    <automated>echo "xxljob:"; grep -rcE "XxlJob|com\.xxl" zgbas-system/src/main/java/com/spt/bas/purchase/wx/ | grep -v ":0" || echo "clean"; echo "impl:"; ls zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/service/impl/*.java | wc -l; echo "iface:"; ls zgbas-system/src/main/java/com/spt/bas/purchase/wx/server/service/I*.java | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - grep xxl-job 在 WX 包内零命中
    - grep purchase-client 私服 jar import 在 service/ 内零命中(com.spt.tools.* 合法)
    - service/impl/ ≥ 19 文件,service/ I*Service ≥ 18 文件
  </acceptance_criteria>
  <done>三组静态断言全绿</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 本地构建 → 编译产物 | 纯 mvn compile + grep 只读断言,无网络/无部署 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06a | Tampering | 编译假阳性(locale/grep 漏判) | mitigate | locale 无关 grep(三关键词并集);tee 全量日志 |
| T-06-06-SC | Tampering | npm/pip installs | accept | 仅 mvn compile,无包管理器 |
</threat_model>

<verification>
本 plan 即 Phase 6 验收门本身。产出 06-VERIFICATION.md(status: passed 当且仅当 Task1 编译零 [ERROR] + Task2 三组断言全绿)。
</verification>

<success_criteria>
1. `mvn compile -pl zgbas-system` 零 [ERROR](SC#4,locale 无关 grep)
2. xxl-job scrub 干净 + 无私服 jar 残余 import + 结构数到位
3. 06-VERIFICATION.md 记录结果与任何修复级联
</success_criteria>

<output>
Create `.planning/phases/06-service/06-VERIFICATION.md`(status: passed/failed)与 `.planning/phases/06-service/06-06-SUMMARY.md` when done
</output>
