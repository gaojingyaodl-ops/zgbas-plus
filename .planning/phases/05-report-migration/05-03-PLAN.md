---
phase: 05-report-migration
plan: 03
type: execute
wave: 2
depends_on: [05-02]
files_modified:
  - zgbas-system/src/main/java/com/spt/bas/report/server/dao/Rpt{BaseCost,UserRoi,Evaluate,Person,Fund,Invoice,Budget,Profit,CtrDailySales,Region,Province}*Mapper.java
  - zgbas-system/src/main/java/com/spt/bas/report/server/service/IRpt{...,SummaryRoi}*Service.java
  - zgbas-system/src/main/java/com/spt/bas/report/server/service/impl/Rpt{...,SummaryRoi}*ServiceImpl.java
  - zgbas-system/src/main/resources/mybatis/mappers/Rpt{...}*Mapper.xml
autonomous: true
requirements: [REPORT-01, PERSIST-02]
tags: [domain-batch, dependency-critical, summary-roi, user-roi, finance, bulk-copy]

must_haves:
  truths:
    - "13 Mapper + 1 mapperless service（SummaryRoi）迁入 zgbas-system，包含 Wave 3 Ctr* 的 cross-service 依赖（IRptSummaryRoiService + IRptUserRoiService）"
    - "RptSummaryRoiServiceImpl + RptUserRoiServiceImpl 就位（RptCtrContractReportServiceImpl 的编译依赖，Pitfall 4）"
    - "每 sub-task 后 mvn -pl zgbas-system -am compile grep ^[ERROR] == 0"
    - "Wave 1 + Wave 2 合计 34/53 Mapper 就位"
  artifacts:
    - path: "zgbas-system/src/main/java/com/spt/bas/report/server/service/IRptSummaryRoiService.java"
      provides: "SummaryRoi service iface（mapperless — 无独立 Mapper，复用 RptBaseCostMapper）"
      contains: "IRptSummaryRoiService"
    - path: "zgbas-system/src/main/java/com/spt/bas/report/server/service/impl/RptSummaryRoiServiceImpl.java"
      provides: "SummaryRoi impl（@Autowired RptBaseCostMapper，被 Wave 3 RptCtrContractReportServiceImpl 引用）"
      contains: "IRptSummaryRoiService"
    - path: "zgbas-system/src/main/java/com/spt/bas/report/server/dao/RptBaseCostMapper.java"
      provides: "BaseCost Mapper（SummaryRoi + UserRoi 共用）"
      contains: "@MyBatisDao"
    - path: "zgbas-system/src/main/java/com/spt/bas/report/server/dao/RptUserRoiMapper.java"
      provides: "UserRoi Mapper"
      contains: "@MyBatisDao"
  key_links:
    - from: "Wave 3 RptCtrContractReportServiceImpl @Autowired IRptSummaryRoiService"
      to: "RptSummaryRoiServiceImpl（本 Wave 就位）"
      via: "cross-service dependency（Pitfall 4 测序铁律：W3 必须先于 W4）"
      pattern: "IRptSummaryRoiService"
    - from: "RptSummaryRoiServiceImpl @Autowired RptBaseCostMapper"
      to: "RptBaseCostMapper（本 Wave Task 1 就位）"
      via: "mapperless service 复用其它 Mapper"
      pattern: "RptBaseCostMapper"
---

<objective>
Wave 2：13 Mapper + 1 mapperless service（SummaryRoi）迁入 zgbas-system。**关键约束（Pitfall 4）：** Task 1 先迁 dependency-critical 服务（BaseCost + UserRoi + SummaryRoi + Evaluate + Person），因 Wave 3 的 RptCtrContractReportServiceImpl 编译依赖 IRptSummaryRoiService + IRptUserRoiService。RptSummaryRoi 无独立 Mapper——它复用 RptBaseCostMapper（mapperless service）。

Purpose: 为 Wave 3（Ctr* family 19 Mapper，含最复杂的 RptCtrContractReportServiceImpl）扫除编译依赖。SummaryRoi + UserRoi 是 Ctr* 的 cross-service dep，必须先就位。

Output: 13 Mapper + 13 iface + 13 impl + 13 XML + 1 mapperless iface + 1 mapperless impl = 55 文件净新增（全部 verbatim 保包名）。
</objective>

<execution_context>
@/Users/alan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-report-migration/05-CONTEXT.md
@.planning/phases/05-report-migration/05-RESEARCH.md
@.planning/phases/05-report-migration/05-PATTERNS.md

**BUILD PREFIX:** `JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml`

**源仓库:** `/Users/alan/WorkSpace/IDEA/zgbas` 分支 `feat-系统重构v5.0`

**Wave 1 已就位（05-02）:** 21 低风险域 Mapper + 2 util（ReportCalculateUtil 已迁，Wave 3 Ctr* 可用）。

**Pitfall 4 测序铁律:** RptCtrContractReportServiceImpl（Wave 3）`@Autowired IRptSummaryRoiService + IRptUserRoiService`——本 Wave 必须先就位这两个 service。RptSummaryRoi 无独立 Mapper（复用 RptBaseCostMapper），RptUserRoi 有独立 Mapper。

<interfaces>
<!-- Cross-service dependency chain (VERIFIED from source) -->
RptCtrContractReportServiceImpl (Wave 3) imports:
  - com.spt.bas.report.server.service.IRptSummaryRoiService  ← 本 Wave Task 1
  - com.spt.bas.report.server.service.IRptUserRoiService      ← 本 Wave Task 1

RptSummaryRoiServiceImpl (mapperless — no own Mapper):
  - @Autowired RptBaseCostMapper rptBaseCostMapper  ← 复用 BaseCost Mapper
  - uses: rptBaseCostMapper.selectUser(...) + rptBaseCostMapper.findBaseCostAndContract(...)

RptUserRoiServiceImpl (has own Mapper RptUserRoiMapper):
  - @Autowired RptBaseCostMapper rptBaseCostMapper  ← also uses BaseCost Mapper
  - imports com.spt.bas.client.entity.CtrContractSettlement (Phase 4 已迁)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 照搬 dependency-critical 域 BaseCost/UserRoi/SummaryRoi/Evaluate/Person（5 Mapper + 1 mapperless SummaryRoi = ~22 文件）</name>
  <files>
    zgbas-system/src/main/java/com/spt/bas/report/server/dao/,
    zgbas-system/src/main/java/com/spt/bas/report/server/service/,
    zgbas-system/src/main/java/com/spt/bas/report/server/service/impl/,
    zgbas-system/src/main/resources/mybatis/mappers/
  </files>
  <read_first>
    - .planning/phases/05-report-migration/05-RESEARCH.md §Pitfall 4（cross-service dep 测序）+ §Code Examples（RptFundReceivableStatisticsServiceImpl 标准模式）
    - .planning/phases/05-report-migration/05-PATTERNS.md §W1 1a–1d（Repeatable Task-Shape）
    - 源 RptSummaryRoiServiceImpl（mapperless 验证）: /Users/alan/WorkSpace/IDEA/zgbas/basReport/reportServer/src/main/java/com/spt/bas/report/server/service/impl/RptSummaryRoiServiceImpl.java（重点看 import RptBaseCostMapper + 无独立 Mapper）
    - 源 RptUserRoiServiceImpl: /Users/alan/WorkSpace/IDEA/zgbas/basReport/reportServer/src/main/java/com/spt/bas/report/server/service/impl/RptUserRoiServiceImpl.java
    - 源 dao/service/service-impl/mappers 目录（同 05-02 read_first）
  </read_first>
  <action>
    照搬保包名（D-P2-07），**优先迁 cross-service dependency 链**（Pitfall 4）：

    (1) **BaseCost 套件**（SummaryRoi + UserRoi 的 Mapper 依赖）：dao/RptBaseCostMapper.java + service/IRptBaseCostService.java + service/impl/RptBaseCostServiceImpl.java + mappers/RptBaseCostMapper.xml。

    (2) **UserRoi 套件**（有独立 Mapper）：dao/RptUserRoiMapper.java + service/IRptUserRoiService.java + service/impl/RptUserRoiServiceImpl.java + mappers/RptUserRoiMapper.xml。RptUserRoiServiceImpl 也 import RptBaseCostMapper（步骤 1 已迁）。

    (3) **SummaryRoi 套件**（mapperless — 只有 service iface + impl，**无 dao/Mapper、无 XML**）：service/IRptSummaryRoiService.java + service/impl/RptSummaryRoiServiceImpl.java。此 service 复用 RptBaseCostMapper（步骤 1 已迁）。注意：`find` 源 dao/ 和 mappers/ 目录时 RptSummaryRoi* 不会命中（无 Mapper 文件）——这是正常的，service 层照搬即可。

    (4) **Evaluate 套件**（2: EvaluateTotal + EvaluateUserDetail）：每套 4 件照搬。

    (5) **Person 套件**（1: PersonCostChart）：4 件照搬。

    完成后跑 zgbas-system compile gate——此 task 验证 cross-service dep 链编译通过（SummaryRoi → BaseCostMapper + UserRoi → BaseCostMapper）。
  </action>
  <verify>
    <automated>
cd /Users/alan/WorkSpace/IDEA/zgbas-plus && \
echo "=== dependency-critical 套件 ===" && \
echo "BASECOST_MAPPER=$(test -f zgbas-system/src/main/java/com/spt/bas/report/server/dao/RptBaseCostMapper.java && echo 1 || echo 0)" && \
echo "USERROI_MAPPER=$(test -f zgbas-system/src/main/java/com/spt/bas/report/server/dao/RptUserRoiMapper.java && echo 1 || echo 0)" && \
echo "SUMMARYROI_IFACE=$(test -f zgbas-system/src/main/java/com/spt/bas/report/server/service/IRptSummaryRoiService.java && echo 1 || echo 0)" && \
echo "SUMMARYROI_IMPL=$(test -f zgbas-system/src/main/java/com/spt/bas/report/server/service/impl/RptSummaryRoiServiceImpl.java && echo 1 || echo 0)" && \
echo "DAO_5=$(find zgbas-system/src/main/java/com/spt/bas/report/server/dao -name 'Rpt*Mapper.java' \( -name 'RptBaseCost*' -o -name 'RptUserRoi*' -o -name 'RptEvaluate*' -o -name 'RptPerson*' \) | wc -l)" && \
echo "=== compile gate（验证 cross-service dep 链）===" && \
JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home \
/Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml \
-pl zgbas-system -am compile 2>&1 | tee /tmp/p5-03-t1.log | tail -5 && \
echo "ERROR_COUNT=$(grep -c '^\[ERROR\]' /tmp/p5-03-t1.log)" && \
echo "CANNOT_FIND=$(grep -cE 'cannot find symbol|找不到符号' /tmp/p5-03-t1.log)"
    </automated>
  </verify>
  <done>
    - BASECOST_MAPPER == 1 且 USERROI_MAPPER == 1（cross-service dep 的 Mapper 源就位）
    - SUMMARYROI_IFACE == 1 且 SUMMARYROI_IMPL == 1（mapperless service 就位，Pitfall 4 关键）
    - DAO_5 == 5（BaseCost + UserRoi + Evaluate×2 + PersonCostChart）
    - SummaryRoi 无独立 Mapper（dao/ 无 RptSummaryRoiMapper.java，正常）
    - compile ERROR_COUNT == 0 且 CANNOT_FIND == 0（cross-service dep 链编译通过）
  </done>
</task>

<task type="auto">
  <name>Task 2: 照搬 Fund/Invoice/Budget/Profit/Sales/Region/Province 域 8 Mapper + XML + service（~32 文件）</name>
  <files>
    zgbas-system/src/main/java/com/spt/bas/report/server/dao/,
    zgbas-system/src/main/java/com/spt/bas/report/server/service/,
    zgbas-system/src/main/java/com/spt/bas/report/server/service/impl/,
    zgbas-system/src/main/resources/mybatis/mappers/
  </files>
  <read_first>
    - .planning/phases/05-report-migration/05-PATTERNS.md §Repeatable Task-Shape（同 05-02 模板）
    - 源目录（同 05-02/05-03 Task 1 read_first）
  </read_first>
  <action>
    照搬保包名（D-P2-07），8 Mapper 套件（按文件名前缀 glob）：
    - RptFund*（1: FundReceivableStatistics）
    - RptInvoice*（2: InvoiceBill + InvoiceBillStatistics）
    - RptBudget*（1: BudgetSettlementTotal）
    - RptProfit*（1: ProfitStatistics）
    - RptCtrDailySales*（1: CtrDailySalesReport — Sales 域，非 Ctr* 合同域）
    - RptRegion*（1: RegionMonthSales）
    - RptProvince*（1: ProvinceCustomerSales）

    每套 4 件照搬（dao + iface + impl + XML）。注意 RptCtrDailySalesReport 是 Sales 域报表（按日销售），不是 Wave 3 的 Ctr* 合同台账域——勿混淆。

    完成后跑 zgbas-system compile gate + Wave 2 全量计数。
  </action>
  <verify>
    <automated>
cd /Users/alan/WorkSpace/IDEA/zgbas-plus && \
echo "=== Task 2 Mapper 套件 ===" && \
echo "DAO_8=$(find zgbas-system/src/main/java/com/spt/bas/report/server/dao -name 'Rpt*Mapper.java' \( -name 'RptFund*' -o -name 'RptInvoice*' -o -name 'RptBudget*' -o -name 'RptProfit*' -o -name 'RptCtrDailySales*' -o -name 'RptRegion*' -o -name 'RptProvince*' \) | wc -l)" && \
echo "=== Wave 1+2 合计 ===" && \
echo "DAO_CUMULATIVE=$(find zgbas-system/src/main/java/com/spt/bas/report/server/dao -name 'Rpt*Mapper.java' -not -name 'package-info*' | wc -l)" && \
echo "XML_CUMULATIVE=$(find zgbas-system/src/main/resources/mybatis/mappers -name 'Rpt*Mapper.xml' | wc -l)" && \
echo "=== compile gate ===" && \
JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home \
/Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml \
-pl zgbas-system -am compile 2>&1 | tee /tmp/p5-03-t2.log | tail -5 && \
echo "ERROR_COUNT=$(grep -c '^\[ERROR\]' /tmp/p5-03-t2.log)" && \
echo "CANNOT_FIND=$(grep -cE 'cannot find symbol|找不到符号' /tmp/p5-03-t2.log)"
    </automated>
  </verify>
  <done>
    - DAO_8 == 8（Fund-1 + Invoice-2 + Budget-1 + Profit-1 + CtrDailySales-1 + Region-1 + Province-1）
    - DAO_CUMULATIVE == 34（Wave 1 的 21 + Wave 2 的 13）且 XML_CUMULATIVE == 34
    - compile ERROR_COUNT == 0 且 CANNOT_FIND == 0
    - 对应 service iface + impl 各 34 文件累计就位（含 SummaryRoi mapperless service）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| report Mapper XML → MySQL sptbasdb_pd | 同 05-02：mybatis 复杂 SQL（含 `${}` 动态拼接）经 @Primary DataSource 执行 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-T | Tampering | mybatis `${}` 动态 SQL（13 XML） | accept | 同 05-02：行为等价优先（PROJECT.md），安全改造非本期；照搬即等价 |
| T-05-03-I | Information Disclosure | 报表查询跨租户数据（Fund/Invoice/Profit 等财务报表） | accept | 沿用源 service 过滤逻辑（照搬即等价）；Shiro filter chain 保护 api 层（W4 落 admin 后生效） |
| T-05-SC | Tampering | Maven installs | accept | 无包安装——全部源码 verbatim copy |

无 high 级威胁。`${}` 照搬是 accepted project decision（PROJECT.md / 05-CONTEXT.md Claude's Discretion）。
</threat_model>

<verification>
- zgbas-system compile 零 ERROR（grep `^\[ERROR\]` locale 无关）
- 13 Mapper + 13 XML + 14 service iface（含 SummaryRoi mapperless）+ 14 impl 累计就位
- cross-service dep 链编译通过（SummaryRoi → BaseCostMapper + UserRoi → BaseCostMapper + Ctr* → SummaryRoi/UserRoi 就绪）
- Wave 1+2 累计 34/53 Mapper
</verification>

<success_criteria>
- REPORT-01: 累计 34/53 套 Mapper + XML 迁入 zgbas-system
- PERSIST-02: cross-service dep（SummaryRoi/UserRoi）就位为 Wave 3 Ctr* 扫除编译阻塞
- Pitfall 4 测序铁律满足：Wave 2（Summary/UserRoi）先于 Wave 3（Ctr*）
</success_criteria>

<output>
Create `.planning/phases/05-report-migration/05-03-SUMMARY.md` when done.
</output>
