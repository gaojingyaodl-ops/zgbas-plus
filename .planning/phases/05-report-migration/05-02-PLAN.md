---
phase: 05-report-migration
plan: 02
type: execute
wave: 1
depends_on: [05-01]
files_modified:
  - zgbas-system/src/main/java/com/spt/bas/report/server/dao/Rpt{Apply,Wx,WeChat,Stock,BsCompany,Company,Supplier,Business,NotBill,NotInvoice,Risk}*Mapper.java
  - zgbas-system/src/main/java/com/spt/bas/report/server/service/IRpt{...}*Service.java
  - zgbas-system/src/main/java/com/spt/bas/report/server/service/impl/Rpt{...}*ServiceImpl.java
  - zgbas-system/src/main/java/com/spt/bas/report/server/util/{MyBigDecimalUtils,ReportCalculateUtil}.java
  - zgbas-system/src/main/resources/mybatis/mappers/Rpt{...}*Mapper.xml
autonomous: true
requirements: [REPORT-01, PERSIST-02]
tags: [domain-batch, low-risk, bulk-copy, mapper, xml, service]

must_haves:
  truths:
    - "21 套低风险域报表 Mapper + iface + impl + XML 迁入 zgbas-system 保包名 com.spt.bas.report.server.*"
    - "2 个 util（MyBigDecimalUtils + ReportCalculateUtil）迁入 zgbas-system（运行依赖）"
    - "每 sub-task 后 mvn -pl zgbas-system -am compile grep ^[ERROR] == 0"
    - "全部 XML resultType / parameterType 用 FQN（com.spt.bas.report.client.*），无 simple-name alias（Pitfall 5 验证）"
  artifacts:
    - path: "zgbas-system/src/main/java/com/spt/bas/report/server/dao/"
      provides: "21 Rpt*Mapper @MyBatisDao 接口"
      contains: "@MyBatisDao"
    - path: "zgbas-system/src/main/java/com/spt/bas/report/server/service/"
      provides: "21 IRpt*Service iface + 21 impl"
      contains: "@Service"
    - path: "zgbas-system/src/main/resources/mybatis/mappers/"
      provides: "21 Rpt*Mapper.xml（namespace = Mapper FQN，resultType 全 FQN）"
      contains: "com.spt.bas.report.server.dao"
    - path: "zgbas-system/src/main/java/com/spt/bas/report/server/util/ReportCalculateUtil.java"
      provides: "报表计算工具（service impl 运行依赖，RptCtrContractReportServiceImpl 等引用）"
      contains: "ReportCalculateUtil"
  key_links:
    - from: "Rpt*Mapper.xml namespace"
      to: "Rpt*Mapper.java @MyBatisDao 接口"
      via: "mybatis binding（namespace = dao interface FQN）"
      pattern: "com\\.spt\\.bas\\.report\\.server\\.dao\\.Rpt"
    - from: "Rpt*Mapper.xml resultType"
      to: "reportClient 内联实体（Wave 0 已就位）"
      via: "FQN typeAlias 解析"
      pattern: "com\\.spt\\.bas\\.report\\.client\\.(entity|vo)"
---

<objective>
Wave 1：21 套低风险域报表（Apply/Wx/WeChat/Stock/Company/Supplier/Business/NotBill/NotInvoice/Risk）Mapper + XML + service iface + impl 迁入 zgbas-system，加 2 个 util（后续 Wave Ctr* service impl 运行依赖）。按域分 2 sub-task，每 sub-task 后 compile 绿灯（D-P5-04 逐层 compile 纪律）。

Purpose: 为 Wave 2/3 的中-高风险域提供 compile-clean 基线 + 验证 mybatis 配置接线（Wave 0 的 @MapperScan + type-aliases + mapper-locations）在首批 Mapper 上生效。这 21 套几乎无 cross-service dep，是安全的「首批探针」（D-P5-04 分批控级联）。

Output: 21 Mapper + 21 iface + 21 impl + 21 XML + 2 util ≈ 86 文件净新增（全部 verbatim 保包名 D-P2-07）。
</objective>

<execution_context>
@/Users/alan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-report-migration/05-CONTEXT.md
@.planning/phases/05-report-migration/05-RESEARCH.md
@.planning/phases/05-report-migration/05-PATTERNS.md

**BUILD PREFIX:** `JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml`

**源仓库:** `/Users/alan/WorkSpace/IDEA/zgbas` 分支 `feat-系统重构v5.0`

**Wave 0 已就位（05-01）:** reportClient 265 java 内联（entity/vo/remote）+ @MapperScan 含 report.server.dao + type-aliases 含 report entity/vo + ReportFeignPathConfig + report-client jar 移除。

**照搬保包名（D-P2-07 元模式）:** 全部源码 verbatim 复制保 `com.spt.bas.report.server.*` 包名，零 import 改动、零 rename。
</context>

<tasks>

<task type="auto">
  <name>Task 1: 照搬 Apply/Wx/WeChat/Stock/Company/Supplier/Util 域 13 Mapper + XML + service（~53 文件）</name>
  <files>
    zgbas-system/src/main/java/com/spt/bas/report/server/dao/,
    zgbas-system/src/main/java/com/spt/bas/report/server/service/,
    zgbas-system/src/main/java/com/spt/bas/report/server/service/impl/,
    zgbas-system/src/main/java/com/spt/bas/report/server/util/,
    zgbas-system/src/main/resources/mybatis/mappers/
  </files>
  <read_first>
    - .planning/phases/05-report-migration/05-PATTERNS.md §W1 1a–1e（Mapper + XML + service iface + impl + util task-shape）+ §Repeatable Task-Shape（bulk-copy 模板）
    - .planning/phases/05-report-migration/05-RESEARCH.md §Pattern 6（wave 测序约束）+ §Pitfall 5（RptBaseCostVo 同名歧义 — XML 全用 FQN）
    - 源 dao 目录: /Users/alan/WorkSpace/IDEA/zgbas/basReport/reportServer/src/main/java/com/spt/bas/report/server/dao/
    - 源 service iface 目录: /Users/alan/WorkSpace/IDEA/zgbas/basReport/reportServer/src/main/java/com/spt/bas/report/server/service/
    - 源 service impl 目录: /Users/alan/WorkSpace/IDEA/zgbas/basReport/reportServer/src/main/java/com/spt/bas/report/server/service/impl/
    - 源 XML 目录: /Users/alan/WorkSpace/IDEA/zgbas/basReport/reportServer/src/main/resources/mybatis/mappers/
    - 源 util 目录: /Users/alan/WorkSpace/IDEA/zgbas/basReport/reportServer/src/main/java/com/spt/bas/report/server/util/
    - 代表性源文件（验证 namespace + FQN resultType 模式）: 源 dao/RptFundReceivableStatisticsMapper.java + service/impl/RptFundReceivableStatisticsServiceImpl.java + mappers/RptFundReceivableStatisticsMapper.xml
  </read_first>
  <action>
    照搬保包名（D-P2-07）+ 同位 drop XML + 迁 2 util：

    (1) **util 先迁**（service impl 运行依赖）：把源 `reportServer/util/{MyBigDecimalUtils,ReportCalculateUtil}.java`（2 文件，排除 pkg-info）verbatim 复制到 `zgbas-system/.../report/server/util/`。ReportCalculateUtil 被 Wave 3 RptCtrContractReportServiceImpl 引用，先迁消除 forward-ref。

    (2) **13 Mapper 套件照搬**（按文件名前缀 glob）：
    - RptApply*（3: ApplyBusinessPay + ApplyDelivery + ApplyInternal）
    - RptWeChat*（1: WeChatWork）
    - RptWx*（1: WxBrandFollow）
    - RptStock*（4: StockContractReport + StockDetailPresell + StockInventory + StockVirtual）
    - RptBsCompany*（1: BsCompany）
    - RptCompany*（2: Company + CompanyReceivables）
    - RptSupplier*（1: Supplier）

    每套照搬 4 件：dao/Rpt*Mapper.java + service/IRpt*Service.java + service/impl/Rpt*ServiceImpl.java + resources/mybatis/mappers/Rpt*Mapper.xml。命令：`cp` 源文件到对应目标目录（保包名、保目录结构）。XML 落 `zgbas-system/src/main/resources/mybatis/mappers/`（与 Phase 2 SampleMapper.xml 同位，mapper-locations 已覆盖）。

    不改源码：XML 的 `${}` / 动态 `<if>` / `<where>` 全部 verbatim（D-P5 Claude's Discretion，行为等价优先）。

    完成后跑 zgbas-system compile gate。
  </action>
  <verify>
    <automated>
cd /Users/alan/WorkSpace/IDEA/zgbas-plus && \
echo "=== util 就位 ===" && \
echo "UTIL_COUNT=$(find zgbas-system/src/main/java/com/spt/bas/report/server/util -name '*.java' -not -name 'package-info*' | wc -l)" && \
echo "=== Task 1 Mapper 套件计数 ===" && \
echo "DAO_13=$(find zgbas-system/src/main/java/com/spt/bas/report/server/dao -name 'Rpt*Mapper.java' \( -name 'RptApply*' -o -name 'RptWeChat*' -o -name 'RptWx*' -o -name 'RptStock*' -o -name 'RptBsCompany*' -o -name 'RptCompany*' -o -name 'RptSupplier*' \) | wc -l)" && \
echo "MYBATISDAO_13=$(find zgbas-system/src/main/java/com/spt/bas/report/server/dao -name 'Rpt*Mapper.java' \( -name 'RptApply*' -o -name 'RptWeChat*' -o -name 'RptWx*' -o -name 'RptStock*' -o -name 'RptBsCompany*' -o -name 'RptCompany*' -o -name 'RptSupplier*' \) -exec grep -l '@MyBatisDao' {} \; | wc -l)" && \
echo "=== compile gate ===" && \
JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home \
/Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml \
-pl zgbas-system -am compile 2>&1 | tee /tmp/p5-02-t1.log | tail -5 && \
echo "ERROR_COUNT=$(grep -c '^\[ERROR\]' /tmp/p5-02-t1.log)" && \
echo "CANNOT_FIND=$(grep -cE 'cannot find symbol|找不到符号' /tmp/p5-02-t1.log)"
    </automated>
  </verify>
  <done>
    - UTIL_COUNT == 2（MyBigDecimalUtils + ReportCalculateUtil）
    - DAO_13 == 13 且 MYBATISDAO_13 == 13（全部 @MyBatisDao 标注）
    - 对应 XML / service iface / impl 各 13 文件就位
    - compile ERROR_COUNT == 0 且 CANNOT_FIND == 0
    - XML resultType 全用 FQN（grep 验证无 simple-name alias）
  </done>
</task>

<task type="auto">
  <name>Task 2: 照搬 Business/NotBill/NotInvoice/Risk 域 8 Mapper + XML + service（~32 文件）</name>
  <files>
    zgbas-system/src/main/java/com/spt/bas/report/server/dao/,
    zgbas-system/src/main/java/com/spt/bas/report/server/service/,
    zgbas-system/src/main/java/com/spt/bas/report/server/service/impl/,
    zgbas-system/src/main/resources/mybatis/mappers/
  </files>
  <read_first>
    - .planning/phases/05-report-migration/05-PATTERNS.md §Repeatable Task-Shape（同 Task 1 模板）
    - 源目录（同 Task 1 read_first 的源 dao/service/service-impl/mappers 目录）
  </read_first>
  <action>
    照搬保包名（D-P2-07），8 Mapper 套件（按文件名前缀 glob）：
    - RptBusiness*（3: BusinessAccount + BusinessManagerWorkbench + BusinessOverview）
    - RptNotBill*（1: NotBillStatistics）
    - RptNotInvoice*（1: NotInvoiceBillStatistics）
    - RptRisk*（3: RiskCompanyReport + RiskFinanceReport + RiskMatchUser）

    每套照搬 4 件（dao Mapper + service iface + service impl + XML），命令同 Task 1。不改源码（含 `${}` 动态 SQL）。

    完成后跑 zgbas-system compile gate + 全 21 套 Pitfall 5 验证（grep 全 XML resultType 应全用 FQN）。
  </action>
  <verify>
    <automated>
cd /Users/alan/WorkSpace/IDEA/zgbas-plus && \
echo "=== Task 2 Mapper 套件计数 ===" && \
echo "DAO_8=$(find zgbas-system/src/main/java/com/spt/bas/report/server/dao -name 'Rpt*Mapper.java' \( -name 'RptBusiness*' -o -name 'RptNotBill*' -o -name 'RptNotInvoice*' -o -name 'RptRisk*' \) | wc -l)" && \
echo "=== Wave 1 全量 21 Mapper ===" && \
echo "DAO_TOTAL=$(find zgbas-system/src/main/java/com/spt/bas/report/server/dao -name 'Rpt*Mapper.java' -not -name 'package-info*' | wc -l)" && \
echo "XML_TOTAL=$(find zgbas-system/src/main/resources/mybatis/mappers -name 'Rpt*Mapper.xml' | wc -l)" && \
echo "=== Pitfall 5: XML resultType 全 FQN ===" && \
echo "FQN_RESULTTYPE=$(grep -c 'resultType=\"com.spt.bas.report.client' zgbas-system/src/main/resources/mybatis/mappers/Rpt{Apply,Wx,WeChat,Stock,BsCompany,Company,Supplier,Business,NotBill,NotInvoice,Risk}*Mapper.xml 2>/dev/null || grep -rc 'resultType=\"com.spt.bas.report.client' zgbas-system/src/main/resources/mybatis/mappers/ | awk -F: '{s+=\$2} END{print s}')" && \
echo "=== compile gate ===" && \
JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home \
/Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml \
-pl zgbas-system -am compile 2>&1 | tee /tmp/p5-02-t2.log | tail -5 && \
echo "ERROR_COUNT=$(grep -c '^\[ERROR\]' /tmp/p5-02-t2.log)" && \
echo "CANNOT_FIND=$(grep -cE 'cannot find symbol|找不到符号' /tmp/p5-02-t2.log)"
    </automated>
  </verify>
  <done>
    - DAO_8 == 8（Business-3 + NotBill-1 + NotInvoice-1 + Risk-3）
    - DAO_TOTAL == 21（Wave 1 全量）且 XML_TOTAL == 21
    - FQN_RESULTTYPE ≥ 21（每 XML 至少 1 个 FQN resultType，Pitfall 5 全用 FQN 验证通过）
    - compile ERROR_COUNT == 0 且 CANNOT_FIND == 0
    - 对应 service iface + impl 各 21 文件就位
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| report Mapper XML → MySQL sptbasdb_pd | mybatis 复杂 SQL 经 @Primary DataSource 执行；参数化 `#{}` + 动态 `${}` 拼接（照搬不改写） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-T | Tampering | mybatis `${}` 动态 SQL 拼接（21 XML） | accept | 行为等价优先（PROJECT.md「搬运而非重造」）—— 照搬即等价，安全改造（改 `${}` 为 `#{}`）非本期决策，留后续阶段；报表查询 Vo 经 Spring Jackson 反序列化（沿用源行为） |
| T-05-02-I | Information Disclosure | 报表查询跨租户数据 | accept | 沿用源 service impl 的过滤逻辑（照搬即等价）；本期不新增审计，查询权限由 Phase 3 Shiro filter chain 保护（report api 在 W4 落 admin 后受 Shiro 链保护） |
| T-05-SC | Tampering | Maven installs | accept | 本 plan 无任何包安装——全部源码 verbatim copy（D-P2-07 照搬保包名） |

无 high 级威胁。`${}` 动态 SQL 的安全改造是已知的 accepted project decision（非 gap），在 PROJECT.md / 05-CONTEXT.md / 05-RESEARCH.md §Security Domain 均有记录。
</threat_model>

<verification>
- zgbas-system `mvn -pl zgbas-system -am compile` 零 ERROR（grep `^\[ERROR\]` locale 无关）
- 21 Mapper + 21 XML + 21 service iface + 21 service impl + 2 util 全部就位
- XML resultType 全用 FQN（Pitfall 5 验证通过）
- 包名 verbatim（grep 确认 `com.spt.bas.report.server.*`）
</verification>

<success_criteria>
- REPORT-01: 21/53 套报表 Mapper + XML 迁入 zgbas-system 报表包（保包名）
- PERSIST-02: mybatis 复杂报表查询编译通过（XML namespace 正确 + resultType FQN 解析 + @MapperScan 覆盖）
- D-P5-04: 逐层 compile 绿灯（每 sub-task 后 ERROR_COUNT == 0）
- 2 util 就位为 Wave 3 Ctr* family 提供运行依赖基础
</success_criteria>

<output>
Create `.planning/phases/05-report-migration/05-02-SUMMARY.md` when done.
</output>
