---
phase: 05-report-migration
plan: 06
type: execute
wave: 5
depends_on: [05-05]
files_modified:
  - zgbas-admin/src/main/java/com/spt/bas/web/controller/{MyIndexController,BBusinessOverviewController,BusinessManagerWorkbenchController}.java   # stub-port upgrade (grep-driven)
  - zgbas-admin/src/main/java/com/spt/bas/web/cache/WorkBenchCache.java   # if has IRpt*Client stub
  - zgbas-system/src/main/java/com/spt/bas/server/service/impl/{CtrContractUpdateServiceImpl,BsCompanyApi,WeChatWorkServiceImpl,ApplyDeliveryOutServiceImpl,RptBaseCostServiceImpl,CtrContractSettlementServiceImpl,PushContractServiceImpl,BsCompanyServiceImpl,PerformanceCommissionUserServiceImpl}.java   # if stubbed
  - zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java   # ADD W6 proofs
autonomous: true
requirements: [REPORT-02, PERSIST-02]
tags: [stub-port-upgrade, acceptance-proof, sampling-query, http-proof, d-p5-03-closure, d-p4-02-lift]

must_haves:
  truths:
    - "所有 IRpt*Client 字段的 @Autowired(required = false) 降级已解除（恢复正常 required=true 默认）"
    - "关联 null 守卫已移除（恢复源行为——report 数据正常获取，不再 skip）"
    - "sampleReportQuery_proof 就位（@Disabled 默认，手动激活跑真实 DB 抽样查询）"
    - "HTTP reachability proof 绿：/spt-bas-report/rpt/... 非 404 + /business/overview/api/... 非 404（D-P5-03 Feign 自回环闭环）"
    - "Phase 3 MyIndexController 缺口关闭（report 契约接通，首页 report 数据可获取）"
    - "Phase 5 全量验收通过：启动 + 53 Mapper 解析 + 抽样查询 + Feign 闭环"
  artifacts:
    - path: "zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java"
      provides: "W6 acceptance proof（sampleReportQuery_proof + HTTP reachability）"
      contains: "sampleReportQuery_proof"
  key_links:
    - from: "BFF controller @Autowired IRpt*Client"
      to: "Feign proxy → localhost:8080/spt-bas-report/rpt/... → report api（Wave 4 已就位）"
      via: "D-P5-03 自回环闭环（解除 D-P4-02 stub 降级后真实调用）"
      pattern: "IRpt.*Client"
    - from: "POST /business/overview/api/findBusinessOverviewList"
      to: "IRptBusinessOverviewClient → RptBusinessOverviewApi → RptBusinessOverviewMapper"
      via: "完整 BFF→Feign→api→service→Mapper 链路"
      pattern: "findBusinessOverviewList"
---

<objective>
Wave 5 收尾（D-P5-03 接通 + D-P5-08 验收）。两件事：(1) BFF stub-port 升级——移除 Phase 3/4 对 report 契约的 `@Autowired(required=false)` + null 守卫降级，恢复源行为（report 数据正常获取）；(2) 验收 proof——抽样报表查询（PERSIST-02 实证）+ HTTP reachability（D-P5-03 Feign 自回环闭环实证），关闭 Phase 3 MyIndexController 缺口。

Purpose: 证明 Phase 5 全部 3 条 ROADMAP 成功标准达成——53 Mapper+XML 迁入并可执行（PERSIST-02）+ Controller 在 admin 可达（REPORT-02）+ 查询行为等价（D-P5-03 Feign 契约接通，非 404 stub 态）。这是 Phase 5 的 verify-work 闸。

Output: stub 字段升级（grep-driven 文件集）+ 2 个 W6 proof 测试方法。
</objective>

<execution_context>
@/Users/alan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-report-migration/05-CONTEXT.md
@.planning/phases/05-report-migration/05-RESEARCH.md
@.planning/phases/05-report-migration/05-PATTERNS.md
@.planning/phases/05-report-migration/05-VALIDATION.md

**BUILD PREFIX:** `JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml`

**NON-HERMETIC（D-P5-08 / D-P4-plaintext-secrets）:** dev profile 明文密钥已就位（47.104.15.98:3306/sptbasdb_pd），启动测试不需 export DB_PASSWORD / SPT_APP_SECRET。抽样查询 proof 需真实 DB 有报表数据（@Disabled 默认防无数据环境失败）。

**Wave 0-4 已就位:** reportClient 内联 + ReportFeignPathConfig + @MapperScan + 53 Mapper+XML+service + 54 api + W0/W5 probe 全绿。

**D-P4-02 stub 降级背景（Phase 4 04-06）:** Phase 4 对 report 契约（无本地实现）在 BFF field 层施加 `@Autowired(required=false)` + null 守卫——proxy 自回环 404 时不 crash。本期 path-prefix + 54 api 落位后 proxy 调用不再 404 → 降级可解除。purchase 契约（v2-deferred）仍维持此模式。

<interfaces>
<!-- D-P4-02 stub 降级的应用点（Phase 3/4 已施加固）-->
BFF controllers (zgbas-admin) with @Autowired(required=false) IRpt*Client:
  - MyIndexController（Phase 3 P5-deferred 缺口）
  - BusinessOverviewController / BusinessManagerWorkbenchController / WorkBenchCache
  → 升级目标：移除 (required=false) + 移除 null 守卫（if (client != null) {...} 改为直接调用）

basServer service impls (zgbas-system) — 可能也有 required=false（grep 确认）:
  - CtrContractUpdateServiceImpl / BsCompanyApi / WeChatWorkServiceImpl
  - ApplyDeliveryOutServiceImpl / RptBaseCostServiceImpl / CtrContractSettlementServiceImpl
  - PushContractServiceImpl / BsCompanyServiceImpl / PerformanceCommissionUserServiceImpl
  → 注意：这些 service impl 在 Phase 4 迁入时可能用了正常 @Autowired（proxy 已生成，只是 404）；
     grep 确认哪些有 required=false 再升级（不臆测）

<!-- D-P5-08 Sampling Strategy（3 代表性报表）-->
1. RptFundReceivableStatisticsMapper.findPage — 简单 entity 单表查询
2. RptCtrContractReportMapper.findRptContractPage — cross-service dep + multi-join（最复杂）
3. RptBusinessOverviewMapper（任一 method）— BFF BusinessOverviewController 依赖（D-P5-03 闭环关键）

<!-- D-P5-03 HTTP Reachability Proof 路由 -->
- POST /spt-bas-report/rpt/fundReceivableStatistics/findPage → 直接命中 report api（path-prefix + api 可达）
- POST /business/overview/api/findBusinessOverviewList → BFF→IRptBusinessOverviewClient 自回环→RptBusinessOverviewApi（完整 Feign 链路）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BFF stub-port 升级（解除 report 契约 D-P4-02 降级）</name>
  <files>
    zgbas-admin/src/main/java/com/spt/bas/web/controller/,
    zgbas-admin/src/main/java/com/spt/bas/web/cache/,
    zgbas-system/src/main/java/com/spt/bas/server/service/impl/
  </files>
  <read_first>
    - .planning/phases/05-report-migration/05-RESEARCH.md §Pitfall 9（9 basServer svc refs + BFF refs 清单）+ §Pattern 1（Feign 自回环闭环条件）
    - .planning/phases/05-report-migration/05-PATTERNS.md §W6（BFF stub-port 升级 task-shape）
    - .planning/phases/05-report-migration/05-CONTEXT.md §decisions D-P5-03（解除 D-P4-02 stub 降级）+ §canonical_refs Phase 3 MyIndexController deferred
    - .planning/phases/03-auth-homepage/03-CONTEXT.md §decisions D-P3-10（stub-port 模式：required=false + null 守卫，本期反向解除）
    - Phase 4 04-06-PLAN.md（D-P4-02 stub-port 的施加方式，本期反向操作）
    - 当前单体 stub 降级点（grep 驱动）: 跑 `grep -rln '@Autowired(required = false)' zgbas-admin/src zgbas-system/src | xargs grep -l 'IRpt.*Client'` 确认实际文件清单
  </read_first>
  <action>
    **grep 驱动的 stub-port 升级**（不臆测文件——先 grep 确认实际降级点）：

    (1) **列出降级点**：跑 `grep -rln 'required = false\|required=false' zgbas-admin/src zgbas-system/src | xargs grep -l 'IRpt.*Client'` 得到全部对 report 契约施加了 required=false 的文件。**只处理含 `IRpt*Client` 的字段**——purchase 契约（`IWx*Client`/`ISaveTemp*Client`）的 required=false 维持不动（v2-deferred，D-P4-02 保留）。

    (2) **对每个文件**：
    - 找到 `@Autowired(required = false)` 标注在 `IRpt*Client` 类型字段上的，改为 `@Autowired`（移除 `required = false`，恢复默认 required=true）。
    - 找到关联的 null 守卫（如 `if (rptClient != null) { rptClient.findPage(...); }`），改为直接调用 `rptClient.findPage(...)`（移除 null 检查 + else 分支）。参考 Phase 3 IndexController stub-port 的反向操作。
    - **不修改 purchase 契约字段**（`IWxUserDetailClient`/`ISaveTempClient` 等）——这些维持 required=false（v2-deferred）。

    (3) **MyIndexController 特殊处理**：Phase 3 因 report 契约缺口 deferred 的 MyIndexController——其 `IRptIndexReportClient`（或类似 report 契约）字段的 required=false + null 守卫移除，恢复源行为（首页 report 数据正常获取）。这是 D-P5-03 关闭 Phase 3 缺口的核心。

    完成后跑全模块 compile gate + ZgbasApplicationTest（验证 stub 移除后 bean 注入成功——proxy 已有，required=true 不触发 NoSuchBeanDefinitionException）。
  </action>
  <verify>
    <automated>
cd /Users/alan/WorkSpace/IDEA/zgbas-plus && \
echo "=== report 契约 stub 降级残留检查 ===" && \
echo "RPT_STUB_REMAINING=$(grep -rln 'required = false\|required=false' zgbas-admin/src zgbas-system/src 2>/dev/null | xargs grep -l 'IRpt.*Client' 2>/dev/null | wc -l)" && \
echo "=== purchase 契约 stub 保留（不应为 0，v2-deferred）===" && \
echo "WX_STUB_RETAINED=$(grep -rln 'required = false\|required=false' zgbas-admin/src zgbas-system/src 2>/dev/null | xargs grep -l 'IWx.*Client\|ISaveTemp.*Client' 2>/dev/null | wc -l)" && \
echo "=== compile gate（全模块）===" && \
JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home \
/Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml \
-am compile 2>&1 | tee /tmp/p5-06-t1.log | tail -5 && \
echo "ERROR_COUNT=$(grep -c '^\[ERROR\]' /tmp/p5-06-t1.log)" && \
echo "CANNOT_FIND=$(grep -cE 'cannot find symbol|找不到符号' /tmp/p5-06-t1.log)"
    </automated>
  </verify>
  <done>
    - RPT_STUB_REMAINING == 0（所有 report 契约的 required=false 已移除）
    - WX_STUB_RETAINED ≥ 0（purchase 契约 stub 可保留——v2-deferred 不在本期范围）
    - compile ERROR_COUNT == 0 且 CANNOT_FIND == 0（bean 注入成功，proxy 已有）
    - MyIndexController 的 report 契约字段升级（Phase 3 缺口关闭）
  </done>
</task>

<task type="auto">
  <name>Task 2: W6 验收 proof（sampleReportQuery_proof @Disabled + HTTP reachability proof）</name>
  <files>
    zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java
  </files>
  <read_first>
    - .planning/phases/05-report-migration/05-VALIDATION.md §Sampling Strategy（3 代表性报表）+ §Manual-Only Verifications + §HTTP Reachability Proof
    - .planning/phases/05-report-migration/05-RESEARCH.md §Validation Architecture §Sampling Strategy + §HTTP Reachability Proof
    - .planning/phases/05-report-migration/05-PATTERNS.md §W6 验证 probe 扩展（sampleReportQuery_proof 模板）
    - 当前 ZgbasApplicationTest（含 W0+W5 probe）: /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java
  </read_first>
  <action>
    在 ZgbasApplicationTest 新增 2 个 W6 proof 测试方法：

    (1) **`sampleReportQuery_proof`**（PERSIST-02 实证，**@Disabled 默认**——手动激活跑真实 DB）：注入 3 个代表性 Mapper（或经 service 调用），跑 findPage 查询断言返回非空：
    - `RptFundReceivableStatisticsMapper.findPage(vo)` → 断言 `list != null && !list.isEmpty()`（简单 entity 单表，证明 typeAliases + namespace + DataSource 接通）
    - `RptCtrContractReportMapper.findRptContractPage(vo)` → 断言非空（最复杂 multi-join + cross-service dep，证明 wave 测序 + 复杂 SQL 可执行）
    - `RptBusinessOverviewMapper`（任一 method）→ 断言非空（D-P5-03 闭环关键——结果返回给 BFF BusinessOverviewController）
    `@Disabled("D-P5-08 sample query proof — activate manually with real DB seeded data")`——非 hermetic，依赖真实 DB 有报表数据。激活时手动移除 @Disabled。

    构造查询 Vo 时用最小过滤条件（如 page=1, rows=10），若返回空 list 可能是 DB 无数据（不算 proof 失败——记录到 SUMMARY 并标注需 DB seed）。

    (2) **`reportHttpReachability_proof`**（D-P5-03 闭环实证，**不加 @Disabled，自动运行**）：用 TestRestTemplate（Phase 4 同款）跑 2 个 HTTP 请求断言非 404：
    - `POST /spt-bas-report/rpt/fundReceivableStatistics/findPage`（直接命中 report api）→ 断言 `statusCode != 404`（200/302/401 均可——200=有数据，302=重定向，401=需认证，**404=path-prefix 或 api 未就位**）
    - `POST /business/overview/api/findBusinessOverviewList`（BFF→Feign→report api 完整链路）→ 断言 `statusCode != 404`（证明 D-P5-03 自回环闭环——解除 D-P4-02 stub 后 Feign proxy 调用不再 404）

    断言风格沿用 Phase 4 endpoint reachability：`assertThat(response.getStatusCodeValue()).isNotEqualTo(404);` 或 `assertThat(response.getStatusCode().is4xxClientError() && response.getStatusCodeValue() != 404).isTrue();`。用 `restTemplate.postForEntity(url, null, String.class)` 发请求。

    跑 `mvn test -Dtest=ZgbasApplicationTest` 确认 W6 HTTP proof 绿 + @Disabled sample proof 跳过 + 现有全量测试不回归。
  </action>
  <verify>
    <automated>
cd /Users/alan/WorkSpace/IDEA/zgbas-plus && \
echo "=== W6 proof 方法存在 ===" && \
echo "PROOF_SAMPLE=$(grep -c 'sampleReportQuery_proof' zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java)" && \
echo "PROOF_HTTP=$(grep -c 'reportHttpReachability_proof' zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java)" && \
echo "=== 启动验证（含 W0+W5+W6 probe，sample proof @Disabled 跳过）===" && \
JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home \
/Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml \
-pl zgbas-admin -am test -Dtest=ZgbasApplicationTest 2>&1 | tee /tmp/p5-06-t2.log | tail -15 && \
echo "ERROR_COUNT=$(grep -c '^\[ERROR\]' /tmp/p5-06-t2.log)" && \
echo "TEST_FAIL=$(grep -cE 'FAIL|FAILURE' /tmp/p5-06-t2.log)" && \
echo "HTTP_404=$(grep -c '404' /tmp/p5-06-t2.log)"
    </automated>
  </verify>
  <done>
    - ZgbasApplicationTest 含 sampleReportQuery_proof（@Disabled）+ reportHttpReachability_proof（不加 @Disabled）
    - reportHttpReachability_proof 绿：/spt-bas-report/rpt/... 非 404 + /business/overview/api/... 非 404（D-P5-03 闭环证明）
    - sampleReportQuery_proof @Disabled 跳过（手动激活时验证抽样查询非空——PERSIST-02 实证）
    - 现有 ZgbasApplicationTest 全部测试绿（含 W0 reportFeignSelfLoopbackWiring + W5 allReportMappersResolve/reportApiPathPrefixWiring，无 Phase 2/3/4 回归）
    - mvn test ERROR_COUNT == 0 + TEST_FAIL == 0 + HTTP_404 == 0（日志中无 404）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → BFF → Feign self-loopback → report api | 完整链路在本进程 8080；stub 降级解除后 Feign proxy 真实调用 report api（非 404 态） |
| report api → MySQL sptbasdb_pd | 同前 wave：mybatis 查询经 @Primary DataSource |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-S | Spoofing | stub 降级解除后 Feign 自回环真实调用 | accept | 同 W0：仅 localhost:8080 自跳；同进程鉴权沿用 Phase 3 Shiro 链（BFF 路由受 Shiro filter chain 保护） |
| T-05-06-I | Information Disclosure | report 契约接通后报表数据通过 BFF 暴露 | accept | 行为等价于源微服务（源 reportServer 同样向 web BFF 暴露报表数据）；查询权限由 Shiro session+cookie 保证（Phase 3）；越权审计留后续安全阶段 |
| T-05-06-D | DoS | 抽样查询 + HTTP proof 在测试中执行真实 DB 查询 | accept | sampleReportQuery_proof @Disabled 默认（仅在手动激活时跑真实查询）；HTTP reachability proof 只断言非 404（不验证大数据量）；测试期查询负载可控 |
| T-05-SC | Tampering | Maven installs | accept | 无包安装——仅 Java 源码编辑（stub 字段升级）+ 测试方法新增 |

无 high 级威胁。stub 降级解除是恢复源行为（源微服务 reportServer 从未 stub），不引入新攻击面。
</threat_model>

<verification>
- 全模块 `mvn -am compile` 零 ERROR
- report 契约 stub 降级残留 == 0（grep 驱动验证）
- ZgbasApplicationTest 全绿（W0+W5+W6 probe + 现有 Phase 2/3/4 测试无回归）
- reportHttpReachability_proof：2 个 HTTP 请求均非 404（D-P5-03 Feign 自回环闭环）
- sampleReportQuery_proof @Disabled 就位（手动激活时验证 PERSIST-02 抽样查询）
</verification>

<success_criteria>
- D-P5-03: report Feign 契约接通——BFF stub-port 降级解除 + HTTP proof 证明自回环闭环（非 404）
- D-P5-08: 验收 proof 就位——sampleReportQuery_proof（PERSIST-02 抽样查询）+ reportHttpReachability_proof（HTTP 可达性）
- Phase 3 MyIndexController 缺口关闭（report 契约接通，首页 report 数据可获取）
- REPORT-02: 查询行为与旧系统等价（Feign 契约真实调用替代 404 stub 态）
- Phase 5 全量验收：53 Mapper+XML 迁入可执行 + 54 Controller 可达 + Feign 契约闭环
</success_criteria>

<output>
Create `.planning/phases/05-report-migration/05-06-SUMMARY.md` when done.
</output>
