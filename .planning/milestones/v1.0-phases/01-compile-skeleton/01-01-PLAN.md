---
phase: 01-compile-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pom.xml
  - .gitignore
  - zgbas-common/pom.xml
  - zgbas-common/src/main/java/com/spt/tools/core/PackageMarker.java
  - zgbas-framework/pom.xml
  - zgbas-framework/src/main/java/com/spt/framework/PackageMarker.java
  - zgbas-system/pom.xml
  - zgbas-system/src/main/java/com/spt/system/PackageMarker.java
  - zgbas-quartz/pom.xml
  - zgbas-quartz/src/main/java/com/spt/quartz/PackageMarker.java
  - zgbas-admin/pom.xml
  - zgbas-admin/src/main/java/com/spt/ZgbasApplication.java
  - zgbas-admin/src/main/resources/application.yml
  - zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java
autonomous: true
requirements: [BUILD-01, BUILD-02, BUILD-03, BUILD-04, BUILD-05, ALIGN-03]
user_setup: []

must_haves:
  truths:
    - "D-01/D-02/D-03: Root pom.xml inherits spring-boot-starter-parent:2.5.9 as grandparent; does NOT inherit spt-parent and does NOT import bas-parent or spt-tools-parent BOM"
    - "D-04/D-05: zgbas-common exists as placeholder jar (reserved for Phase 2 spt-tools inline); no spt-tools-* and no external SDK jars on classpath"
    - "D-06/D-07: Skeleton has 5 modules + root parent + @SpringBootApplication + application.yml; boots empty Spring context (no DataSource, no external SDK, no business code)"
    - "D-08/D-09: Module topology matches: common<-none, framework<-common, system<-{common,framework}, quartz<-{common,framework,system}, admin<-all; system does NOT depend on quartz (anti-cycle)"
    - "D-10/ALIGN-03: mvn clean compile (JDK8 + zg_settings.xml) produces zero [ERROR] lines and prints BUILD SUCCESS across all 5 modules"
    - "D-10/BUILD-04: mvn spring-boot:run starts ZgbasApplication empty context successfully (Started ZgbasApplication in log)"
    - "D-11/BUILD-05: Only zgbas-admin produces a repackaged fat jar (.jar.original exists); common/framework/system/quartz produce plain jars with no .jar.original"
  artifacts:
    - path: "pom.xml"
      provides: "Root aggregator parent POM (grandparent=spring-boot-starter-parent:2.5.9, 5 modules, dependencyManagement for internal modules)"
      contains: "spring-boot-starter-parent"
    - path: "zgbas-common/pom.xml"
      provides: "common module POM (plain jar, no internal deps)"
    - path: "zgbas-framework/pom.xml"
      provides: "framework module POM (plain jar, depends on common)"
    - path: "zgbas-system/pom.xml"
      provides: "system module POM (plain jar, depends on common+framework)"
    - path: "zgbas-quartz/pom.xml"
      provides: "quartz module POM (plain jar, depends on common+framework+system)"
    - path: "zgbas-admin/pom.xml"
      provides: "admin module POM (boot module, depends on all 4, has spring-boot-maven-plugin repackage + spring-boot-starter-web + spring-boot-starter-test)"
      contains: "spring-boot-maven-plugin"
    - path: "zgbas-admin/src/main/java/com/spt/ZgbasApplication.java"
      provides: "Sole @SpringBootApplication entry point with empty context"
      contains: "@SpringBootApplication"
    - path: "zgbas-admin/src/main/resources/application.yml"
      provides: "Minimal config (port 8080, profile dev, scan com.spt, no datasource)"
    - path: "zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java"
      provides: "Context-load smoke test (@SpringBootTest contextLoads)"
      contains: "contextLoads"
    - path: ".gitignore"
      provides: "Ignores target/, .idea/, *.iml, logs/"
  key_links:
    - from: "pom.xml"
      to: "spring-boot-starter-parent:2.5.9"
      via: "<parent> inheritance (grandparent)"
      pattern: "spring-boot-starter-parent"
    - from: "pom.xml"
      to: "5 internal modules"
      via: "<modules> + <dependencyManagement> with ${project.version}"
      pattern: "\\$\\{project\\.version\\}"
    - from: "zgbas-admin/pom.xml"
      to: "spring-boot-maven-plugin repackage"
      via: "<build><plugins> (ONLY admin module declares this)"
      pattern: "spring-boot-maven-plugin"
    - from: "zgbas-quartz/pom.xml"
      to: "zgbas-system"
      via: "<dependency> (D-08/D-09 intentional edge; system has no reverse dep on quartz)"
      pattern: "zgbas-system"
    - from: "ZgbasApplication.java"
      to: "Spring context"
      via: "SpringApplication.run with @SpringBootApplication (no exclude, no DB)"
      pattern: "SpringApplication\\.run"
---

<objective>
Create the 5-module Maven aggregator monolith skeleton from scratch on the empty zgbas-plus repo. The root parent POM inherits spring-boot-starter-parent:2.5.9 (grandparent pattern, breaking the old spt-parent chain). All 5 modules wire their dependencies per the D-08 topology. The admin module is the sole boot/fat-jar module with a @SpringBootApplication that boots an empty Spring context. A context-load smoke test solidifies BUILD-04.

Purpose: Provide the "compiles clean + boots successfully" skeleton baseline (ALIGN-03) that all subsequent phases (infrastructure, auth, business migration) build upon.

Output: Root pom.xml + 5 module POMs + boot class + application.yml + smoke test + .gitignore. All verified with JDK8-prefixed mvn commands producing zero [ERROR] and a successful empty-context boot.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-compile-skeleton/01-CONTEXT.md
@.planning/phases/01-compile-skeleton/01-RESEARCH.md
@.planning/phases/01-compile-skeleton/01-VALIDATION.md

<interfaces>
<!-- No existing codebase interfaces — repo is empty (greenfield). -->
<!-- RESEARCH.md § Code Examples (lines ~316-510) is the POC-verified source of truth for all POM/boot/yml files. -->
<!-- CONTEXT.md § Decisions D-01..D-11 are LOCKED constraints that every artifact must honor. -->

Environment contract (apply to EVERY mvn invocation):
  JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home
  MVN=/Users/alan/App/apache-maven-3.8.6/bin/mvn
  SETTINGS=/Users/alan/App/apache-maven-3.8.6/zg_settings.xml

Locale-independent zero-ERROR assertion:
  grep -c '^\[ERROR\]' <logfile> must equal 0
  grep -q 'BUILD SUCCESS' <logfile> must succeed
  (Maven's [ERROR] prefix and BUILD SUCCESS are hardcoded English regardless of OS locale)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Maven aggregator skeleton (root pom + 5 module poms + .gitignore + placeholder classes)</name>
  <files>
    pom.xml,
    .gitignore,
    zgbas-common/pom.xml,
    zgbas-common/src/main/java/com/spt/tools/core/PackageMarker.java,
    zgbas-framework/pom.xml,
    zgbas-framework/src/main/java/com/spt/framework/PackageMarker.java,
    zgbas-system/pom.xml,
    zgbas-system/src/main/java/com/spt/system/PackageMarker.java,
    zgbas-quartz/pom.xml,
    zgbas-quartz/src/main/java/com/spt/quartz/PackageMarker.java,
    zgbas-admin/pom.xml
  </files>
  <read_first>
    - .planning/phases/01-compile-skeleton/01-RESEARCH.md — § Code Examples (lines ~316-510): POC-verified root pom.xml, library module pom.xml template with dependency table, zgbas-admin/pom.xml. These are the copy-ready source of truth for all POM contents.
    - .planning/phases/01-compile-skeleton/01-CONTEXT.md — § Decisions D-01..D-11: locked structural constraints (parent chain, topology, fat-jar-only-admin).
    - .planning/phases/01-compile-skeleton/01-VALIDATION.md — § Per-Task Verification Map: acceptance command patterns.
  </read_first>
  <behavior>
    - Root pom.xml: packaging=pom, parent=spring-boot-starter-parent:2.5.9 (groupId=org.springframework.boot, relativePath empty), groupId=com.spt, artifactId=zgbas-plus, version=1.0.0-SNAPSHOT, 5 modules listed in build order (common, framework, system, quartz, admin), properties java.version=1.8, dependencyManagement declaring 4 internal modules (common/framework/system/quartz) with ${project.version}, NO spt-parent inheritance, NO bas-parent or spt-tools-parent BOM import.
    - zgbas-common/pom.xml: parent=zgbas-plus (relativePath ../pom.xml), artifactId=zgbas-common, packaging=jar, NO internal dependencies, NO spring-boot-maven-plugin.
    - zgbas-framework/pom.xml: parent=zgbas-plus, artifactId=zgbas-framework, packaging=jar, depends on zgbas-common only (no version tag — managed by root dependencyManagement), NO spring-boot-maven-plugin.
    - zgbas-system/pom.xml: parent=zgbas-plus, artifactId=zgbas-system, packaging=jar, depends on zgbas-common AND zgbas-framework, NO spring-boot-maven-plugin.
    - zgbas-quartz/pom.xml: parent=zgbas-plus, artifactId=zgbas-quartz, packaging=jar, depends on zgbas-common, zgbas-framework, AND zgbas-system, NO spring-boot-maven-plugin.
    - zgbas-admin/pom.xml: parent=zgbas-plus, artifactId=zgbas-admin, packaging=jar, depends on all 4 internal modules + spring-boot-starter-web + spring-boot-starter-test (test scope), declares spring-boot-maven-plugin with mainClass=com.spt.ZgbasApplication (NO layout=ZIP, NO excludeGroupIds, NO maven-dependency-plugin, NO executions block — starter-parent pluginManagement binds repackage to package phase automatically).
    - Each library module (common/framework/system/quartz) has a placeholder PackageMarker.java class to avoid empty-source edge cases. common placeholder uses package com.spt.tools.core (aligned to Phase 2 spt-tools inline target); framework uses com.spt.framework; system uses com.spt.system; quartz uses com.spt.quartz.
    - .gitignore ignores: target/, .idea/, *.iml, logs/.
  </behavior>
  <action>
    Create all files listed in <files> using RESEARCH.md § Code Examples as the copy-ready source of truth.

    ROOT pom.xml (per D-01/D-02/D-03/D-08):
    - <parent> = org.springframework.boot:spring-boot-starter-parent:2.5.9 with empty <relativePath/>
    - groupId=com.spt, artifactId=zgbas-plus, version=1.0.0-SNAPSHOT, packaging=pom
    - <modules> lists: zgbas-common, zgbas-framework, zgbas-system, zgbas-quartz, zgbas-admin (in dependency order)
    - <properties> has <java.version>1.8</java.version>
    - <dependencyManagement> declares 4 internal modules (zgbas-common/framework/system/quartz) each with groupId=com.spt and version=${project.version}. NOTE: admin is NOT in dependencyManagement (it is never depended upon by other modules).
    - MUST NOT contain: any <parent> to spt-parent, any <scope>import</scope> BOM for bas-parent or spt-tools-parent, any maven-dependency-plugin, any layout=ZIP.

    MODULE POMs (per D-08/D-09/D-11 topology):
    Each library module pom has <parent> pointing to com.spt:zgbas-plus:1.0.0-SNAPSHOT with <relativePath>../pom.xml</relativePath>. Dependencies per the D-08 dependency table in RESEARCH.md § Code Examples (framework←common; system←common,framework; quartz←common,framework,system). All internal dependency declarations omit <version> (managed by root dependencyManagement). None of the 4 library modules declare spring-boot-maven-plugin.

    zgbas-admin/pom.xml additionally:
    - Dependencies: all 4 internal modules + org.springframework.boot:spring-boot-starter-web (no version, managed by grandparent BOM) + org.springframework.boot:spring-boot-starter-test with <scope>test</scope>
    - <build><plugins> declares spring-boot-maven-plugin with <configuration><mainClass>com.spt.ZgbasApplication</mainClass></configuration> only. NO <executions>, NO <layout>.

    PLACEHOLDER CLASSES:
    Each library module gets a minimal PackageMarker.java — a simple class with package declaration and a private constructor (or just an empty class body). Use the exact package names specified in <behavior>. These prevent empty-source-directory edge cases in Maven/IDE.

    .gitignore:
    Standard Java/Maven entries: target/, .idea/, *.iml, logs/, *.log. Do NOT ignore .java-version or .planning/.
  </action>
  <verify>
    <automated>
      export JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home
      /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml -f /Users/alan/WorkSpace/IDEA/zgbas-plus/pom.xml clean compile 2>&1 | tee /tmp/zgbas-task1-compile.log
      echo "--- ERROR COUNT ---"
      grep -c '^\[ERROR\]' /tmp/zgbas-task1-compile.log
      echo "--- BUILD RESULT ---"
      grep 'BUILD SUCCESS' /tmp/zgbas-task1-compile.log
      # Assert: grep -c output is 0 AND BUILD SUCCESS line exists
    </automated>
  </verify>
  <acceptance_criteria>
    1. Root pom.xml contains the string "spring-boot-starter-parent" (D-02 grandparent inheritance) and does NOT contain "spt-parent" or "spt-tools-parent" or "bas-parent" (D-01/D-03).
    2. Root pom.xml contains "${project.version}" in dependencyManagement for exactly 4 internal modules (D-08 version unification).
    3. zgbas-quartz/pom.xml contains dependency artifactId "zgbas-system" (D-08/D-09 intentional quartz→system edge).
    4. zgbas-system/pom.xml does NOT contain dependency artifactId "zgbas-quartz" (D-09 anti-cycle constraint).
    5. Only zgbas-admin/pom.xml contains "spring-boot-maven-plugin"; none of the 4 library module POMs contain it (D-11 fat-jar-only-admin).
    6. zgbas-admin/pom.xml does NOT contain "<layout>ZIP</layout>" or "excludeGroupIds" or "maven-dependency-plugin" (D-11 drop old strategy).
    7. `grep -c '^\[ERROR\]' /tmp/zgbas-task1-compile.log` outputs "0" (locale-independent, ALIGN-03/BUILD-02).
    8. `/tmp/zgbas-task1-compile.log` contains the string "BUILD SUCCESS" (BUILD-02).
    9. `mvn -s zg_settings.xml -q dependency:tree` for zgbas-admin shows all 4 internal modules as transitive/direct dependencies (BUILD-01 topology).
    10. Root pom.xml java.version property is "1.8" (BUILD-03).
  </acceptance_criteria>
  <done>
    All 11 files created. mvn clean compile (JDK8 + zg_settings.xml) exits 0 with zero [ERROR] lines and prints BUILD SUCCESS across all 5 modules. Root parent chain is spring-boot-starter-parent:2.5.9 only (no spt-parent). Module topology matches D-08. Only admin declares spring-boot-maven-plugin.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create boot module artifacts (ZgbasApplication + application.yml + context-load test) and run full Phase 1 acceptance suite</name>
  <files>
    zgbas-admin/src/main/java/com/spt/ZgbasApplication.java,
    zgbas-admin/src/main/resources/application.yml,
    zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java
  </files>
  <read_first>
    - .planning/phases/01-compile-skeleton/01-RESEARCH.md — § Code Examples (lines ~444-510): POC-verified Boot Class, application.yml, and Acceptance Commands. These are the source of truth for file contents and verification command patterns.
    - .planning/phases/01-compile-skeleton/01-RESEARCH.md — § Common Pitfalls §3: explains why NO exclude=DataSourceAutoConfiguration is needed (classpath has no db starter). DO NOT add exclude.
    - .planning/phases/01-compile-skeleton/01-VALIDATION.md — § Wave 0 Requirements: context-load smoke test spec.
    - zgbas-admin/pom.xml (created in Task 1) — confirm spring-boot-starter-web and spring-boot-starter-test are declared.
  </read_first>
  <behavior>
    - ZgbasApplication.java: package com.spt, bare @SpringBootApplication (NO exclude parameter), main method calling SpringApplication.run(ZgbasApplication.class, args). POC-verified to boot empty context in ~0.5s.
    - application.yml: server.port=8080, server.servlet.context-path=/, spring.application.name=zgbas-plus, spring.profiles.active=dev, logging.level.com.spt=info, logging.level.org.springframework=warn. NO datasource, NO redis, NO nacos, NO mybatis, NO shiro config (all deferred to Phase 2+). NO secrets/credentials of any kind.
    - ZgbasApplicationTest.java: package com.spt, @SpringBootTest annotation, single test method contextLoads() with empty body (asserts Spring context starts without exceptions). Uses JUnit 4 (Spring@SpringBootTest semantics from spring-boot-starter-test).
    - Full Phase 1 acceptance: compile zero ERROR, context-load test passes, spring-boot:run boots empty context, package yields fat jar only in admin.
  </behavior>
  <action>
    Create the 3 files using RESEARCH.md § Code Examples as the source of truth.

    ZgbasApplication.java:
    - Package: com.spt
    - Imports: org.springframework.boot.SpringApplication, org.springframework.boot.autoconfigure.SpringBootApplication
    - Annotation: @SpringBootApplication (bare — NO exclude. Per Pitfall §3, classpath has no db starter so DataSourceAutoConfiguration is not present to exclude.)
    - Class: public class ZgbasApplication with public static void main(String[] args) calling SpringApplication.run(ZgbasApplication.class, args)
    - Add a Javadoc comment noting: Phase 1 skeleton boot class, empty Spring context, com.spt as ComponentScan base for future business packages.

    application.yml:
    - server.port: 8080
    - server.servlet.context-path: /
    - spring.application.name: zgbas-plus
    - spring.profiles.active: dev
    - logging.level.com.spt: info
    - logging.level.org.springframework: warn
    - DO NOT add any datasource/jdbc/jpa/redis/nacos/mybatis/shiro blocks (D-07 empty context).

    ZgbasApplicationTest.java:
    - Package: com.spt
    - Imports: org.junit.jupiter.api.Test (or org.junit.Test for JUnit 4 — check which spring-boot-starter-test:2.5.9 provides by default; Spring Boot 2.5.9 ships JUnit 5 via junit-jupiter). Use import org.junit.jupiter.api.Test and org.springframework.boot.test.context.SpringBootTest.
    - Annotation: @SpringBootTest
    - Method: @Test void contextLoads() — empty body (Spring Test loads the application context; if it starts without exceptions, the test passes).

    After creating all 3 files, run the full Phase 1 acceptance suite in this exact order:

    Step A — Context-load smoke test (BUILD-04):
      export JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home
      /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml -f /Users/alan/WorkSpace/IDEA/zgbas-plus/pom.xml -pl zgbas-admin -am test 2>&1 | tee /tmp/zgbas-task2-test.log
      # Assert: Tests run: 1, Failures: 0, Errors: 0, Skipped: 0 AND BUILD SUCCESS

    Step B — Empty-context boot (D-10/BUILD-04):
      /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml -f /Users/alan/WorkSpace/IDEA/zgbas-plus/pom.xml -pl zgbas-admin spring-boot:run > /tmp/zgbas-task2-boot.log 2>&1 &
      BOOT_PID=$!
      # Poll up to 30s for "Started ZgbasApplication" in /tmp/zgbas-task2-boot.log
      # Once found (or timeout), kill $BOOT_PID
      # Assert: /tmp/zgbas-task2-boot.log contains "Started ZgbasApplication" AND does NOT contain "APPLICATION FAILED TO START"

    Step C — Fat jar only in admin (BUILD-05/D-11):
      /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml -f /Users/alan/WorkSpace/IDEA/zgbas-plus/pom.xml clean package -DskipTests 2>&1 | tee /tmp/zgbas-task2-package.log
      # Assert: zgbas-admin/target/*.jar.original exists (admin repackaged)
      # Assert: zgbas-common/target/*.jar.original does NOT exist
      # Assert: zgbas-framework/target/*.jar.original does NOT exist
      # Assert: zgbas-system/target/*.jar.original does NOT exist
      # Assert: zgbas-quartz/target/*.jar.original does NOT exist
  </action>
  <verify>
    <automated>
      # Step A: context-load test
      export JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home
      /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml -f /Users/alan/WorkSpace/IDEA/zgbas-plus/pom.xml -pl zgbas-admin -am test 2>&1 | tee /tmp/zgbas-task2-test.log
      grep 'Tests run:.*Failures: 0.*Errors: 0' /tmp/zgbas-task2-test.log && grep 'BUILD SUCCESS' /tmp/zgbas-task2-test.log

      # Step C: fat jar only admin
      /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml -f /Users/alan/WorkSpace/IDEA/zgbas-plus/pom.xml clean package -DskipTests 2>&1 | tee /tmp/zgbas-task2-package.log
      ls /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-admin/target/*.jar.original >/dev/null 2>&1 && echo "admin repackaged (OK)"
      ls /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-{common,framework,system,quartz}/target/*.jar.original 2>/dev/null && echo "BAD: library module repackaged" || echo "all library modules plain jar (OK)"
      grep -c '^\[ERROR\]' /tmp/zgbas-task2-package.log
      # Assert: error count is 0, BUILD SUCCESS present, admin has .jar.original, no library module has .jar.original
    </automated>
    <human-check>
      Step B (spring-boot:run boot verification) — run the boot command manually and confirm the log shows "Started ZgbasApplication" within a few seconds. Kill the process after confirming. This validates D-10/BUILD-04 empty-context single-process startup.
    </human-check>
  </verify>
  <acceptance_criteria>
    1. ZgbasApplication.java contains "@SpringBootApplication" with NO "exclude" parameter (D-07, Pitfall §3).
    2. ZgbasApplication.java package declaration is "package com.spt;" (BUILD-04 ComponentScan base).
    3. application.yml does NOT contain "datasource" or "password" or "url" or "redis" or "nacos" (D-07 empty context, security: no secrets).
    4. ZgbasApplicationTest.java contains "@SpringBootTest" and "contextLoads" (VALIDATION Wave 0 coverage for BUILD-04).
    5. `grep 'Tests run:.*Failures: 0.*Errors: 0' /tmp/zgbas-task2-test.log` succeeds AND `grep 'BUILD SUCCESS' /tmp/zgbas-task2-test.log` succeeds (BUILD-04 context-load).
    6. /tmp/zgbas-task2-boot.log contains the string "Started ZgbasApplication" (D-10/BUILD-04 empty-context boot).
    7. /tmp/zgbas-task2-boot.log does NOT contain "APPLICATION FAILED TO START" (boot failure check).
    8. `find /Users/alan/WorkSpace/IDEA/zgbas-plus -name '*.jar.original'` matches ONLY files under zgbas-admin/target/ (D-11/BUILD-05 fat-jar-only-admin).
    9. `grep -c '^\[ERROR\]' /tmp/zgbas-task2-package.log` outputs "0" (ALIGN-03 final compile gate).
    10. /tmp/zgbas-task2-package.log contains "BUILD SUCCESS" (BUILD-02 clean package).
  </acceptance_criteria>
  <done>
    ZgbasApplication boots empty Spring context successfully. Context-load smoke test passes (Tests run: 1, 0 failures, 0 errors). spring-boot:run produces "Started ZgbasApplication". mvn clean package produces a fat jar ONLY in zgbas-admin (all 4 library modules are plain jars). Zero [ERROR] across all mvn invocations.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in Phase 1) | Empty Spring context, no endpoints, no DB, no auth, no external API calls. No untrusted input crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | application.yml | mitigate | application.yml contains NO secrets/credentials — no datasource, no redis, no API keys. Only port/profile/logging config. (Per RESEARCH § Security Domain: keep yml pure, no DB creds.) |
| T-01-02 | Spoofing/Tampering | nexus HTTP private repo | accept | zg_settings.xml mirrors to HTTP nexus (47.104.66.178:8081). This is an existing infrastructure constraint outside Phase 1 scope. No code change mitigates network-layer HTTP. |
| T-01-SC | Tampering | Maven dependency resolution | accept | No third-party packages installed — only spring-boot-starter-web and spring-boot-starter-test, both framework starters managed by spring-boot-starter-parent:2.5.9 official BOM. No slopcheck needed (RESEARCH § Package Legitimacy Audit confirms N/A). |
</threat_model>

<verification>
Phase 1 acceptance gate (all must pass before /gsd:verify-work):

1. **ALIGN-03 / BUILD-02** — Zero-error compile:
   `grep -c '^\[ERROR]' == 0` AND `BUILD SUCCESS` in compile log (locale-independent, JDK8-prefixed).

2. **BUILD-01** — Topology correct:
   `dependency:tree` confirms common←none, framework←common, system←{common,framework}, quartz←{common,framework,system}, admin←all 4. system does NOT depend on quartz.

3. **BUILD-03** — Version lock:
   Root pom parent = spring-boot-starter-parent:2.5.9, java.version=1.8, `mvn --version` shows Java 1.8.0_482.

4. **BUILD-04** — Single-process boot:
   contextLoads() test passes. `spring-boot:run` shows "Started ZgbasApplication" with empty context.

5. **BUILD-05** — Fat jar only in admin:
   `find . -name '*.jar.original'` matches only zgbas-admin/target/. No library module has .jar.original.

6. **D-01/D-02/D-03** — Parent chain broken:
   Root pom does NOT reference spt-parent, bas-parent, or spt-tools-parent anywhere.
</verification>

<success_criteria>
- [ ] 5-module Maven aggregator structure exists with correct D-08 topology
- [ ] Root parent inherits spring-boot-starter-parent:2.5.9 (no spt-parent)
- [ ] mvn clean compile (JDK8 + zg_settings.xml) = zero [ERROR] + BUILD SUCCESS
- [ ] ZgbasApplication @SpringBootApplication boots empty context (no DB, no SDK)
- [ ] contextLoads() smoke test passes
- [ ] Only zgbas-admin has .jar.original (fat jar); 4 library modules are plain jars
- [ ] application.yml has no secrets or datasource config
- [ ] No spt-parent / bas-parent / spt-tools-parent BOM anywhere in the project
</success_criteria>

<output>
Create `.planning/phases/01-compile-skeleton/01-01-SUMMARY.md` when done
</output>
