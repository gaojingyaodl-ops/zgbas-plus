---
phase: 02-infrastructure
plan: 06
type: execute
wave: 5
depends_on: [02-04, 02-05]
files_modified:
  - zgbas-admin/src/main/java/com/spt/ZgbasApplication.java
  - zgbas-admin/src/main/java/com/spt/proof/InProcessContract.java
  - zgbas-admin/src/main/java/com/spt/proof/InProcessContractImpl.java
  - zgbas-admin/src/test/java/com/spt/proof/InProcessContractTest.java
  - zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java
  - zgbas-admin/src/main/resources/application.yml
  - zgbas-admin/src/main/resources/application-dev.yml
  - zgbas-admin/src/main/resources/application-prod.yml
autonomous: true
requirements: [INFRA-02, INFRA-04, EXT-03, EXT-04, PERSIST-03, PERSIST-04]

must_haves:
  truths:
    - "ZgbasApplication has 4 infra annotations: @EntityScan(basePackageClasses=IdEntity.class + basePackages=com.spt.bas.client.entity), @EnableJpaRepositories(com.spt.bas.server.dao), @MapperScan(com.spt.bas.system.dao, annotationClass=MyBatisDao.class), @EnableFeignClients(com.spt.sign.client.remote ONLY)"
    - "Config is consolidated to application.yml + application-dev.yml + application-prod.yml (D-P2-14), datasource prefix spring.datasource.druid.* (D-P2-15)"
    - "Flyway disabled + ddl-auto=validate (D-P2-02); secrets are ${VAR:} placeholders in prod, plaintext dev defaults in dev (D-P2-13)"
    - "The interface-as-contract Feign proof compiles + an @Autowired interface resolves to a local @RestController impl (INFRA-02 / D-P2-10)"
    - "@SpringBootTest contextLoads brings up the full context: DataSource + JpaTransactionManager + sample Mapper + 3 external SDK beans + cfca Feign"
  artifacts:
    - path: "zgbas-admin/src/main/java/com/spt/ZgbasApplication.java"
      provides: "boot class with 4 infra annotations (EntityScan/JpaRepositories/MapperScan/EnableFeignClients narrowed)"
      contains: "EnableFeignClients"
    - path: "zgbas-admin/src/main/java/com/spt/proof/InProcessContract.java"
      provides: "pure contract interface (NO @FeignClient) proving D-P2-10 in-process pattern"
      contains: "GetMapping"
    - path: "zgbas-admin/src/main/resources/application.yml"
      provides: "shared config: port 8080, Flyway disabled, ddl-auto=validate, mybatis-plus.mapper-locations, JPA ehcache"
      contains: "flyway"
    - path: "zgbas-admin/src/main/resources/application-prod.yml"
      provides: "all secrets as env-var placeholders (D-P2-13)"
      contains: "DB_PASSWORD"
    - path: "zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java"
      provides: "startup assertions: primaryDataSource, jpaTransactionManager, sampleMapper, externalSdkBeans"
      contains: "jpaTransactionManagerIsPrimary"
  key_links:
    - from: "ZgbasApplication @EntityScan"
      to: "com.spt.bas.client.entity (Plan 05) + com.spt.tools.jpa.vo.IdEntity (Plan 03)"
      via: "basePackageClasses=IdEntity.class adds the IdEntity package — Pitfall 2 defense"
      pattern: "basePackageClasses.*IdEntity"
    - from: "application-dev/prod.yml spring.datasource.druid.*"
      to: "ZgbasDataSourceConfig @ConfigurationProperties(prefix=spring.datasource.druid) (Plan 04)"
      via: "prefix binding — D-P2-15 unified prefix"
      pattern: "spring\\.datasource\\.druid"
    - from: "InProcessContractTest @Autowired InProcessContract"
      to: "InProcessContractImpl @RestController (local bean)"
      via: "Spring MVC resolves interface @GetMapping on local impl — D-P2-10 proof"
      pattern: "InProcessContract"
---

<objective>
Wire the admin boot class with 4 infra annotations, consolidate all config into 3 YAML profiles, prove the interface-as-contract Feign pattern in-process, and extend the startup test to assert the full context (DataSource + JPA TM + sample Mapper + external SDK beans). This is the Wave 5 capstone — it pulls together Plans 01-05 and delivers the final PERSIST-03/04 startup verification + INFRA-02/04 + EXT-03/04.

Purpose: Everything from Waves 1-4 is infrastructure code that cannot prove itself until the boot class wires the annotations and the config provides the keys. This plan makes the @SpringBootTest context-load test the single source of truth for Phase 2 completion (D-P2-03 startup-verification-primary). If context loads without exception, all 14 requirements are satisfied at the infrastructure level.

Output: Modified ZgbasApplication + 2 proof classes + 1 proof test + extended ZgbasApplicationTest + 3 consolidated YAML configs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-infrastructure/02-CONTEXT.md
@.planning/phases/02-infrastructure/02-RESEARCH.md
@.planning/phases/02-infrastructure/02-PATTERNS.md
@.planning/phases/02-infrastructure/02-04-SUMMARY.md
@.planning/phases/02-infrastructure/02-05-SUMMARY.md

**BUILD COMMAND PREFIX:** `JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml`

**Source config files (translate keys from):**
- `/Users/alan/WorkSpace/IDEA/zgbas/basCore/basServer/src/main/resources/application.properties`
- `/Users/alan/WorkSpace/IDEA/zgbas/basCore/basServer/src/main/resources/jdbc.properties`
- `/Users/alan/WorkSpace/IDEA/zgbas/basCore/basServer/src/main/resources/config.properties`
- `/Users/alan/WorkSpace/IDEA/zgbas/basCore/basServer/src/main/resources/application-dev.properties`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Boot annotations + interface-as-contract Feign proof</name>
  <files>zgbas-admin/src/main/java/com/spt/ZgbasApplication.java, zgbas-admin/src/main/java/com/spt/proof/InProcessContract.java, zgbas-admin/src/main/java/com/spt/proof/InProcessContractImpl.java, zgbas-admin/src/test/java/com/spt/proof/InProcessContractTest.java</files>
  <read_first>
    - /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-admin/src/main/java/com/spt/ZgbasApplication.java (the Phase 1 bare @SpringBootApplication you are modifying)
    - .planning/phases/02-infrastructure/02-RESEARCH.md §"B. Boot Class Annotations" lines 648-674 (the EXACT target code for ZgbasApplication)
    - .planning/phases/02-infrastructure/02-RESEARCH.md §"D. Trivial Feign In-Process Proof" lines 705-758 (the EXACT target code for InProcessContract + Impl + Test)
    - .planning/phases/02-infrastructure/02-RESEARCH.md §"Pitfall 2" lines 477-481 (@EntityScan basePackageClasses=IdEntity.class MANDATORY or Unknown entity)
    - .planning/phases/02-infrastructure/02-RESEARCH.md §"Pitfall 5" lines 495-499 (@EnableFeignClients must narrow to com.spt.sign.client.remote ONLY — never broaden)
    - /Users/alan/WorkSpace/IDEA/zgbas/basCore/basServer/src/main/java/com/spt/bas/server/BasServer.java:27-37 (source boot class — the annotation set to adapt/narrow/drop)
    - .planning/phases/02-infrastructure/02-RESEARCH.md §"Pitfall 9" lines 521-525 (sign.server.url required for cfca Feign SpEL resolution — config in Task 2)
  </read_first>
  <action>
    Modify ZgbasApplication.java (package com.spt) per RESEARCH §B lines 650-673. Keep @SpringBootApplication (NO auto-config exclusions — we WANT DataSource + JPA auto-config). Add 4 annotations:
    - @EnableFeignClients(basePackages = "com.spt.sign.client.remote") — D-P2-12: NARROW to sign client ONLY. Never broaden. This prevents double-bean conflict when Phase 4 internal interfaces get local impls (Pitfall 5).
    - @EntityScan(basePackageClasses = IdEntity.class, basePackages = {"com.spt.bas.client.entity"}) — Pitfall 2: basePackageClasses=IdEntity.class adds com.spt.tools.jpa.vo (where IdEntity lives) so @MappedSuperclass resolves; basePackages covers the 239 entities from Plan 05. Import com.spt.tools.jpa.vo.IdEntity.
    - @EnableJpaRepositories(basePackages = {"com.spt.bas.server.dao"}) — targets the 240 Dao from Plan 05.
    - @MapperScan(basePackages = "com.spt.bas.system.dao", annotationClass = MyBatisDao.class) — targets the sample Mapper from Plan 05. Import com.spt.tools.mybatis.annotation.MyBatisDao + org.mybatis.spring.annotation.MapperScan.

    Do NOT add @EnableDiscoveryClient (nacos removed, D-P2-11). Do NOT add @PropertySource (D-P2-14 native profile). Do NOT add @Import of ReportClientConfig (microservice residue). Do NOT add @ComponentScan (com.spt base package from @SpringBootApplication covers all).

    Create the Feign in-process proof (3 files per RESEARCH §D):
    - InProcessContract.java (com.spt.proof): pure interface with @GetMapping("/proof/echo") String echo(@RequestParam("msg") String msg). NO @FeignClient — this is a plain contract interface proving D-P2-10.
    - InProcessContractImpl.java (com.spt.proof): @RestController implements InProcessContract, returns "echo:" + msg. This is the local bean callers @Autowired.
    - InProcessContractTest.java (com.spt.proof, src/test): @SpringBootTest, @Autowired InProcessContract contract, assert contract.echo("hi").isEqualTo("echo:hi"). Proves Spring MVC resolves interface @GetMapping on local impl — the exact pattern Phase 4 uses for 295 clients.

    Compile gate (test execution deferred to Task 2): `mvn -pl zgbas-admin -am compile`. InProcessContractTest is a @SpringBootTest that needs the datasource config created in Task 2 — it CANNOT pass in isolation, so Task 1 verifies compile + file presence only. InProcessContractTest EXECUTES in Task 2's verify (after application-dev.yml exists). This split fixes the misleading per-task red gate flagged by plan-checker W2.
  </action>
  <verify>
    <automated>cd /Users/alan/WorkSpace/IDEA/zgbas-plus && JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml -pl zgbas-admin -am compile 2>&1 | tee /tmp/p2-proof.log | tail -5; echo "ERROR_COUNT=$(grep -c '^\[ERROR\]' /tmp/p2-proof.log)"; echo "FEIGN_NARROW=$(grep -c 'com.spt.sign.client.remote' zgbas-admin/src/main/java/com/spt/ZgbasApplication.java)"; echo "ENTITYSCAN=$(grep -c 'basePackageClasses.*IdEntity' zgbas-admin/src/main/java/com/spt/ZgbasApplication.java)"; echo "PROOF_TEST=$(grep -c 'echo' zgbas-admin/src/test/java/com/spt/proof/InProcessContractTest.java)"</automated>
  </verify>
  <done>
    - ZgbasApplication has @EnableFeignClients(basePackages = "com.spt.sign.client.remote") (FEIGN_NARROW >= 1 — D-P2-12).
    - ZgbasApplication has @EntityScan(basePackageClasses = IdEntity.class, ...) (ENTITYSCAN >= 1 — Pitfall 2 defense).
    - InProcessContract + InProcessContractImpl + InProcessContractTest exist (PROOF_TEST >= 1).
    - zgbas-admin COMPILES clean (ERROR_COUNT == 0). InProcessContractTest EXECUTION + the D-P2-10 proof are gated in Task 2 (after application-dev.yml exists) — plan-checker W2 fix; do NOT expect the proof test to pass in Task 1.
  </done>
</task>

<task type="auto">
  <name>Task 2: Config consolidation (3 YAMLs) + startup test assertions</name>
  <files>zgbas-admin/src/main/resources/application.yml, zgbas-admin/src/main/resources/application-dev.yml, zgbas-admin/src/main/resources/application-prod.yml, zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java</files>
  <read_first>
    - /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-admin/src/main/resources/application.yml (the Phase 1 minimal config you are replacing/extending)
    - .planning/phases/02-infrastructure/02-RESEARCH.md §"E. Config Consolidation Map" lines 761-896 (the EXACT target YAML for all 3 files + the source-to-target key migration table)
    - .planning/phases/02-infrastructure/02-RESEARCH.md §"Validation Architecture / Startup Assertions" lines 1003-1058 (the EXACT target code for extended ZgbasApplicationTest)
    - /Users/alan/WorkSpace/IDEA/zgbas/basCore/basServer/src/main/resources/jdbc.properties (source datasource config — bas.datasource.* + plaintext password tech debt)
    - /Users/alan/WorkSpace/IDEA/zgbas/basCore/basServer/src/main/resources/config.properties (source external URL keys — auth.url/push.server.url/file.server.url/spt.app.secretKey/appCode)
    - /Users/alan/WorkSpace/IDEA/zgbas/basCore/basServer/src/main/resources/application-dev.properties (source sign.server.url for cfca Feign SpEL — Pitfall 9)
    - .planning/phases/02-infrastructure/02-VALIDATION.md §"Wave 0 Requirements" lines 60-66 (the test gaps this task closes)
  </read_first>
  <action>
    Write application.yml (shared config) per RESEARCH §E lines 780-820: server.port=8080, context-path=/ (D-P2-16); spring.profiles.active=dev; spring.flyway.enabled=false (D-P2-02); spring.jpa.hibernate.ddl-auto=validate (D-P2-02); spring.jpa.show-sql=false; hibernate 2nd-level ehcache ENABLE_SELECTIVE (Discretion — copy source behavior); mybatis-plus.mapper-locations=classpath:/mybatis/mappers/*Mapper.xml; mybatis-plus.type-aliases-package=com.spt.bas.client.entity (Pitfall 4 defense). Drop all xxl.job.* keys, drop all spring.cloud.nacos.* keys (INFRA-01).

    Write application-dev.yml per RESEARCH §E lines 822-861: spring.datasource.druid.* with the DEV database url/username/pool params (D-P2-15 prefix — bind to ZgbasDataSourceConfig @ConfigurationProperties); driver-class-name com.mysql.cj.jdbc.Driver; DEV password as a placeholder DEV_PLACEHOLDER_ROTATE_BEFORE_COMMIT (D-P2-13 — NEVER commit the real prod password; the executor must replace this with the actual dev-only password or leave as placeholder). Add the external URL keys: spt.app.secretKey, spt.app.appCode=zgbas, auth.url, push.server.url, file.server.url, sign.server.url (EXT-04 + Pitfall 9 — sign.server.url is REQUIRED for cfca Feign SpEL resolution).

    Write application-prod.yml per RESEARCH §E lines 863-896: ALL secrets as env-var placeholders — spring.datasource.druid.url=${DB_URL:}, username=${DB_USERNAME:}, password=${DB_PASSWORD:} (D-P2-13); spt.app.secretKey=${SPT_APP_SECRET:}, appCode=${SPT_APP_CODE:}; auth.url=${AUTH_URL:}, push.server.url=${PUSH_SERVER_URL:}, file.server.url=${FILE_SERVER_URL:}, sign.server.url=${SIGN_SERVER_URL:}. Use ${VAR} (fail-fast if unset) for secrets per Open Question 3.

    Extend ZgbasApplicationTest.java per RESEARCH §Validation lines 1005-1058. Keep the existing contextLoads() test. Add 4 assertion tests: primaryDataSourceIsPresent() (context.getBean(DataSource.class) not null — PERSIST-03); jpaTransactionManagerIsPrimary() (context.getBean(PlatformTransactionManager.class) instanceof JpaTransactionManager — PERSIST-03 D-P2-03); sampleMapperBeanRegistered() (context.containsBean("sampleMapper") — PERSIST-03 sample Mapper); externalSdkBeansRegistered() (containsBean authOpenFacade + pushClientHttp + fileRemote — EXT-01..03).

    Run the full startup verification: `mvn -pl zgbas-admin -am test -Dtest=ZgbasApplicationTest`. This @SpringBootTest loads the full context (DataSource + JPA TM + mybatis + 239 entities validate + sample Mapper + 3 external beans + cfca Feign). If it fails, diagnose by the exception (Pitfall 1-9 reference in RESEARCH). Common: Pitfall 2 (Unknown entity → fix @EntityScan), Pitfall 9 (SpEL → add sign.server.url to dev yml), Pitfall 1 (ambiguous datasource → confirm @Primary in ZgbasDataSourceConfig from Plan 04).
  </action>
  <verify>
    <automated>cd /Users/alan/WorkSpace/IDEA/zgbas-plus && echo "YML_CHECK=$(ls zgbas-admin/src/main/resources/application.yml zgbas-admin/src/main/resources/application-dev.yml zgbas-admin/src/main/resources/application-prod.yml 2>/dev/null | wc -l)"; echo "FLYWAY_DISABLED=$(grep -c 'flyway:' zgbas-admin/src/main/resources/application.yml)"; echo "DDL_VALIDATE=$(grep -c 'ddl-auto: validate' zgbas-admin/src/main/resources/application.yml)"; echo "PROD_SECRET=$(grep -c 'DB_PASSWORD' zgbas-admin/src/main/resources/application-prod.yml)"; echo "XXL_DROPPED=$(grep -c 'xxl' zgbas-admin/src/main/resources/application.yml)"; echo "NACOS_DROPPED=$(grep -c 'nacos' zgbas-admin/src/main/resources/application.yml)"; echo "PREFIX=$(grep -c 'spring.datasource.druid' zgbas-admin/src/main/resources/application-dev.yml)"; echo "SIGN_URL=$(grep -c 'sign' zgbas-admin/src/main/resources/application-dev.yml)"; echo "TEST_ASSERTIONS=$(grep -c 'jpaTransactionManagerIsPrimary\|sampleMapperBeanRegistered\|externalSdkBeansRegistered\|primaryDataSourceIsPresent' zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java)"; JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml -pl zgbas-admin -am test -Dtest=ZgbasApplicationTest,InProcessContractTest 2>&1 | tee /tmp/p2-startup.log | tail -8; echo "BUILD_RESULT=$(grep -c 'BUILD SUCCESS' /tmp/p2-startup.log)"</automated>
  </verify>
  <done>
    - 3 YAML files exist (YML_CHECK == 3).
    - application.yml has flyway disabled (FLYWAY_DISABLED >= 1), ddl-auto: validate (DDL_VALIDATE >= 1), NO xxl keys (XXL_DROPPED == 0), NO nacos keys (NACOS_DROPPED == 0).
    - application-dev.yml has spring.datasource.druid prefix (PREFIX >= 1 — D-P2-15) + sign.server.url (SIGN_URL >= 1 — Pitfall 9).
    - application-prod.yml has ${DB_PASSWORD} placeholder (PROD_SECRET >= 1 — D-P2-13).
    - ZgbasApplicationTest has 4 new assertions (TEST_ASSERTIONS >= 4).
    - `mvn -pl zgbas-admin -am test -Dtest=ZgbasApplicationTest,InProcessContractTest` BUILD SUCCESS (BUILD_RESULT >= 1) — full context loads AND InProcessContractTest passes (D-P2-10 in-process pattern proven, executes here after application-dev.yml exists): DataSource + JpaTransactionManager + sampleMapper + 3 external beans + 239 entities validate + cfca Feign. This is the D-P2-03 startup-verification gate for ALL of Phase 2.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config files → Spring Environment | Secrets cross from YAML into @Bean init() and DataSource binding — must be env-var in prod |
| Boot class → full context | @EntityScan/@EnableJpaRepositories/@MapperScan/@EnableFeignClients define what gets loaded — misconfiguration = startup failure or security gap |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-P2-secret | Information Disclosure | Plaintext DB password / spt.app.secretKey in git | mitigate | D-P2-13: prod YAML uses ${DB_PASSWORD}/${SPT_APP_SECRET:} placeholders only; dev uses DEV_PLACEHOLDER (never real prod password). Rotate prod DB password (jdbc.properties plaintext is in git history). |
| T-P2-xxl | Information Disclosure | xxl.job accessToken in config | mitigate | Drop all xxl.job.* keys from application.yml entirely (D-P2-11). If Phase 6 needs config, use env-var placeholder. |
| T-P2-druid | Information Disclosure | Druid stat filter exposure | accept | filters: stat mirrors source behavior; no /druid/* auto-exposed without druid.stat-view-servlet.enabled=true (safe by default). Prod should firewall if enabled later. |
| T-P2-feign-narrow | Elevation | @EnableFeignClients too broad → double-bean / proxy injection | mitigate | D-P2-12: narrow to com.spt.sign.client.remote ONLY. Pitfall 5 defense. Never broaden. |
| T-P2-entityscan | DoS / Tampering | Missing IdEntity package → Unknown entity at runtime | mitigate | Pitfall 2: @EntityScan(basePackageClasses=IdEntity.class) mandatory. Startup test catches mapping errors via ddl-auto=validate. |
| T-P2-sign-spel | DoS | cfca Feign SpEL #{signServerConfig.url} unresolvable | mitigate | Pitfall 9: sign.server.url set in application-dev.yml + application-prod.yml. SignClientConfig in jar auto-registers via com.spt scan. Startup test catches BeanExpressionException. |
</threat_model>

<verification>
- 3 YAML files present; Flyway disabled; ddl-auto=validate; no xxl/nacos keys; datasource.druid prefix; prod secrets env-var.
- ZgbasApplication: @EnableFeignClients narrowed + @EntityScan with IdEntity basePackageClasses + @EnableJpaRepositories + @MapperScan.
- InProcessContractTest passes (D-P2-10 in-process pattern proven).
- ZgbasApplicationTest passes with 4 new assertions (full context load = D-P2-03 gate for all Phase 2).
- This is the final gate: `mvn clean test` from root green.
</verification>

<success_criteria>
Boot class wired with 4 infra annotations (EXT-03 narrowed Feign, PERSIST-03 dual-ORM scan). Config consolidated to 3 YAML profiles with secrets externalized (INFRA-04, EXT-04, D-P2-13/14/15). Interface-as-contract Feign pattern proven in-process (INFRA-02, D-P2-10). Full @SpringBootTest context-load passes with assertions proving DataSource + JpaTransactionManager @Primary + sample Mapper + 3 external SDK beans (PERSIST-03/04, EXT-01..03). Phase 2 infrastructure is complete and verified.
</success_criteria>

<output>
Create `.planning/phases/02-infrastructure/02-06-SUMMARY.md` when done
</output>
