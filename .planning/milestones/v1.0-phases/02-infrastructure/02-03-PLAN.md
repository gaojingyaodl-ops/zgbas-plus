---
phase: 02-infrastructure
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - zgbas-common/src/main/java/com/spt/tools/jpa/**
  - zgbas-common/src/main/java/com/spt/tools/web/**
  - zgbas-common/src/main/java/com/spt/tools/mybatis/**
  - zgbas-common/src/main/java/com/spt/tools/shiro/**
  - zgbas-common/src/main/java/com/spt/tools/aop/**
  - zgbas-common/src/main/java/com/spt/tools/config/**
  - zgbas-common/pom.xml
autonomous: true
requirements: [INLINE-03, INLINE-04, PERSIST-04]

must_haves:
  truths:
    - "spt-tools-jpa/web/mybatis/shiro/aop (60 source files) are inlined verbatim into zgbas-common"
    - "spt-tools-config (2 source files) is inlined with kafka/redis deps dropped (D-P2-06 gotcha)"
    - "The JPA audit chain (IdEntity @MappedSuperclass + @EntityListeners(EntityListener.class) + EntityListener @PrePersist/@PreUpdate) is preserved byte-for-byte (PERSIST-04)"
    - "After all 10 modules are inlined, no spt-tools-* jar remains in the dependency graph (INLINE-04)"
    - "Each layer compiles green before the next (compile-gate ordering)"
  artifacts:
    - path: "zgbas-common/src/main/java/com/spt/tools/jpa/vo/IdEntity.java"
      provides: "@MappedSuperclass @EntityListeners(EntityListener.class) — the audit-enabled base entity all 239 entities extend"
      contains: "EntityListener"
    - path: "zgbas-common/src/main/java/com/spt/tools/jpa/dao/BaseDao.java"
      provides: "@NoRepositoryBean base Dao interface (PagingAndSortingRepository + JpaSpecificationExecutor) all 240 Dao extend"
      contains: "PagingAndSortingRepository"
    - path: "zgbas-common/src/main/java/com/spt/tools/jpa/listener/EntityListener.java"
      provides: "@PrePersist/@PreUpdate audit callback (createdDate/updatedDate via PropertyUtils.setProperty)"
      contains: "PrePersist"
    - path: "zgbas-common/src/main/java/com/spt/tools/mybatis/annotation/MyBatisDao.java"
      provides: "@Component-meta-annotated mapper marker used by @MapperScan(annotationClass=MyBatisDao.class)"
      contains: "Component"
  key_links:
    - from: "zgbas-common/src/main/java/com/spt/tools/jpa/config/ToolsJpaConfig.java"
      to: "framework ZgbasDataSourceConfig (Wave 4)"
      via: "@ConditionalOnMissingBean backoff — framework @Primary DataSource defeats this collision (Pitfall 1)"
      pattern: "ConditionalOnMissingBean"
    - from: "zgbas-common/src/main/java/com/spt/tools/jpa/vo/IdEntity.java"
      to: "zgbas-system entities (Wave 4 Plan 05)"
      via: "all 239 entities extend IdEntity — verbatim package must match or compile breaks"
      pattern: "extends IdEntity|extends DataEntity"
---

<objective>
Inline spt-tools layers 3 and 4 (jpa/web/mybatis/shiro/aop, then config) verbatim into zgbas-common. This completes INLINE-03 (the hardest layer — jpa has 1226 references and contains the IdEntity/BaseDao/EntityListener audit chain), PERSIST-04 (audit code preserved byte-for-byte), and finalizes INLINE-04 (verify no spt-tools jar remains).

Purpose: The jpa layer is the most critical — it contains IdEntity (the @MappedSuperclass all 239 entities extend), BaseDao (all 240 Dao extend it), EntityListener (the @PrePersist/@PreUpdate audit callback), and ToolsJpaConfig (the colliding @Bean("datasource") that Wave 4 must defeat). The mybatis layer contains MyBatisDao annotation + ToolsMybatisConfig (the other colliding datasource bean). These must be inlined before Wave 4 wiring and Wave 4 entity copy.

Output: ~62 new source files under zgbas-common/src/main/java/com/spt/tools/{jpa,web,mybatis,shiro,aop,config}/, compile-gated green, with final INLINE-04 verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-infrastructure/02-CONTEXT.md
@.planning/phases/02-infrastructure/02-RESEARCH.md
@.planning/phases/02-infrastructure/02-PATTERNS.md
@.planning/phases/02-infrastructure/02-02-SUMMARY.md

**BUILD COMMAND PREFIX:** `JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml`

**Source tree (verbatim copy origin):** `/Users/alan/WorkSpace/IDEA/tools/spt-tools-{jpa,web,mybatis,shiro,aop,config}/src/main/java/com/spt/tools/`

<interfaces>
<!-- Load-bearing exports for downstream plans. Copy verbatim — do NOT modify. -->

From spt-tools-jpa (consumed by Plan 05 entities/Dao + Plan 04 wiring):
- com.spt.tools.jpa.vo.IdEntity — @MappedSuperclass @EntityListeners(EntityListener.class) extends DataEntity
- com.spt.tools.jpa.dao.BaseDao<T> — @NoRepositoryBean extends PagingAndSortingRepository<T,Long>, JpaSpecificationExecutor<T>
- com.spt.tools.jpa.listener.EntityListener — @PrePersist/@PreUpdate, uses PropertyUtils.setProperty for createdDate/updatedDate
- com.spt.tools.jpa.config.ToolsJpaConfig — declares @Bean("datasource") @ConditionalOnMissingBean (the collision Wave 4 defeats)

From spt-tools-mybatis (consumed by Plan 04 wiring + Plan 05 sample Mapper):
- com.spt.tools.mybatis.annotation.MyBatisDao — @Component-meta-annotated mapper marker
- com.spt.tools.mybatis.config.ToolsMybatisConfig — declares @Bean("datasource") @ConditionalOnMissingBean (the other collision) + MyMetaObjectHandler + PerformanceInterceptor

From spt-tools-config:
- com.spt.tools.config.EnableToolsWebConfig + EnableToolsServiceConfig — composite @Enable annotations (import core/http/jpa/shiro/aop only — NOT kafka/redis)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inline spt-tools-jpa/web/mybatis/shiro/aop (layer 3) — compile gate</name>
  <files>zgbas-common/src/main/java/com/spt/tools/jpa/**, zgbas-common/src/main/java/com/spt/tools/web/**, zgbas-common/src/main/java/com/spt/tools/mybatis/**, zgbas-common/src/main/java/com/spt/tools/shiro/**, zgbas-common/src/main/java/com/spt/tools/aop/**, zgbas-common/pom.xml</files>
  <read_first>
    - The 5 source directories under /Users/alan/WorkSpace/IDEA/tools/spt-tools-{jpa,web,mybatis,shiro,aop}/src/main/java/com/spt/tools/ (verbatim copy origins)
    - The 5 source module poms (spt-tools-{jpa,web,mybatis,shiro,aop}/pom.xml) — confirm their internal + 3rd-party deps (jpa depends on core+data; web depends on core+data; mybatis depends on core+data; shiro depends on core; aop depends on core)
    - /Users/alan/WorkSpace/IDEA/tools/spt-tools-jpa/src/main/java/com/spt/tools/jpa/vo/IdEntity.java (confirm @MappedSuperclass + @EntityListeners(EntityListener.class) — PERSIST-04 audit chain root)
    - /Users/alan/WorkSpace/IDEA/tools/spt-tools-jpa/src/main/java/com/spt/tools/jpa/listener/EntityListener.java (confirm @PrePersist/@PreUpdate audit callback)
    - /Users/alan/WorkSpace/IDEA/tools/spt-tools-jpa/src/main/java/com/spt/tools/jpa/dao/BaseDao.java (confirm @NoRepositoryBean + PagingAndSortingRepository + JpaSpecificationExecutor)
    - .planning/phases/02-infrastructure/02-RESEARCH.md §"Pattern 1" cross-module dep map lines 407-422
  </read_first>
  <action>
    Copy 5 directory trees verbatim (D-P2-07 — zero renames, zero import edits) into zgbas-common/src/main/java/com/spt/tools/:
    - jpa/ (14 files — IdEntity, BaseDao, EntityListener, DataEntity, ToolsJpaConfig; depends on core+data; brings spring-boot-starter-data-jpa + hibernate-ehcache usage)
    - web/ (6 files; depends on core+data)
    - mybatis/ (13 files — MyBatisDao annotation, ToolsMybatisConfig, MybatisPlusCodeGenerator; depends on core+data; brings mybatis-plus-boot-starter 3.1.2 + mybatis-plus-generator + freemarker usage)
    - shiro/ (18 files — ShiroUtil etc; depends on core; brings shiro-spring/ehcache/cas 1.8.0 usage. Phase 2 only compiles these; Shiro activates in Phase 3)
    - aop/ (9 files — @ServiceLogAop etc; depends on core; brings spring-boot-starter-aop + jdbc usage)

    Run the compile gate: `mvn -pl zgbas-common -am compile`. If `[ERROR]` lines appear, add missing 3rd-party deps to zgbas-common/pom.xml (never modify copied source). Repeat until ERROR_COUNT == 0. This is the largest layer (60 files) — expect possible dep gaps (e.g., shiro-cas for CAS client classes, mybatis-plus-generator transitive deps). Fix pom only.

    CRITICAL — do not touch ToolsJpaConfig.java or ToolsMybatisConfig.java: their @Bean("datasource") @ConditionalOnMissingBean declarations are intentionally left as-is; Wave 4 ZgbasDataSourceConfig will declare @Primary DataSource first so both back off (Pitfall 1).
  </action>
  <verify>
    <automated>cd /Users/alan/WorkSpace/IDEA/zgbas-plus && JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml -pl zgbas-common -am compile 2>&1 | tee /tmp/p2-l3.log | tail -3; echo "ERROR_COUNT=$(grep -c '^\[ERROR\]' /tmp/p2-l3.log)"; echo "JPA_AUDIT=$(grep -c 'EntityListeners' zgbas-common/src/main/java/com/spt/tools/jpa/vo/IdEntity.java)"; echo "BASEDAO=$(grep -c 'PagingAndSortingRepository' zgbas-common/src/main/java/com/spt/tools/jpa/dao/BaseDao.java)"; echo "MYBATISDAO=$(grep -c 'Component' zgbas-common/src/main/java/com/spt/tools/mybatis/annotation/MyBatisDao.java)"</automated>
  </verify>
  <done>
    - jpa/ 14 files, web/ 6 files, mybatis/ 13 files, shiro/ 18 files, aop/ 9 files present (±1 each).
    - `mvn -pl zgbas-common -am compile` BUILD SUCCESS, ERROR_COUNT == 0.
    - IdEntity.java contains @EntityListeners (JPA_AUDIT >= 1 — PERSIST-04 audit chain preserved).
    - BaseDao.java contains PagingAndSortingRepository (BASEDAO >= 1).
    - MyBatisDao.java contains @Component meta-annotation (MYBATISDAO >= 1).
    - ToolsJpaConfig.java and ToolsMybatisConfig.java are unmodified (grep `ConditionalOnMissingBean` present in both).
    - All packages com.spt.tools.{jpa,web,mybatis,shiro,aop}.* verbatim (D-P2-07).
  </done>
</task>

<task type="auto">
  <name>Task 2: Inline spt-tools-config (layer 4) + final INLINE-04 verification</name>
  <files>zgbas-common/src/main/java/com/spt/tools/config/**</files>
  <read_first>
    - /Users/alan/WorkSpace/IDEA/tools/spt-tools-config/src/main/java/com/spt/tools/config/EnableToolsWebConfig.java (confirm imports: core/http/jpa/shiro/aop only — NOT kafka/redis)
    - /Users/alan/WorkSpace/IDEA/tools/spt-tools-config/src/main/java/com/spt/tools/config/EnableToolsServiceConfig.java (same check)
    - /Users/alan/WorkSpace/IDEA/tools/spt-tools-config/pom.xml (NOTE: declares spt-tools-kafka + spt-tools-redis as provided — DROP these, do not add to common/pom.xml; verified RESEARCH §424 + PATTERNS §41)
    - .planning/phases/02-infrastructure/02-RESEARCH.md §424 (the config-module gotcha)
  </read_first>
  <action>
    Copy the 2 source files (EnableToolsWebConfig.java + EnableToolsServiceConfig.java) verbatim into zgbas-common/src/main/java/com/spt/tools/config/. Do NOT add spt-tools-kafka or spt-tools-redis to zgbas-common/pom.xml — the 2 source files only import EnableTools annotations from core/http/jpa/shiro/aop (verified by reading both files), so kafka/redis classes are never referenced.

    Run the final compile gate: `mvn -pl zgbas-common -am compile` (0 `[ERROR]`). Then run the full reactor compile to confirm all modules still build: `mvn clean compile`.

    Run the INLINE-04 final verification: `mvn -pl zgbas-admin dependency:tree -Dincludes=com.spt.tools:spt-tools-*` must return empty (no spt-tools jar anywhere in the graph — all 10 modules are now source-inline).
  </action>
  <verify>
    <automated>cd /Users/alan/WorkSpace/IDEA/zgbas-plus && JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml clean compile 2>&1 | tee /tmp/p2-l4.log | tail -3; echo "FULL_ERROR_COUNT=$(grep -c '^\[ERROR\]' /tmp/p2-l4.log)"; echo "CONFIG_COUNT=$(find zgbas-common/src/main/java/com/spt/tools/config -name '*.java' | wc -l)"; echo "SPT_TOOLS_JAR=$(JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml -pl zgbas-admin dependency:tree -Dincludes=com.spt.tools:spt-tools-* 2>/dev/null | grep -c 'spt-tools')"</automated>
  </verify>
  <done>
    - config/ has 2 files (EnableToolsWebConfig + EnableToolsServiceConfig).
    - zgbas-common/pom.xml does NOT declare spt-tools-kafka or spt-tools-redis (grep returns 0).
    - `mvn clean compile` across all 6 reactor projects BUILD SUCCESS, FULL_ERROR_COUNT == 0.
    - INLINE-04 final: `dependency:tree -Dincludes=com.spt.tools:spt-tools-*` returns SPT_TOOLS_JAR == 0 (zero spt-tools jar deps — all 10 modules are source-inline).
    - Total inlined: all 10 spt-tools modules (~172 main classes) now live in zgbas-common.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source project → zgbas-common | Verbatim copy from /Users/alan/WorkSpace/IDEA/tools (trusted migration origin) |
| Compile classpath → common jar | All 10 spt-tools modules now compile into a single common jar; no external spt-tools jar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-P2-03-copy | Tampering | Verbatim source copy integrity (jpa is highest-risk — 1226 refs) | accept | Trusted migration origin; compile gate validates. IdEntity/BaseDao/EntityListener chain must compile intact or entities fail in Plan 05. |
| T-P2-03-kafka | Tampering | spt-tools-config declares kafka/redis as provided | mitigate | DROP kafka/redis deps from common pom — verified the 2 config source files never import them (RESEARCH §424). |
| T-P2-04-audit | Information Disclosure / Repudiation | Audit chain (PERSIST-04) | mitigate | IdEntity @EntityListeners(EntityListener.class) + EntityListener @PrePersist/@PreUpdate preserved verbatim — createdDate/updatedDate auto-populated on all entity persists. Compile gate confirms no breakage. |
</threat_model>

<verification>
- Layer 3 (jpa/web/mybatis/shiro/aop): `mvn -pl zgbas-common -am compile` 0 `[ERROR]`, 60 files present.
- Layer 4 (config): 2 files present, no kafka/redis deps.
- Full reactor: `mvn clean compile` 0 `[ERROR]`.
- INLINE-04 final: `dependency:tree -Dincludes=com.spt.tools:spt-tools-*` empty.
- PERSIST-04: IdEntity.java @EntityListeners present; EntityListener.java @PrePersist present.
</verification>

<success_criteria>
All 10 spt-tools modules (~172 classes) are inlined verbatim into zgbas-common (INLINE-01..03 complete). No spt-tools jar remains in the dependency graph (INLINE-04). The JPA audit chain (IdEntity + EntityListener) is preserved byte-for-byte (PERSIST-04). Foundation is ready for Wave 4 (framework wiring + entity/Dao copy).
</success_criteria>

<output>
Create `.planning/phases/02-infrastructure/02-03-SUMMARY.md` when done
</output>
