---
phase: 05-report-migration
plan: 05
type: execute
wave: 4
depends_on: [05-04]
files_modified:
  - zgbas-admin/src/main/java/com/spt/bas/report/server/api/   # 54 @RestController NEW
  - zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java   # ADD W5 probes
autonomous: true
requirements: [REPORT-02]
tags: [api-controllers, path-prefix, admin, w5-probes, mapper-resolve, ambiguous-mapping]

must_haves:
  truths:
    - "54 report @RestController 迁入 zgbas-admin 保包名 com.spt.bas.report.server.api"
    - "ReportFeignPathConfig path-prefix 生效：/spt-bas-report/rpt/... 映射到 report api（reportApiPathPrefixWiring_probe 断言）"
    - "无 AmbiguousMappingException（14 处 BFF 冲突被 /spt-bas-report 前缀隔离）"
    - "53 report Mapper namespace 全注册（allReportMappersResolve 断言）"
    - "config/ 目录 SKIP（BasicErrorController 不迁，无 excludeFilter 需要——0 imports 已验证）"
  artifacts:
    - path: "zgbas-admin/src/main/java/com/spt/bas/report/server/api/"
      provides: "54 前端直连 @RestController（D-P5-01 落 admin）"
      contains: "@RestController"
    - path: "zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java"
      provides: "W5 probe（mapper resolve + path-prefix wiring）"
      contains: "allReportMappersResolve"
  key_links:
    - from: "54 @RestController @RequestMapping"
      to: "ReportFeignPathConfig addPathPrefix"
      via: "HandlerTypePredicate.forBasePackage(com.spt.bas.report.server.api)"
      pattern: "spt-bas-report"
    - from: "Feign 契约 path = spt-bas-report + /rpt/..."
      to: "report api 实际暴露路径 /spt-bas-report/rpt/..."
      via: "path-prefix 使契约路径与 api 路径天然匹配（D-P5-03 闭环）"
      pattern: "spt-bas-report/rpt"
    - from: "report api @Autowired IRpt*Service"
      to: "zgbas-system report service（Wave 1-3 已就位）"
      via: "admin 依赖 system（D-08 拓扑）"
      pattern: "IRpt.*Service"
---

<objective>
Wave 4：54 report `@RestController`（前端直连 edge controller）迁入 `zgbas-admin` 保包名 `com.spt.bas.report.server.api`（D-P5-01）。Wave 0 已建 `ReportFeignPathConfig` 给此包的 @RequestMapping 加 `/spt-bas-report` 前缀——本 Wave api 落位后前缀生效，解 14 处 BFF 路径冲突（Pitfall 1）+ 使 Feign 契约路径与 api 路径匹配（D-P5-03 闭环）。

新增 2 个 W5 probe：`allReportMappersResolve`（53 Mapper namespace 注册验证）+ `reportApiPathPrefixWiring_probe`（path-prefix 生效验证）。

Purpose: 关闭 REPORT-02（报表 Controller 迁入 admin，HTTP 接口可访问）+ 证明 D-P5-03 path-prefix 机制在 54 api 落位后运行正确（启动期无 AmbiguousMappingException）。

Output: 54 api net-new + 2 W5 probe 方法新增。
</objective>

<execution_context>
@/Users/alan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-report-migration/05-CONTEXT.md
@.planning/phases/05-report-migration/05-RESEARCH.md
@.planning/phases/05-report-migration/05-PATTERNS.md
@.planning/phases/05-report-migration/05-VALIDATION.md

**BUILD PREFIX:** `JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml`

**源仓库:** `/Users/alan/WorkSpace/IDEA/zgbas` 分支 `feat-系统重构v5.0`

**Wave 0-3 已就位:** reportClient 内联 + ReportFeignPathConfig + @MapperScan + type-aliases + 53 Mapper+XML+service 全量。

**config/ 目录 SKIP 决策（05-RESEARCH Pattern 5）：** reportServer/config/（6 文件）全部 SKIP——WebAppConfig/FrameworkConfig 重复 Phase 2/4 bean / TransactionConfig 整文件注释 / ScheduleConfig 无 @Scheduled / BasReportJobConfig xxl-job→Phase 6 / BasicErrorController 同 simple-name 冲突。**已验证 0 个 api/service/dao/util 文件 import `com.spt.bas.report.server.config`**——无需迁 config 目录，无需加 excludeFilter（类不存在于单体）。

<interfaces>
<!-- 54 api 标准签名（来自源 RptFundReceivableStatisticsApi.java） -->
@RestController
@RequestMapping(value = "/rpt/fundReceivableStatistics")   // 无 /spt-bas-report 前缀（ReportFeignPathConfig 运行期加）
public class RptFundReceivableStatisticsApi {
    @Autowired private IRptFundReceivableStatisticsService service;
    @PostMapping("findPage")
    public Page<RptFundReceivableStatistics> findPage(@RequestBody RptFundReceivableStatisticsVo vo) { ... }
}
→ 54 个此型 controller，每个 @Autowired system 的 IRpt*Service（Wave 1-3 已就位）

<!-- ReportFeignPathConfig（Wave 0 已建）作用机制 -->
addPathPrefix("/spt-bas-report", HandlerTypePredicate.forBasePackage("com.spt.bas.report.server.api"))
→ @RequestMapping("/rpt/foo") 运行期暴露为 /spt-bas-report/rpt/foo
→ Feign 契约 path = "spt-bas-report" + "/rpt/foo" 匹配（D-P5-03 闭环）

<!-- 14 处 BFF 冲突清单（Pitfall 1，path-prefix 解决）-->
冲突路径：/bs/company, /budget/settlementTotal, /business/manager/workbench, /evaluate/total,
/person/cost/chart, /region/month/sales, /rpt/business, /rpt/companyReceivables, /rpt/contractFinance,
/rpt/fundReceivableStatistics, /rpt/invoiceBill, /rpt/invoiceBillStatistics, /rpt/notBillStatistics, /rpt/notInvoiceBillStatistics
→ 加 /spt-bas-report 前缀后 report api 在 /spt-bas-report/* 命名空间，BFF 在根 / —— 零冲突
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 照搬 54 report @RestController 到 zgbas-admin + compile gate</name>
  <files>
    zgbas-admin/src/main/java/com/spt/bas/report/server/api/
  </files>
  <read_first>
    - .planning/phases/05-report-migration/05-PATTERNS.md §W5（54 api bulk-copy task-shape + path-prefix 关键约束 + config/ SKIP 决策）
    - .planning/phases/05-report-migration/05-RESEARCH.md §Pitfall 1（14 处 BFF 冲突清单）+ §Pitfall 2（Feign path diverge）+ §Pattern 5（config/ SKIP 矩阵）
    - .planning/phases/05-report-migration/05-CONTEXT.md §decisions D-P5-01（Controller 落 admin 理由）
    - 源 api 全目录: /Users/alan/WorkSpace/IDEA/zgbas/basReport/reportServer/src/main/java/com/spt/bas/report/server/api/（54 Rpt*Api.java + 1 pkg-info）
    - 代表性源文件（验证 @RestController + @RequestMapping + @Autowired IRpt*Service 签名）: 源 api/RptFundReceivableStatisticsApi.java
    - 当前单体 ReportFeignPathConfig（验证 path-prefix 已指向 com.spt.bas.report.server.api）: /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-system/src/main/java/com/spt/bas/client/config/ReportFeignPathConfig.java
  </read_first>
  <action>
    照搬保包名（D-P2-07 / D-P5-01），把源 `reportServer/api/` 目录的 54 个 `Rpt*Api.java`（排除 pkg-info.java）verbatim 复制到 `zgbas-admin/src/main/java/com/spt/bas/report/server/api/`（保包名 `com.spt.bas.report.server.api`）。

    关键约束：
    1. **54 api 文件 1:1 照搬，零修改**——`@RequestMapping("/rpt/...")` 保留原值，运行期由 `ReportFeignPathConfig`（Wave 0 已建）自动加 `/spt-bas-report/` 前缀。
    2. **不迁 config/ 目录**（Pattern 5 SKIP 决策）——BasicErrorController / FrameworkConfig / WebAppConfig 等全部 SKIP。已验证 0 个 api 文件 import `report.server.config`，无编译依赖。
    3. **不迁 task/ + command/ + listener/**（Phase 6 xxl-job/quartz 范围）。
    4. **不加 excludeFilter**——config/ 不迁则 `com.spt.bas.report.server.config.BasicErrorController` 类不存在于单体，无法加入 excludeFilters（类不存在会导致编译错误）。

    完成后跑全模块 compile gate——验证 54 api 编译通过（@Autowired IRpt*Service 全部在 system 就位）+ 启动期无 AmbiguousMappingException（path-prefix 隔离生效）。
  </action>
  <verify>
    <automated>
cd /Users/alan/WorkSpace/IDEA/zgbas-plus && \
echo "=== 54 api 计数 ===" && \
echo "API_COUNT=$(find zgbas-admin/src/main/java/com/spt/bas/report/server/api -name 'Rpt*Api.java' -not -name 'package-info*' | wc -l)" && \
echo "RESTCONTROLLER=$(find zgbas-admin/src/main/java/com/spt/bas/report/server/api -name 'Rpt*Api.java' -exec grep -l '@RestController' {} \; | wc -l)" && \
echo "=== config/ 未迁验证 ===" && \
echo "CONFIG_ABSENT=$(find zgbas-admin/src/main/java/com/spt/bas/report/server/config -name '*.java' 2>/dev/null | wc -l)" && \
echo "=== compile gate（全模块）===" && \
JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home \
/Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml \
-am compile 2>&1 | tee /tmp/p5-05-t1.log | tail -5 && \
echo "ERROR_COUNT=$(grep -c '^\[ERROR\]' /tmp/p5-05-t1.log)" && \
echo "CANNOT_FIND=$(grep -cE 'cannot find symbol|找不到符号' /tmp/p5-05-t1.log)" && \
echo "AMBIGUOUS=$(grep -c 'AmbiguousMappingException' /tmp/p5-05-t1.log)"
    </automated>
  </verify>
  <done>
    - API_COUNT == 54 且 RESTCONTROLLER == 54（全部 @RestController 标注）
    - CONFIG_ABSENT == 0（config/ 目录未迁，无编译依赖）
    - compile ERROR_COUNT == 0 且 CANNOT_FIND == 0 且 AMBIGUOUS == 0（无路径冲突——path-prefix 隔离生效）
  </done>
</task>

<task type="auto">
  <name>Task 2: W5 probes（allReportMappersResolve + reportApiPathPrefixWiring_probe）</name>
  <files>
    zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java
  </files>
  <read_first>
    - .planning/phases/05-report-migration/05-VALIDATION.md §Per-Task Verification Map（05-05-xx 行）+ §Wave 0 Requirements
    - .planning/phases/05-report-migration/05-PATTERNS.md §W6 验证 probe 扩展（allReportMappersResolve + reportApiPathPrefixWiring_probe 全文模板）
    - .planning/phases/05-report-migration/05-RESEARCH.md §Validation Architecture §Bean-Resolution Proof + §Wave 5 gaps
    - 当前 ZgbasApplicationTest（含 Wave 0 reportFeignSelfLoopbackWiring_probe）: /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java
  </read_first>
  <action>
    在 ZgbasApplicationTest 新增 2 个 W5 probe 测试方法（**不加 @Disabled，Wave 4 即运行**）：

    (1) **`allReportMappersResolve`**（REPORT-01 + PERSIST-02）：注入 `SqlSessionFactory`，断言 report Mapper namespace 已注册。验证代表性 namespace：
    - `sf.getConfiguration().hasStatement("com.spt.bas.report.server.dao.RptFundReceivableStatisticsMapper.findPage")` == true
    - `sf.getConfiguration().hasStatement("com.spt.bas.report.server.dao.RptCtrContractReportMapper.findRptContractPage")` == true（最复杂 mapper）
    - `sf.getConfiguration().hasStatement("com.spt.bas.report.server.dao.RptBusinessOverviewMapper.findBusinessOverviewList")` == true（D-P5-03 闭环关键）
    证明：53 XML 全部绑定 + mapper-locations 接通 + namespace = dao FQN + @MapperScan 覆盖 report.server.dao。

    (2) **`reportApiPathPrefixWiring_probe`**（REPORT-02 / D-P5-03 / Pitfall 1）：注入 `ApplicationContext`，获取 `RequestMappingHandlerMapping` bean，构造 `MockHttpServletRequest("POST", "/spt-bas-report/rpt/fundReceivableStatistics/findPage")`，调 `mapping.getHandler(request)`，断言返回的 `HandlerExecutionChain` 非 null。证明：
    - path-prefix 生效（/spt-bas-report/ 前缀注入到 com.spt.bas.report.server.api 包的 @RequestMapping）
    - 54 api 路径在 /spt-bas-report/* 命名空间下可达
    - Feign 契约 path = "spt-bas-report/rpt/..." 匹配实际 api 路径（D-P5-03 闭环）
    - 14 处 BFF 冲突已隔离（report api 在前缀命名空间，BFF 在根路径）

    断言风格沿用 Phase 4 feignSelfLoopbackWiring_probe（assertThat + isTrue + isNotNull + as 描述）。需要 import `org.springframework.web.servlet.mvc.method.RequestMappingHandlerMapping` + `org.springframework.mock.web.MockHttpServletRequest` + `org.springframework.web.servlet.HandlerExecutionChain`（spring-test 已在 test scope）。

    跑 `mvn test -Dtest=ZgbasApplicationTest` 确认现有测试 + Wave 0 probe + W5 新 probe 全绿。
  </action>
  <verify>
    <automated>
cd /Users/alan/WorkSpace/IDEA/zgbas-plus && \
echo "=== W5 probe 方法存在 ===" && \
echo "PROBE_MAPPER=$(grep -c 'allReportMappersResolve' zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java)" && \
echo "PROBE_PATH=$(grep -c 'reportApiPathPrefixWiring_probe' zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java)" && \
echo "=== 启动验证（含 mapper resolve + path-prefix + 无 AmbiguousMappingException）===" && \
JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home \
/Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml \
-pl zgbas-admin -am test -Dtest=ZgbasApplicationTest 2>&1 | tee /tmp/p5-05-t2.log | tail -15 && \
echo "ERROR_COUNT=$(grep -c '^\[ERROR\]' /tmp/p5-05-t2.log)" && \
echo "TEST_FAIL=$(grep -cE 'FAIL|FAILURE' /tmp/p5-05-t2.log)" && \
echo "AMBIGUOUS=$(grep -c 'AmbiguousMappingException' /tmp/p5-05-t2.log)"
    </automated>
  </verify>
  <done>
    - ZgbasApplicationTest 含 allReportMappersResolve + reportApiPathPrefixWiring_probe（不加 @Disabled）
    - allReportMappersResolve：至少 3 个代表性 namespace（FundReceivableStatistics + CtrContractReport + BusinessOverview）全部 hasStatement == true
    - reportApiPathPrefixWiring_probe：/spt-bas-report/rpt/fundReceivableStatistics/findPage 映射到 RptFundReceivableStatisticsApi（HandlerExecutionChain 非 null）
    - 启动无 AmbiguousMappingException（14 处 BFF 冲突被 path-prefix 隔离）
    - 现有 ZgbasApplicationTest 全部测试绿（含 Wave 0 probe，无 Phase 2/3/4 回归）
    - mvn test ERROR_COUNT == 0 + TEST_FAIL == 0 + AMBIGUOUS == 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → report api @RestController | 54 api 路径在 /spt-bas-report/* 命名空间；受 Phase 3 Shiro 根 filter chain 保护（path-prefix 仅作用于 report api @RestController，不影响 Shiro 链） |
| report api → system report service | 同进程 @Autowired（admin 依赖 system，D-08 拓扑）；无跨进程调用 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-T | Tampering | report api path-prefix 注入 | accept | ReportFeignPathConfig 仅给 com.spt.bas.report.server.api 包加硬编码 `/spt-bas-report` 前缀，无动态 URL 变换；Phase 4 BasFeignPathConfig 同构实证 |
| T-05-05-I | Information Disclosure | 54 report api HTTP 端点暴露 | accept | 无新外部端点——54 api 路径在单体根已通过 Phase 3 Shiro filter chain 保护（path-prefix /spt-bas-report 仅是 URL 命名空间隔离，不绕过 Shiro 链）；行为等价于源微服务 ReportServer 的端点暴露（源同样受 web 层认证保护） |
| T-05-05-E | Elevation of Privilege | report api 跨租户查询 | accept | 沿用源 service impl 的过滤逻辑（照搬即等价）；Shiro session+cookie 认证保证用户身份（Phase 3 已就位）；本期不审计报表越权（留后续安全阶段） |
| T-05-SC | Tampering | Maven installs | accept | 无包安装——54 api 是源码 verbatim copy |

无 high 级威胁。path-prefix 不影响 Shiro 根 filter chain（HandlerTypePredicate 仅匹配 report api @RestController，web @Controller 不受影响——Phase 3 AUTH-03 保全）。
</threat_model>

<verification>
- 全模块 `mvn -am compile` 零 ERROR
- ZgbasApplicationTest 启动成功（54 api bean 全解析 + 无 AmbiguousMappingException）
- allReportMappersResolve：53 Mapper namespace 注册（抽样 3 个代表性 namespace 断言）
- reportApiPathPrefixWiring_probe：/spt-bas-report/rpt/... 映射到 report api
- 现有测试全绿（无 Phase 2/3/4 + Wave 0 回归）
</verification>

<success_criteria>
- REPORT-02: 54 report Controller 迁入 zgbas-admin（保包名 com.spt.bas.report.server.api），HTTP 接口可访问
- D-P5-03: path-prefix 闭环验证——Feign 契约路径匹配 api 实际路径 + 14 处 BFF 冲突隔离（启动无 AmbiguousMappingException）
- W5 probe 双绿：allReportMappersResolve（PERSIST-02 mapper binding 验证）+ reportApiPathPrefixWiring_probe（D-P5-03 path-prefix 验证）
</success_criteria>

<output>
Create `.planning/phases/05-report-migration/05-05-SUMMARY.md` when done.
</output>
