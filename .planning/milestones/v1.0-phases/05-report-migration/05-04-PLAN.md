---
phase: 05-report-migration
plan: 04
type: execute
wave: 3
depends_on: [05-03]
files_modified:
  - zgbas-system/src/main/java/com/spt/bas/report/server/dao/RptCtr{Contract}*Mapper.java
  - zgbas-system/src/main/java/com/spt/bas/report/server/dao/Rpt{CreditAmountMonitor,FactBis,PmApprove,IndexStatistics,PrintHistory}*Mapper.java
  - zgbas-system/src/main/java/com/spt/bas/report/server/service/IRpt{...}*Service.java
  - zgbas-system/src/main/java/com/spt/bas/report/server/service/impl/Rpt{...}*ServiceImpl.java
  - zgbas-system/src/main/resources/mybatis/mappers/Rpt{...}*Mapper.xml
autonomous: true
requirements: [REPORT-01, PERSIST-02]
tags: [domain-batch, ctr-family, largest-batch, cross-service-dep, bulk-copy]

must_haves:
  truths:
    - "19 Mapper（14 Ctr* 合同域 + 5 misc）+ 对应 service iface/impl + XML 迁入 zgbas-system"
    - "RptCtrContractReportServiceImpl 编译通过（依赖 Wave 2 已迁的 IRptSummaryRoiService + IRptUserRoiService）"
    - "53/53 Mapper 全量就位（Wave 1 21 + Wave 2 13 + Wave 3 19 = 53）"
    - "每 sub-task 后 mvn -pl zgbas-system -am compile grep ^[ERROR] == 0"
  artifacts:
    - path: "zgbas-system/src/main/java/com/spt/bas/report/server/service/impl/RptCtrContractReportServiceImpl.java"
      provides: "合同台账报表 impl（最复杂：cross-service dep IRptSummaryRoiService + IRptUserRoiService）"
      contains: "IRptSummaryRoiService"
    - path: "zgbas-system/src/main/java/com/spt/bas/report/server/dao/RptCtrContractReportMapper.java"
      provides: "合同台账报表 Mapper（multi-join 复杂 SQL）"
      contains: "@MyBatisDao"
    - path: "zgbas-system/src/main/resources/mybatis/mappers/RptCtrContractReportMapper.xml"
      provides: "最复杂报表 XML（cross-service dep + multi-join + 动态 SQL）"
      contains: "com.spt.bas.report.server.dao.RptCtrContractReportMapper"
  key_links:
    - from: "RptCtrContractReportServiceImpl @Autowired IRptSummaryRoiService"
      to: "RptSummaryRoiServiceImpl（Wave 2 已就位）"
      via: "cross-service dep（Pitfall 4 — Wave 2 先于 Wave 3 已满足）"
      pattern: "IRptSummaryRoiService"
    - from: "53 Mapper 全量 @MapperScan 覆盖"
      to: "启动期 Mapper bean 注册（Wave 4 allReportMappersResolve probe 验证）"
      via: "ZgbasMybatisConfig @MapperScan basePackages 含 report.server.dao"
      pattern: "com\\.spt\\.bas\\.report\\.server\\.dao"
---

<objective>
Wave 3：19 Mapper（14 Ctr* 合同台账域 + 5 misc：CreditAmountMonitor/FactBisReport/PmApprove/IndexStatistics/PrintHistory）迁入 zgbas-system。含本期最复杂的 RptCtrContractReportServiceImpl（cross-service dep IRptSummaryRoiService + IRptUserRoiService，Wave 2 已就位）。本 Wave 完成后 53/53 Mapper 全量就位。

Purpose: 关闭 REPORT-01（53 套 Mapper+XML 迁入 system 报表包）+ 为 Wave 4（54 api 落 admin）提供全部 service 层基础。RptCtrContractReportServiceImpl 的 cross-service dep 在 Wave 2 已扫除——本 Wave 编译应一次通过。

Output: 19 Mapper + 19 iface + 19 impl + 19 XML = 76 文件净新增（全部 verbatim 保包名）。
</objective>

<execution_context>
@/Users/alan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-report-migration/05-CONTEXT.md
@.planning/phases/05-report-migration/05-RESEARCH.md
@.planning/phases/05-report-migration/05-PATTERNS.md

**BUILD PREFIX:** `JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml`

**源仓库:** `/Users/alan/WorkSpace/IDEA/zgbas` 分支 `feat-系统重构v5.0`

**Wave 2 已就位（05-03）:** IRptSummaryRoiService + RptSummaryRoiServiceImpl + IRptUserRoiService + RptUserRoiServiceImpl（mapperless + UserRoi Mapper）+ BaseCost/Evaluate/Person/Fund/Invoice/Budget/Profit/Sales/Region/Province = 34 Mapper 累计。

**Pitfall 4 约束状态:** Wave 2（Summary/UserRoi）已先于本 Wave（Ctr*）——测序铁律满足，RptCtrContractReportServiceImpl 编译依赖已扫除。
</context>

<tasks>

<task type="auto">
  <name>Task 1: 照搬 Ctr* 合同台账域 14 Mapper + XML + service（~56 文件，含最复杂 RptCtrContractReportServiceImpl）</name>
  <files>
    zgbas-system/src/main/java/com/spt/bas/report/server/dao/,
    zgbas-system/src/main/java/com/spt/bas/report/server/service/,
    zgbas-system/src/main/java/com/spt/bas/report/server/service/impl/,
    zgbas-system/src/main/resources/mybatis/mappers/
  </files>
  <read_first>
    - .planning/phases/05-report-migration/05-RESEARCH.md §Pitfall 4（cross-service dep 证据）+ §Code Examples（service impl 标准模式）
    - .planning/phases/05-report-migration/05-PATTERNS.md §W1 1d（service impl bulk-copy + ERROR_COUNT gate）
    - 源 RptCtrContractReportServiceImpl（验证 cross-service dep）: /Users/alan/WorkSpace/IDEA/zgbas/basReport/reportServer/src/main/java/com/spt/bas/report/server/service/impl/RptCtrContractReportServiceImpl.java（重点看 import IRptSummaryRoiService + IRptUserRoiService + @Autowired 字段）
    - 源 dao/service/service-impl/mappers 目录（同 05-02 read_first，glob RptCtrContract*）
  </read_first>
  <action>
    照搬保包名（D-P2-07），14 Ctr* 合同台账域 Mapper 套件（按文件名前缀 `RptCtrContract*` glob）：
    - RptCtrContractAgencyMapper
    - RptCtrContractAsseMentMapper
    - RptCtrContractFinanceVoMapper
    - RptCtrContractMapper
    - RptCtrContractMatchingMapper
    - RptCtrContractOrverdurMapper
    - RptCtrContractPayAndReceiveMapper
    - RptCtrContractReceiveDetailMapper
    - RptCtrContractRelaMapper
    - RptCtrContractReportMapper（最复杂：cross-service dep）
    - RptCtrContractSellOnCreditMapper
    - RptCtrContractSettlementMapper
    - RptCtrContractStatisticsMapper
    - RptCtrContractUnDeliveryMapper

    每套 4 件照搬（dao Mapper + service iface + service impl + XML）。RptCtrContractReportServiceImpl 的 `@Autowired IRptSummaryRoiService` + `IRptUserRoiService` 依赖在 Wave 2（05-03）已就位——编译应通过。不改源码（含 XML 的 `${}` / 动态 SQL / multi-join）。

    完成后跑 zgbas-system compile gate。
  </action>
  <verify>
    <automated>
cd /Users/alan/WorkSpace/IDEA/zgbas-plus && \
echo "=== Ctr* 14 Mapper ===" && \
echo "DAO_14=$(find zgbas-system/src/main/java/com/spt/bas/report/server/dao -name 'RptCtrContract*Mapper.java' | wc -l)" && \
echo "XML_14=$(find zgbas-system/src/main/resources/mybatis/mappers -name 'RptCtrContract*Mapper.xml' | wc -l)" && \
echo "=== cross-service dep 验证 ===" && \
echo "CTRREPORT_DEP=$(grep -c 'IRptSummaryRoiService\|IRptUserRoiService' zgbas-system/src/main/java/com/spt/bas/report/server/service/impl/RptCtrContractReportServiceImpl.java)" && \
echo "=== compile gate ===" && \
JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home \
/Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml \
-pl zgbas-system -am compile 2>&1 | tee /tmp/p5-04-t1.log | tail -5 && \
echo "ERROR_COUNT=$(grep -c '^\[ERROR\]' /tmp/p5-04-t1.log)" && \
echo "CANNOT_FIND=$(grep -cE 'cannot find symbol|找不到符号' /tmp/p5-04-t1.log)"
    </automated>
  </verify>
  <done>
    - DAO_14 == 14 且 XML_14 == 14（Ctr* 合同域全量）
    - CTRREPORT_DEP ≥ 2（RptCtrContractReportServiceImpl 引用 IRptSummaryRoiService + IRptUserRoiService，Pitfall 4 证据）
    - compile ERROR_COUNT == 0 且 CANNOT_FIND == 0（cross-service dep 编译通过，Wave 2 测序正确性证明）
  </done>
</task>

<task type="auto">
  <name>Task 2: 照搬 misc 域 5 Mapper + XML + service + 53 全量 compile gate（~20 文件）</name>
  <files>
    zgbas-system/src/main/java/com/spt/bas/report/server/dao/,
    zgbas-system/src/main/java/com/spt/bas/report/server/service/,
    zgbas-system/src/main/java/com/spt/bas/report/server/service/impl/,
    zgbas-system/src/main/resources/mybatis/mappers/
  </files>
  <read_first>
    - .planning/phases/05-report-migration/05-PATTERNS.md §Repeatable Task-Shape
    - 源目录（同前，glob RptCreditAmountMonitor* / RptFactBis* / RptPmApprove* / RptIndexStatistics* / RptPrintHistory*）
  </read_first>
  <action>
    照搬保包名（D-P2-07），5 misc Mapper 套件：
    - RptCreditAmountMonitor*（1: 额度监控）
    - RptFactBisReport*（1: 保理业务报表）
    - RptPmApprove*（1: PM 审批报表）
    - RptIndexStatistics*（1: 首页统计——MyIndexController 依赖）
    - RptPrintHistory*（1: 打印历史）

    每套 4 件照搬。完成后跑 **53 全量 compile gate** + 全量计数验证（REPORT-01 关闭条件）。
  </action>
  <verify>
    <automated>
cd /Users/alan/WorkSpace/IDEA/zgbas-plus && \
echo "=== misc 5 Mapper ===" && \
echo "DAO_MISC=$(find zgbas-system/src/main/java/com/spt/bas/report/server/dao -name 'Rpt*Mapper.java' \( -name 'RptCreditAmount*' -o -name 'RptFactBis*' -o -name 'RptPmApprove*' -o -name 'RptIndexStatistics*' -o -name 'RptPrintHistory*' \) | wc -l)" && \
echo "=== 53 全量计数（REPORT-01 关闭条件）===" && \
echo "DAO_53=$(find zgbas-system/src/main/java/com/spt/bas/report/server/dao -name 'Rpt*Mapper.java' -not -name 'package-info*' | wc -l)" && \
echo "XML_53=$(find zgbas-system/src/main/resources/mybatis/mappers -name 'Rpt*Mapper.xml' | wc -l)" && \
echo "IFACE_53=$(find zgbas-system/src/main/java/com/spt/bas/report/server/service -maxdepth 1 -name 'IRpt*Service.java' -not -name 'package-info*' | wc -l)" && \
echo "IMPL_53=$(find zgbas-system/src/main/java/com/spt/bas/report/server/service/impl -name 'Rpt*.java' -not -name 'package-info*' | wc -l)" && \
echo "=== 53 全量 compile gate（全模块）===" && \
JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home \
/Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml \
-am compile 2>&1 | tee /tmp/p5-04-t2.log | tail -5 && \
echo "ERROR_COUNT=$(grep -c '^\[ERROR\]' /tmp/p5-04-t2.log)" && \
echo "CANNOT_FIND=$(grep -cE 'cannot find symbol|找不到符号' /tmp/p5-04-t2.log)"
    </automated>
  </verify>
  <done>
    - DAO_MISC == 5
    - DAO_53 == 53 且 XML_53 == 53 且 IFACE_53 ≥ 53（含 SummaryRoi mapperless）且 IMPL_53 ≥ 53（含 SummaryRoi mapperless + RptWxBrandFollowService 非 Impl 后缀）
    - 全模块 mvn compile ERROR_COUNT == 0 且 CANNOT_FIND == 0
    - REPORT-01 关闭：53 套 Mapper + XML 全量迁入 zgbas-system 报表包
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| report Mapper XML → MySQL sptbasdb_pd | 同前 wave：mybatis 复杂 SQL（合同台账 multi-join + `${}` 动态拼接）经 @Primary DataSource |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-T | Tampering | mybatis `${}` 动态 SQL（19 XML，含最复杂 Ctr* multi-join） | accept | 同 05-02/03：行为等价优先（PROJECT.md），照搬即等价；Ctr* 合同台账报表 SQL 含大量 `${}` 动态排序/表名拼接——安全改造留后续阶段 |
| T-05-04-I | Information Disclosure | 合同台账/额度监控报表跨租户数据 | accept | 沿用源 service 过滤逻辑（照搬即等价）；Shiro filter chain 保护 api 层（W4 落 admin 后生效） |
| T-05-04-D | DoS | 复杂 multi-join 报表查询（Ctr* 合同台账）性能 | accept | 分页性能改造永久 Out-of-Scope（PROJECT.md）；分页正确性 1:1 等价（D-P5-06），重 count 查询仅作 runtime-blocking 逃生阀（D-P4-05） |
| T-05-SC | Tampering | Maven installs | accept | 无包安装——全部源码 verbatim copy |

无 high 级威胁。`${}` 照搬 + 分页性能不优化均是 accepted project decision。
</threat_model>

<verification>
- 全模块 `mvn -am compile` 零 ERROR（grep `^\[ERROR\]` locale 无关）
- 53 Mapper + 53 XML + 53+ service iface（含 SummaryRoi mapperless）+ 53+ impl 全量就位
- RptCtrContractReportServiceImpl 编译通过（cross-service dep IRptSummaryRoiService + IRptUserRoiService 解析成功）
- REPORT-01 关闭条件满足
</verification>

<success_criteria>
- REPORT-01: 53/53 套报表 Mapper + XML 迁入 zgbas-system 报表包（全量完成）
- PERSIST-02: 53 Mapper 编译通过 + cross-service dep 链完整解析（Wave 2→3 测序正确性实证）
- D-P5-04: 逐层 compile 绿灯（Wave 1→2→3 每 wave 后 ERROR_COUNT == 0）
- Pitfall 4 测序铁律验证通过（Wave 2 Summary/UserRoi 先于 Wave 3 Ctr*，RptCtrContractReportServiceImpl 编译无 cannot find symbol）
</success_criteria>

<output>
Create `.planning/phases/05-report-migration/05-04-SUMMARY.md` when done.
</output>
