---
phase: 05-report-migration
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - zgbas-system/src/main/java/com/spt/bas/report/client/   # 265 reportClient java inline (new tree)
  - zgbas-system/src/main/java/com/spt/bas/client/config/ReportFeignPathConfig.java   # NEW
  - zgbas-framework/src/main/java/com/spt/framework/config/ZgbasMybatisConfig.java   # EDIT @MapperScan
  - zgbas-admin/src/main/resources/application.yml   # EDIT type-aliases-package
  - zgbas-system/pom.xml   # REMOVE report-client dep block
  - zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java   # ADD W0 probe
autonomous: true
requirements: [REPORT-02, PERSIST-02]
tags: [wiring, reportClient-inline, path-prefix, mapper-scan, feign-self-loopback, wr05-scaffold]

must_haves:
  truths:
    - "reportClient 265 java 内联进 zgbas-system 保包名 com.spt.bas.report.client.*，编译通过"
    - "zgbas-system/pom.xml 的 report-client:2.0.1-SNAPSHOT jar 依赖块已移除（classpath 无双定义）"
    - "ReportFeignPathConfig 落位，addPathPrefix(\"/spt-bas-report\", forBasePackage(\"com.spt.bas.report.server.api\"))"
    - "ZgbasMybatisConfig @MapperScan basePackages 含 com.spt.bas.report.server.dao（双串数组）"
    - "application.yml mybatis-plus.type-aliases-package 含 com.spt.bas.report.client.entity + com.spt.bas.report.client.vo"
    - "reportFeignSelfLoopbackWiring_probe 绿（IRpt*Client proxy + reportServerConfig bean + ReportFeignPathConfig bean 三段断言）"
    - "Phase 4 已验的现有 ZgbasApplicationTest 测试不回归"
  artifacts:
    - path: "zgbas-system/src/main/java/com/spt/bas/report/client/config/ReportClientConfig.java"
      provides: "reportServerConfig bean 读 spt.bas.report.url（265 内联文件之一）"
      contains: "ReportConstant.SERVER_BEAN_NAME"
    - path: "zgbas-system/src/main/java/com/spt/bas/client/config/ReportFeignPathConfig.java"
      provides: "D-P5-03 path-prefix for report Feign self-loopback（仿 BasFeignPathConfig）"
      contains: "com.spt.bas.report.server.api"
    - path: "zgbas-framework/src/main/java/com/spt/framework/config/ZgbasMybatisConfig.java"
      provides: "@MapperScan 放宽覆盖 report.server.dao"
      contains: "com.spt.bas.report.server.dao"
    - path: "zgbas-admin/src/main/resources/application.yml"
      provides: "type-aliases-package 追加 report entity + vo 包"
      contains: "com.spt.bas.report.client.entity"
    - path: "zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java"
      provides: "W0 fail-fast probe（report Feign 接线验证）"
      contains: "reportFeignSelfLoopbackWiring_probe"
  key_links:
    - from: "IRpt*Client.url SpEL"
      to: "reportServerConfig bean.url"
      via: "ReportConstant.SERVER_URL = #{reportServerConfig.url}"
      pattern: "reportServerConfig\\.url"
    - from: "ReportFeignPathConfig.addPathPrefix"
      to: "com.spt.bas.report.server.api @RestController 路径"
      via: "HandlerTypePredicate.forBasePackage"
      pattern: "spt-bas-report"
    - from: "@MapperScan basePackages"
      to: "53 Rpt*Mapper @MyBatisDao bean 注册"
      via: "com.spt.bas.report.server.dao 扫描"
      pattern: "com\\.spt\\.bas\\.report\\.server\\.dao"
---

<objective>
Phase 5 前置接线 wave（D-P5-02 reportClient 内联 + D-P5-03 path-prefix + D-P5-07 mybatis 配置）。此 plan 落地报表侧 Feign 自回环 + mybatis Mapper 扫描 + typeAliases 解析的全部运行期锚点，为 Wave 1+ 的 53 套报表批量迁移提供编译与运行基础。

Purpose: 为 Wave 1–3 的 Mapper+XML+service 提供类型解析基础（reportClient 265 java 含 53 报表实体 + 119 vo + 54 Feign 契约），为 Wave 4 的 54 api 提供 path-prefix 命名空间隔离（防 14 处 BFF 路径冲突），为启动期 Mapper bean 注册提供 @MapperScan 覆盖。无此 plan 则 Wave 1 首批 Mapper 编译即报 cannot find symbol（reportClient 类型缺失）+ 启动期 BindingException（Mapper 未扫描）。

Output: 265 reportClient java 内联 + 1 新 ReportFeignPathConfig + 2 config 编辑（@MapperScan + type-aliases）+ 1 pom 移除 + 1 W0 fail-fast probe。
</objective>

<execution_context>
@/Users/alan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-report-migration/05-CONTEXT.md
@.planning/phases/05-report-migration/05-RESEARCH.md
@.planning/phases/05-report-migration/05-PATTERNS.md
@.planning/phases/05-report-migration/05-VALIDATION.md

**BUILD PREFIX:** `JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml`

**源仓库（搬运参考，绝对路径，非本仓库）:** `/Users/alan/WorkSpace/IDEA/zgbas` 分支 `feat-系统重构v5.0`

**NON-HERMETIC（D-P5-08 / D-P4-plaintext-secrets）:** dev profile 明文密钥已就位（47.104.15.98:3306/sptbasdb_pd），启动测试不需 export DB_PASSWORD / SPT_APP_SECRET。

**Phase 4 已就位资产（本 plan 不重做）:**
- `spt.bas.report.url: http://localhost:8080` 在 `application-dev.yml:49-50` 已就位
- `spt.bas.report.url: ${SPT_BAS_REPORT_URL}` 在 `application-prod.yml:35-36` 已就位
- `@EnableFeignClients` 已含 `"com.spt.bas.report.client.remote"`（`ZgbasApplication.java:122`，Phase 4 04-05）

<interfaces>
<!-- D-P5-03 path-prefix 直接模板（Phase 4 BasFeignPathConfig，仅改 2 常量） -->
From zgbas-system/src/main/java/com/spt/bas/client/config/BasFeignPathConfig.java:
- @Configuration implements WebMvcConfigurer
- private static final String API_PATH_PREFIX = "/spt-bas-server"
- configurePathMatch(PathMatchConfigurer) → addPathPrefix(API_PATH_PREFIX, HandlerTypePredicate.forBasePackage("com.spt.bas.server.api", "com.spt.pm.api"))
→ ReportFeignPathConfig 克隆：prefix="/spt-bas-report", basePackage="com.spt.bas.report.server.api"（单串，非双包）

<!-- D-P5-07 @MapperScan 放宽目标（当前 Phase 2 状态） -->
From zgbas-framework/src/main/java/com/spt/framework/config/ZgbasMybatisConfig.java L24:
- @MapperScan(basePackages = "com.spt.bas.system.dao", annotationClass = MyBatisDao.class)
→ 放宽为 basePackages = {"com.spt.bas.system.dao", "com.spt.bas.report.server.dao"}

<!-- reportClient 关键 bean（内联后 component-scanned） -->
From source reportClient/.../config/ReportClientConfig.java:
- @Configuration @DependsOn({"propertiesUtil"})
- @Bean(ReportConstant.SERVER_BEAN_NAME) → LocalServerConfig with setUrlKey("spt.bas.report.url")

From source reportClient/.../constant/ReportConstant.java:
- SERVER_NAME = "spt-bas-report"
- SERVER_BEAN_NAME = "reportServerConfig"
- SERVER_URL = "#{" + SERVER_BEAN_NAME + ".url}"   // SpEL
- SERVER_URL_KEY = "spt.bas.report.url"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: reportClient 265 java 内联 + ReportFeignPathConfig 新建 + @MapperScan 放宽 + type-aliases 追加 + pom 移除（5 个原子操作）</name>
  <files>
    zgbas-system/src/main/java/com/spt/bas/report/client/,
    zgbas-system/src/main/java/com/spt/bas/client/config/ReportFeignPathConfig.java,
    zgbas-framework/src/main/java/com/spt/framework/config/ZgbasMybatisConfig.java,
    zgbas-admin/src/main/resources/application.yml,
    zgbas-system/pom.xml
  </files>
  <read_first>
    - .planning/phases/05-report-migration/05-PATTERNS.md §W0 0a–0e（5 个原子操作的完整 task-shape + 源码全文）
    - .planning/phases/05-report-migration/05-RESEARCH.md §Pattern 1–4（path-prefix 方案 + @MapperScan 放宽 + reportClient 内联 + mybatis 配置合并）+ §Pitfall 7（jar 双定义风险）
    - .planning/phases/05-report-migration/05-CONTEXT.md §decisions D-P5-02 / D-P5-03 / D-P5-07
    - 源 reportClient 全目录树: /Users/alan/WorkSpace/IDEA/zgbas/basReport/reportClient/src/main/java/com/spt/bas/report/client/  （重点看 config/ReportClientConfig.java + constant/ReportConstant.java + remote/IRptFundReceivableStatisticsClient.java）
    - Phase 4 直接模板 BasFeignPathConfig 全文: /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-system/src/main/java/com/spt/bas/client/config/BasFeignPathConfig.java
    - 当前 @MapperScan: /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-framework/src/main/java/com/spt/framework/config/ZgbasMybatisConfig.java
    - 当前 application.yml mybatis-plus 段: /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-admin/src/main/resources/application.yml L46-48
    - 当前 zgbas-system pom report-client dep 块: /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-system/pom.xml L137-141
  </read_first>
  <action>
    一次性落地 5 个原子操作（D-P5-02 + D-P5-03 + D-P5-07），分步：

    (1) **reportClient 265 java 内联（D-P5-02）**：把源 `/Users/alan/WorkSpace/IDEA/zgbas/basReport/reportClient/src/main/java/com/spt/bas/report/client/` 整目录 recursive copy 到 `zgbas-system/src/main/java/com/spt/bas/report/client/`（保包名 `com.spt.bas.report.client.*`，D-P2-07）。包含 entity(83) + vo(119) + remote(54 IRpt*Client) + config(1 ReportClientConfig) + constant(2) + payload(3) + utils(2) + pkg-info。零改动、零 rename。注意 `com.spt.bas.report.client.vo.RptBaseCostVo`（report row 实体）与 Phase 4 `com.spt.bas.client.vo.RptBaseCostVo`（查询条件 Vo）同名不同包不同内容——编译无冲突（不同包），运行无冲突（XML 全用 FQN，Pitfall 5 已 de-risk）。

    (2) **pom 移除 report-client jar 依赖（D-P5-02 收尾，Pitfall 7）**：从 `zgbas-system/pom.xml` 移除 L137-141 的 `<dependency><groupId>com.spt.bas</groupId><artifactId>report-client</artifactId><version>2.0.1-SNAPSHOT</version></dependency>` 整块。必须与步骤 (1) 同 task——否则 classpath 上有两份 `com.spt.bas.report.client.*`（jar + 内联源码）。

    (3) **ReportFeignPathConfig 新建（D-P5-03，仿 BasFeignPathConfig verbatim 克隆）**：在 `zgbas-system/src/main/java/com/spt/bas/client/config/ReportFeignPathConfig.java` 新建 `@Configuration implements WebMvcConfigurer`，仅改 BasFeignPathConfig 的 2 处常量：`API_PATH_PREFIX = "/spt-bas-report"`，`HandlerTypePredicate.forBasePackage("com.spt.bas.report.server.api")`（单包，非 BasFeignPathConfig 的双包）。完整文件内容见 05-PATTERNS.md §W0 0b（含 javadoc 解释 path-prefix 解 14 处 BFF 冲突 + D-P5-03 path diverge + Phase 3 AUTH-03 保全）。

    (4) **@MapperScan 放宽（D-P5-07 part 2）**：把 `ZgbasMybatisConfig.java:24` 的 `@MapperScan(basePackages = "com.spt.bas.system.dao", annotationClass = MyBatisDao.class)` 改为数组形式 `@MapperScan(basePackages = {"com.spt.bas.system.dao", "com.spt.bas.report.server.dao"}, annotationClass = MyBatisDao.class)`。仅改 1 行（单串→双串数组）。53 report Mapper 全部 `@MyBatisDao`（已 grep 验证 53/53），与 SampleMapper 同 annotationClass。

    (5) **type-aliases-package 追加（D-P5-07 part 1）**：把 `application.yml:48` 的 `type-aliases-package: com.spt.bas.client.entity` 改为 YAML 折叠块标量追加 report 包：`type-aliases-package: >-` 换行 `com.spt.bas.client.entity,` 换行 `com.spt.bas.report.client.entity,` 换行 `com.spt.bas.report.client.vo`。mapper-locations 不变（53 report XML 同位 drop 即被覆盖）。

    完成后跑 compile gate（zgbas-system + 全模块）。
  </action>
  <verify>
    <automated>
cd /Users/alan/WorkSpace/IDEA/zgbas-plus && \
echo "=== reportClient 内联文件数 ===" && \
echo "REPORTCLIENT_COUNT=$(find zgbas-system/src/main/java/com/spt/bas/report/client -name '*.java' | wc -l)" && \
echo "FEIGNCLIENT_COUNT=$(grep -rl '@FeignClient' zgbas-system/src/main/java/com/spt/bas/report/client/remote | wc -l)" && \
echo "=== report-client jar 已移除 ===" && \
echo "REPORT_JAR_REFS=$(grep -c 'report-client' zgbas-system/pom.xml)" && \
echo "=== ReportFeignPathConfig 就位 ===" && \
grep -c 'spt-bas-report' zgbas-system/src/main/java/com/spt/bas/client/config/ReportFeignPathConfig.java && \
grep -c 'com.spt.bas.report.server.api' zgbas-system/src/main/java/com/spt/bas/client/config/ReportFeignPathConfig.java && \
echo "=== @MapperScan 放宽 ===" && \
grep -c 'com.spt.bas.report.server.dao' zgbas-framework/src/main/java/com/spt/framework/config/ZgbasMybatisConfig.java && \
echo "=== type-aliases 追加 ===" && \
grep -c 'com.spt.bas.report.client.entity' zgbas-admin/src/main/resources/application.yml && \
grep -c 'com.spt.bas.report.client.vo' zgbas-admin/src/main/resources/application.yml && \
echo "=== compile gate (全模块) ===" && \
JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home \
/Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml \
-am compile 2>&1 | tee /tmp/p5-01-t1.log | tail -5 && \
echo "ERROR_COUNT=$(grep -c '^\[ERROR\]' /tmp/p5-01-t1.log)" && \
echo "CANNOT_FIND=$(grep -cE 'cannot find symbol|找不到符号' /tmp/p5-01-t1.log)"
    </automated>
  </verify>
  <done>
    - REPORTCLIENT_COUNT ≥ 263（含 pkg-info，265 是含所有子目录的精确数）
    - FEIGNCLIENT_COUNT ≥ 53（54 IRpt*Client + 可能 1 package-info 不含 @FeignClient）
    - REPORT_JAR_REFS == 0（pom 无 report-client 残留）
    - ReportFeignPathConfig.java 含 'spt-bas-report' 且含 'com.spt.bas.report.server.api'
    - ZgbasMybatisConfig.java 含 'com.spt.bas.report.server.dao'
    - application.yml 含 'com.spt.bas.report.client.entity' 且含 'com.spt.bas.report.client.vo'
    - 全模块 mvn compile ERROR_COUNT == 0 且 CANNOT_FIND == 0
    - classpath 无双定义（mvn dependency:tree 无 report-client）
  </done>
</task>

<task type="auto">
  <name>Task 2: W0 fail-fast probe（reportFeignSelfLoopbackWiring_probe）</name>
  <files>
    zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java
  </files>
  <read_first>
    - .planning/phases/05-report-migration/05-VALIDATION.md §Wave 0 Requirements + §Per-Task Verification Map（05-00-xx 行）
    - .planning/phases/05-report-migration/05-PATTERNS.md §W6 验证 probe 扩展（reportFeignSelfLoopbackWiring_probe 全文）
    - .planning/phases/05-report-migration/05-RESEARCH.md §Validation Architecture §Bean-Resolution Proof
    - Phase 4 直接模板 feignSelfLoopbackWiring_probe: /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java（重点看 Phase 4 同名 probe 的三段断言风格）
  </read_first>
  <action>
    在 ZgbasApplicationTest 新增 1 个 `reportFeignSelfLoopbackWiring_probe` 测试方法（**不加 @Disabled，Wave 0 即运行**，仿 Phase 4 feignSelfLoopbackWiring_probe 范式）。三段断言：

    (1) **ReportFeignPathConfig bean 注册**：`context.getBean(com.spt.bas.client.config.ReportFeignPathConfig.class)` 非 null——证明 @Configuration 被 component-scanned，path-prefix WebMvcConfigurer 装配成功。

    (2) **IRpt*Client Feign proxy 解析**：`context.getBean(com.spt.bas.report.client.remote.IRptFundReceivableStatisticsClient.class)` 非 null——证明 @EnableFeignClients（Phase 4 已含 com.spt.bas.report.client.remote）生成了 proxy + reportServerConfig bean（ReportClientConfig 内联后产出）让 SpEL `#{reportServerConfig.url}` 解析成功。若 bean 缺失或 SpEL 断裂此步抛 NoSuchBeanDefinitionException / SpelEvaluationException，fail-fast。

    (3) **reportServerConfig URL 解析**：`context.getBean("reportServerConfig", com.spt.tools.core.bean.LocalServerConfig.class)` 非 null + `cfg.getUrl()` 包含 "localhost:8080"——证明 application-dev.yml 的 `spt.bas.report.url: http://localhost:8080` 被 ReportClientConfig 读取并注入 LocalServerConfig。

    断言风格沿用 Phase 4 feignSelfLoopbackWiring_probe（assertThat + isNotNull + contains）。跑 `mvn test -Dtest=ZgbasApplicationTest` 确认现有测试不回归 + 新 probe 绿。
  </action>
  <verify>
    <automated>
cd /Users/alan/WorkSpace/IDEA/zgbas-plus && \
echo "=== probe 方法存在 ===" && \
grep -c 'reportFeignSelfLoopbackWiring_probe' zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java && \
echo "=== 启动验证（非 hermetic, dev 明文密钥）===" && \
JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home \
/Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml \
-pl zgbas-admin -am test -Dtest=ZgbasApplicationTest 2>&1 | tee /tmp/p5-01-t2.log | tail -15 && \
echo "ERROR_COUNT=$(grep -c '^\[ERROR\]' /tmp/p5-01-t2.log)" && \
echo "TEST_FAIL=$(grep -cE 'FAIL|FAILURE' /tmp/p5-01-t2.log)"
    </automated>
  </verify>
  <done>
    - ZgbasApplicationTest 含 reportFeignSelfLoopbackWiring_probe 方法（不加 @Disabled）
    - probe 三段断言全过：ReportFeignPathConfig bean + IRptFundReceivableStatisticsClient Feign proxy + reportServerConfig URL resolves to localhost:8080
    - 现有 ZgbasApplicationTest 全部测试绿（无 Phase 2/3/4 回归）
    - mvn test ERROR_COUNT == 0 + TEST_FAIL == 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Feign proxy → 本进程 8080 | 54 report 契约经 Feign 自回环调用本进程 Spring MVC；仅 localhost，不跨进程 |
| reportClient 内联源码 → classpath | 265 java 从源仓库搬运进 zgbas-system；无外部包安装 |
| yml 配置 → DataSource | type-aliases + mapper-locations 追加 report 包，复用 @Primary DataSource（Phase 2） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-S | Spoofing | report Feign 自回环调用 | accept | 仅 localhost:8080 自跳，不暴露新攻击面；同进程鉴权沿用 Phase 3 Shiro 链（path-prefix 仅作用于 report api @RestController，不影响 Shiro 根 filter chain） |
| T-05-01-T | Tampering | ReportFeignPathConfig URL prefix 注入 | accept | 仅给 com.spt.bas.report.server.api 包的 @RequestMapping 加硬编码 `/spt-bas-report` 前缀，无动态 URL 变换；作用域限于 report api |
| T-05-01-I | Info Disclosure | reportClient 内联源码（265 POJO + Feign 契约） | accept | 纯数据载体（entity/vo）+ 接口定义（remote）；无密钥、无 PII；源码来自受控内部仓库 /Users/alan/WorkSpace/IDEA/zgbas |
| T-05-SC | Tampering | Maven installs | accept | 本 plan 无任何新包安装——reportClient 是源码内联（非 jar 安装），report-client jar 被**移除**（非添加）；pom 编辑为纯删除操作 |

无 high 级威胁。report-client jar 移除不引入供应链风险（是删减依赖）。path-prefix 方案有 Phase 4 BasFeignPathConfig 直接运行实证。
</threat_model>

<verification>
- 全模块 `mvn -am compile` 零 ERROR（grep `^\[ERROR\]` locale 无关）
- ZgbasApplicationTest 现有测试全绿（无 Phase 2/3/4 回归）+ reportFeignSelfLoopbackWiring_probe 绿
- 5 处 grep 断言（reportClient 文件数 + pom jar 移除 + ReportFeignPathConfig + @MapperScan + type-aliases）见 Task 1 verify
- mvn dependency:tree 无 report-client 残留
</verification>

<success_criteria>
- D-P5-02 reportClient 265 java 内联 + report-client jar 移除（classpath 无双定义）
- D-P5-03 ReportFeignPathConfig 就位（path-prefix 防 14 处 BFF 冲突 + 解 Feign 契约 path diverge）
- D-P5-07 @MapperScan 放宽覆盖 report.server.dao + type-aliases 含 report entity/vo 包
- D-P5-03 fail-fast：reportFeignSelfLoopbackWiring_probe 证明 report Feign 自回环接线 Wave 0 即通过
- Phase 4 已验的现有测试不回归
</success_criteria>

<output>
Create `.planning/phases/05-report-migration/05-01-SUMMARY.md` when done.
</output>
