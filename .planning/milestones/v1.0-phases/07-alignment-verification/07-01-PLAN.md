---
wave: 1
depends_on: []
files_modified:
  - zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java
  - .planning/phases/07-alignment-verification/07-RESEARCH.md
  - .planning/phases/07-alignment-verification/07-VALIDATION.md
autonomous: true
requirements:
  - ALIGN-01
---

# Plan 07-01: Smoke Proof + 全链路端到端可达验证

**Objective:** 在 ZgbasApplicationTest 中新增 @Disabled smoke proof 方法，证明单服务启动后登录→首页→核心业务→报表→定时任务全链路端到端可达。维持全 reactor 编译 + 测试绿灯基线。

**Requirements:** ALIGN-01（单服务启动后全链路端到端可用）

## Tasks

<task id="07-01-01">
<read_first>
- zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java
- .planning/phases/07-alignment-verification/07-RESEARCH.md §6 "Smoke Chain 端点枚举"
- .planning/phases/07-alignment-verification/07-VALIDATION.md
</read_first>

<action>
在 `ZgbasApplicationTest.java` 新增以下 @Disabled proof 方法（默认 off，手动启用验证，D-P6-06-01 纪律）：

1. **`fullChainSmoke_proof()`** — @Disabled("D-P7-01 smoke 全链路 proof — 手动启用验证 ALIGN-01 端到端可达")
   - 断言 `contextLoads` 基线通过（已有）
   - 断言 `restTemplate.getForEntity("/login", String.class)` 返回 2xx
   - 断言 `restTemplate.getForEntity("/index", String.class)` 返回 2xx/3xx
   - 断言 4 核心 API findPage 端点返回 2xx/3xx/401（非 404）：
     - POST `/apply/brand/findAll`
     - POST `/ctr/contract/findPage`
     - POST `/stock/stockContract/findPage`
     - POST `/ctr/loading/findPage`
   - 断言 2 报表 API 端点非 404：
     - POST `/spt-bas-report/rpt/fundReceivableStatistics/findPage`
     - POST `/spt-bas-report/rpt/baseCost/findPage`
   - 断言 quartz 调度：`scheduler.getJobKeys(GroupMatcher.anyJobGroup()).size() == EXPECTED_JOB_COUNT`
   - 断言 1 个手动触发：`sysJobService.run(sysJobMapper.selectJobById(1L))` → sys_job_log 新行写入 + status='0'

   模式：沿用现有 `sampleQuartzJobDryRun_proof` 的 `waitForNewJobLog()` 辅助方法。所有端点断言沿用现有 `basContractEndpointReachable_*` 的 2xx/3xx/401 模式。

2. **`coreApiReachabilityExtended_probe()`** — @Disabled("D-P7-01 扩展 API 可达性抽样 — 手动启用")
   - 断言以下端点非 404：
     - POST `/ctr/contractFee/findPageContractFee`
     - POST `/apply/pay/findPage`（如存在，否则 `/apply/pay/findAll`）
     - POST `/spt-bas-report/rpt/businessPay/findPageContract`
     - POST `/spt-bas-report/business/overview/api/findBusinessOverviewList`
   - 断言对应 BFF controller bean 存在（沿用 `bffControllersRegistered_sample` 模式）

注：不新增 import（所有类已在现有 proof 中 import）。方法插入位置在 `sampleQuartzJobDryRun_proof()` 之后。
</action>

<acceptance_criteria>
- `ZgbasApplicationTest.java` 包含 `void fullChainSmoke_proof()` 方法
- `ZgbasApplicationTest.java` 包含 `void coreApiReachabilityExtended_probe()` 方法
- 两个方法均标注 `@Disabled`
- 全 reactor `JAVA_HOME=... mvn -s ... test` 返回 BUILD SUCCESS（30/0/0/3 skipped 基线不退步）
- `grep -c '@Disabled' ZgbasApplicationTest.java` 返回 5（原 3 + 新增 2）
- `grep 'fullChainSmoke_proof' ZgbasApplicationTest.java` 有匹配
- `grep 'coreApiReachabilityExtended_probe' ZgbasApplicationTest.java` 有匹配
</acceptance_criteria>
</task>

<task id="07-01-02">
<read_first>
- zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java
</read_first>

<action>
全 reactor 编译 + 测试绿灯验证。

执行：
1. `JAVA_HOME=/Library/Java/JavaVirtualMachines/jdk1.8.0_351.jdk/Contents/Home /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml clean compile -DskipTests`
2. `JAVA_HOME=... mvn -s ... test`（full reactor）
3. 确认测试计数 ≥ 30（baseline），0 failures，0 errors，3 skipped（@Disabled proofs）

如遇编译错误或测试回归，修复后再验证。修复原则：仅修阻塞型（D-P4-05），不全量 reconcile。
</action>

<acceptance_criteria>
- `mvn clean compile` 返回 BUILD SUCCESS（grep `^\[ERROR\]` 无匹配，locale 无关）
- `mvn test` 返回 BUILD SUCCESS
- 测试计数 ≥ 30，failures=0，errors=0
- 3 skipped（3 个原有 @Disabled proof + 2 个新增 @Disabled = 5 skipped 是可接受的；如果原 3 变 5 则正确）
</acceptance_criteria>
</task>

## Verification

1. ZgbasApplicationTest.java 包含 2 个新 @Disabled proof 方法
2. 全 reactor 编译 + 测试绿灯
3. @Disabled proof 方法签名与 RESEARCH.md §6 smoke chain 端点对齐

## must_haves

- `ZgbasApplicationTest.java` 新增 `fullChainSmoke_proof()` @Disabled 方法覆盖 ALIGN-01 全链路（登录→首页→4核心→2报表→1 quartz 触发）
- 全 reactor `mvn test` BUILD SUCCESS 基线不退步
