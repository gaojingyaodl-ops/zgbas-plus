---
wave: 3
depends_on:
  - 07-02
  - 07-03
files_modified:
  - .planning/phases/07-alignment-verification/07-HUMAN-UAT.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - ALIGN-01
  - ALIGN-02
---

# Plan 07-04: UAT 整合 + 验收收口

**Objective:** 创建 07-HUMAN-UAT.md 整合 Phase 6 carry-over + Phase 7 新增 UAT 项，执行人工浏览器验收，编译 known-gap 清单，完成 Phase 7 验收收口。

**Requirements:** ALIGN-01 + ALIGN-02（最终 sign-off）

## Tasks

<task id="07-04-01">
<read_first>
- .planning/phases/06-quartz-migration/06-HUMAN-UAT.md
- .planning/phases/07-alignment-verification/07-CONTEXT.md
- .planning/phases/07-alignment-verification/07-RESEARCH.md
</read_first>

<action>
**创建 07-HUMAN-UAT.md**

沿用 06-HUMAN-UAT.md 的 frontmatter 结构（status/phase/source/started/updated），整合以下 UAT 项：

**Phase 6 carry-over（2 个 pending 项）：**
- 项 1：/monitor/job UI 浏览器验收（承 06-HUMAN-UAT pending 项 2，07-03-02 执行后此项应 PASS）
- 项 2：15 REVIEW sys_job operator review（承 06-HUMAN-UAT pending 项 3，07-03-01 执行后此项应 PASS）

**Phase 7 新增项：**
- 项 3：全链路 smoke 验收 — 手动启用 `fullChainSmoke_proof()`，确认全链路端点可达 + quartz 触发成功
- 项 4：写类真实回归验收 — 手动启用 `writeClassRealRun_proof()`，确认 autoPay + refreshContractStatusTask 真跑 + DB 行为正确
- 项 5：报表导出抽样验收 — 手动启用 `reportExportSample_proof()`，确认 5 个导出端点 Content-Type + body 正确
- 项 6：报表分页抽样验收 — 手动启用 `reportFindPageSample_proof()`，确认分页 size 约束
- 项 7：核心业务浏览器验收 — 登录后手动走合同查询→授信→库存→放款关键流
- 项 8：报表浏览器验收 — 登录后手动走合同台账报表→收付统计→导出 Excel
- 项 9：定时任务浏览器验收 — 登录后 /monitor/job 手动触发 2 个不同 handler 任务

frontmatter：
```
---
status: partial
phase: 07-alignment-verification
source: [07-VERIFICATION.md]
started: 2026-07-19
updated: 2026-07-19
---
```
</action>

<acceptance_criteria>
- `.planning/phases/07-alignment-verification/07-HUMAN-UAT.md` 文件存在
- 包含 `## Tests` 章节
- 包含 ≥9 个 UAT 项（2 carry-over + 7 新增）
- frontmatter status 为 `partial`
- Phase 6 carry-over 项标注来源 "承 06-HUMAN-UAT pending 项 2/3"
</acceptance_criteria>
</task>

<task id="07-04-02">
<read_first>
- .planning/phases/07-alignment-verification/07-HUMAN-UAT.md
- zgbas-admin/src/test/java/com/spt/ZgbasApplicationTest.java
</read_first>

<action>
**执行人工浏览器 UAT + @Disabled proof 手动启用验收**

按 07-HUMAN-UAT.md 清单逐项验收：

1. **后端 proof 手动启用验收（项 3-6）：**
   - 临时移除 `fullChainSmoke_proof()` 的 @Disabled → 运行 → 确认全链路通过 → 恢复 @Disabled
   - 临时移除 `writeClassRealRun_proof()` 的 @Disabled → 运行 → 确认写类真跑 + sys_job_log → 恢复 @Disabled
   - 临时移除 `reportExportSample_proof()` 的 @Disabled → 运行 → 确认导出端点 → 恢复 @Disabled
   - 临时移除 `reportFindPageSample_proof()` 的 @Disabled → 运行 → 确认分页 → 恢复 @Disabled

2. **浏览器验收（项 7-9）：**
   - 登录 zgbas-plus → 走核心业务关键流（合同查询/授信/库存/放款）
   - 走报表关键流（合同台账/收付统计/导出 Excel）
   - 走定时任务流（/monitor/job → 手动触发 2 个任务 → 确认 sys_job_log）

3. **记录每项结果：** 更新 07-HUMAN-UAT.md 每项的 result 字段为 PASS/FAIL

此任务需人工操作，checkpoint:human-verify。
</action>

<acceptance_criteria>
- 07-HUMAN-UAT.md 所有 UAT 项 result 字段不为空（PASS 或 FAIL）
- 所有 @Disabled proof 手动启用后通过（运行后恢复 @Disabled）
- 浏览器验收关键流（核心业务 + 报表 + 定时任务）全部 PASS
- `ZgbasApplicationTest.java` 所有 @Disabled proof 恢复 @Disabled 状态
</acceptance_criteria>
</task>

<task id="07-04-03">
<read_first>
- .planning/phases/07-alignment-verification/07-RESEARCH.md
- .planning/phases/07-alignment-verification/07-CONTEXT.md
</read_first>

<action>
**编译 known-gap 清单**

在 07-HUMAN-UAT.md 的 `## Gaps` 章节记录以下 known-gap（D-P7-03 长尾标 known-gap）：

1. **53 报表不全跑** — P7 抽样 5-6 个域，余者记 known-gap。全量回归若需另立 task。
2. **64 任务不全触** — P7 抽样 2-3 个 handler，余者记 known-gap。
3. **28 未迁 handler**（含 11 行 GuTu/工商 batch）— Phase 6 gap-closure plan 或 v2，不阻塞 P7 sign-off。
4. **7 ambiguous executeCommand** — ops 确认映射或 skip。
5. **xxl-job admin 退役** — ops 事务，P7 sign-off 之后。
6. **side-by-side 降级** — 老系统不可起，全量走独立+golden（D-P7-02 降级决策）。
7. **非阻塞 drift** — 全量实体 schema drift reconcile + ddl-auto=validate 为 tech debt，P7 仅修运行阻塞型。

每条标注：来源决策 ID（D-P7-03/D-P7-04）、不影响 sign-off 的理由、建议后续处理。
</action>

<acceptance_criteria>
- 07-HUMAN-UAT.md 包含 `## Gaps` 章节
- Gaps 章节列出 ≥7 条 known-gap
- 每条标注来源决策 ID + 不影响 sign-off 理由
</acceptance_criteria>
</task>

<task id="07-04-04">
<read_first>
- .planning/STATE.md
- .planning/ROADMAP.md
- .planning/phases/07-alignment-verification/07-HUMAN-UAT.md
</read_first>

<action>
**验收收口 — 更新 STATE.md + ROADMAP.md**

1. 确认 07-HUMAN-UAT.md 所有项 PASS（或 FAIL 项有 acceptable 理由记录）
2. 确认 known-gap 清单完整透明
3. 确认全 reactor `mvn test` BUILD SUCCESS
4. 更新 STATE.md：
   - `status: complete`
   - `stopped_at: "Phase 7 验收完成 — ALIGN-01/02 sign-off"`
   - `progress.completed_phases: 7`
   - `progress.percent: 100`
5. 更新 ROADMAP.md Phase 7 行：
   - `Plans: 4/4`
   - `Status: Complete`
   - `Completed: 2026-07-19`

此任务标志着单体化重构全部 7 个阶段完成。
</action>

<acceptance_criteria>
- STATE.md `status` 字段为 `complete`
- STATE.md `progress.completed_phases` 为 7
- ROADMAP.md Phase 7 行 `Status` 为 `Complete`
- ROADMAP.md Phase 7 行 `Plans` 为 `4/4`
- 全 reactor `mvn test` BUILD SUCCESS
</acceptance_criteria>
</task>

## Verification

1. 07-HUMAN-UAT.md 包含 ≥9 UAT 项 + known-gap 清单
2. 所有 UAT 项有 result（PASS/FAIL）
3. 所有 @Disabled proof 手动启用验证后恢复
4. STATE.md + ROADMAP.md 反映 Phase 7 完成
5. 全 reactor 绿灯

## must_haves

- 07-HUMAN-UAT.md 创建且所有 UAT 项有结果
- known-gap 清单透明记录 53 报表/64 任务/28 handler/7 ambiguous/xxl-job 退役/side-by-side 降级/drift
- STATE.md 反映 Phase 7 complete + 7/7 phases
- 全 reactor `mvn test` BUILD SUCCESS
