---
phase: 06-quartz-migration
plan: 03
type: execute
wave: 2  # W2 plan-checker fix: 与 06-02 无文件冲突，可同 wave 并行（原 wave 3 over-serialized）
depends_on: [06-01]
files_modified:
  - zgbas-quartz/src/main/java/com/spt/quartz/task/SynchronizedApplyMatchDetailTask.java
  - zgbas-quartz/src/main/java/com/spt/quartz/task/SynchronizedApplyMatchTask.java
  - zgbas-quartz/src/main/java/com/spt/quartz/task/SynchronizedBsCompanyTask.java
  - zgbas-quartz/src/main/java/com/spt/quartz/task/SynchronizedCtrContractOphisTask.java
  - zgbas-quartz/src/main/java/com/spt/quartz/task/SynchronizedCtrContractTask.java
  - zgbas-quartz/src/main/java/com/spt/quartz/task/SynchronizedCtrProductTask.java
  - zgbas-quartz/src/main/java/com/spt/quartz/task/SynchronizedPmApproveTask.java
  - zgbas-quartz/src/main/java/com/spt/quartz/task/SynchronizedWorkTargetTask.java
autonomous: true
requirements: [QUARTZ-03]

must_haves:
  truths:
    # Outcome-level truths (executor-observable) — W3 rebalance
    - "8 个 Synchronized*Task handler 在 Spring context 可解析为 @Component bean（sys_job.invoke_target 可反射命中）"
    - "源 xxl-job 痕迹在该批 8 个 Synchronized*Task 文件内清零（grep @XxlJob / XxlJobHelper / com.xxl.job 全批 0 命中）"
    - "handler 业务逻辑等价保形（@Autowired com.spt.bas.server.service.* + rocketmq.* 包名不变 D-P6-09；方法体业务编排保留作 sys_job 定时入口）"
    # Compile-gate evidence truths (kept as implementation-level signals)
    - "每个类带 @Component(\"<xxlJobValue>\") 显式 bean 名（对齐 sys_job.invoke_target 短名）"
    - "XxlJobHelper.log → log.info / handleSuccess → return / handleFail → throw / getJobParam → 方法签名加 String 参数（Pattern 3 翻译规则）"
    - "合并编译门：06-02 + 06-03 一起跑 mvn -pl zgbas-quartz -am compile 除 06-04 cascade 外零 ERROR"
  artifacts:
    - path: "zgbas-quartz/src/main/java/com/spt/quartz/task/SynchronizedCtrContractTask.java"
      provides: "合同同步任务 handler（D-P6-11 MQApi /ctrContractTask 端点原本直调此 handler 内 synchronizedAllCtrContract 方法）"
      contains: "@Component(\"synchronizedCtrContractTask\")"
  key_links:
    - from: "zgbas-quartz/.../task/Synchronized*Task.java @Component"
      to: "sys_job.invoke_target"
      via: "JobInvokeUtil via SpringUtils.getBean"
      pattern: "@Component\\(\"synchronized[A-Z][a-zA-Z]+Task\"\\)"
    - from: "zgbas-quartz/.../task/Synchronized*Task.java @Autowired"
      to: "zgbas-system com.spt.bas.server.rocketmq.* + service.*"
      via: "Spring @Autowired"
      pattern: "com\\.spt\\.bas\\.server\\.(service|rocketmq)\\."
---

<objective>
Phase 6 Wave 3 first half — 把 basServer/rocketmq/task/ 下 8 个 Synchronized*Task handler 类（共 ~9 个 @XxlJob 方法，含 1 个可能的多方法文件）按 D-P6-09 repackage 到 com.spt.quartz.task，xxl-job→quartz 翻译。这批 handler 的特殊性：源 MQApi.java（basServer/api）原本 HTTP 同步触发它们，D-P6-11 决策保留 MQApi HTTP 端点改内部直调 service（不经 handler）— 因此 Synchronized*Task 本身的 handler 角色独立存在（由 sys_job 定时调度），与 MQApi 直调 service 共存（两条入口）。

Purpose: 完成 basServer 全部 58 个 @XxlJob handler 中除 command/ 外的 rocketmq/task 部分，为 06-04 command executors + MQApi 改造提供前置条件。
Output: zgbas-quartz/.../task/ 下 8 个 Synchronized*Task handler 类，全部 @Component + 零 xxl-job。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-quartz-migration/06-CONTEXT.md
@.planning/phases/06-quartz-migration/06-RESEARCH.md
@.planning/phases/06-quartz-migration/06-01-SUMMARY.md
@.planning/phases/06-quartz-migration/06-02-SUMMARY.md

# 关键上下文摘要
- 06-01 已落地 zgbas-quartz 基础设施 + xxl-job-core 删除
- 06-02 已迁 21 个 basServer/task/ handler（本 plan 与之无文件冲突，可同 wave 并行；本 plan depends_on 仅 06-01 — 见 wave frontmatter）
- D-P6-11 MQApi 直调改造在 06-04 处置（本 plan 只迁 handler 代码，不动 MQApi.java）
- 翻译规则同 06-02（Pattern 3）
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port basServer/rocketmq/task/ 8 个 Synchronized*Task handler + xxl-job→quartz 翻译</name>
  <files>
    zgbas-quartz/src/main/java/com/spt/quartz/task/SynchronizedApplyMatchDetailTask.java
    zgbas-quartz/src/main/java/com/spt/quartz/task/SynchronizedApplyMatchTask.java
    zgbas-quartz/src/main/java/com/spt/quartz/task/SynchronizedBsCompanyTask.java
    zgbas-quartz/src/main/java/com/spt/quartz/task/SynchronizedCtrContractOphisTask.java
    zgbas-quartz/src/main/java/com/spt/quartz/task/SynchronizedCtrContractTask.java
    zgbas-quartz/src/main/java/com/spt/quartz/task/SynchronizedCtrProductTask.java
    zgbas-quartz/src/main/java/com/spt/quartz/task/SynchronizedPmApproveTask.java
    zgbas-quartz/src/main/java/com/spt/quartz/task/SynchronizedWorkTargetTask.java
  </files>
  <read_first>
    - /Users/alan/WorkSpace/IDEA/zgbas/basCore/basServer/src/main/java/com/spt/bas/server/rocketmq/task/ 全部 8 个 .java 文件（SynchronizedApplyMatchDetailTask / SynchronizedApplyMatchTask / SynchronizedBsCompanyTask / SynchronizedCtrContractOphisTask / SynchronizedCtrContractTask / SynchronizedCtrProductTask / SynchronizedPmApproveTask / SynchronizedWorkTargetTask）
    - /Users/alan/WorkSpace/IDEA/zgbas/basCore/basServer/src/main/java/com/spt/bas/server/api/MQApi.java （D-P6-11：理解源 HTTP 端点 → Synchronized*Task 方法的映射，作为 06-04 MQApi 直调改造的输入；本 Task 仅阅读理解不修改 MQApi）
    - /Users/alan/WorkSpace/IDEA/spt-auth/auth-quartz/src/main/java/com/spt/quartz/task/RyTask.java （RuoYi handler 写法参考）
    - .planning/phases/06-quartz-migration/06-RESEARCH.md §Pattern 6（MQApi D-P6-11 改造）+ §Open Questions Q2（Synchronized*Task 内部依赖分析粒度）+ §Code Examples §3（MQApi 改造范例）+ §Common Pitfalls 4（@XxlJob 实测计数）
    - .planning/phases/06-quartz-migration/06-02-SUMMARY.md （同 plan 套路参考：bean 名表 + @XxlJob value 列表格式）
  </read_first>
  <action>
    【A. 源枚举】源 basServer/rocketmq/task/ 共 8 .java：SynchronizedApplyMatchDetailTask / SynchronizedApplyMatchTask / SynchronizedBsCompanyTask / SynchronizedCtrContractOphisTask / SynchronizedCtrContractTask / SynchronizedCtrProductTask / SynchronizedPmApproveTask / SynchronizedWorkTargetTask。全部迁（research 实测 9 个 @XxlJob 方法，意味着其中 1 个文件含 2 个 @XxlJob 方法 — 执行时按源实际分布处理，类名 - "Task" 后缀的驼峰部分作 bean 名约定）。

    【B. 落位 + repackage（D-P6-09，同 06-02 规则）】每个源文件复制到 zgbas-quartz/src/main/java/com/spt/quartz/task/<同名>.java，变换：
      1. package com.spt.bas.server.rocketmq.task; → package com.spt.quartz.task;
      2. 删 import com.xxl.job.* 全部
      3. handler 内 import com.spt.bas.server.service.* + com.spt.bas.server.rocketmq.* + 其他业务引用保留（仅 handler 自身包名变）
    
    【C. 类级 @Component 显式 bean 名】每个类 @Component("<xxlJobValue>")（取该类内 @XxlJob 注解 value；约定 SynchronizedXxxTask 类的 bean 名为类名首字母小写 synchronizedXxxTask，与源 @XxlJob value 通常对齐；多方法的类 bean 名取主方法对应 value）。
    
    【D. 方法级翻译规则（同 06-02 Pattern 3）】
      - @XxlJob(value="<name>") public void xxx() { ... } → 删注解，方法签名保留（或加 String 参数当有 getJobParam）
      - XxlJobHelper.log → log.info（补 slf4j Logger 若源类无）
      - XxlJobHelper.handleSuccess → 删或 log.info
      - XxlJobHelper.handleFail → throw new RuntimeException
      - XxlJobHelper.getJobParam → 方法签名加 String 参数
    
    【E. D-P6-11 兼容性保护】这 8 个 Synchronized*Task 的业务方法（如 synchronizedAllCtrContract）原本被 MQApi HTTP 端点同步调用。D-P6-11 决策 MQApi 改直调底层 service（不经 handler），但**这些 handler 仍作为 sys_job 定时调度入口独立存在**。本 Task 翻译时**保留** Synchronized*Task 内业务方法的方法体不动（不剥离到 service 层），让 sys_job 能经 invoke_target = "synchronizedCtrContractTask.synchronizedAllCtrContract" 触发；MQApi 直调改造的 service-side 业务方法下沉在 06-04 Task 3 处置（不在此 Task）。
    
    【F. 不做的事】
      - 不动 MQApi.java（06-04 负责）
      - 不剥离 Synchronized*Task 内业务方法到 service（保 handler 自洽，06-04 D-P6-11 决定直调目标后再下沉）
      - 不建 sys_job INSERT 数据（06-05）
      - 不动 command/ + reportServer/command/ + web/config/（06-04）
  </action>
  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 1.8) mvn -pl zgbas-quartz -am compile -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml 2>&1 | tee /tmp/p6-03-t1.log | tail -20; 断言：①ls zgbas-quartz/src/main/java/com/spt/quartz/task/Synchronized*.java | wc -l = 8；②grep -rl "@XxlJob\|XxlJobHelper\|com.xxl.job" zgbas-quartz/src/main/java/com/spt/quartz/task/Synchronized*.java | wc -l = 0；③grep -c "package com.spt.quartz.task" zgbas-quartz/src/main/java/com/spt/quartz/task/SynchronizedCtrContractTask.java = 1；④grep -c "@Component" zgbas-quartz/src/main/java/com/spt/quartz/task/SynchronizedCtrContractTask.java = 1；⑤本 Task 编译预期除 06-04 cascade（com.spt.bas.server.command.BasCommandExecutor 缺失）外零 ERROR</automated>
  </verify>
  <done>
    - 8 个 Synchronized*Task 文件落 com.spt.quartz.task
    - 每个类 package com.spt.quartz.task + @Component 显式 bean 名
    - grep "@XxlJob\|XxlJobHelper\|com.xxl.job" Synchronized*.java = 0
    - XxlJobHelper.log/handleSuccess/handleFail/getJobParam 翻译完成
    - 业务方法体保留（保 sys_job 定时入口；D-P6-11 MQApi 直调下沉在 06-04）
    - mvn -pl zgbas-quartz -am compile 除 06-04 deferred cascade 外零 ERROR
  </done>
</task>

<task type="auto">
  <name>Task 2: 合并编译门断言 + Synchronized*Task 内部依赖映射表（D-P6-11 输入）</name>
  <files>
    .planning/phases/06-quartz-migration/06-03-SUMMARY.md
  </files>
  <read_first>
    - /Users/alan/WorkSpace/IDEA/zgbas/basCore/basServer/src/main/java/com/spt/bas/server/rocketmq/task/ （二次核对 8 个 Synchronized*Task 内部 @Autowired 业务方法分布）
    - /Users/alan/WorkSpace/IDEA/zgbas/basCore/basServer/src/main/java/com/spt/bas/server/api/MQApi.java （理解 HTTP 端点 → Synchronized*Task 方法 → 内部业务编排的调用链）
    - .planning/phases/06-quartz-migration/06-RESEARCH.md §Open Questions Q2（Synchronized*Task 内部依赖分析粒度：1:1 直调 vs 1:N 需 service 加组合方法）
    - 06-03 Task 1 编译输出（/tmp/p6-03-t1.log）
  </read_first>
  <action>
    【A. 合并编译门】mvn -pl zgbas-quartz -am compile；除 06-04 cascade（command/ + reportServer/command/ + web/config/ 类未迁）外零 ERROR。若有其他 ERROR 分类处置（同 06-02 Task 2 模式）。
    
    【B. 生成 Synchronized*Task 内部依赖映射表】在 06-03-SUMMARY.md 记录 8 个 Synchronized*Task 的：
      1. 类名 + @Component bean 名 + 原 @XxlJob value
      2. 每个 handler 业务方法（如 synchronizedAllCtrContract / synchronizedApplyMatch 等）的方法体业务编排分析：
         - 1:1 case：方法体只调 1 个 service.method（如只调 ICtrContractService.synchronizedAllCtrContract）→ MQApi 可直接下沉到该 service.method
         - 1:N case：方法体调多 service 或含数据加工/异步消息（如调 ICtrContractService + IPmApproveService + 发 RocketMQ 消息）→ service iface 需加一个组合方法（如 ICtrContractService.synchronizedAllCtrContractBatch()）封装多 service 调用，MQApi 调该组合方法
      3. 对应 MQApi 端点（/ctrContractTask / /applyMatchTask 等）的"当前调 handler" → "D-P6-11 后直调目标 service method"映射
      这张表是 06-04 Task 3 MQApi 直调改造的直接输入。
    
    【C. handler 清单与 sys_job 翻译字段对齐】同 06-02 Task 2，为 06-05 D-P6-02 翻译流程预提取字段。
  </action>
  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 1.8) mvn -pl zgbas-quartz -am compile -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml 2>&1 | tee /tmp/p6-03-t2.log | tail -5; 断言：①grep "^\[ERROR\]" /tmp/p6-03-t2.log | grep -v "com.spt.bas.server.command\|com.spt.bas.report.server.command\|com.spt.bas.web.config" | wc -l = 0；②test -f .planning/phases/06-quartz-migration/06-03-SUMMARY.md；③grep -c "synchronizedCtrContractTask\|synchronizedApplyMatchTask" .planning/phases/06-quartz-migration/06-03-SUMMARY.md ≥ 2（代表性 Synchronized*Task 覆盖）；④grep -c 'synchronized[A-Z][a-zA-Z]*Task' .planning/phases/06-quartz-migration/06-03-SUMMARY.md = 8（W6 plan-checker fix：**全部 8 个 Synchronized*Task 各占 1 个依赖映射行** — 防 06-04 MQApi 直调改造漏掉某个 Synchronized*Task 的 1:N service 组合方法导致运行期 NoSuchMethodError）</automated>
  </verify>
  <done>
    - 06-03-SUMMARY.md 存在，含 8 个 Synchronized*Task 清单 + 内部依赖映射（1:1 或 1:N 标注）
    - 映射表覆盖 06-04 D-P6-11 MQApi 直调改造所需全部 Synchronized*Task 业务方法（**8 个 Synchronized*Task 全部占行 — W6 修正**）
    - mvn -pl zgbas-quartz -am compile 除 06-04 cascade 外零 ERROR
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scheduler → Synchronized*Task reflection | sys_job.invoke_target 经反射调 Synchronized*Task bean，业务方法体执行（多 service 编排 + 可能发 RocketMQ）|
| Synchronized*Task → service 层 | handler @Autowired service iface（Phase 4 已就位），service 层 ACL 已就位 |
| Synchronized*Task 双入口 | 同一业务方法既被 sys_job 定时调度（quartz 线程池异步）又被 MQApi HTTP 同步触发（D-P6-11 改造后直调 service）；并发与事务边界需保等价 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Tampering | D-P6-09 repackage（rocketmq.task → quartz.task）影响 service iface 包名 | mitigate | Task action B 严格只改 handler 自身 package，业务 import 保留；编译门 + grep 断言 |
| T-06-03-02 | DoS | Synchronized*Task 业务方法多 service 编排 + RocketMQ 发送，被恶意高频触发拖垮 | mitigate | sys_job.concurrent='1' 保串行型任务不堆积（D-P6-12）；@DisallowConcurrent 在 Job class 层级；admin ShiroFilter 已认证才可达 |
| T-06-03-03 | Repudiation | handler 异常静默吞 | mitigate | handleFail → throw 翻译规则；AbstractQuartzJob.after 写 sys_job_log.status=FAIL |
| T-06-03-04 | Tampering | D-P6-11 双入口并发风险（sys_job cron 触发 + MQApi HTTP 同步触发同一业务方法） | accept | 源 xxl-job 系统同样存在双入口（admin cron + 手动 HTTP），行为等价；service 层 @Transactional 边界已就位；本 plan 不动业务方法体，保源行为 |
| T-06-03-SC | Tampering | 源 handler 代码 | mitigate | 本地副本 /Users/alan/WorkSpace/IDEA/zgbas 读取；research §Sources 一手核 |
</threat_model>

<verification>
- 8 个 Synchronized*Task 落 com.spt.quartz.task 包
- 每个类 @Component 显式 bean 名 + 零 @XxlJob/零 import com.xxl.job.*
- 业务方法体保留（D-P6-11 双入口语义）
- mvn -pl zgbas-quartz -am compile 除 06-04 cascade 外零 ERROR
- 06-03-SUMMARY.md 含 8 handler 清单 + 内部依赖映射表（供 06-04 Task 3 D-P6-11 改造）
</verification>

<success_criteria>
- [ ] QUARTZ-03（8 rocketmq/task Synchronized*Task 迁移部分）— 代码翻译维度关闭
- [ ] D-P6-09（repackage）— 落地
- [ ] D-P6-11 准备（依赖映射表）— 为 06-04 MQApi 直调改造提供输入
</success_criteria>

<output>
Create `.planning/phases/06-quartz-migration/06-03-SUMMARY.md` when done
</output>
