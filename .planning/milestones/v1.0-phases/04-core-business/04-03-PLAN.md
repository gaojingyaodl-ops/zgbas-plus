---
phase: 04-core-business
plan: 03
type: execute
wave: 2
depends_on: [04-02]
files_modified:
  - zgbas-system/src/main/java/com/spt/bas/server/cache/**
  - zgbas-system/src/main/java/com/spt/bas/server/util/**
  - zgbas-system/src/main/java/com/spt/bas/server/enums/**
  - zgbas-system/src/main/java/com/spt/bas/server/annotation/**
  - zgbas-system/src/main/java/com/spt/bas/server/filter/**
  - zgbas-system/src/main/java/com/spt/bas/server/listener/**
  - zgbas-system/src/main/java/com/spt/bas/server/command/**
  - zgbas-system/src/main/java/com/spt/bas/server/event/**
  - zgbas-system/src/main/java/com/spt/bas/server/rocketmq/**
  - zgbas-system/src/main/java/com/spt/bas/server/config/**
autonomous: true
requirements: [BIZ-01]
tags: [bulk-copy, wave-2a, infra, rocketmq, config-dedup]

must_haves:
  truths:
    - "Wave 2a infra 包（cache/util/enums/annotation/filter/listener/command/event/rocketmq）全照搬 ~76 文件"
    - "Wave 2a config 选择性照搬 6 文件（WebAppConfig/BasicErrorController/RtConfig/GuTuConfig/BasPiccConfig/ScheduleConfig）"
    - "FrameworkConfig 不抄（与 Phase 2 @Primary DataSource + EXT-01..03 重复 bean 冲突）"
    - "BasJobConfig 不抄（引 XxlJobSpringExecutor → Phase 6 INFRA-03）"
    - "task 包 23 文件不抄（Phase 6）"
    - "zgbas-system mvn compile 零 ERROR（含 rocketmq 22 文件 + starter 2.2.2 在 pom）"
  artifacts:
    - path: "zgbas-system/src/main/java/com/spt/bas/server/{cache,util,enums,annotation,filter,listener,command,event,rocketmq}/"
      provides: "Wave 2a infra 包（~76 文件）"
    - path: "zgbas-system/src/main/java/com/spt/bas/server/rocketmq/"
      provides: "22 rocketmq 文件（producer/listener/dto/util/tags/task/send/customProperties）"
    - path: "zgbas-system/src/main/java/com/spt/bas/server/config/"
      provides: "6 业务 @Configuration（去 FrameworkConfig + BasJobConfig）"
  key_links:
    - from: "command/BasCommandExecutor"
      to: "rocketmq/TestRocketmqProducer"
      via: "import com.spt.bas.server.rocketmq.TestRocketmqProducer"
      pattern: "import com\\.spt\\.bas\\.server\\.rocketmq"
    - from: "rocketmq/* @Component"
      to: "RocketMQTemplate（rocketmq-spring-boot-starter:2.2.2 提供）"
      via: "@Autowired RocketMQTemplate"
      pattern: "RocketMQTemplate"
    - from: "config/WebAppConfig 等 6 文件"
      to: "Spring context（@Configuration + @ConfigurationProperties）"
      via: "@ComponentScan(basePackages=com.spt) 自动扫"
      pattern: "@Configuration"
---

<objective>
Wave 2a infra 先行：照搬源 basServer 的 infra 包（cache/util/enums/annotation/filter/listener/command/event + INCLUDE rocketmq 22）+ 选择性照搬 config 6 文件（去 FrameworkConfig 与 BasJobConfig），为 Wave 2b service 照搬提供编译前提。

Purpose: 落地 D-P4-04 INCLUDE 结论（rocketmq 22 文件 + starter，非延后；3 个非 rocketmq 文件 import 此包）；执行 D-P4-04 选择性 config 去重（FrameworkConfig 与 Phase 2 @Primary 冲突、BasJobConfig 引 xxl-job 延 P6）。Wave 2 必须先 2a 后 2b（RESEARCH §Pitfall 5 gotcha 级联）。
Output: ~76 infra 文件 + 6 config 文件 + pom rocketmq starter 已在 04-01 加（本 plan verify）+ dev/prod yml rocketmq.* 已在 04-01 加（本 plan verify）。
</objective>

<execution_context>
@/Users/alan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-core-business/04-CONTEXT.md
@.planning/phases/04-core-business/04-RESEARCH.md
@.planning/phases/04-core-business/04-PATTERNS.md
@.planning/phases/04-core-business/04-02-SUMMARY.md

**BUILD PREFIX:** `JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml`

<interfaces>
<!-- Phase 2 dedup 蓝本（@Primary 击败 ConditionalOnMissingBean） -->
From zgbas-framework/src/main/java/com/spt/config/ZgbasDataSourceConfig.java:
- @Bean("datasource") @Primary public DataSource dataSource(...)
- @Bean @ConfigurationProperties(prefix="spring.datasource.druid") public DataSourceConfig dataSourceConfig()
→ 这是为何 FrameworkConfig（bas.datasource DataSource）必须 DEDUP 的依据

<!-- 04-01 已加的 pom/yml 锚点（本 plan verify 存在） -->
- zgbas-system/pom.xml 含 rocketmq-spring-boot-starter:2.2.2
- application-dev.yml 含 rocketmq.name-server=47.104.66.178:9876 + producer.group + AK/SK 占位
- application-prod.yml 含 ${ROCKETMQ_NAMESERVER} / ${ROCKETMQ_AK} / ${ROCKETMQ_SK} 占位

<!-- rocketmq starter 关键行为（RESEARCH §D-P4-04 verdict） -->
- producer 懒连接：context boot 期不连 broker，首次 send 才连
- 故 starter 在 classpath 不阻塞 Spring context boot（D-P4-06 启动验证可通过）
- @Component RocketMQTemplate 自动配（starter 提供）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 2a infra 包照搬（cache/util/enums/annotation/filter/listener/command/event/rocketmq）</name>
  <files>
    zgbas-system/src/main/java/com/spt/bas/server/cache/**,
    zgbas-system/src/main/java/com/spt/bas/server/util/**,
    zgbas-system/src/main/java/com/spt/bas/server/enums/**,
    zgbas-system/src/main/java/com/spt/bas/server/annotation/**,
    zgbas-system/src/main/java/com/spt/bas/server/filter/**（含 filter/impl/ 子目录）,
    zgbas-system/src/main/java/com/spt/bas/server/listener/**,
    zgbas-system/src/main/java/com/spt/bas/server/command/**,
    zgbas-system/src/main/java/com/spt/bas/server/event/**,
    zgbas-system/src/main/java/com/spt/bas/server/rocketmq/**
  </files>
  <read_first>
    - .planning/phases/04-core-business/04-PATTERNS.md §Wave 2a（task-shape + accept 数字 + dedup 表）+ §Shared Patterns 1/2（照搬保包名 + 逐层 compile 绿灯）
    - .planning/phases/04-core-business/04-RESEARCH.md §D-P4-04 Infra Filter Confirmation（实测计数表 + rocketmq INCLUDE verdict + filter 6 非 3）+ §Pitfall 2（rocketmq 延后必破坏 Wave 2/3 编译）+ §Pitfall 3（filter impl 漏抄）
    - .planning/phases/04-core-business/04-CONTEXT.md §decisions D-P4-04（infra 筛选照搬）
    - 源 infra 各包目录: /Users/alan/WorkSpace/IDEA/zgbas/basCore/basServer/src/main/java/com/spt/bas/server/{cache,util,enums,annotation,filter,listener,command,event,rocketmq}/
    - 源 filter 完整结构（3 接口 + 3 impl）: /Users/alan/WorkSpace/IDEA/zgbas/basCore/basServer/src/main/java/com/spt/bas/server/filter/ + filter/impl/
    - 源 rocketmq 22 文件结构: /Users/alan/WorkSpace/IDEA/zgbas/basCore/basServer/src/main/java/com/spt/bas/server/rocketmq/（producer 1 + customProperties 1 + dto 2 + listener 1 + util 3 + tags 5 + task 8 + send 1）
    - Phase 2 02-03-PLAN bulk-copy compile-gate 蓝本: /Users/alan/WorkSpace/IDEA/zgbas-plus/.planning/phases/02-infrastructure/02-03-PLAN.md
    - Phase 2 ZgbasDataSourceConfig @Primary 蓝本: /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-framework/src/main/java/com/spt/config/ZgbasDataSourceConfig.java
  </read_first>
  <action>
    照搬保包名（D-P2-07）：从源 `basCore/basServer/src/main/java/com/spt/bas/server/{cache,util,enums,annotation,filter,listener,command,event,rocketmq}/` 各自递归 `cp -r` 到 `zgbas-system/src/main/java/com/spt/bas/server/<subpkg>/`，保目录结构（filter/ 含 impl/ 子目录，rocketmq/ 含 producer/listener/util/tags/task/send/dto/customProperties 多类）。

    关键 INCLUDE 考量（D-P4-04）：
    - **rocketmq 22 文件全照搬**（非延后）——3 个非 rocketmq 文件 import 此包：`api/MQApi`（Wave 3）、`api/WorkTargetApi`（Wave 3）、`command/BasCommandExecutor`（本 Wave 2a）。漏抄必导致 Wave 2a/3 编译失败。
    - **filter 6 文件全照搬**（3 接口 + `filter/impl/` 3 impl）——CONTEXT 误写 3（仅接口），实测 6 含 impl。
    - **command 2 文件照搬**——`BasCommandExecutor` 引 rocketmq.TestRocketmqProducer，必须与 rocketmq 同波。
    - 排除 `task/`（23 xxl-job handler，Phase 6）——本 plan 不进入 task 目录。

    不改任何源码（零 rename / 零 import 调整）。pom/yml 已在 04-01 加好 rocketmq starter 与配置，本 plan 仅 verify 存在。

    完成后跑 zgbas-system compile gate，遇 cannot find symbol 级联迭代至零错。**若 ERROR > 50**，停止机械修复，回查是否漏抄某子包（Phase 1 gotcha 级联教训：一处错掩一处）。**2a 绿灯后才能开 Plan 04-04 Wave 2b**（Pitfall 5）。
  </action>
  <verify>
    <automated>
cd /Users/alan/WorkSpace/IDEA/zgbas-plus && \
echo "=== infra 计数 ===" && \
echo "CACHE=$(find zgbas-system/src/main/java/com/spt/bas/server/cache -name '*.java' | wc -l)" && \
echo "UTIL=$(find zgbas-system/src/main/java/com/spt/bas/server/util -name '*.java' | wc -l)" && \
echo "ENUMS=$(find zgbas-system/src/main/java/com/spt/bas/server/enums -name '*.java' | wc -l)" && \
echo "ANNOTATION=$(find zgbas-system/src/main/java/com/spt/bas/server/annotation -name '*.java' | wc -l)" && \
echo "FILTER=$(find zgbas-system/src/main/java/com/spt/bas/server/filter -name '*.java' | wc -l)" && \
echo "LISTENER=$(find zgbas-system/src/main/java/com/spt/bas/server/listener -name '*.java' | wc -l)" && \
echo "COMMAND=$(find zgbas-system/src/main/java/com/spt/bas/server/command -name '*.java' | wc -l)" && \
echo "EVENT=$(find zgbas-system/src/main/java/com/spt/bas/server/event -name '*.java' | wc -l)" && \
echo "ROCKETMQ=$(find zgbas-system/src/main/java/com/spt/bas/server/rocketmq -name '*.java' | wc -l)" && \
echo "=== pom + yml 锚点 ===" && \
echo "STARTER=$(grep -c 'rocketmq-spring-boot-starter' zgbas-system/pom.xml)" && \
echo "YML_DEV=$(grep -c 'rocketmq' zgbas-admin/src/main/resources/application-dev.yml)" && \
echo "YML_PROD=$(grep -c 'ROCKETMQ' zgbas-admin/src/main/resources/application-prod.yml)" && \
echo "=== xxl-job 不应出现 ===" && \
echo "XXLJOB_ABSENT=$(grep -rl 'XxlJobSpringExecutor\|xxl-job' zgbas-system/src/main/java | wc -l)" && \
echo "=== task 包不应出现 ===" && \
echo "TASK_ABSENT=$(find zgbas-system/src/main/java/com/spt/bas/server/task -name '*.java' 2>/dev/null | wc -l)" && \
echo "=== compile gate ===" && \
JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home \
/Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml \
-pl zgbas-system -am compile 2>&1 | tee /tmp/p4-03-t1.log | tail -5 && \
echo "ERROR_COUNT=$(grep -c '^\[ERROR\]' /tmp/p4-03-t1.log)" && \
echo "CANNOT_FIND=$(grep -cE 'cannot find symbol|找不到符号' /tmp/p4-03-t1.log)"
    </automated>
  </verify>
  <done>
    - CACHE ≥ 7, UTIL ≥ 34, ENUMS ≥ 2, ANNOTATION ≥ 1, FILTER ≥ 6（含 impl）, LISTENER ≥ 1, COMMAND ≥ 2, EVENT ≥ 1, ROCKETMQ ≥ 22
    - 合计 infra ≥ 76 文件
    - STARTER ≥ 1（pom 已声明）
    - YML_DEV ≥ 1（dev yml 含 rocketmq）
    - YML_PROD ≥ 3（prod yml 含 3 个 ROCKETMQ_* 占位）
    - XXLJOB_ABSENT == 0（未引入 xxl-job）
    - TASK_ABSENT == 0（task 包未抄）
    - ERROR_COUNT == 0 且 CANNOT_FIND == 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Wave 2a config 选择性照搬（6 保 + dedup FrameworkConfig + exclude BasJobConfig）</name>
  <files>
    zgbas-system/src/main/java/com/spt/bas/server/config/WebAppConfig.java,
    zgbas-system/src/main/java/com/spt/bas/server/config/BasicErrorController.java,
    zgbas-system/src/main/java/com/spt/bas/server/config/RtConfig.java,
    zgbas-system/src/main/java/com/spt/bas/server/config/GuTuConfig.java,
    zgbas-system/src/main/java/com/spt/bas/server/config/BasPiccConfig.java,
    zgbas-system/src/main/java/com/spt/bas/server/config/ScheduleConfig.java
  </files>
  <read_first>
    - .planning/phases/04-core-business/04-PATTERNS.md §Wave 2a config（8 文件分类表 + 处置）
    - .planning/phases/04-core-business/04-RESEARCH.md §D-P4-04 verdict + §config 8 文件分类（DEDUP/EXCLUDE/COPY 标注）+ §Pitfall 3
    - 源 config 8 文件: /Users/alan/WorkSpace/IDEA/zgbas/basCore/basServer/src/main/java/com/spt/bas/server/config/{FrameworkConfig,BasJobConfig,WebAppConfig,BasicErrorController,RtConfig,GuTuConfig,BasPiccConfig,ScheduleConfig}.java
    - Phase 2 ZgbasDataSourceConfig 蓝本（dedup 依据）: /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-framework/src/main/java/com/spt/config/ZgbasDataSourceConfig.java
    - Phase 2 framework 外部 SDK bean 接线（EXT-01..03 依据）: /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-framework/src/main/java/com/spt/config/
  </read_first>
  <action>
    选择性照搬源 `basServer/config/` 8 文件中的 6 文件到 `zgbas-system/src/main/java/com/spt/bas/server/config/`，保包名（D-P2-07）：

    **COPY 6 文件**：
    - `WebAppConfig.java`（Servlet filter + multipart，业务必需）
    - `BasicErrorController.java`（Error controller）
    - `RtConfig.java`（@ConfigurationProperties(prefix="rt.config")，被 `rt/RtApi` 引用）
    - `GuTuConfig.java`（@ConfigurationProperties(prefix="gutu.config")）
    - `BasPiccConfig.java`（@ConfigurationProperties(prefix="picc.config")）
    - `ScheduleConfig.java`（@EnableScheduling + ScheduledTaskRegistrar，Spring 内置 @Scheduled，**注意与 BasJobConfig 的 xxl-job 不同**，照搬安全）

    **DEDUP（不抄）**：`FrameworkConfig.java`——含 bas.datasource DataSource + 3 SDK bean。Phase 2 `ZgbasDataSourceConfig` 已用 @Primary 击败 spt-tools 的 @ConditionalOnMissingBean DataSource；Phase 2 framework 已接 EXT-01..03（auth/push/file/sign SDK bean）。照搬会重复 bean 冲突。

    **EXCLUDE（不抄 → Phase 6）**：`BasJobConfig.java`——含 @Bean XxlJobSpringExecutor，引入 xxl-job 依赖（违 INFRA-03），同 task 包延 Phase 6。

    完成后跑 zgbas-system compile gate。若 `framework/ZgbasDataSourceConfig` 与抄进来的 6 文件中某个 @ConfigurationProperties prefix 冲突（如同 prefix 重复），按 Phase 2 dedup 模式处理（@Primary 或改 prefix）。
  </action>
  <verify>
    <automated>
cd /Users/alan/WorkSpace/IDEA/zgbas-plus && \
echo "=== config 计数 + 排除项验证 ===" && \
echo "CONFIG_COUNT=$(find zgbas-system/src/main/java/com/spt/bas/server/config -name '*.java' | wc -l)" && \
echo "WEBCONFIG=$(test -f zgbas-system/src/main/java/com/spt/bas/server/config/WebAppConfig.java && echo 1 || echo 0)" && \
echo "BASICERR=$(test -f zgbas-system/src/main/java/com/spt/bas/server/config/BasicErrorController.java && echo 1 || echo 0)" && \
echo "RTCONFIG=$(test -f zgbas-system/src/main/java/com/spt/bas/server/config/RtConfig.java && echo 1 || echo 0)" && \
echo "GUTUCONFIG=$(test -f zgbas-system/src/main/java/com/spt/bas/server/config/GuTuConfig.java && echo 1 || echo 0)" && \
echo "PICCCONFIG=$(test -f zgbas-system/src/main/java/com/spt/bas/server/config/BasPiccConfig.java && echo 1 || echo 0)" && \
echo "SCHEDULECONFIG=$(test -f zgbas-system/src/main/java/com/spt/bas/server/config/ScheduleConfig.java && echo 1 || echo 0)" && \
echo "FRAMEWORK_ABSENT=$(test -f zgbas-system/src/main/java/com/spt/bas/server/config/FrameworkConfig.java && echo FOUND || echo OK_ABSENT)" && \
echo "BASJOB_ABSENT=$(test -f zgbas-system/src/main/java/com/spt/bas/server/config/BasJobConfig.java && echo FOUND || echo OK_ABSENT)" && \
echo "=== xxl-job 全模块缺失 ===" && \
echo "XXLJOB=$(grep -rl 'XxlJobSpringExecutor\|xxl-job' zgbas-system/src/main/java zgbas-admin/src/main/java zgbas-framework/src/main/java 2>/dev/null | wc -l)" && \
echo "=== compile gate ===" && \
JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home \
/Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml \
-pl zgbas-system -am compile 2>&1 | tee /tmp/p4-03-t2.log | tail -5 && \
echo "ERROR_COUNT=$(grep -c '^\[ERROR\]' /tmp/p4-03-t2.log)" && \
echo "CANNOT_FIND=$(grep -cE 'cannot find symbol|找不到符号' /tmp/p4-03-t2.log)"
    </automated>
  </verify>
  <done>
    - CONFIG_COUNT == 6（WebAppConfig + BasicErrorController + RtConfig + GuTuConfig + BasPiccConfig + ScheduleConfig）
    - 6 文件各自存在（WEBCONFIG..SCHEDULECONFIG == 1）
    - FRAMEWORK_ABSENT == "OK_ABSENT"（不抄）
    - BASJOB_ABSENT == "OK_ABSENT"（不抄，延 P6）
    - XXLJOB == 0（全模块未引入 xxl-job）
    - ERROR_COUNT == 0 且 CANNOT_FIND == 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 业务 config → Spring context | 6 @Configuration / @ConfigurationProperties bean 注册到 context；4 个 prefix（rt/gutu/picc/）来自 yml，非用户输入 |
| RocketMQ producer → broker | 22 rocketmq @Component 含 RocketMQTemplate；启动期懒连接，不阻塞 boot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-T | Tampering | FrameworkConfig DEDUP | mitigate | 不抄 FrameworkConfig 避免 DataSource 重复 bean；Phase 2 @Primary ZgbasDataSourceConfig 已击败 spt-tools ConditionalOnMissingBean，单一权威 DataSource |
| T-04-03-E | EoP | BasJobConfig EXCLUDE | mitigate | 不抄 BasJobConfig 避免 xxl-job 引入（INFRA-03），延 Phase 6 RuoYi quartz |
| T-04-03-I | Info Disclosure | rocketmq 凭证 | mitigate | prod profile 走 ${ROCKETMQ_AK}/${ROCKETMQ_SK}/${ROCKETMQ_NAMESERVER} 占位外置（04-01 已配） |
| T-04-03-D | DoS | ScheduleConfig @EnableScheduling | accept | Spring 内置 @Scheduled 调度，沿用源项目行为（行为等价优先） |

无 high 级威胁。Mitigate 项落地（DEDUP + EXCLUDE + 占位外置）；Accept 有依据（行为等价）。
</threat_model>

<verification>
- zgbas-system mvn compile 零 ERROR（含 76 infra + 6 config + 22 rocketmq + starter）
- 04-01 pom/yml 锚点存在（rocketmq starter + rocketmq.* yml）
- xxl-job 全模块未出现（XXLJOB_ABSENT == 0）
- task 包未抄（Phase 6 范围）
- FrameworkConfig/BasJobConfig 未抄（dedup + P6 延后）
</verification>

<success_criteria>
- BIZ-01 编译前提：infra（cache/util/.../rocketmq）+ 6 config 就位，Wave 2b service 可解析 `com.spt.bas.server.{cache,util,...}` 引用
- D-P4-04 INCLUDE 落地：rocketmq 22 文件 + starter + yml 配置
- D-P4-04 dedup 落地：FrameworkConfig 不抄（避免 bean 冲突）+ BasJobConfig 不抄（避免 xxl-job）
- 逐层绿灯：zgbas-system compile 零 ERROR 后才进 Wave 2b（Plan 04-04）
</success_criteria>

<output>
Create `.planning/phases/04-core-business/04-03-SUMMARY.md` when done.
</output>
