---
phase: 04-core-business
plan: 04
type: execute
wave: 2
depends_on: [04-03]
files_modified:
  - zgbas-system/src/main/java/com/spt/bas/server/service/**
  - zgbas-system/src/main/java/com/spt/bas/server/service/impl/**
  - zgbas-system/src/main/java/com/spt/bas/server/ctr/**
  - zgbas-system/src/main/java/com/spt/bas/server/logistics/**
  - zgbas-system/src/main/java/com/spt/bas/server/performance/**
  - zgbas-system/src/main/java/com/spt/bas/server/rt/**
  - zgbas-system/src/main/java/com/spt/bas/server/stock/**
autonomous: true
requirements: [BIZ-01]
tags: [bulk-copy, wave-2b, service, impl, domain-subpkg, jpa]

must_haves:
  truths:
    - "Wave 2b service + impl + 域子包（ctr/logistics/performance/rt/stock）全照搬 ~533 文件"
    - "所有 service impl 标 @Service 且可被 @Autowired 解析（运行期由 ZgbasApplication @ComponentScan(basePackages=com.spt) 自动接线）"
    - "所有 service impl 的 @Transactional 沿用 Phase 2 JpaTransactionManager @Primary（无需额外事务设计）"
    - "zgbas-system mvn compile 零 ERROR（gotcha 级联迭代至零错）"
  artifacts:
    - path: "zgbas-system/src/main/java/com/spt/bas/server/service/"
      provides: "241 顶-level service 接口（I*Service）"
    - path: "zgbas-system/src/main/java/com/spt/bas/server/service/impl/"
      provides: "248 service impl（@Service）"
    - path: "zgbas-system/src/main/java/com/spt/bas/server/{ctr,logistics,performance,rt,stock}/"
      provides: "44 域子包文件（合同 ctr 9 + 物流 logistics 6 + 业绩 performance 4 + 融拓 rt 1 + 库存 stock 24）"
  key_links:
    - from: "service impl @Service"
      to: "Spring context（@ComponentScan 自动注册）"
      via: "@Service + @Autowired"
      pattern: "@Service"
    - from: "service impl @Transactional"
      to: "Phase 2 JpaTransactionManager @Primary"
      via: "默认事务管理器解析"
      pattern: "@Transactional"
    - from: "service extends BaseDataService/IDatasource"
      to: "Phase 2 已内联的 spt-tools-data BaseDataService"
      via: "extends 关系"
      pattern: "extends Base"
---

<objective>
Wave 2b service + impl + 域子包照搬（~533 文件，保包名 D-P2-07）到 zgbas-system/com/spt/bas/server/。这是 BIZ-01 的核心交付——业务 Service 层落位，JPA 增删改查通过 Phase 2 已就位的实体/Dao 可执行。

Purpose: 落地 BIZ-01 主体（合同/授信/库存/放款业务逻辑层）。Wave 2 必须先 2a infra 后 2b service（Pitfall 5 gotcha 级联）；service impl 引用 Wave 1 remote 契约 + Wave 2a infra + Phase 2 实体/Dao + Phase 2 内联 spt-tools-data 的 BaseDataService。
Output: ~533 net-new 文件，全保包名 com.spt.bas.server.*。
</objective>

<execution_context>
@/Users/alan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-core-business/04-CONTEXT.md
@.planning/phases/04-core-business/04-RESEARCH.md
@.planning/phases/04-core-business/04-PATTERNS.md
@.planning/phases/04-core-business/04-03-SUMMARY.md

**BUILD PREFIX:** `JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml`

<interfaces>
<!-- Phase 2 已就位资产（service 编译依赖） -->
- zgbas-system/src/main/java/com/spt/bas/client/entity/ (239 实体)
- zgbas-system/src/main/java/com/spt/bas/server/dao/ (229 Dao)
- zgbas-system/src/main/java/com/spt/bas/client/vo/ (293 vo)
- zgbas-common/src/main/java/com/spt/tools/data/service/ (BaseDataService/IDatasource/BaseApi 等已内联)

<!-- Phase 2 事务管理器（@Transactional 自动解析） -->
- zgbas-framework ZgbasDataSourceConfig: @Bean @Primary DataSource
- 隐式 @Primary JpaTransactionManager（JPA 默认配置，单 DataSource）

<!-- Wave 2a 已就位（service 引用 infra） -->
- zgbas-system/com/spt/bas/server/{cache,util,enums,annotation,filter,listener,command,event,rocketmq,config}/

<!-- Wave 1 已就位（service 引用契约） -->
- zgbas-system/com/spt/bas/client/{remote,dto,util,common,riskScore}/

<!-- 域子包映射（源 basServer 同名子包，照搬保包名） -->
- ctr/ (9 合同业务)
- logistics/ (6 物流)
- performance/ (4 业绩)
- rt/ (1 融拓对接)
- stock/ (24 库存)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 2b service + impl + 域子包照搬（~533 文件）+ compile 绿灯</name>
  <files>
    zgbas-system/src/main/java/com/spt/bas/server/service/**（顶-level 241 接口）,
    zgbas-system/src/main/java/com/spt/bas/server/service/impl/**（248 实现）,
    zgbas-system/src/main/java/com/spt/bas/server/ctr/**（9）,
    zgbas-system/src/main/java/com/spt/bas/server/logistics/**（6）,
    zgbas-system/src/main/java/com/spt/bas/server/performance/**（4）,
    zgbas-system/src/main/java/com/spt/bas/server/rt/**（1）,
    zgbas-system/src/main/java/com/spt/bas/server/stock/**（24）
  </files>
  <read_first>
    - .planning/phases/04-core-business/04-PATTERNS.md §Wave 2b（task-shape + 预期 gotcha + accept 数字）+ §Shared Patterns 1/2/5/6
    - .planning/phases/04-core-business/04-RESEARCH.md §Wave Composition Wave 2b（533 文件细分）+ §Pitfall 5（gotcha 级联）+ §D-P4-05（schema drift 仅修运行阻塞）
    - .planning/phases/04-core-business/04-CONTEXT.md §decisions D-P4-03（wave 测序）+ D-P4-05（ddl-auto=none）
    - .planning/phases/04-core-business/04-03-SUMMARY.md（确认 2a 已绿，可开 2b）
    - 源 service 各包目录: /Users/alan/WorkSpace/IDEA/zgbas/basCore/basServer/src/main/java/com/spt/bas/server/{service,service/impl,ctr,logistics,performance,rt,stock}/
    - 源代表性 service: /Users/alan/WorkSpace/IDEA/zgbas/basCore/basServer/src/main/java/com/spt/bas/server/service/IApplyBrandService.java + service/impl/ApplyBrandServiceImpl.java
    - Phase 2 02-05-PLAN Task 1 entity/Dao bulk-copy 蓝本（同型 479 文件操作经验）: /Users/alan/WorkSpace/IDEA/zgbas-plus/.planning/phases/02-infrastructure/02-05-PLAN.md
  </read_first>
  <action>
    照搬保包名（D-P2-07）：从源 `basCore/basServer/src/main/java/com/spt/bas/server/{service,service/impl,ctr,logistics,performance,rt,stock}/` 各自递归 `cp -r` 到 `zgbas-system/src/main/java/com/spt/bas/server/<subpkg>/`，保目录结构与包名。

    **5 个源目录 → 目标 mapping**：
    - `service/`（顶-level 241 接口）→ `zgbas-system/.../service/`
    - `service/impl/`（248 实现）→ `zgbas-system/.../service/impl/`
    - `ctr/`（9 合同业务）→ `zgbas-system/.../ctr/`
    - `logistics/`（6 物流）→ `zgbas-system/.../logistics/`
    - `performance/`（4 业绩）→ `zgbas-system/.../performance/`
    - `rt/`（1 融拓）→ `zgbas-system/.../rt/`
    - `stock/`（24 库存）→ `zgbas-system/.../stock/`

    不改任何源码（零 import 调整 / 零 rename）。完成后跑 zgbas-system compile gate。

    **预期 gotcha（Phase 1/2 教训）**：
    - `@Query` 引用未抄实体 → 已被 Phase 2 全量 239 实体覆盖，风险低
    - 引用 `com.spt.bas.server.{cache,util,...,rocketmq}` → 必须 Wave 2a 绿灯后开 2b（Plan 04-03 闭环）
    - 引用 Wave 1 basClient remote 契约 → 必须 Wave 1 绿灯（Plan 04-02 闭环）
    - **若 ERROR > 50**：停止机械修复，回查是否漏抄某 2a infra 子包或 Wave 1 remote 子包（一处错掩一处，级联从 40→30→20→0）
    - 实体运行期 schema drift（D-P4-05）：compile 期不暴露，留待 MockMvc HTTP proof 阶段（Plan 04-06），此 plan 仅关注 compile 绿灯

    完成后跑 zgbas-system compile gate，迭代至零 ERROR。
  </action>
  <verify>
    <automated>
cd /Users/alan/WorkSpace/IDEA/zgbas-plus && \
echo "=== service + impl + 域子包计数 ===" && \
echo "SVC_IFACE=$(find zgbas-system/src/main/java/com/spt/bas/server/service -maxdepth 1 -name 'I*.java' | wc -l)" && \
echo "SVC_OTHER=$(find zgbas-system/src/main/java/com/spt/bas/server/service -maxdepth 1 -name '*.java' ! -name 'I*.java' | wc -l)" && \
echo "SVC_IMPL=$(find zgbas-system/src/main/java/com/spt/bas/server/service/impl -name '*.java' | wc -l)" && \
echo "CTR=$(find zgbas-system/src/main/java/com/spt/bas/server/ctr -name '*.java' | wc -l)" && \
echo "LOGISTICS=$(find zgbas-system/src/main/java/com/spt/bas/server/logistics -name '*.java' | wc -l)" && \
echo "PERFORMANCE=$(find zgbas-system/src/main/java/com/spt/bas/server/performance -name '*.java' | wc -l)" && \
echo "RT=$(find zgbas-system/src/main/java/com/spt/bas/server/rt -name '*.java' | wc -l)" && \
echo "STOCK=$(find zgbas-system/src/main/java/com/spt/bas/server/stock -name '*.java' | wc -l)" && \
echo "=== 包名 verbatim 抽样 ===" && \
head -1 zgbas-system/src/main/java/com/spt/bas/server/service/IApplyBrandService.java 2>/dev/null && \
head -1 zgbas-system/src/main/java/com/spt/bas/server/service/impl/ApplyBrandServiceImpl.java 2>/dev/null && \
echo "=== compile gate ===" && \
JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home \
/Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml \
-pl zgbas-system -am compile 2>&1 | tee /tmp/p4-04-t1.log | tail -10 && \
echo "ERROR_COUNT=$(grep -c '^\[ERROR\]' /tmp/p4-04-t1.log)" && \
echo "CANNOT_FIND=$(grep -cE 'cannot find symbol|找不到符号' /tmp/p4-04-t1.log)"
    </automated>
  </verify>
  <done>
    - SVC_IFACE ≥ 241（顶-level I*Service 接口）
    - SVC_IMPL ≥ 248（service/impl 实现）
    - CTR ≥ 9, LOGISTICS ≥ 6, PERFORMANCE ≥ 4, RT ≥ 1, STOCK ≥ 24
    - 合计 ~533 ±10 文件
    - 包名 `com.spt.bas.server.service[.impl].*` 与 `com.spt.bas.server.{ctr,logistics,performance,rt,stock}.*` verbatim
    - ERROR_COUNT == 0 且 CANNOT_FIND == 0
    - 若 ERROR > 50 曾出现，已在 SUMMARY 记录回查路径与修复点
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service impl → JPA Dao → MySQL sptbasdb_pd | @Transactional 业务逻辑通过 Phase 2 @Primary JpaTransactionManager 写同一 DataSource |
| service → service 跨域调用 | 同进程 bean DI，无网络面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-T | Tampering | service @Transactional 边界 | accept | 沿用 Phase 2 JpaTransactionManager @Primary（单 DataSource）；源项目行为等价，不重新设计事务 |
| T-04-04-R | Repudiation | service 业务逻辑审计 | accept | 沿用 Phase 2 PERSIST-04 审计字段（createdDate/updatedDate + @EntityListeners），照搬源行为 |
| T-04-04-I | Info Disclosure | service SQL 注入风险 | accept | 照搬源 JPA/Query，使用 Spring Data JPA 参数化查询；非本期新增代码 |

本 plan 为纯 service 照搬，无新增端点 / 无新增外部输入面。所有运行期访问控制由 Wave 4 BFF controller + Phase 3 Shiro 链覆盖；事务边界沿用 Phase 2。
</threat_model>

<verification>
- zgbas-system mvn compile 零 ERROR（533 service + impl + 域子包 + 上游 Wave 1/2a 全绿）
- service 顶-level 接口 ≥ 241，impl ≥ 248，5 域子包计数达标
- 包名 verbatim 抽样验证（package com.spt.bas.server.service[.impl]）
- 无 @Transactional 设计偏差（沿用 Phase 2 JpaTransactionManager）
</verification>

<success_criteria>
- BIZ-01 主体交付：合同/授信/库存/放款业务 Service 层全就位，JPA 增删改查编译期可解析（运行期由 Wave 5 api 暴露 + Wave 4 BFF 调用）
- D-P4-03 wave 测序落地：2a 绿灯后开 2b，2b 绿灯后才能开 Wave 3
- D-P4-05 schema drift 不主动 reconcile：compile 期不暴露，留 Wave 4 验收 MockMvc HTTP proof 自然发现
</success_criteria>

<output>
Create `.planning/phases/04-core-business/04-04-SUMMARY.md` when done.
</output>
