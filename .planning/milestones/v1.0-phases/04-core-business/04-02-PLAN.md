---
phase: 04-core-business
plan: 02
type: execute
wave: 1
depends_on: [04-01]
files_modified:
  - zgbas-system/src/main/java/com/spt/bas/client/remote/**
  - zgbas-system/src/main/java/com/spt/bas/client/dto/**
  - zgbas-system/src/main/java/com/spt/bas/client/util/**
  - zgbas-system/src/main/java/com/spt/bas/client/common/**
  - zgbas-system/src/main/java/com/spt/bas/client/riskScore/**
autonomous: true
requirements: [BIZ-01, BIZ-03]
tags: [bulk-copy, basClient, remote-contracts, feign-contract]

must_haves:
  truths:
    - "238 个 @FeignClient extends BaseClient 契约（I*Client）就位于 zgbas-system/com/spt/bas/client/remote/"
    - "basClient 数据载体 dto/util/common/riskScore 全就位"
    - "zgbas-system mvn compile 零 ERROR（含新内联的 234 契约 + 14 数据载体）"
    - "契约 url SpEL #{basServerConfig.url} 解析锚点 basServerConfig bean 由 04-01 已就位"
  artifacts:
    - path: "zgbas-system/src/main/java/com/spt/bas/client/remote/"
      provides: "238 个 @FeignClient 契约（Phase 2 已 4 个 + 本期净新增 ~234）"
    - path: "zgbas-system/src/main/java/com/spt/bas/client/dto/"
      provides: "2 dto 类"
    - path: "zgbas-system/src/main/java/com/spt/bas/client/util/"
      provides: "6 util 类"
    - path: "zgbas-system/src/main/java/com/spt/bas/client/common/"
      provides: "3 common 类"
    - path: "zgbas-system/src/main/java/com/spt/bas/client/riskScore/"
      provides: "3 riskScore 类"
  key_links:
    - from: "238 I*Client.url SpEL"
      to: "basServerConfig bean.url（04-01 产出）"
      via: "@FeignClient url = BasConstants.SERVER_URL"
      pattern: "url\\s*=\\s*BasConstants\\.SERVER_URL"
    - from: "I*Client extends BaseClient<Entity>"
      to: "Phase 2 已内联实体（com.spt.bas.client.entity）"
      via: "泛型参数解析"
      pattern: "extends BaseClient<"
---

<objective>
照搬源 basClient 的剩余数据载体（remote 契约 ~234 + dto/util/common/riskScore ~14，合计 ~249 文件，保包名 verbatim D-P2-07）到 zgbas-system。这些是 Wave 2/3/4 编译与运行的契约前提——Wave 2 service/api 引用 I*Client 作业务依赖；Wave 4 BFF 通过 @Autowired I*Client 经 Feign 自回环调本进程 api。

Purpose: 满足 CONTEXT 修正发现——Phase 2/3 实际仅内联 4 个 remote 契约（IBsDictClient/IBsCompanyOurClient/IApproveWaitDealClient/IPmProcessClient），剩余 234 是本期 Wave 1 真实工作（RESEARCH §Pitfall 6）。无此 plan 则 Wave 2/4 直接 ClassNotFoundException。
Output: ~249 net-new 文件，全保包名 com.spt.bas.client.*。
</objective>

<execution_context>
@/Users/alan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-core-business/04-CONTEXT.md
@.planning/phases/04-core-business/04-RESEARCH.md
@.planning/phases/04-core-business/04-PATTERNS.md
@.planning/phases/04-core-business/04-01-SUMMARY.md

**BUILD PREFIX:** `JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml`

<interfaces>
<!-- 契约接口 shape 蓝本（Phase 2 已就位的 IBsDictClient） -->
From zgbas-system/src/main/java/com/spt/bas/client/remote/IBsDictClient.java:
- @FeignClient(name = BasConstants.SERVER_NAME,
               path = BasConstants.SERVER_NAME + "/api/dict",
               url = BasConstants.SERVER_URL,    // SpEL #{basServerConfig.url}
               configuration = FeignConfig.class)
- public interface IBsDictClient extends BaseClient<BsDictType> { ... }

<!-- 当前已就位的 4 契约（不覆盖，仅参考） -->
- IBsDictClient.java (Phase 2)
- IBsCompanyOurClient.java (Phase 2)
- IApproveWaitDealClient.java (Phase 3 stub，本 plan Task 1 用源真接口覆盖)
- IPmProcessClient.java (Phase 3 stub，本 plan Task 1 用源真接口覆盖)

<!-- 依赖锚点（04-01 已就位） -->
- BasClientConfig bean "basServerConfig" 读 spt.bas.server.url
- BasConstants.SERVER_URL = "#{basServerConfig.url}"
- ZgbasApplication @EnableFeignClients 已扫 com.spt.bas.client.remote（04-01 Task 1）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: basClient remote 契约 bulk 照搬（~234 净新增 + 2 stub 升级）</name>
  <files>
    zgbas-system/src/main/java/com/spt/bas/client/remote/**（含子目录 basTrade）
  </files>
  <read_first>
    - .planning/phases/04-core-business/04-PATTERNS.md §Wave 1 1a（task-shape + accept 数字 + 1c BasClientConfig 锚点）
    - .planning/phases/04-core-business/04-RESEARCH.md §Wave Composition Wave 1 表 + §Pitfall 6（Phase 2 仅 4 内联）+ §D-P4-02 Enumeration（PM 域 13 簇 + stub 处置）
    - .planning/phases/04-core-business/04-CONTEXT.md §code_context Reusable Assets（已就位 4 契约）
    - 源 remote 目录树: /Users/alan/WorkSpace/IDEA/zgbas/basCore/basClient/src/main/java/com/spt/bas/client/remote/
    - 源代表性契约: /Users/alan/WorkSpace/IDEA/zgbas/basCore/basClient/src/main/java/com/spt/bas/client/remote/IApplyBrandClient.java
    - 当前单体已就位 4 契约（避免覆盖误判）: /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-system/src/main/java/com/spt/bas/client/remote/{IBsDictClient,IBsCompanyOurClient,IApproveWaitDealClient,IPmProcessClient}.java
    - Phase 2 bulk-copy 蓝本: /Users/alan/WorkSpace/IDEA/zgbas-plus/.planning/phases/02-infrastructure/02-05-PLAN.md（Task 1 entity/Dao 照搬模式）
  </read_first>
  <action>
    照搬保包名（D-P2-07）：从源 `basCore/basClient/src/main/java/com/spt/bas/client/remote/` 递归 `cp -r` 到 `zgbas-system/src/main/java/com/spt/bas/client/remote/`，保目录结构（含 basTrade 子目录 + package-info）。

    覆盖规则（重要）：
    - **IBsDictClient.java / IBsCompanyOurClient.java**：Phase 2 已照搬源版，不覆盖（用 `cp -n` 或先列后排除）。
    - **IApproveWaitDealClient.java / IPmProcessClient.java**：Phase 3 stub-port（非 @FeignClient，纯方法签名），**本 plan 用源真接口覆盖**——源是 `@FeignClient extends BaseClient` 完整契约。若 IndexController 调用点因签名变化编译失败，同步修 IndexController 调用（保持 stub 行为，参考 Phase 3 D-P3-10 模式：源真接口已落地后 IndexController 的 @Autowired(required=false) 仍保留以防 bean 未注册，null 守卫自然 fallback）。

    排除 PM 域 13 簇的处理（D-P4-02）：本 plan Task 1 照搬源 basClient/remote 全部接口（含 IPmProcessClient/IPmApplySetClient/IPmApproveClient 等），让源接口 verbatim 落位。stub 降级在 Plan 04-06 Wave 4 BFF 字段层处理（@Autowired(required=false)），不在契约接口层 stub——契约接口保留源签名作 Feign 自回环目标。PM 域若无对应 api 实现（源 basServer/api 无 Pm*Api），Wave 4 BFF 字段标 required=false，启动期 Feign proxy 仍生成但首次调用 404（业务降级裸 404，D-P4-02 接受）。

    不改任何源码（零 import 调整、零 rename）。完成后跑 zgbas-system compile gate，遇 cannot find symbol 级联（Phase 1 gotcha 教训）迭代修复至零错——典型 gotcha：契约引用未抄的 vo/dto（Task 2 解决）→ 必要时 Task 2 先于本 Task compile green（同 plan 内顺序可调）。
  </action>
  <verify>
    <automated>
cd /Users/alan/WorkSpace/IDEA/zgbas-plus && \
echo "=== remote 契约文件计数 ===" && \
echo "REMOTE_COUNT=$(find zgbas-system/src/main/java/com/spt/bas/client/remote -name '*.java' | wc -l)" && \
echo "FEIGNCLIENT_COUNT=$(grep -rl '@FeignClient' zgbas-system/src/main/java/com/spt/bas/client/remote/ | wc -l)" && \
echo "EXTENDS_BASECLIENT=$(grep -rl 'extends BaseClient' zgbas-system/src/main/java/com/spt/bas/client/remote/ | wc -l)" && \
echo "=== zgbas-system compile gate ===" && \
JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home \
/Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml \
-pl zgbas-system -am compile 2>&1 | tee /tmp/p4-02-t1.log | tail -5 && \
echo "ERROR_COUNT=$(grep -c '^\[ERROR\]' /tmp/p4-02-t1.log)" && \
echo "CANNOT_FIND=$(grep -cE 'cannot find symbol|找不到符号' /tmp/p4-02-t1.log)"
    </automated>
  </verify>
  <done>
    - REMOTE_COUNT ≥ 238（含 basTrade 子包 + package-info，预期 239 ±2）
    - FEIGNCLIENT_COUNT ≥ 235
    - EXTENDS_BASECLIENT ≥ 230
    - ERROR_COUNT == 0 且 CANNOT_FIND == 0
    - IBsDictClient.java / IBsCompanyOurClient.java 未被覆盖（diff 空）
    - IApproveWaitDealClient.java / IPmProcessClient.java 已升级为源真 @FeignClient 契约（grep `@FeignClient` 命中）
    - IndexController 调用点（若签名变化）已同步修
  </done>
</task>

<task type="auto">
  <name>Task 2: basClient 数据载体（dto/util/common/riskScore）照搬（~14）</name>
  <files>
    zgbas-system/src/main/java/com/spt/bas/client/dto/**,
    zgbas-system/src/main/java/com/spt/bas/client/util/**,
    zgbas-system/src/main/java/com/spt/bas/client/common/**,
    zgbas-system/src/main/java/com/spt/bas/client/riskScore/**
  </files>
  <read_first>
    - .planning/phases/04-core-business/04-PATTERNS.md §Wave 1 1b（task-shape）
    - .planning/phases/04-core-business/04-RESEARCH.md §Wave Composition Wave 1 表（dto 2/util 6/common 3/riskScore 3）
    - 源数据载体目录: /Users/alan/WorkSpace/IDEA/zgbas/basCore/basClient/src/main/java/com/spt/bas/client/{dto,util,common,riskScore}/
    - Phase 2 已就位的 vo/constant/cache（同型数据载体）: /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-system/src/main/java/com/spt/bas/client/{vo,constant,cache}/
  </read_first>
  <action>
    照搬保包名（D-P2-07）：从源 `basCore/basClient/src/main/java/com/spt/bas/client/{dto,util,common,riskScore}/` 各自 `cp -r` 到 `zgbas-system/src/main/java/com/spt/bas/client/<subpkg>/`，保目录结构与包名。

    4 子包合计 ~14 文件（dto 2 + util 6 + common 3 + riskScore 3，数字源自 RESEARCH §Wave Composition Wave 1 实测）。这些是被动数据载体/工具类，无对外契约语义，照搬零修改。

    完成后跑 zgbas-system compile gate。此 Task 可能与 Task 1 存在双向依赖（remote 契约引用 dto/util，dto/util 也可能引用 vo/constant），若 Task 1 编译时报 dto/util 缺失，先跑 Task 2 再回 Task 1 闭环。
  </action>
  <verify>
    <automated>
cd /Users/alan/WorkSpace/IDEA/zgbas-plus && \
echo "=== 数据载体计数 ===" && \
echo "DTO=$(find zgbas-system/src/main/java/com/spt/bas/client/dto -name '*.java' | wc -l)" && \
echo "UTIL=$(find zgbas-system/src/main/java/com/spt/bas/client/util -name '*.java' | wc -l)" && \
echo "COMMON=$(find zgbas-system/src/main/java/com/spt/bas/client/common -name '*.java' | wc -l)" && \
echo "RISKSCORE=$(find zgbas-system/src/main/java/com/spt/bas/client/riskScore -name '*.java' | wc -l)" && \
echo "=== zgbas-system compile gate ===" && \
JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home \
/Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml \
-pl zgbas-system -am compile 2>&1 | tee /tmp/p4-02-t2.log | tail -5 && \
echo "ERROR_COUNT=$(grep -c '^\[ERROR\]' /tmp/p4-02-t2.log)" && \
echo "CANNOT_FIND=$(grep -cE 'cannot find symbol|找不到符号' /tmp/p4-02-t2.log)"
    </automated>
  </verify>
  <done>
    - DTO ≥ 2, UTIL ≥ 6, COMMON ≥ 3, RISKSCORE ≥ 3（合计 ~14 ±2）
    - 包名 `com.spt.bas.client.{dto,util,common,riskScore}.*` verbatim
    - ERROR_COUNT == 0 且 CANNOT_FIND == 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bas 契约接口（ compile-time 类型） | 238 接口被 zgbas-system/admin 多模块共用；本 plan 仅照搬接口，无运行期新增端点 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-E | EoP | 契约 path SpEL 注入 | accept | SpEL `#{basServerConfig.url}` 仅读 yml 固定 key `spt.bas.server.url`，不接受用户输入；非外部可控 |
| T-04-02-I | Info Disclosure | 契约 url 经 Feign 自回环 | accept | 自回环仅 localhost（04-01 配 spt.bas.server.url=http://localhost:8080）；运行期请求经 Shiro 链（Phase 3） |

本 plan 为纯接口照搬，无运行期新增端点 / 无外部输入面 / 无密钥处理，威胁面极小。沿用 Phase 3 Shiro 链覆盖运行期访问控制。
</threat_model>

<verification>
- zgbas-system mvn compile 零 ERROR（含 238 remote 契约 + 14 数据载体）
- remote/ 目录 java 文件 ≥ 238
- @FeignClient 注解文件 ≥ 235
- extends BaseClient 文件 ≥ 230
- IBsDictClient/IBsCompanyOurClient 未被覆盖（Phase 2 资产保护）
- IApproveWaitDealClient/IPmProcessClient 升级为源真 @FeignClient 契约
</verification>

<success_criteria>
- BIZ-01 编译前提：238 契约 + 14 数据载体就位（Wave 2 service/api 编译可解析 I*Client 类型）
- BIZ-03 运行前提：238 契约 Feign proxy 可生成（@EnableFeignClients 扫包 + basServerConfig bean SpEL 锚点就位）
- 逐层绿灯：zgbas-system compile 零 ERROR 后才进 Wave 2a（Plan 04-03）
</success_criteria>

<output>
Create `.planning/phases/04-core-business/04-02-SUMMARY.md` when done.
</output>
