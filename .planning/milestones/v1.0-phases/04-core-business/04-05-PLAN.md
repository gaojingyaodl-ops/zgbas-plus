---
phase: 04-core-business
plan: 05
type: execute
wave: 3
depends_on: [04-04]
files_modified:
  - zgbas-system/src/main/java/com/spt/bas/server/api/**
autonomous: true
requirements: [BIZ-01, BIZ-03]
tags: [bulk-copy, wave-3, rest-controller, base-api, self-loopback-endpoint]

must_haves:
  truths:
    - "Wave 3 api 包照搬 224 个 @RestController extends BaseApi<Entity> 文件"
    - "0 个 api 加 implements I*Client（D-P4-01 方案 A 关键约束——签名不兼容 Page/PageDown 反协变）"
    - "api 端点暴露在单体根路径（如 /apply/brand/*，无 spt-bas-server context-path 前缀，由 04-01 BasFeignPathConfig 剥离 Feign 请求前缀）"
    - "zgbas-system mvn compile 零 ERROR"
  artifacts:
    - path: "zgbas-system/src/main/java/com/spt/bas/server/api/"
      provides: "224 个 @RestController extends BaseApi<Entity>"
    - path: "zgbas-system/src/main/java/com/spt/bas/server/api/{basData,basTrade,fund,sign}/"
      provides: "api 子目录（basData 1 + basTrade 1 + fund 2 + sign 3）"
  key_links:
    - from: "api @RestController"
      to: "Spring MVC handler mapping"
      via: "@RequestMapping + @RestController"
      pattern: "@RestController"
    - from: "api extends BaseApi<Entity>"
      to: "spt-tools-data BaseApi（Phase 2 已内联）"
      via: "extends 关系"
      pattern: "extends BaseApi<"
    - from: "api @Autowired I*Service"
      to: "Wave 2b service impl bean"
      via: "@Autowired"
      pattern: "@Autowired.*I[A-Z][a-zA-Z]*Service"
---

<objective>
Wave 3 api 照搬（224 @RestController extends BaseApi，保包名 D-P2-07）到 zgbas-system/com/spt/bas/server/api/。这些是 BIZ-01 的 REST 实现层，也是 D-P4-01 方案 A Feign 自回环的目标端点——BFF 通过 Feign proxy 调到这些 api（同进程 HTTP 跳）。

Purpose: 落地 BIZ-01 REST 层 + 满足 BIZ-03 自回环目标端点。**关键约束（D-P4-01 方案 A）**：224 api 1:1 照搬零修改，**不加 implements I*Client**（源 api extends BaseApi 与 BaseClient 的 findPage 返回类型不兼容：Page<T> vs PageDown<T> 反协变；详见 RESEARCH §D-P4-01 Critical Finding）。路径前缀由 04-01 BasFeignPathConfig.RequestInterceptor 剥离，不动 api 源码。
Output: 224 net-new 文件，全保包名 com.spt.bas.server.api.*。
</objective>

<execution_context>
@/Users/alan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-core-business/04-CONTEXT.md
@.planning/phases/04-core-business/04-RESEARCH.md
@.planning/phases/04-core-business/04-PATTERNS.md
@.planning/phases/04-core-business/04-04-SUMMARY.md

**BUILD PREFIX:** `JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml`

<interfaces>
<!-- ApplyBrandApi 照搬蓝本（224 文件全部同型） -->
From 源 basCore/basServer/src/main/java/com/spt/bas/server/api/ApplyBrandApi.java:
- @RestController @RequestMapping(value = "apply/brand")
- public class ApplyBrandApi extends BaseApi<BasBrand>
- @Autowired private IApplyBrandService applyBrandService;
- @Override public IDataService<BasBrand> getService() { return applyBrandService; }

<!-- 关键约束（D-P4-01 方案 A） -->
- ❌ 不加 implements IApplyBrandClient（BaseApi.findPage 返 Page<T> vs BaseClient 返 PageDown<T> 反协变）
- ✅ extends BaseApi<Entity> 照搬零修改
- ✅ Feign 自回环经 BasFeignPathConfig.RequestInterceptor 剥离 spt-bas-server/ 前缀

<!-- 依赖锚点（已就位） -->
- Wave 1: 238 I*Client 契约（Plan 04-02）
- Wave 2a: infra + config（Plan 04-03）
- Wave 2b: service + impl + 域子包（Plan 04-04）
- 04-01: BasFeignPathConfig.RequestInterceptor + ZgbasApplication @EnableFeignClients 放宽

<!-- api 子目录文件分布（RESEARCH §Wave Composition Wave 3） -->
- api 顶-level: 217
- api/basData: 1
- api/basTrade: 1
- api/fund: 2
- api/sign: 3
- 合计 224
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 3 api @RestController 照搬（224）+ implements check + compile 绿灯</name>
  <files>
    zgbas-system/src/main/java/com/spt/bas/server/api/**（含子目录 basData/basTrade/fund/sign）
  </files>
  <read_first>
    - .planning/phases/04-core-business/04-PATTERNS.md §Wave 3（task-shape + 关键约束 + accept 数字）+ §Shared Patterns 1/2
    - .planning/phases/04-core-business/04-RESEARCH.md §D-P4-01 Critical Finding（0/224 implements + Page/PageDown 反协变）+ §Wave Composition Wave 3（224 细分）+ §Pitfall 1（无脑 implements 导致 NoSuchBeanDef）+ §Pitfall 4（path 前缀）
    - .planning/phases/04-core-business/04-CONTEXT.md §decisions D-P4-01（方案 A 修正）+ D-P4-01a（path 前缀）
    - .planning/phases/04-core-business/04-04-SUMMARY.md（确认 2b 已绿，可开 Wave 3）
    - 源 api 目录: /Users/alan/WorkSpace/IDEA/zgbas/basCore/basServer/src/main/java/com/spt/bas/server/api/（含子目录 basData/basTrade/fund/sign）
    - 源代表性 api: /Users/alan/WorkSpace/IDEA/zgbas/basCore/basServer/src/main/java/com/spt/bas/server/api/ApplyBrandApi.java
    - 源 MQApi（引 rocketmq）: /Users/alan/WorkSpace/IDEA/zgbas/basCore/basServer/src/main/java/com/spt/bas/server/api/MQApi.java
    - 源 WorkTargetApi（引 rocketmq.send）: /Users/alan/WorkSpace/IDEA/zgbas/basCore/basServer/src/main/java/com/spt/bas/server/api/WorkTargetApi.java
    - Phase 2 02-05-PLAN Task 1 bulk-copy 蓝本: /Users/alan/WorkSpace/IDEA/zgbas-plus/.planning/phases/02-infrastructure/02-05-PLAN.md
    - Phase 3 03-02-PLAN Task 1 controller 干净照搬蓝本: /Users/alan/WorkSpace/IDEA/zgbas-plus/.planning/phases/03-auth-homepage/03-02-PLAN.md
  </read_first>
  <action>
    照搬保包名（D-P2-07）：从源 `basCore/basServer/src/main/java/com/spt/bas/server/api/`（含子目录 basData/basTrade/fund/sign）递归 `cp -r` 到 `zgbas-system/src/main/java/com/spt/bas/server/api/`，保目录结构与包名。

    **关键约束（D-P4-01 方案 A，必须遵守）**：
    - **224 api 文件 1:1 照搬，零修改**——不加 `implements I*Client`，不改 `@RequestMapping` 路径，不改 import。
    - 验证手段：照搬完成后跑 `grep -rln 'implements.*I[A-Z][a-zA-Z]*Client' api/` 必须 == 0（与源码事实一致，确认未误加 implements）。
    - **不处理 path 前缀**——源 api `@RequestMapping("apply/brand")` 照搬后暴露在单体根 `/apply/brand/*`（无 context-path），Feign 自回环的 `spt-bas-server/` 前缀由 04-01 BasFeignPathConfig.RequestInterceptor 剥离。**不动 api 源码，不动 238 契约。**

    依赖核查（编译期）：
    - api 引用 Wave 2b `I*Service`（Plan 04-04 已就位）
    - api 引用 Phase 2 实体/vo（已就位）
    - `MQApi` / `WorkTargetApi` 引用 Wave 2a rocketmq（Plan 04-03 已就位）
    - api extends BaseApi<Entity>（BaseApi 在 Phase 2 内联的 spt-tools-data）

    不改任何源码。完成后跑 zgbas-system compile gate，迭代至零 ERROR。完成后跑启动验证 `ZgbasApplicationTest`（前置 export DB_PASSWORD + SPT_APP_SECRET），确认 224 api bean 全注册（无 NoSuchBeanDefinitionException）——此时 04-01 Task 2 加的 3 个 @Disabled basContractEndpointReachable_* 测试可以移除 @Disabled 试跑（HTTP proof），但留到 Plan 04-06 统一启用。

    **若启动验证暴露 path 前缀 404**（Feign 自回环 `/spt-bas-server/apply/brand/findAll` 返 404）：BasFeignPathConfig.RequestInterceptor 未生效，在 SUMMARY 记录并尝试 fallback（servlet URL rewrite filter）；不擅自改 238 契约；上报 autonomous:false 节点。
  </action>
  <verify>
    <automated>
cd /Users/alan/WorkSpace/IDEA/zgbas-plus && \
echo "=== api 计数 ===" && \
echo "API_COUNT=$(find zgbas-system/src/main/java/com/spt/bas/server/api -name '*.java' | wc -l)" && \
echo "API_TOP=$(find zgbas-system/src/main/java/com/spt/bas/server/api -maxdepth 1 -name '*.java' | wc -l)" && \
echo "API_BASDATA=$(find zgbas-system/src/main/java/com/spt/bas/server/api/basData -name '*.java' 2>/dev/null | wc -l)" && \
echo "API_BASTRADE=$(find zgbas-system/src/main/java/com/spt/bas/server/api/basTrade -name '*.java' 2>/dev/null | wc -l)" && \
echo "API_FUND=$(find zgbas-system/src/main/java/com/spt/bas/server/api/fund -name '*.java' 2>/dev/null | wc -l)" && \
echo "API_SIGN=$(find zgbas-system/src/main/java/com/spt/bas/server/api/sign -name '*.java' 2>/dev/null | wc -l)" && \
echo "=== D-P4-01 关键约束：implements I*Client 必须为 0 ===" && \
echo "IMPLEMENTS_ICLIENT=$(grep -rln 'implements.*I[A-Z][a-zA-Z]*Client' zgbas-system/src/main/java/com/spt/bas/server/api/ | wc -l)" && \
echo "=== extends BaseApi 抽样 ===" && \
echo "EXTENDS_BASEAPI=$(grep -rl 'extends BaseApi' zgbas-system/src/main/java/com/spt/bas/server/api/ | wc -l)" && \
echo "=== 包名 verbatim 抽样 ===" && \
head -1 zgbas-system/src/main/java/com/spt/bas/server/api/ApplyBrandApi.java 2>/dev/null && \
echo "=== compile gate ===" && \
JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home \
/Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml \
-pl zgbas-system -am compile 2>&1 | tee /tmp/p4-05-t1.log | tail -10 && \
echo "ERROR_COUNT=$(grep -c '^\[ERROR\]' /tmp/p4-05-t1.log)" && \
echo "CANNOT_FIND=$(grep -cE 'cannot find symbol|找不到符号' /tmp/p4-05-t1.log)"
    </automated>
  </verify>
  <done>
    - API_COUNT == 224（±2，含子目录 basData 1 + basTrade 1 + fund 2 + sign 3 + 顶-level 217）
    - **IMPLEMENTS_ICLIENT == 0**（D-P4-01 方案 A 关键约束，未误加 implements）
    - EXTENDS_BASEAPI ≥ 217（顶-level + 子目录多数 extends BaseApi）
    - 包名 `com.spt.bas.server.api[.<subdir>].*` verbatim
    - ERROR_COUNT == 0 且 CANNOT_FIND == 0
    - ZgbasApplicationTest 启动验证通过（224 api bean 全注册，无 NoSuchBeanDefinitionException）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 客户端 → api @RestController | 224 业务端点暴露在单体根路径（如 /apply/brand/*），运行期访问控制由 Phase 3 Shiro 链 + Wave 4 BFF 间接覆盖 |
| Feign proxy → api（自回环） | 238 契约 Feign 自回环经 04-01 RequestInterceptor 剥离前缀后调本进程 api；仅 localhost |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-I | Info Disclosure | api 业务端点公网可达 | mitigate | Phase 3 Shiro `/**=user` 链随 Wave 4 BFF 照搬自动覆盖业务路径；api 路径不在 Shiro 链 exclude |
| T-04-05-S | Spoofing | Feign 自回环调用 | accept | 仅 localhost:8080 自跳；同进程鉴权沿用 Shiro 链 |
| T-04-05-T | Tampering | BasFeignPathConfig.RequestInterceptor path 剥离 | accept | 仅剥离硬编码 `spt-bas-server/` 前缀；不做其它 URL 变换 |
| T-04-05-E | EoP | api 业务逻辑未做权限校验 | mitigate | BaseApi 的 findPage/save/findAll 等方法照搬源实现；权限校验由 BFF 层（Wave 4）+ Shiro 链（Phase 3）覆盖；api 不直接对外（前端只连 BFF） |

无 high 级威胁。运行期访问控制由 Phase 3 Shiro 链 + Wave 4 BFF 间接覆盖（前端不直连 api，api 经 BFF 转发或 Feign 自回环访问）。
</threat_model>

<verification>
- zgbas-system mvn compile 零 ERROR（含 224 api + 上游 Wave 1/2a/2b 全绿）
- API_COUNT == 224（含 basData/basTrade/fund/sign 子目录）
- IMPLEMENTS_ICLIENT == 0（D-P4-01 方案 A 关键约束）
- EXTENDS_BASEAPI ≥ 217
- ZgbasApplicationTest 启动验证通过（前置 export DB_PASSWORD + SPT_APP_SECRET；local-export = passing）
</verification>

<success_criteria>
- BIZ-01 REST 层交付：224 业务 api 就位，JPA CRUD 端点暴露（findAll/save/findPage/getEntity 等 BaseApi 继承方法）
- BIZ-03 自回环目标端点就位：238 契约的 Feign proxy 可路由到本进程 api（经 04-01 RequestInterceptor 剥离前缀）
- D-P4-01 方案 A 关键约束满足：0 implements + 224 extends BaseApi + 零修改
- Wave 3 绿灯后才开 Wave 4 BFF（Plan 04-06）
</success_criteria>

<output>
Create `.planning/phases/04-core-business/04-05-SUMMARY.md` when done.
</output>
