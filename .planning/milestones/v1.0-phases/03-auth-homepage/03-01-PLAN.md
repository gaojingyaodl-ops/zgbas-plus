---
phase: 03-auth-homepage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - zgbas-framework/src/main/java/com/spt/bas/web/shiro/ShiroDbRealm.java
  - zgbas-framework/src/main/java/com/spt/bas/web/shiro/ShiroUtil.java
  - zgbas-framework/src/main/java/com/spt/bas/web/shiro/ShiroService.java
  - zgbas-framework/src/main/java/com/spt/bas/web/shiro/SsoUsernamePasswordToken.java
  - zgbas-framework/src/main/java/com/spt/bas/web/config/ShiroPropConfig.java
  - zgbas-framework/src/main/java/com/spt/bas/web/util/ServletUtils.java
  - zgbas-framework/src/main/java/com/spt/bas/web/util/Convert.java
  - zgbas-framework/src/main/java/com/spt/bas/web/util/StringUtils.java
  - zgbas-framework/src/main/java/com/spt/bas/web/util/CharsetKit.java
  - zgbas-framework/pom.xml
  - pom.xml
  - zgbas-admin/src/main/java/com/spt/ZgbasApplication.java
  - zgbas-admin/src/main/resources/application.yml
  - zgbas-admin/src/main/resources/application-dev.yml
autonomous: true
requirements: [AUTH-01, AUTH-03, AUTH-04]
tags: [shiro, framework, realm, security]

must_haves:
  truths:
    - "ShiroDbRealm (@Component) 存在于 zgbas-framework 且 extends AbstractShiroDbRealm"
    - "ShiroService (@Component) 存在且 implements IShiroService，使 ShiroChainMetaSource 能注入它"
    - "ShiroUtil/ShiroService/SsoUsernamePasswordToken 与 ShiroDbRealm 在同一 com.spt.bas.web.shiro 包内（同包常量解析正确）"
    - "ZgbasApplication excludeFilters 不再包含 ToolsShiroConfig（Shiro 自动配置激活）"
    - "mvn compile framework+admin 零错误"
    - "application-dev.yml 包含 shiro.prop.mock-password / shiro.prop.app-cd / zgBas.secret 键"
    - "application.yml 包含 spring.mvc.static-path-pattern=/static/**（BLOCKER 1: 模板 @{/static/**} 引用解析正确，否则 double static/ 前缀 → 404）"
    - "application.yml 包含 session 行为等价配置（tracking-modes=cookie / cookie.http-only / timeout）"
    - "application-dev.yml 包含 spring.thymeleaf.cache=false（dev 热重载，WARNING 1）"
  artifacts:
    - path: "zgbas-framework/src/main/java/com/spt/bas/web/shiro/ShiroDbRealm.java"
      provides: "Shiro 认证 Realm（含 mock 后门 + SSO reLogin + assertCredentialsMatch no-op）"
      contains: "class ShiroDbRealm extends AbstractShiroDbRealm"
    - path: "zgbas-framework/src/main/java/com/spt/bas/web/shiro/ShiroService.java"
      provides: "Shiro 过滤器链初始化（调 authOpenFacade.findAllMenu HTTP）"
      contains: "implements IShiroService"
    - path: "zgbas-framework/src/main/java/com/spt/bas/web/config/ShiroPropConfig.java"
      provides: "shiro.prop.* 配置绑定（mockPassword / appCd / sessionEnable）"
      contains: "ConfigurationProperties(prefix = \"shiro.prop\")"
    - path: "zgbas-admin/src/main/java/com/spt/ZgbasApplication.java"
      provides: "ToolsShiroConfig 排除移除（Shiro 激活）"
  key_links:
    - from: "ShiroDbRealm"
      to: "AbstractShiroDbRealm (inlined common)"
      via: "extends"
      pattern: "extends AbstractShiroDbRealm"
    - from: "ShiroService.initSection"
      to: "authOpenFacade.findAllMenu"
      via: "@Autowired IAuthOpenFacade HTTP call"
      pattern: "authOpenFacade\\.findAllMenu"
    - from: "ZgbasApplication"
      to: "ToolsShiroConfig (inlined common)"
      via: "excludeFilters removed → auto-config engages"
      pattern: "ToolsShiroConfig"
---

<objective>
将源 zgbas/web 的 Shiro 认证链路 4 文件单元 + 依赖 Web 工具类照搬进 zgbas-framework，取消 ZgbasApplication 对 ToolsShiroConfig 的 exclude，让 Phase 2 已内联的 Shiro 自动配置激活。添加 ruoyi-common jar 依赖解决跨模块编译阻塞。同时补齐基础 application.yml 的行为等价配置（静态路径模式 BLOCKER 1 + session 行为 WARNING 1）和 dev profile Shiro/thymeleaf 配置键。

Purpose: AUTH-01/03/04 — 让 Shiro session+cookie 认证链路在单体内可用，密码校验行为等价（SHA-1 + 盐 1024 次迭代，实际由外部 spt-auth 侧完成，Realm assertCredentialsMatch 为 no-op）。同时确保模板 @{/static/**} 资源引用正确解析（BLOCKER 1），session/thymeleaf 行为与旧系统等价（WARNING 1）。
Output: framework 内 Shiro Realm/Service/Util/Token + 配置绑定 + Web 工具类；admin 内 exclude 移除 + 基础/dev 配置键。
</objective>

<execution_context>
@/Users/alan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-auth-homepage/03-CONTEXT.md
@.planning/phases/03-auth-homepage/03-PATTERNS.md

**BUILD PREFIX (JDK 1.8 — 本机默认 JDK 21，每条 mvn 必须前缀):**
`JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml`

<interfaces>
<!-- 已内联的 spt-tools-shiro 资产（Phase 2，DO NOT re-port）-->
<!-- AbstractShiroDbRealm: com.spt.tools.shiro.AbstractShiroDbRealm (common) — ShiroDbRealm extends 它 -->
<!-- IShiroService: com.spt.tools.shiro.IShiroService (common) — ShiroService implements 它 -->
<!-- ToolsShiroConfig: com.spt.tools.shiro.config.ToolsShiroConfig (common) — 被 un-exclude 后自动激活 -->
<!-- ShiroChainMetaSource: com.spt.tools.shiro.ShiroChainMetaSource — @Autowired(required=false) IShiroService -->
<!-- MyFormAuthenticationFilter / MyLogoutFilter: filters (common) — 已内联 -->
<!-- ShiroProp: com.spt.tools.shiro.config.ShiroProp (common) — 与 ported ShiroPropConfig 同绑 shiro.prop.* -->
<!-- com.spt.tools.shiro.util.ShiroUtil (base): ShiroUtil extends 它 -->
<!-- IAuthOpenFacade: com.spt.auth.sdk.open.IAuthOpenFacade — Phase 2 @Bean HTTP injected in ZgbasExternalBeansConfig -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 照搬 Shiro 4 文件单元 + Web 工具类 + 配置 + ruoyi-common 依赖</name>
  <files>
    zgbas-framework/src/main/java/com/spt/bas/web/shiro/ShiroDbRealm.java,
    zgbas-framework/src/main/java/com/spt/bas/web/shiro/ShiroUtil.java,
    zgbas-framework/src/main/java/com/spt/bas/web/shiro/ShiroService.java,
    zgbas-framework/src/main/java/com/spt/bas/web/shiro/SsoUsernamePasswordToken.java,
    zgbas-framework/src/main/java/com/spt/bas/web/config/ShiroPropConfig.java,
    zgbas-framework/src/main/java/com/spt/bas/web/util/ServletUtils.java,
    zgbas-framework/src/main/java/com/spt/bas/web/util/Convert.java,
    zgbas-framework/src/main/java/com/spt/bas/web/util/StringUtils.java,
    zgbas-framework/src/main/java/com/spt/bas/web/util/CharsetKit.java,
    zgbas-framework/pom.xml,
    pom.xml
  </files>
  <read_first>
    - .planning/phases/03-auth-homepage/03-PATTERNS.md §ShiroDbRealm, §ShiroUtil/ShiroService/SsoUsernamePasswordToken, §ShiroPropConfig, §Inlined assets already present
    - 源文件（照搬来源，绝对路径）:
      - /Users/alan/WorkSpace/IDEA/zgbas/web/src/main/java/com/spt/bas/web/shiro/ShiroDbRealm.java
      - /Users/alan/WorkSpace/IDEA/zgbas/web/src/main/java/com/spt/bas/web/shiro/ShiroUtil.java
      - /Users/alan/WorkSpace/IDEA/zgbas/web/src/main/java/com/spt/bas/web/shiro/ShiroService.java
      - /Users/alan/WorkSpace/IDEA/zgbas/web/src/main/java/com/spt/bas/web/shiro/SsoUsernamePasswordToken.java
      - /Users/alan/WorkSpace/IDEA/zgbas/web/src/main/java/com/spt/bas/web/config/ShiroPropConfig.java
      - /Users/alan/WorkSpace/IDEA/zgbas/web/src/main/java/com/spt/bas/web/util/ServletUtils.java
      - /Users/alan/WorkSpace/IDEA/zgbas/web/src/main/java/com/spt/bas/web/util/Convert.java
      - /Users/alan/WorkSpace/IDEA/zgbas/web/src/main/java/com/spt/bas/web/util/StringUtils.java
      - /Users/alan/WorkSpace/IDEA/zgbas/web/src/main/java/com/spt/bas/web/util/CharsetKit.java
    - 当前 root pom: /Users/alan/WorkSpace/IDEA/zgbas-plus/pom.xml (dependencyManagement 段)
    - 当前 framework pom: /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-framework/pom.xml
  </read_first>
  <action>
    **照搬保包名（D-P2-07）：所有源文件 1:1 复制到 zgbas-framework 对应目录，不改包名、不改代码。**

    1. **Shiro 4 文件单元**（com.spt.bas.web.shiro 包，必须同模块同包 — ShiroDbRealm 引用 ShiroUtil.APPID/ENTERPRISEID/DEPTID/DEPTABBR/INDUSTRY 及 ShiroUtil.appCd **无 import，靠同包解析**）:
       将 4 个源文件从 `/Users/alan/WorkSpace/IDEA/zgbas/web/src/main/java/com/spt/bas/web/shiro/` 复制到 `zgbas-framework/src/main/java/com/spt/bas/web/shiro/`:
       - `ShiroDbRealm.java` — @Component，extends 已内联 AbstractShiroDbRealm；含 mock 后门（D-P3-06 照搬）、authOpenFacade.login() HTTP 调外部 spt-auth、reLoginSso SSO 路径、assertCredentialsMatch no-op 空体
       - `ShiroUtil.java` — extends 已内联 com.spt.tools.shiro.util.ShiroUtil；含 APPID/ENTERPRISEID/INDUSTRY/DEPTID/DEPTABBR 常量 + getCurrAppId() HTTP via SpringContextHolder
       - `ShiroService.java` — @Component implements IShiroService；initSection 调 authOpenFacade.findAllMenu() HTTP 构建 perms 链
       - `SsoUsernamePasswordToken.java` — extends UsernamePasswordToken，含 appCode + ssoLogin 字段

    2. **ShiroPropConfig**（com.spt.bas.web.config 包）:
       将源文件复制到 `zgbas-framework/src/main/java/com/spt/bas/web/config/ShiroPropConfig.java`。@Configuration @ConfigurationProperties(prefix="shiro.prop")，字段 appCd/mockPassword/sessionEnable。

    3. **Web 工具类 4 文件**（com.spt.bas.web.util 包，ShiroDbRealm 硬依赖 ServletUtils → ServletUtils 同包引用 Convert + StringUtils → Convert 同包引用 CharsetKit + StringUtils，因此 4 文件必须一起搬运保持包完整）:
       将 4 个源文件从 `/Users/alan/WorkSpace/IDEA/zgbas/web/src/main/java/com/spt/bas/web/util/` 复制到 `zgbas-framework/src/main/java/com/spt/bas/web/util/`:
       - `ServletUtils.java` — getRequest()/getHeader()/checkAgentIsMobile()，ShiroDbRealm 5 处引用
       - `Convert.java` — 类型转换工具（同包引用 StringUtils.isEmpty + CharsetKit.CHARSET_UTF_8）
       - `StringUtils.java` — extends org.apache.commons.lang3.StringUtils，含 nvl/isEmpty 等增强方法
       - `CharsetKit.java` — 字符集常量（同包引用 StringUtils.isEmpty）

    4. **ruoyi-common jar 依赖** — ShiroDbRealm 引用 `com.ruoyi.common.enums.UserStatus` + `com.ruoyi.common.utils.IpUtils`。跨阶段 IndexController 还需 AjaxResult/ShiroConstants/ruoyi.ServletUtils/ruoyi.StringUtils（共 6+ 类），引用面已大到值得 jar 依赖而非内联（phase-essence 指令"fall back to jar dep ONLY if surface grows large"已满足）。jar `ruoyi-common-4.7.2` 已在本地仓库。
       - 在 root `pom.xml` 的 `<dependencyManagement><dependencies>` 段末尾（EXT-04 SDK 之前）添加:
         groupId=com.ruoyi, artifactId=ruoyi-common, version=4.7.2
       - 在 `zgbas-framework/pom.xml` 的 `<dependencies>` 段添加 ruoyi-common 声明（admin 传递依赖自动可见）

    5. **不改 ZgbasApplication.java** — exclude 移除在 Task 2。
  </action>
  <verify>
    <automated>
cd /Users/alan/WorkSpace/IDEA/zgbas-plus && \
echo "REALM=$(test -f zgbas-framework/src/main/java/com/spt/bas/web/shiro/ShiroDbRealm.java && echo EXISTS)" && \
echo "UTIL=$(test -f zgbas-framework/src/main/java/com/spt/bas/web/shiro/ShiroUtil.java && echo EXISTS)" && \
echo "SERVICE=$(test -f zgbas-framework/src/main/java/com/spt/bas/web/shiro/ShiroService.java && echo EXISTS)" && \
echo "SSO_TOKEN=$(test -f zgbas-framework/src/main/java/com/spt/bas/web/shiro/SsoUsernamePasswordToken.java && echo EXISTS)" && \
echo "PROP_CFG=$(test -f zgbas-framework/src/main/java/com/spt/bas/web/config/ShiroPropConfig.java && echo EXISTS)" && \
echo "SERVLET_UT=$(test -f zgbas-framework/src/main/java/com/spt/bas/web/util/ServletUtils.java && echo EXISTS)" && \
echo "CONVERT=$(test -f zgbas-framework/src/main/java/com/spt/bas/web/util/Convert.java && echo EXISTS)" && \
echo "STR_UT=$(test -f zgbas-framework/src/main/java/com/spt/bas/web/util/StringUtils.java && echo EXISTS)" && \
echo "CHARSET=$(test -f zgbas-framework/src/main/java/com/spt/bas/web/util/CharsetKit.java && echo EXISTS)" && \
echo "RUOYI_ROOT=$(grep -c 'ruoyi-common' pom.xml)" && \
echo "RUOYI_FW=$(grep -c 'ruoyi-common' zgbas-framework/pom.xml)"
    </automated>
  </verify>
  <acceptance_criteria>
    - zgbas-framework/src/main/java/com/spt/bas/web/shiro/ShiroDbRealm.java 存在且包含 'class ShiroDbRealm extends AbstractShiroDbRealm'
    - zgbas-framework/src/main/java/com/spt/bas/web/shiro/ShiroService.java 存在且包含 'implements IShiroService'
    - zgbas-framework/src/main/java/com/spt/bas/web/config/ShiroPropConfig.java 存在且包含 'ConfigurationProperties(prefix = "shiro.prop")'
    - zgbas-framework/src/main/java/com/spt/bas/web/util/ServletUtils.java + Convert.java + StringUtils.java + CharsetKit.java 均存在
    - pom.xml grep 'ruoyi-common' 返回 ≥1
    - zgbas-framework/pom.xml grep 'ruoyi-common' 返回 ≥1
    - ShiroDbRealm.java 包含 'SUPER_PWD' 和 'isMockLogin'（mock 后门照搬验证，D-P3-06）
  </acceptance_criteria>
  <done>
    Shiro 4 文件 + ShiroPropConfig + 4 Web 工具类均存在于 zgbas-framework 对应源包；ruoyi-common 在 root pom dependencyManagement + framework pom 声明。
  </done>
</task>

<task type="auto">
  <name>Task 2: 移除 ToolsShiroConfig exclude + 基础+dev 配置键（静态路径/session/thymeleaf/Shiro）+ 编译绿灯</name>
  <files>
    zgbas-admin/src/main/java/com/spt/ZgbasApplication.java,
    zgbas-admin/src/main/resources/application.yml,
    zgbas-admin/src/main/resources/application-dev.yml
  </files>
  <read_first>
    - /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-admin/src/main/java/com/spt/ZgbasApplication.java (当前完整文件)
    - /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-admin/src/main/resources/application.yml (当前完整文件)
    - /Users/alan/WorkSpace/IDEA/zgbas-plus/zgbas-admin/src/main/resources/application-dev.yml (当前完整文件)
    - .planning/phases/03-auth-homepage/03-PATTERNS.md §ZgbasApplication (before/after 代码)
    - 源配置值: /Users/alan/WorkSpace/IDEA/zgbas/web/src/main/resources/application-dev.properties (mock-password=123456)
    - 源配置值: /Users/alan/WorkSpace/IDEA/zgbas/web/src/main/resources/application.properties (L9 tracking-modes=cookie, L10 cookie.http-only=true, L12 session.timeout=21600, L18 thymeleaf.cache=false, L21 static-path-pattern=/static/**, app-cd=zgbas, zgBas.secret=DUHZOTTIUTWNDJFRGLUKSHBGVBKWJRUY)
  </read_first>
  <action>
    1. **编辑 ZgbasApplication.java** — 从 excludeFilters 的 classes 数组中移除 `com.spt.tools.shiro.config.ToolsShiroConfig.class`（当前在第 63 行）。两个 FeignConfig exclude **保留不动**。编辑后 classes 数组仅含:
       `com.spt.tools.http.feign.FeignConfig.class` + `com.spt.sign.client.config.FeignConfig.class`
       更新上方 Javadoc（第 46-49 行）反映 ToolsShiroConfig 已激活（Phase 3 AUTH-01..04）。

    2. **添加行为等价配置到 application.yml**（BLOCKER 1 + WARNING 1）— 照搬源 `application.properties` 的 server/session/spring.mvc/spring.thymeleaf 行为键。这些是**通用行为**（dev 和 prod 一致），放 base application.yml。
       当前 application.yml 已有 `spring:` 和 `server:` 顶级节点，将子键**合并进去**（勿重复声明顶级节点）。
       在 `spring:` 节下添加:
       ```yaml
         mvc:
           static-path-pattern: /static/**   # BLOCKER 1: 模板 @{/static/css/...} 解析。Spring Boot 默认 /** 导致 /static/css/foo.css → classpath:/static/static/css/foo.css（double static/）→ 404。源 application.properties L21。
       ```
       在 `server.servlet:` 节下（已有 `context-path: /`）添加:
       ```yaml
         session:
           tracking-modes: cookie           # WARNING 1: 禁用 URL jsessionid（避免 redirect 400）。源 L9。
           cookie:
             http-only: true                # WARNING 1: 源 L10。
           timeout: 21600s                  # WARNING 1: 6h 会话。源 L12 `server.session.timeout=21600`（旧 key），SpringBoot 2.5 映射为 `server.servlet.session.timeout`，Duration 后缀 s = 秒。
       ```

    3. **添加 dev Shiro + thymeleaf 配置键到 application-dev.yml** — 以下 3 个 Shiro 键缺失会导致 `@Value("${zgBas.secret}")` 启动期占位符解析失败。照搬源 dev 值（D-P3-07 dev 保明文）。
       在 application-dev.yml 末尾追加:
       ```
       shiro:
         prop:
           mock-password: 123456        # D-P3-06/07 照搬 dev 明文后门密码
           app-cd: zgbas                # D-P3-02 照搬源 appCode
       zgBas:
         secret: DUHZOTTIUTWNDJFRGLUKSHBGVBKWJRUY   # D-P3-12 SSO secret 照搬 dev 值
       spring:
         thymeleaf:
           cache: false                 # WARNING 1: dev 热重载（源 application.properties L18）。prod profile 覆盖为 true（Plan 04）。
       ```
       注意: 若 application-dev.yml 已有 `spring:` 顶级节点，将 `thymeleaf:` 子节点合并进去。

    4. **编译绿灯（gotcha 级联迭代）** — 运行:
       `JAVA_HOME=...mvn... -pl zgbas-framework,zgbas-admin -am compile 2>&1 | tee /tmp/p3w1-compile.log`
       检查: `grep -cE '^\[ERROR\]|cannot find symbol|找不到符号' /tmp/p3w1-compile.log`（locale 无关 grep，MEMORY 教训）
       若 >0 错误: 读错误输出 → 识别缺失类型 → 从源 web 模块搬运缺失文件 → 重编译。**迭代到 0 错误**（Phase 1 gotcha 级联教训: 修一个 unmask 下一个）。
       预期 gotcha: Web 工具类可能引用其他同包类未搬运 → 补搬；ruoyi-common 传递依赖冲突 → 调整。
  </action>
  <verify>
    <automated>
cd /Users/alan/WorkSpace/IDEA/zgbas-plus && \
echo "EXCLUDE_REMOVED=$(grep -v '^ *//' zgbas-admin/src/main/java/com/spt/ZgbasApplication.java | grep -v '^ *\*' | grep -c 'ToolsShiroConfig')" && \
echo "FEIGN_KEPT=$(grep -v '^ *//' zgbas-admin/src/main/java/com/spt/ZgbasApplication.java | grep -c 'FeignConfig')" && \
echo "STATIC_PATTERN=$(grep -c 'static-path-pattern' zgbas-admin/src/main/resources/application.yml)" && \
echo "SESSION_TRACKING=$(grep -c 'tracking-modes' zgbas-admin/src/main/resources/application.yml)" && \
echo "SESSION_HTTPONLY=$(grep -c 'http-only' zgbas-admin/src/main/resources/application.yml)" && \
echo "SESSION_TIMEOUT=$(grep -c 'timeout' zgbas-admin/src/main/resources/application.yml)" && \
echo "DEV_MOCK=$(grep -c 'mock-password' zgbas-admin/src/main/resources/application-dev.yml)" && \
echo "DEV_SECRET=$(grep -c 'zgBas' zgbas-admin/src/main/resources/application-dev.yml)" && \
echo "DEV_THEMELEAF=$(grep -c 'thymeleaf' zgbas-admin/src/main/resources/application-dev.yml)" && \
JAVA_HOME=/Users/alan/Library/Java/JavaVirtualMachines/corretto-1.8.0_482/Contents/Home /Users/alan/App/apache-maven-3.8.6/bin/mvn -s /Users/alan/App/apache-maven-3.8.6/zg_settings.xml -pl zgbas-framework,zgbas-admin -am compile 2>&1 | tee /tmp/p3w1-compile.log | tail -5 && \
echo "ERROR_COUNT=$(grep -cE '^\[ERROR\]|cannot find symbol|找不到符号' /tmp/p3w1-compile.log)"
    </automated>
  </verify>
  <acceptance_criteria>
    - ZgbasApplication.java 非注释行中 grep 'ToolsShiroConfig' 返回 0（exclude 已移除）
    - ZgbasApplication.java 非注释行中 grep 'FeignConfig' 返回 ≥2（两个 FeignConfig exclude 保留）
    - application.yml grep 'static-path-pattern' 返回 ≥1（BLOCKER 1）
    - application.yml grep 'tracking-modes' 返回 ≥1（WARNING 1）
    - application.yml grep 'http-only' 返回 ≥1（WARNING 1）
    - application.yml grep 'timeout' 返回 ≥1（WARNING 1）
    - application-dev.yml 包含 'mock-password'、'app-cd'、'zgBas' 键
    - application-dev.yml grep 'thymeleaf' 返回 ≥1 且 cache 值为 false（WARNING 1 dev 热重载）
    - `mvn compile -pl zgbas-framework,zgbas-admin -am` 输出 BUILD SUCCESS 且 `grep -cE '^\[ERROR\]|cannot find symbol|找不到符号'` == 0
  </acceptance_criteria>
  <done>
    ToolsShiroConfig exclude 已移除（Shiro 自动配置激活）；基础 application.yml 行为等价配置就位（static-path-pattern BLOCKER 1 + session 行为 WARNING 1）；dev Shiro + thymeleaf 配置键就位；framework+admin 编译零错。
  </done>
</task>

</tasks>

<threat_model>
## 信任边界

| 边界 | 描述 |
|------|------|
| 用户→ShiroFilter | 未认证请求经 Shiro authc/user 过滤器 |
| ShiroDbRealm→外部 spt-auth | HTTP 调 authOpenFacade.login() 跨网络认证 |

## STRIDE 威胁登记

| 威胁 ID | 类别 | 组件 | 处置 | 缓解计划 |
|---------|------|------|------|---------|
| T-03-01 | 欺骗(Spoofing) | ShiroDbRealm.isMockLogin super: 后门 | **接受风险** | D-P3-06 有意保留（行为等价优先，PROJECT.md 已列已知技术债）。**不修改、不移除。** Prod 缓解: application-prod.yml 使用 `${ZGBAS_MOCK_PASSWORD:}` 占位（空默认=后门关闭，D-P3-07）；dev 保 `123456` 明文照搬。 |
| T-03-02 | 篡改(Tampering) | SSO ticket MD5(secretKey+user+date) | **接受风险** | 弱加密（MD5）是照搬源行为，行为等价优先（D-P3-12）。Phase 7 对齐验证时评估升级。 |
| T-03-03 | 信息泄露 | Shiro session 内存存储 | **接受风险** | 照搬源行为（D-P3-05，无 Redis/SessionDAO）。单进程内存会话，无 session fixation 处理（源无此逻辑）。 |
| T-03-04 | 信息泄露 | CR-01 生产库密码泄漏 | **跨阶段延迟** | jdbc.properties 明文密码已进 git 历史。Phase 2 已环境变量外置（`${DB_PASSWORD}`）。真实轮换是跨阶段安全债，不在 Phase 3 行动。 |
| T-03-SC | 篡改(Tampering) | ruoyi-common jar (npm/pip/cargo 等效) | **缓解** | jar 来自本地仓库 `/Users/alan/App/Repository/com/ruoyi/ruoyi-common/4.7.2/`，源 web 项目已使用同版本，行为等价验证。标注为 [ASSUMED]。 |
</threat_model>

<verification>
- [x] Shiro 4 文件单元 + ShiroPropConfig + 4 Web 工具类存在于 framework
- [x] ruoyi-common 在 root pom dependencyManagement + framework pom
- [x] ZgbasApplication excludeFilters 不含 ToolsShiroConfig
- [x] application.yml 含 spring.mvc.static-path-pattern=/static/** + session 行为键（BLOCKER 1 + WARNING 1）
- [x] application-dev.yml 含 shiro.prop.mock-password / app-cd / zgBas.secret / thymeleaf.cache=false
- [x] mvn compile framework+admin 零错误
</verification>

<success_criteria>
- Shiro 认证链路源码就位（Realm/Service/Util/Token/Config），包名照搬
- ToolsShiroConfig 自动配置激活（exclude 移除）
- 编译零错（gotcha 级联迭代至零）
- 基础+dev 配置键就位（启动期 @Value 占位符可解析；静态资源路径正确解析；session/thymeleaf 行为等价）
- Mock 后门照搬保留（D-P3-06），dev 默认明文（D-P3-07）
</success_criteria>

<output>
Create `.planning/phases/03-auth-homepage/03-01-SUMMARY.md` when done
</output>
