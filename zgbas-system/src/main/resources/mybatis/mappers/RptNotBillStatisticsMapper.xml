<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spt.bas.report.server.dao.RptNotBillStatisticsMapper">
    <select id="findRptNotBillStatisticsPage" resultType="com.spt.bas.report.client.entity.RptNotBillStatistics">

		SELECT
		    id,
			profit_type,
			buy_contract_no,
			our_company_name,
			buy_company_name,
			product_name,
			total_number,
			buy_total_amount,
			contract_time,
			pay_amount,
			pay_full_date,
			receipt_bill_amount,
			receipt_bill_date
		FROM
			t_ctr_contract_profit
		WHERE
			business_type = 'ZY-BB'
			AND buy_contract_status != 'C'
			AND seal_flg = TRUE
			AND pay_amount >= buy_total_amount
			AND buy_total_amount > receipt_bill_amount
		<if test="ourCompanyName != null and ourCompanyName != '' ">
			AND our_company_name = #{ourCompanyName}
		</if>
		<if test="companyName != null and companyName != '' ">
			and buy_company_name LIKE CONCAT('%',#{companyName},'%')
		</if>
		<if test="ourCompanyNameList != null and ourCompanyNameList.size > 0 ">
			and our_company_name IN
			<foreach collection="ourCompanyNameList" item="var" index="index" open="(" close=")" separator=",">
				#{ourCompanyNameList[${index}],jdbcType=VARCHAR}
			</foreach>
		</if>
		<if test="productType != null and productType eq 'NHG'.toString() and hgMatchUserIdList != null and hgMatchUserIdList.size() != 0">
			and match_user_id not in
			<foreach collection="hgMatchUserIdList" item="item" index="index" open="(" close=")" separator=",">
				#{hgMatchUserIdList[${index}]}
			</foreach>
		</if>
		<if test="productType != null and productType eq 'HG'.toString() and hgMatchUserIdList != null and hgMatchUserIdList.size() != 0">
			and match_user_id in
			<foreach collection="hgMatchUserIdList" item="item" index="index" open="(" close=")" separator=",">
				#{hgMatchUserIdList[${index}]}
			</foreach>
		</if>
		<if test="payFullDateStart != null and payFullDateEnd != null">
			<![CDATA[AND DATE_FORMAT(pay_full_date, '%Y-%m-%d') >= DATE_FORMAT(#{payFullDateStart}, '%Y-%m-%d') ]]>
			<![CDATA[AND DATE_FORMAT(pay_full_date, '%Y-%m-%d') <= DATE_FORMAT(#{payFullDateEnd}, '%Y-%m-%d') ]]>
		</if>
		<if test="payFullDateStart == null or payFullDateEnd == null">
			<if test="payFullDateStart != null">
				<![CDATA[AND DATE_FORMAT(pay_full_date, '%Y-%m-%d') >= DATE_FORMAT(#{payFullDateStart}, '%Y-%m-%d') ]]>
			</if>
			<if test="payFullDateEnd != null">
				<![CDATA[AND DATE_FORMAT(pay_full_date, '%Y-%m-%d') <= DATE_FORMAT(#{payFullDateEnd}, '%Y-%m-%d') ]]>
			</if>
		</if>
		order by approve_no desc
		
    </select>

	<select id="findRptNotBillStatisticsSum" resultType="com.spt.bas.report.client.entity.RptNotBillStatistics">

		SELECT
			SUM(IFNULL(total_number,0)) AS totalNumber,
			SUM(IFNULL(buy_total_amount,0)) AS buyBotalAmount,
			SUM(IFNULL(pay_amount,0)) AS payAmount,
			SUM(IFNULL(receipt_bill_amount,0)) AS receiptBillAmount
		FROM
			t_ctr_contract_profit
		WHERE
			business_type = 'ZY-BB'
			AND buy_contract_status != 'C'
			AND seal_flg = TRUE
			AND pay_amount >= buy_total_amount
			AND buy_total_amount > receipt_bill_amount
		<if test="ourCompanyName != null and ourCompanyName != '' ">
			AND our_company_name = #{ourCompanyName}
		</if>
		<if test="companyName != null and companyName != '' ">
			and buy_company_name LIKE CONCAT('%',#{companyName},'%')
		</if>
		<if test="ourCompanyNameList != null and ourCompanyNameList.size > 0 ">
			and our_company_name IN
			<foreach collection="ourCompanyNameList" item="var" index="index" open="(" close=")" separator=",">
				#{ourCompanyNameList[${index}],jdbcType=VARCHAR}
			</foreach>
		</if>
		<if test="productType != null and productType eq 'NHG'.toString() and hgMatchUserIdList != null and hgMatchUserIdList.size() != 0">
			and match_user_id not in
			<foreach collection="hgMatchUserIdList" item="item" index="index" open="(" close=")" separator=",">
				#{hgMatchUserIdList[${index}]}
			</foreach>
		</if>
		<if test="productType != null and productType eq 'HG'.toString() and hgMatchUserIdList != null and hgMatchUserIdList.size() != 0">
			and match_user_id in
			<foreach collection="hgMatchUserIdList" item="item" index="index" open="(" close=")" separator=",">
				#{hgMatchUserIdList[${index}]}
			</foreach>
		</if>
		<if test="payFullDateStart != null and payFullDateEnd != null">
			<![CDATA[AND DATE_FORMAT(pay_full_date, '%Y-%m-%d') >= DATE_FORMAT(#{payFullDateStart}, '%Y-%m-%d') ]]>
			<![CDATA[AND DATE_FORMAT(pay_full_date, '%Y-%m-%d') <= DATE_FORMAT(#{payFullDateEnd}, '%Y-%m-%d') ]]>
		</if>
		<if test="payFullDateStart == null or payFullDateEnd == null">
			<if test="payFullDateStart != null">
				<![CDATA[AND DATE_FORMAT(pay_full_date, '%Y-%m-%d') >= DATE_FORMAT(#{payFullDateStart}, '%Y-%m-%d') ]]>
			</if>
			<if test="payFullDateEnd != null">
				<![CDATA[AND DATE_FORMAT(pay_full_date, '%Y-%m-%d') <= DATE_FORMAT(#{payFullDateEnd}, '%Y-%m-%d') ]]>
			</if>
		</if>

	</select>

	<select id="selectFunderByUserId" resultType="com.spt.bas.report.client.vo.RptFunderVo">
		SELECT
			user_id,
			user_name,
			company_names,
			remark,
			enterprise_id
		FROM t_bs_funder
		WHERE user_id = #{userId}
	</select>

</mapper>